---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 06
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - src/lib/validation/api/shifts.ts
  - src/lib/data/shifts.ts
  - src/lib/data/mutations/shifts.ts
  - src/app/api/shifts/route.ts
  - src/app/admin/shifts/shifts-view.tsx
  - src/lib/data/__tests__/shifts.test.ts
autonomous: true
requirements: [AUTH-03, DATA-05, DATA-06]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Màn hình ca làm việc đọc và ghi trên Postgres thật: danh sách ca, số nhân viên mỗi ca, thêm/sửa/nhân bản đều là dữ liệu thật của doanh nghiệp đang chọn."
    - "Ca qua đêm hiển thị đúng: cột `overnight` là cột sinh trong database (`end_time < start_time`), tầng ứng dụng đọc lại giá trị đó chứ không tự tính lại ở client."
    - "`workingDays` đi và về nguyên vẹn giữa mảng `smallint[]` của Postgres và `WeekdayNumber[]` của domain — không phần tử nào bị mất, thêm, hay đổi thứ tự."
    - "`duplicateShift` tạo một ca mới với tên và mã dẫn xuất theo đúng quy tắc V1, và bản sao thuộc đúng doanh nghiệp của ca gốc — không bao giờ nhân bản được ca của doanh nghiệp khác."
    - "Ba thao tác ghi ca (thêm, sửa, nhân bản) mỗi thao tác để lại đúng một dòng `audit_log` mang `actor_user_id`, `before`, `after` (DATA-06)."
    - "Vai trò `employee` và `manager` không thêm/sửa/nhân bản được ca làm việc (AUTH-03)."
    - "`listShifts` cho một doanh nghiệp không có ca nào trả về `[]` với `error: null`, phân biệt được với lỗi (edge DATA-05 empty)."
  artifacts:
    - path: "src/app/api/shifts/route.ts"
      provides: "Route Handler đọc ca làm việc"
      exports: ["GET", "dynamic"]
    - path: "src/lib/data/mutations/shifts.ts"
      provides: "Server Action ghi ca làm việc kèm audit"
      exports: ["createShift", "updateShift", "duplicateShift"]
    - path: "src/lib/data/shifts.ts"
      provides: "listShifts + kiểu ShiftWithStats chuyển nhà từ tầng giả lập"
      exports: ["listShifts", "ShiftWithStats"]
  key_links:
    - from: "src/app/admin/shifts/shifts-view.tsx"
      to: "src/lib/data/shifts.ts"
      via: "đổi đường dẫn import, giữ nguyên tên hàm và chữ ký"
      pattern: "@/lib/data/shifts"
    - from: "src/lib/data/mutations/shifts.ts"
      to: "src/lib/data/audit.ts"
      via: "logMutation trong cùng hàm ghi, before/after là nguyên dòng"
      pattern: "logMutation"
  prohibitions:
    - statement: "MUST NOT tự tính lại `overnight` ở tầng ứng dụng. Cột đó là cột sinh trong database từ chính `start_time`/`end_time` đang lưu; tính lại ở nơi thứ hai tạo ra hai nguồn sự thật, và cái sai sẽ là cái người dùng nhìn thấy trong khi cái đúng nằm im trong database."
      category: transparency
---

<objective>
Trọn lát cắt ca làm việc trên khuôn đã chứng minh ở plan tracer. Chạy song song với plan 02-05 —
hai plan không dùng chung file nào.

Purpose: ca làm việc là nhóm dữ liệu có nhiều kiểu không tầm thường nhất trong V1 (`time`, mảng
`smallint[]`, cột sinh `overnight`), nên nó là phép thử tốt cho việc schema Zod dùng chung ở hai đầu
có thật sự giữ được kiểu end-to-end hay không (D-12d).

Output: một Route Handler, một module dữ liệu, một module Server Action, một màn hình chạy dữ liệu thật.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-PATTERNS.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-04-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Route Handler | `GET /api/shifts` |
| Hàm dữ liệu | `listShifts()` |
| Server Action | `createShift()`, `updateShift()`, `duplicateShift()` |
| Kiểu | `ShiftWithStats` (chuyển nhà từ tầng giả lập sang `src/lib/data/shifts.ts`) |
| Schema Zod | `shiftWithStatsSchema`, `shiftListResponseSchema`, `shiftInputSchema` |
| Test | `src/lib/data/__tests__/shifts.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trọn lát cắt ca làm việc — đọc, ghi, audit</name>
  <files>src/lib/validation/api/shifts.ts, src/lib/data/shifts.ts, src/lib/data/mutations/shifts.ts, src/app/api/shifts/route.ts, src/app/admin/shifts/shifts-view.tsx</files>
  <read_first>
    - `src/app/api/companies/route.ts` và `src/lib/data/mutations/companies.ts` (tạo ở 02-04) —
      khuôn Route Handler và khuôn Server Action đã chốt; plan này lặp lại đúng khuôn đó
    - `src/lib/mock/service.ts` dòng 166-219 — `ShiftWithStats`, `listShifts`, `createShift`,
      `updateShift`, `duplicateShift`: chữ ký chính xác, quy tắc `employeeCount` (chỉ đếm nhân viên
      chưa nghỉ việc), và quy tắc dẫn xuất tên/mã của bản sao
    - `src/lib/types/domain.ts` dòng 92-108 và 305 — `Shift`, `ShiftInput`, `WeekdayNumber`
    - `supabase/migrations/0004_core_entities.sql` dòng 33-52 — cột thật của `shifts`. Ba điểm
      phải xử lý đúng: `start_time`/`end_time` kiểu `time` (Postgres trả `HH:MM:SS`, domain dùng
      `HH:mm`), `overnight` là **cột sinh** `generated always as (end_time < start_time) stored`
      nên **không được** đưa vào câu lệnh ghi, và `working_days` là `smallint[]` có ràng buộc
      `<@ array[1..7]` cùng độ dài từ 1 đến 7
    - `src/app/admin/shifts/shifts-view.tsx` — dòng import và mọi chỗ gọi bốn hàm; giao diện không đổi
    - `src/components/shifts/shift-card.tsx` — không import gì từ tầng giả lập, **không** phải sửa;
      xác nhận lại điều đó thay vì giả định
  </read_first>
  <behavior>
    - `listShifts` trả về `startTime`/`endTime` ở dạng `HH:mm` (không có phần giây), khớp đúng kiểu mà `Shift` của domain khai và mà giao diện đang hiển thị.
    - `listShifts` trả về `overnight` bằng `true` cho ca `22:00`–`06:00` và `false` cho ca `08:00`–`17:00`, lấy từ cột sinh của database.
    - `listShifts` trả về `workingDays` là mảng số nguyên 1..7 đúng thứ tự và đúng số phần tử như đã lưu.
    - `listShifts` cho một doanh nghiệp không có ca nào trả về `[]`, không phải lỗi.
    - `createShift` **không** gửi `overnight` lên database — gửi cột sinh sẽ bị Postgres từ chối.
    - `duplicateShift` trên một `id` thuộc doanh nghiệp khác ném lỗi và không tạo dòng nào.
    - `updateShift` với patch rỗng vẫn ghi đúng một dòng `audit_log` với `before` bằng `after`.
  </behavior>
  <action>
    **(a) `src/lib/validation/api/shifts.ts`.** `shiftWithStatsSchema` khớp `ShiftWithStats`. Ba
    phép biến đổi phải nằm **trong schema** để có đúng một nơi định nghĩa:
    - `start_time`/`end_time`: cắt phần giây khi đọc (`HH:MM:SS` → `HH:mm`), thêm lại `:00` khi ghi.
    - `working_days`: `z.array(z.union([z.literal(1), …, z.literal(7)]))` để giữ đúng kiểu
      `WeekdayNumber[]`, kèm ràng buộc độ dài 1..7 khớp ràng buộc `check` của database.
    - snake_case cột → camelCase domain.
    `shiftInputSchema` khớp `ShiftInput` nhưng **loại bỏ** trường `overnight`: nó là cột sinh, đưa
    vào câu lệnh ghi sẽ bị Postgres từ chối. Ghi comment tại chỗ nói rõ lý do.

    **(b) `src/app/api/shifts/route.ts`.** `GET` theo khuôn 02-04. Truy vấn `shifts` lọc
    `company_id` từ `getSessionContext()`, kèm `employeeCount` là số `employees` có `shift_id` bằng
    id ca đó và `status <> 'terminated'`. Sắp xếp xác định theo `start_time` tăng dần rồi `id` tăng
    dần. Parse bằng `shiftListResponseSchema` trước khi trả. Chỉ xuất `dynamic` và `GET`.

    **(c) `src/lib/data/shifts.ts`.** Xuất kiểu `ShiftWithStats` (chuyển nhà từ tầng giả lập, giữ
    nguyên tên và hình dạng) và `listShifts(companyId: string): Promise<ShiftWithStats[]>` gọi qua
    `fetchJson`. Tham số `companyId` giữ trong chữ ký nhưng không gửi lên server và không ảnh hưởng
    phạm vi — cùng lý do và cùng comment như các module khác. Re-export ba hàm ghi.

    **(d) `src/lib/data/mutations/shifts.ts`** — `"use server"`. Ba hàm giữ nguyên chữ ký:
    `createShift(companyId, input)`, `updateShift(id, patch)`, `duplicateShift(id)`. Mỗi hàm:
    `getSessionContext()` → `requireRole(role, ["owner","admin"])` → đọc nguyên dòng trước → ghi với
    `.eq("company_id", companyId)` (companyId từ session) → đọc nguyên dòng sau → `logMutation` với
    `entityTable: "shifts"`, `before`/`after` nguyên dòng (D-18).
    `duplicateShift` phải đọc ca gốc **có kèm** điều kiện `company_id` từ session; nếu không tìm
    thấy thì ném `Error` mang thông điệp tiếng Việt giống hệt tầng giả lập đang dùng, để giao diện
    không đổi hành vi. Tên bản sao và mã bản sao dẫn xuất theo đúng quy tắc V1 đọc được ở
    `src/lib/mock/service.ts`. Bản sao ghi vào đúng `company_id` của session, không lấy từ ca gốc —
    hai giá trị luôn bằng nhau nhờ điều kiện đọc, nhưng lấy từ session là bất biến an toàn hơn.
    `updateShift` không có nhánh "patch rỗng thì bỏ qua ghi audit".

    **(e) Chuyển call site.** Đổi dòng import ở `src/app/admin/shifts/shifts-view.tsx` sang
    `@/lib/data/shifts` (cả bốn hàm và kiểu `ShiftWithStats`). Không đổi gì khác trong file.
    Xác nhận `src/components/shifts/shift-card.tsx` không cần sửa và ghi kết luận đó vào SUMMARY.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -c "@/lib/mock/service" src/app/admin/shifts/shifts-view.tsx` trả về `0`.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/` trả về `0` trên mọi file.
    - `grep -c 'export const dynamic = "force-dynamic"' src/app/api/shifts/route.ts` trả về 1.
    - Với cookie owner Ngọc Phát: `curl -b <cookie> -s http://localhost:3000/api/shifts` trả về mảng 4 phần tử; với cookie owner Bình Minh trả về mảng 3 phần tử.
    - Trong phản hồi trên, mọi giá trị `startTime` và `endTime` khớp biểu thức `^\d{2}:\d{2}$` — không phần tử nào còn phần giây.
    - Trong phản hồi trên, đúng một ca của mỗi doanh nghiệp có `overnight` bằng `true` (seed dựng mỗi bên một ca đêm).
    - Trong phản hồi trên, mọi `workingDays` là mảng có từ 1 đến 7 phần tử, mọi phần tử nằm trong 1..7.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_table='shifts'"` tăng đúng 1 sau mỗi thao tác thêm/sửa/nhân bản trên giao diện; ba lần đo ghi vào SUMMARY.
    - Sau một lần nhân bản ca: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from shifts where company_id='cty-01'"` tăng đúng 1, và `select count(*) from shifts where company_id='cty-02'"` không đổi.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; node -e "const {execSync}=require('child_process');const fs=require('fs');for(const f of execSync('git ls-files src/app/api',{encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean)){const s=fs.readFileSync(f,'utf8');if(/export\s+(async\s+)?function\s+(POST|PUT|PATCH|DELETE|HEAD|OPTIONS)/.test(s))process.exit(1)}"</automated>
  </verify>
  <done>Màn hình ca làm việc chạy trọn trên Postgres; giờ hiển thị đúng dạng `HH:mm`; cờ ca đêm lấy từ cột sinh; nhân bản không vượt ranh giới doanh nghiệp; ba thao tác ghi đều có audit.</done>
</task>

<task type="auto">
  <name>Task 2: Test biên kiểu dữ liệu ca làm việc — giờ, mảng ngày, cột sinh</name>
  <files>src/lib/validation/api/shifts.ts, src/lib/data/shifts.ts, src/lib/data/__tests__/shifts.test.ts</files>
  <read_first>
    - `src/lib/validation/api/shifts.ts` — ba phép biến đổi cần test
    - `src/lib/data/shifts.ts` — hàm cần test
    - `src/lib/data/__tests__/departments.test.ts` (tạo ở plan 02-05) — khuôn mock `fetch` bằng
      `vi.stubGlobal`; nếu plan 02-05 chưa xong thì tự dựng khuôn tương đương, hai plan chạy song song
      nên không được phụ thuộc file của nhau
  </read_first>
  <action>
    Tạo `src/lib/data/__tests__/shifts.test.ts` với `fetch` được giả lập bằng `vi.stubGlobal`.
    Bảy nhóm khẳng định:
    1. Phản hồi mang `start_time` dạng `08:00:00` → `startTime` bằng `"08:00"`. Đây là biên hay bị
      bỏ sót: định dạng `time` của Postgres không trùng định dạng mà giao diện đang hiển thị.
    2. Phản hồi mang `start_time` dạng `22:00:00` và `end_time` dạng `06:00:00` cùng `overnight: true`
      → `overnight` trong kết quả bằng `true`, và giá trị đó đến từ phản hồi chứ không được tính lại.
    3. Phản hồi mang `overnight: false` cho một ca có `end_time` nhỏ hơn `start_time` (trường hợp
      mâu thuẫn nhân tạo) → kết quả vẫn là `false`. Khẳng định này chốt việc tầng ứng dụng **không**
      tự tính lại cột sinh; nếu ai đó thêm phép tính lại, test này đỏ.
    4. `working_days` bằng `[1,2,3,4,5]` → `workingDays` bằng đúng `[1,2,3,4,5]`, cùng thứ tự.
    5. `working_days` chứa một giá trị ngoài khoảng 1..7 → hàm ném `Error` về định dạng dữ liệu,
      không trả về giá trị chưa kiểm.
    6. `working_days` là mảng rỗng → hàm ném `Error` (ràng buộc `check` của database đòi ít nhất
      một phần tử, nên mảng rỗng nghĩa là dữ liệu đã hỏng ở đâu đó).
    7. Phản hồi 200 với mảng rỗng → hàm trả `[]` và không ném (edge DATA-05 empty).
    Mọi assertion so sánh `err instanceof Error` và `err.message`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/shifts.test.ts` thoát 0 và báo ít nhất 7 test.
    - `npm run test` thoát 0.
    - `npm run typecheck` thoát 0 — không `any` trong file test hay trong mock.
    - Sau khi tạm thêm vào `src/lib/data/shifts.ts` một phép tính lại cờ ca đêm từ `startTime`/`endTime`, `npx vitest run src/lib/data/__tests__/shifts.test.ts` thoát **khác 0** (nhóm 3 đỏ); hoàn tác thì trở lại 0. Hai mã thoát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run test &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Bảy biên kiểu dữ liệu của ca làm việc đều có test; cấm tính lại cột sinh được một test cụ thể canh, và test đó đã được chứng minh có răng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt → `GET /api/shifts` | URL công khai, xác thực bằng cookie |
| Trình duyệt → Server Action ghi ca | Next.js kiểm nguồn gửi sẵn |
| Tầng ứng dụng → Postgres | Lọc `company_id` là lớp 1, RLS là lớp 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Information Disclosure | `duplicateShift` nhận `id` tùy ý từ client, có thể trỏ ca của doanh nghiệp khác | high | mitigate | Câu đọc ca gốc luôn kèm `.eq("company_id", <từ session>)`; không tìm thấy thì ném lỗi; acceptance criteria đo số ca của **cả hai** doanh nghiệp sau một lần nhân bản |
| T-02-06-02 | Elevation of Privilege | Vai trò thấp ghi được ca làm việc | high | mitigate | `requireRole(role, ["owner","admin"])` ở đầu cả ba Server Action |
| T-02-06-03 | Information Disclosure | Route Handler bị cache giữa hai doanh nghiệp | high | mitigate | `force-dynamic` tường minh; acceptance criteria gọi cùng URL bằng hai cookie và đòi 4 với 3 phần tử |
| T-02-06-04 | Tampering | Cột sinh bị ghi đè hoặc tính lại sai ở tầng ứng dụng | medium | mitigate | `shiftInputSchema` loại bỏ trường cột sinh khỏi đường ghi; test nhóm 3 canh việc không tính lại ở đường đọc |
| T-02-06-05 | Tampering | Mảng ngày làm việc sai hình dạng lọt vào giao diện | medium | mitigate | Schema Zod ràng buộc kiểu phần tử và độ dài khớp ràng buộc `check` của database; test nhóm 5 và 6 |
| T-02-06-06 | Repudiation | Sửa ca không để lại vết | medium | mitigate | `logMutation` trong cùng hàm ghi; cấm nhánh "patch rỗng thì bỏ qua"; acceptance criteria đo số dòng audit sau từng thao tác |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test` thoát 0
- `/api/shifts` trả 4 dòng cho Ngọc Phát và 3 dòng cho Bình Minh trên cùng URL
- Mọi giờ trả về đúng dạng `HH:mm`; đúng một ca đêm mỗi doanh nghiệp
- Ba thao tác ghi ca tạo đúng ba dòng audit
- Nhân bản ca không đổi số ca của doanh nghiệp còn lại
</verification>

<success_criteria>
Nhóm dữ liệu có kiểu phức tạp nhất của V1 đi qua khuôn hai transport mà vẫn giữ nguyên kiểu
end-to-end: giờ đúng định dạng, mảng ngày đúng phần tử và thứ tự, cột sinh không bị tính lại ở nơi
thứ hai.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-06-SUMMARY.md` when done
</output>
