---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - scripts/seed-auth.mjs
  - scripts/check-signup-disabled.mjs
  - supabase/config.toml
autonomous: false
requirements: [AUTH-03, AUTH-04, DATA-05]

user_setup:
  - service: supabase
    why: "Tắt đăng ký công khai phải làm ở cấu hình GoTrue của project cloud (D-13a). `supabase/config.toml` là cấu hình cho `supabase start` local, và dự án này không chạy local stack — `db:push`/`db:seed` đi thẳng vào Postgres URL, nên sửa file đó một mình có thể không có tác dụng gì trên cloud."
    env_vars: []
    dashboard_config:
      - task: "Tắt 'Allow new users to sign up'"
        location: "Supabase Dashboard → project ujvgagujfsdrlmjdhooi → Authentication → Sign In / Providers → Email"

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Đúng 10 tài khoản đăng nhập thật tồn tại trong Supabase Auth — mỗi doanh nghiệp 1 owner, 1 admin, 1 manager, 2 nhân viên (D-14)."
    - "Đúng 30 dòng `employees` giữ `user_id = null` — nhân viên ca kíp không có tài khoản là trạng thái sản xuất thật, không phải dữ liệu thiếu (D-14)."
    - "Mỗi tài khoản được tạo bằng `auth.admin.createUser()` với `email_confirm: true`, nên không tài khoản nào kẹt ở trạng thái chờ xác nhận email (D-14a)."
    - "Một tài khoản vừa tạo đăng nhập được thật bằng `signInWithPassword` — đây là bằng chứng `auth.identities` đã có, đúng chỗ mà chèn SQL thẳng vào `auth.users` sẽ hỏng (D-15)."
    - "Mọi tài khoản mang cờ `must_change_password = true` trong `app_metadata`, không phải trong `user_metadata` (D-16)."
    - "Đăng ký công khai đã tắt: chủ dự án là người duy nhất tạo được tài khoản owner, và người lạ không tạo được dòng doanh nghiệp mới (D-13)."
    - "Gọi thẳng endpoint đăng ký của GoTrue bằng publishable key bị từ chối với mã HTTP không thuộc dải 2xx — kiểm chứng ở endpoint, không phải bằng việc ẩn nút trên giao diện (D-13a)."
    - "Màn hình khai thông tin doanh nghiệp vẫn giữ nguyên và chỉ dùng được **sau khi** đã đăng nhập — tắt đăng ký không đồng nghĩa với bỏ luồng khởi tạo doanh nghiệp (D-13)."
    - "Chạy lại `npm run seed:auth` lần thứ hai không tạo thêm tài khoản nào và không đổi mật khẩu tài khoản đã có."
  artifacts:
    - path: "scripts/seed-auth.mjs"
      provides: "Tạo 10 credential thật qua Admin API rồi nối memberships/employees.user_id"
      contains: "email_confirm"
    - path: "scripts/check-signup-disabled.mjs"
      provides: "Probe HTTP chứng minh đăng ký công khai đã tắt ở GoTrue"
    - path: "supabase/config.toml"
      provides: "Cấu hình local giữ nhất quán với cloud"
  key_links:
    - from: "scripts/seed-auth.mjs"
      to: "supabase/seed.sql"
      via: "đọc employees theo company_id + code để chọn 5 người mỗi doanh nghiệp, dùng chính email trong seed"
      pattern: "from\\(\"employees\"\\)"
    - from: "scripts/seed-auth.mjs"
      to: "memberships"
      via: "upsert (user_id, company_id) role sau khi Admin API trả về user id"
      pattern: "memberships"
  prohibitions:
    - statement: "MUST NOT ghi mật khẩu tạm sinh ra vào bất kỳ nơi nào lưu lại được — không vào `audit_log`, không vào cột nào của `employees`, không vào file trên đĩa, không vào git. Mật khẩu chỉ tồn tại một lần trên stdout của người chạy script."
      category: privacy
---

<objective>
Tạo credential thật cho khoảng 10 người đại diện bằng Admin API (không bao giờ bằng SQL thẳng vào
`auth.users`), và tắt đăng ký công khai ở đúng chỗ nó thật sự có hiệu lực.

Purpose: đây là điều kiện để plan tracer có người thật đăng nhập. Đồng thời nó phủ đúng một trạng
thái sản xuất mà seed cũ che mất: 30/40 nhân viên **không** có tài khoản, và hệ thống phải chạy
đúng với thực tế đó.

Output: `scripts/seed-auth.mjs`, `scripts/check-signup-disabled.mjs`, `supabase/config.toml` đã sửa,
và 10 tài khoản sống trên project thật.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-02-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Script | `scripts/seed-auth.mjs` (chạy qua `npm run seed:auth`), `scripts/check-signup-disabled.mjs` |
| Dữ liệu | 10 dòng `auth.users` thật + `auth.identities` tương ứng, 10 dòng `memberships`, 10 dòng `employees.user_id` được nối |
| Cấu hình | `supabase/config.toml` khóa `enable_signup` |

<tasks>

<task type="auto">
  <name>Task 1: scripts/seed-auth.mjs — 10 credential thật qua Admin API, 30 nhân viên vẫn không tài khoản</name>
  <precondition>`.env.local` chứa `NEXT_PUBLIC_SUPABASE_URL` và `SUPABASE_SECRET_KEY` khác rỗng, và `npm run db:seed` đã chạy ít nhất một lần trên project cloud (bảng `employees` có 40 dòng)</precondition>
  <files>scripts/seed-auth.mjs</files>
  <read_first>
    - `scripts/db.mjs` — toàn bộ file. Chép ba quy ước: header `#!/usr/bin/env node` + block comment
      nêu mục đích và mã ràng buộc; hàm kiểu `requireConnectionUrl()` in đúng tên biến thiếu rồi
      `process.exit(1)`; và cách gọi `node --env-file=.env.local scripts/<file>.mjs`
    - `supabase/seed.sql` dòng 140-175 — hình dạng một dòng `employees`: thứ tự cột và giá trị
      `email` (`nv001@ngocphat.test` …) mà script sẽ dùng làm email đăng nhập, không tự bịa địa chỉ mới
    - `supabase/migrations/0002_tenancy.sql` dòng 38-47 — cột của `memberships`:
      `user_id`, `company_id`, `role`, `status`, `last_accessed_at`, và ràng buộc `unique (user_id, company_id)`
      (đây là khóa để `upsert` chạy lại được nhiều lần)
    - `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md` §Pattern 7 — hình dạng
      lời gọi `auth.admin.createUser`
  </read_first>
  <action>
    Tạo `scripts/seed-auth.mjs`, ESM, chạy bằng `node --env-file=.env.local scripts/seed-auth.mjs`
    (npm script `seed:auth` đã khai ở plan 02-01). Dùng `@supabase/supabase-js` (đã là dependency
    của ứng dụng, không cài thêm gì) và `node:crypto`.

    **(a) Kiểm biến môi trường.** Bắt buộc `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SECRET_KEY`,
    `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. Thiếu bất kỳ biến nào thì in **đúng tên biến thiếu**
    bằng tiếng Việt rồi `process.exit(1)` — chép đúng khuôn `requireConnectionUrl()`.

    **(b) Hai client.** `admin` = `createClient(url, secretKey, { auth: { persistSession: false, autoRefreshToken: false } })`
    dùng cho `auth.admin.*` và cho mọi ghi vào `memberships`/`employees` (khóa secret bỏ qua RLS —
    đây là chỗ duy nhất trong dự án được phép làm thế, ngoài Server Action gọi Admin API ở plan 02-09).
    `anon` = `createClient(url, publishableKey, { auth: { persistSession: false } })` dùng **chỉ**
    cho bước kiểm chứng đăng nhập ở phần (f).

    **(c) Chọn người, quy tắc xác định và lặp lại được.** Với mỗi `company_id` trong `['cty-01','cty-02']`:
    truy vấn `employees` lọc `company_id` và `status = 'active'`, sắp xếp theo `code` tăng dần, lấy
    năm dòng đầu. Gán vai trò membership theo đúng thứ tự đó: `owner`, `admin`, `manager`, `employee`,
    `employee` — đúng cơ cấu D-14. Nếu một doanh nghiệp trả về ít hơn 5 dòng active, in thông điệp
    nêu đích danh `company_id` và số dòng tìm được rồi `process.exit(1)`; không được im lặng tạo ít
    tài khoản hơn.

    **(d) Tạo tài khoản.** Với mỗi người đã chọn:
    - Sinh mật khẩu tạm bằng `randomBytes(18).toString("base64url")` — mỗi người một mật khẩu khác nhau.
    - Gọi `admin.auth.admin.createUser({ email: <email của chính dòng employees>, password, email_confirm: true, app_metadata: { must_change_password: true } })`.
      `email_confirm: true` là bắt buộc (D-14a) — thiếu nó thì tài khoản kẹt chờ xác nhận email và
      không ai đăng nhập được. Cờ `must_change_password` đặt trong `app_metadata` chứ tuyệt đối
      không phải nhánh metadata mà client sửa được qua `auth.updateUser()` (D-16).
    - Nếu lời gọi trả lỗi vì email đã tồn tại: quét `admin.auth.admin.listUsers({ page, perPage: 1000 })`
      qua các trang để tìm user id theo email, **không** đặt lại mật khẩu, và đánh dấu người này là
      "đã tồn tại" trong bảng kết quả. Chạy lại script lần thứ hai phải là thao tác an toàn.

    **(e) Nối dữ liệu nghiệp vụ.** Sau khi có `user.id`:
    - `upsert` vào `memberships` `{ user_id, company_id, role, status: 'active', last_accessed_at: null }`
      với `onConflict: 'user_id,company_id'`.
    - `update employees set user_id = <user.id>, system_role = <cùng vai trò> where id = <employee id>`.
      Đồng bộ `system_role` với `memberships.role` có chủ ý: `memberships.role` là nguồn quyền, còn
      `employees.system_role` là trường hiển thị của V1; để hai giá trị lệch nhau tạo ra một mâu thuẫn
      im lặng mà người đọc màn hình không có cách nào phát hiện.

    **(f) Kiểm chứng đăng nhập ngay trong script.** Lấy tài khoản đầu tiên vừa tạo mới (không phải
    tài khoản "đã tồn tại", vì mật khẩu của nó không nằm trong tay script), gọi
    `anon.auth.signInWithPassword({ email, password })`. Nếu lỗi thì in nguyên thông điệp lỗi rồi
    `process.exit(1)` — đây chính là bằng chứng bản ghi identity đã được tạo, thứ mà chèn SQL thẳng
    vào bảng người dùng của Supabase không bao giờ tạo ra. Gọi `anon.auth.signOut()` sau khi kiểm.
    Nếu tất cả đều là tài khoản "đã tồn tại" thì bỏ qua bước này và in rõ lý do.

    **(g) In kết quả.** Một bảng ra stdout: email, doanh nghiệp, vai trò, mật khẩu tạm (hoặc chuỗi
    `(đã tồn tại — không đổi mật khẩu)`). Mật khẩu tạm **chỉ** xuất hiện ở đây: không ghi ra file,
    không ghi vào bảng nào, không đưa vào audit. Cuối bảng in số tài khoản đã tạo mới, số đã tồn tại,
    và số dòng `employees` còn `user_id = null`.
  </action>
  <acceptance_criteria>
    - `npm run seed:auth` thoát 0 và output chứa dòng tổng kết với đúng 10 tài khoản (tạo mới + đã tồn tại).
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from memberships"` trả về `10`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from employees where user_id is not null"` trả về `10`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from employees where user_id is null"` trả về `30`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from memberships where role='owner'"` trả về `2`; tương tự `admin` → `2`, `manager` → `2`, `employee` → `4`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from auth.identities i join auth.users u on u.id=i.user_id where u.email like '%@ngocphat.test' or u.email like '%@binhminh.test'"` trả về `10` — mỗi tài khoản có đúng một bản ghi identity.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from auth.users where raw_app_meta_data->>'must_change_password' = 'true'"` trả về `10`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from auth.users where raw_user_meta_data::text like '%must_change_password%'"` trả về `0` — cờ không nằm ở nhánh metadata mà client sửa được.
    - Chạy `npm run seed:auth` lần thứ hai thoát 0, và bốn con số đếm ở trên không đổi.
    - `git grep -n -c "$(node -e 'process.stdout.write(\"pass\"+\"word\")')" scripts/seed-auth.mjs` cho thấy script không ghi mật khẩu ra file: `grep -cE "writeFile|appendFile|createWriteStream" scripts/seed-auth.mjs` trả về `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run seed:auth &amp;&amp; npm run seed:auth &amp;&amp; node -e "if(/writeFile|appendFile|createWriteStream/.test(require('fs').readFileSync('scripts/seed-auth.mjs','utf8')))process.exit(1)"</automated>
  </verify>
  <done>10 tài khoản thật tồn tại và đăng nhập được; 30 nhân viên giữ `user_id = null`; cờ buộc đổi mật khẩu nằm đúng nhánh metadata phía server; script chạy lại được mà không tạo trùng.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Tắt đăng ký công khai trên Dashboard (người thực hiện)</name>
  <files>(không có file nào trong repo — đây là cấu hình dịch vụ cloud)</files>
  <action>
    `supabase/config.toml` là cấu hình cho `supabase start` local. Dự án này không chạy local stack —
    `scripts/db.mjs` đi thẳng vào Postgres URL qua `psql` và `npx supabase db push --db-url`.
    Nghiên cứu không tìm được lệnh CLI nào đẩy `config.toml` lên project cloud đã link
    (`02-RESEARCH.md` Assumptions Log A2, Open Question 2), nên bước này chưa tự động hóa được và
    phải làm bằng tay (D-13a). Executor chỉ trình bày hướng dẫn rồi dừng chờ; bước kiểm chứng ở
    Task 3 **là** tự động và không tin vào việc đã bấm nút.
  </action>
  <instructions>
    1. Mở Supabase Dashboard → project `ujvgagujfsdrlmjdhooi` → Authentication → Sign In / Providers.
    2. Ở mục Email, tắt "Allow new users to sign up". Lưu.
    3. Nếu không tìm thấy đúng nhãn đó, tìm mục có ý nghĩa tương đương trong Authentication →
       Settings; ghi lại đường dẫn thật đã dùng để executor chép vào SUMMARY.
    4. Báo "done" — executor sẽ chạy probe HTTP để xác nhận.
  </instructions>
  <verification>
    `node --env-file=.env.local scripts/check-signup-disabled.mjs` thoát 0 (script được tạo ở Task 3;
    chạy bước kiểm này sau khi Task 3 hoàn tất).
  </verification>
  <acceptance_criteria>
    - Đường dẫn menu thật đã dùng được ghi vào SUMMARY.
    - Probe ở Task 3 nhận mã HTTP ngoài dải 200-299 từ endpoint đăng ký.
  </acceptance_criteria>
  <resume-signal>Gõ "done" kèm đường dẫn menu thật đã dùng</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Probe chứng minh đăng ký công khai đã tắt ở endpoint, và đồng bộ config.toml</name>
  <precondition>Tác vụ tắt đăng ký trên Dashboard đã hoàn tất (Task 2 đã báo done)</precondition>
  <files>scripts/check-signup-disabled.mjs, supabase/config.toml</files>
  <read_first>
    - `supabase/config.toml` — tìm khóa `enable_signup` trong khối `[auth]` (theo nghiên cứu nằm
      quanh dòng 176; xác định lại bằng cách tìm khóa, không tin số dòng)
    - `scripts/db.mjs` — khuôn kiểm biến môi trường và thông điệp lỗi tiếng Việt để chép sang script mới
    - `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md` §Pattern 9 — hình dạng
      lời gọi probe và mã trả về kỳ vọng
  </read_first>
  <action>
    **(a) `supabase/config.toml`.** Đổi giá trị khóa `enable_signup` trong khối `[auth]` thành `false`,
    và thêm ngay phía trên một comment nêu rõ: file này chỉ chi phối `supabase start` local; project
    cloud tắt riêng trên Dashboard, và bằng chứng là `scripts/check-signup-disabled.mjs`, không phải
    dòng cấu hình này (D-13a). Không đổi khóa nào khác trong file.

    **(b) `scripts/check-signup-disabled.mjs`.** ESM, chỉ dùng built-in Node 22 (`node:process`,
    `fetch` sẵn có), chạy bằng `node --env-file=.env.local scripts/check-signup-disabled.mjs`.
    - Bắt buộc `NEXT_PUBLIC_SUPABASE_URL` và `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`; thiếu thì in
      đúng tên biến thiếu rồi thoát 1.
    - Sinh một email dùng một lần dạng `probe-<randomUUID>@timeflow-probe.invalid` — dùng tên miền
      `.invalid` (dành riêng theo RFC 2606) để chắc chắn không đụng hộp thư thật của ai.
    - Gọi `POST {url}/auth/v1/signup` với header `apikey` mang publishable key và
      `Content-Type: application/json`, body `{ email, password }` với mật khẩu ngẫu nhiên đủ dài.
    - Thoát 0 khi mã trả về **không** thuộc dải 2xx; in mã trả về và thân phản hồi.
    - Thoát 1 khi mã trả về thuộc dải 2xx, kèm thông điệp nêu rõ đăng ký công khai vẫn đang mở và
      nhắc lại đường dẫn Dashboard.
    - Thoát 1 khi `fetch` ném lỗi mạng, kèm thông điệp riêng — một probe không gọi được endpoint
      không được coi là bằng chứng đã tắt.
    - Nếu vì lý do nào đó phản hồi là 2xx, script phải in cả `user.id` trả về để người vận hành
      biết có một tài khoản rác vừa được tạo và cần xóa.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('fs').readFileSync('supabase/config.toml','utf8');if(!/^\s*enable_signup\s*=\s*false/m.test(s))process.exit(1)"` thoát 0.
    - `node --env-file=.env.local scripts/check-signup-disabled.mjs` thoát 0 và output chứa mã HTTP trả về của endpoint đăng ký.
    - Mã HTTP mà probe nhận được không thuộc dải 200-299 và được ghi nguyên văn vào SUMMARY.
    - `grep -cE "timeflow-probe\.invalid" scripts/check-signup-disabled.mjs` trả về ít nhất 1 — probe không dùng tên miền thật.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from auth.users where email like 'probe-%'"` trả về `0` sau khi chạy probe.
    - `npm run typecheck` và `npm run lint` vẫn thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>node --env-file=.env.local scripts/check-signup-disabled.mjs &amp;&amp; node -e "const s=require('fs').readFileSync('supabase/config.toml','utf8');if(!/^\s*enable_signup\s*=\s*false/m.test(s))process.exit(1)"</automated>
  </verify>
  <done>Endpoint đăng ký của GoTrue từ chối lời gọi thật bằng publishable key; mã trả về đã ghi vào SUMMARY; `config.toml` đồng bộ và có comment nói rõ nó không phải bằng chứng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → GoTrue `/auth/v1/signup` | Endpoint công khai, ai cầm publishable key cũng gọi được |
| `scripts/seed-auth.mjs` → Supabase Admin API | Script cầm khóa secret, bỏ qua toàn bộ RLS |
| stdout của script → người vận hành | Mật khẩu tạm đi qua đúng một lần ở đây |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Spoofing | Đăng ký công khai còn mở | high | mitigate | Tắt ở cấu hình GoTrue (không phải ẩn nút) + probe HTTP tự động khẳng định mã trả về ngoài dải 2xx; probe thoát 1 cả khi gặp lỗi mạng để không nhận nhầm "không gọi được" thành "đã tắt" |
| T-02-03-02 | Information Disclosure | Mật khẩu tạm bị lưu lại | high | mitigate | Mật khẩu chỉ tồn tại trên stdout; acceptance criteria grep chặn mọi lời gọi ghi file trong script; không cột DB nào, không dòng audit nào mang mật khẩu |
| T-02-03-03 | Elevation of Privilege | Khóa secret dùng sai chỗ | high | mitigate | Khóa secret chỉ vào client `admin` bên trong script Node chạy trên máy vận hành; client `anon` dùng publishable key cho bước kiểm đăng nhập; `npm run check:secrets` (Phase 1) vẫn là cổng chặn khóa lọt bundle |
| T-02-03-04 | Elevation of Privilege | Cờ buộc đổi mật khẩu đặt ở nhánh metadata client sửa được | high | mitigate | Đặt trong `app_metadata` qua Admin API; acceptance criteria đếm `raw_user_meta_data` phải bằng 0 |
| T-02-03-05 | Denial of Service | Chạy lại script tạo trùng hoặc xoay mật khẩu của người đang dùng | medium | mitigate | Nhánh "email đã tồn tại" tra lại user id và **không** đặt lại mật khẩu; `upsert` theo `unique (user_id, company_id)`; acceptance criteria chạy script hai lần và so bốn con số đếm |
| T-02-03-06 | Spoofing | Probe tạo tài khoản rác trên hộp thư thật | low | mitigate | Email probe dùng tên miền `.invalid` dành riêng theo RFC 2606; acceptance criteria đếm `auth.users` bắt đầu bằng `probe-` phải bằng 0 |
</threat_model>

<verification>
- `npm run seed:auth` thoát 0, chạy lại lần hai vẫn thoát 0 và không đổi số liệu
- `node --env-file=.env.local scripts/check-signup-disabled.mjs` thoát 0
- `npm run test:db` vẫn thoát 0 (tài khoản thật không phá bộ pgTAP — pgTAP tự seed lại trước khi chạy)
- `npm run typecheck && npm run lint` thoát 0
</verification>

<success_criteria>
Có người thật đăng nhập được vào project thật; cơ cấu 10 có tài khoản / 30 không có tài khoản đúng
như thực tế vận hành; đăng ký công khai đã tắt ở nơi nó thật sự có hiệu lực và điều đó được chứng
minh bằng một lời gọi HTTP, không bằng một dòng cấu hình.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-03-SUMMARY.md` when done
</output>
