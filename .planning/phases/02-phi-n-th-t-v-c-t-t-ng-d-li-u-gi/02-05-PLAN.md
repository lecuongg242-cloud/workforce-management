---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - src/lib/validation/api/employees.ts
  - src/lib/data/employees.ts
  - src/app/api/employees/route.ts
  - src/components/layout/admin-topbar.tsx
  - src/lib/validation/api/departments.ts
  - src/lib/data/departments.ts
  - src/lib/data/mutations/departments.ts
  - src/app/api/departments/route.ts
  - src/app/admin/departments/departments-view.tsx
  - src/lib/data/__tests__/departments.test.ts
autonomous: true
requirements: [AUTH-03, AUTH-05, DATA-05, DATA-06]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Màn hình phòng ban đọc và ghi trên Postgres thật: danh sách, số nhân viên mỗi phòng, tên người quản lý, thêm/sửa/xóa đều là dữ liệu thật của doanh nghiệp đang chọn."
    - "Không hàm nào trong `src/lib/data/departments.ts` hay `src/lib/data/employees.ts` nhận `companyId` từ phía client để quyết định phạm vi đọc — phạm vi luôn đến từ `getSessionContext()` (D-12b)."
    - "Ô tìm kiếm nhân viên bỏ dấu đúng như V1: gõ `nguyen` tìm ra `Nguyễn`, gõ `doan` tìm ra `Đoàn` — phép chuẩn hóa chạy ở Postgres qua `tf_normalize()`, không ở client."
    - "Mỗi hàm `list*` trả về mảng rỗng cho doanh nghiệp không có dòng nào, và `useMockQuery` nhận `data: []` với `error: null`; trạng thái rỗng phân biệt được với trạng thái lỗi (`data: null`, `error: string`) — edge DATA-05 empty."
    - "Ba thao tác ghi phòng ban (thêm, sửa, xóa) mỗi thao tác để lại đúng một dòng `audit_log` mang `actor_user_id`, `before`, `after` (DATA-06)."
    - "Vai trò `employee` và `manager` không thêm/sửa/xóa được phòng ban: Server Action ném lỗi quyền trước khi chạm database (AUTH-03)."
  artifacts:
    - path: "src/app/api/departments/route.ts"
      provides: "Route Handler đọc phòng ban"
      exports: ["GET", "dynamic"]
    - path: "src/app/api/employees/route.ts"
      provides: "Route Handler đọc nhân viên, phủ cả danh sách phân trang lẫn danh sách rút gọn"
      exports: ["GET", "dynamic"]
    - path: "src/lib/data/mutations/departments.ts"
      provides: "Server Action ghi phòng ban kèm audit"
      exports: ["createDepartment", "updateDepartment", "deleteDepartment"]
  key_links:
    - from: "src/app/admin/departments/departments-view.tsx"
      to: "src/lib/data/departments.ts"
      via: "đổi đường dẫn import, giữ nguyên tên hàm và chữ ký"
      pattern: "@/lib/data/departments"
    - from: "src/app/api/employees/route.ts"
      to: "public.tf_normalize"
      via: "lọc tìm kiếm bỏ dấu phía server"
      pattern: "tf_normalize"
  prohibitions:
    - statement: "MUST NOT để một thao tác ghi thành công mà không có dòng audit tương ứng. Ghi audit thất bại phải làm cả thao tác thất bại, không được nuốt lỗi rồi trả về như thành công — một bảng audit có lỗ là một bảng audit không ai dám dùng để đối chất."
      category: transparency
---

<objective>
Nhân rộng khuôn đã chứng minh ở plan tracer sang hai nhóm dữ liệu đầu tiên: đường đọc nhân viên
(cần cho ô chọn người quản lý và thanh trên cùng) và trọn lát cắt phòng ban.

Purpose: đây là lần đầu khuôn `Route Handler đọc + Server Action ghi + audit` được lặp lại. Nếu có
chỗ nào trong khuôn không nhân rộng được, phát hiện ở đây rẻ hơn ở hàm thứ hai mươi.

Output: hai Route Handler, hai module dữ liệu, một module Server Action, hai màn hình chạy dữ liệu thật.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-PATTERNS.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-04-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Route Handler | `GET /api/departments`, `GET /api/employees` |
| Hàm dữ liệu | `listDepartments()`, `listEmployees()`, `listAllEmployees()` |
| Server Action | `createDepartment()`, `updateDepartment()`, `deleteDepartment()` |
| Kiểu | `DepartmentWithStats` (chuyển nhà từ tầng giả lập sang `src/lib/data/departments.ts`) |
| Schema Zod | `departmentWithStatsSchema`, `departmentListResponseSchema`, `employeeSchema`, `employeeQuerySchema`, `paginatedEmployeeSchema` |
| Test | `src/lib/data/__tests__/departments.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Đường đọc nhân viên — GET /api/employees phủ cả phân trang lẫn danh sách rút gọn</name>
  <files>src/lib/validation/api/employees.ts, src/lib/data/employees.ts, src/app/api/employees/route.ts, src/components/layout/admin-topbar.tsx</files>
  <read_first>
    - `src/app/api/companies/route.ts` và `src/lib/data/companies.ts` — khuôn đã chốt ở plan 02-04:
      `dynamic = "force-dynamic"`, gọi `getSessionContext()` ở dòng đầu, Zod parse trước khi trả,
      `fetchJson` ở đầu client. Mọi handler ở plan này lặp lại đúng khuôn đó
    - `src/lib/mock/service.ts` dòng 225-282 — chữ ký chính xác của `listEmployees(query: EmployeeQuery): Promise<Paginated<Employee>>`
      và `listAllEmployees(companyId: string): Promise<Employee[]>`, cùng toàn bộ quy tắc lọc:
      bốn bộ lọc dùng giá trị `"all"` làm "không lọc", tìm kiếm ghép bốn trường, và cách kẹp
      `safePage` vào khoảng hợp lệ
    - `src/lib/types/domain.ts` dòng 110-137 và 200-216 — `Employee`, `Paginated<T>`, `EmployeeQuery`
    - `supabase/migrations/0004_core_entities.sql` dòng 58-84 và 153-165 — tên cột thật của
      `employees` và index `employees_company_id_full_name_id_idx` đã dựng sẵn cho đúng phép sắp
      xếp mà phân trang cần
    - `src/lib/format.ts` dòng 200-208 — `normalizeText()`, hành vi mà `tf_normalize()` phải khớp
    - `src/components/layout/admin-topbar.tsx` — call site duy nhất của `listAllEmployees` trong plan này
  </read_first>
  <behavior>
    - `listEmployees` với `search` không dấu tìm ra được nhân viên có tên mang dấu (`nguyen` → `Nguyễn Minh Anh`).
    - `listEmployees` với `departmentId`/`status`/`contractType` bằng `"all"` không lọc theo trường đó.
    - `listEmployees` với `page` lớn hơn `totalPages` trả về trang cuối cùng, không phải mảng rỗng.
    - `listEmployees` cho một doanh nghiệp không có nhân viên nào trả về `{ items: [], total: 0, page: 1, pageSize: <mặc định>, totalPages: 1 }` — `totalPages` tối thiểu là 1, đúng như V1.
    - Hai nhân viên trùng `full_name` xuất hiện theo cùng thứ tự ở mọi lần gọi, và không dòng nào bị lặp hay biến mất giữa trang 1 và trang 2.
    - Gửi kèm một tham số truy vấn mang định danh doanh nghiệp khác không đổi kết quả.
  </behavior>
  <action>
    **(a) `src/lib/validation/api/employees.ts`.** `employeeSchema` khớp `Employee` của `domain.ts`
    (bao gồm biến đổi snake_case của cột sang camelCase của domain ngay trong schema, để phép ánh xạ
    có đúng một nơi định nghĩa). `employeeQuerySchema` khai đúng sáu tham số `search`, `departmentId`,
    `status`, `contractType`, `page`, `pageSize` — **không** khai bất kỳ trường định danh doanh
    nghiệp nào, nên client gửi thêm thì bị bỏ qua (D-12b). `paginatedEmployeeSchema` khớp
    `Paginated<Employee>`. `employeeListResponseSchema` cho danh sách rút gọn.

    **(b) `src/app/api/employees/route.ts`.** Một hàm `GET` phục vụ cả hai nhu cầu, phân biệt bằng
    tham số `mode` với hai giá trị `paged` (mặc định) và `all`:
    - `paged`: áp bốn bộ lọc, sắp theo `(full_name, id)` tăng dần (đúng index đã có), phân trang
      bằng `.range()`, đếm tổng bằng `count: "exact"`, kẹp số trang về khoảng hợp lệ giống V1
      (`totalPages` tối thiểu bằng 1 kể cả khi không có dòng nào), trả `Paginated<Employee>`.
    - `all`: trả toàn bộ nhân viên của doanh nghiệp hiện hành, sắp theo `(full_name, id)`.
    - Bộ lọc tìm kiếm: `public.tf_normalize(full_name || ' ' || code || ' ' || phone || ' ' || email)`
      chứa `public.tf_normalize(<từ khóa>)`. Vì PostgREST không gọi được biểu thức tùy ý trong
      `.filter()`, hiện thực phép lọc này bằng một hàm RPC `public.tf_search_employees` **hoặc**
      bằng cách gọi `.or()` trên các cột đã chuẩn hóa — chọn cách nào thì cũng phải giữ nguyên hành
      vi bỏ dấu và ghi rõ lựa chọn trong SUMMARY. Nếu chọn RPC thì hàm RPC là một migration mới và
      phải kèm một bước `npm run db:push`.
    - `.eq("company_id", companyId)` với `companyId` lấy từ `getSessionContext()`, luôn luôn — RLS
      là lớp hai độc lập, không phải lớp duy nhất.
    - Chỉ xuất `dynamic` và `GET`.

    **(c) `src/lib/data/employees.ts`.** `listEmployees(query: EmployeeQuery): Promise<Paginated<Employee>>`
    và `listAllEmployees(companyId: string): Promise<Employee[]>` — **giữ nguyên chữ ký cũ**, kể cả
    tham số `companyId` của `listAllEmployees`: tham số đó vẫn còn trong chữ ký để 17 call site
    không phải sửa, nhưng nó **không** được gửi lên server và **không** ảnh hưởng phạm vi truy vấn.
    Ghi một comment tại chỗ nói rõ điều đó, kèm ghi chú rằng plan 02-11 là nơi cân nhắc bỏ tham số
    thừa sau khi mọi call site đã chuyển xong. Cả hai hàm gọi qua `fetchJson` với schema tương ứng.

    **(d) Chuyển call site.** Đổi dòng import ở `src/components/layout/admin-topbar.tsx` sang
    `@/lib/data/employees`. Không đổi gì khác trong file.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/` trả về `0` trên mọi file.
    - `grep -c 'export const dynamic = "force-dynamic"' src/app/api/employees/route.ts` trả về 1.
    - `grep -c "@/lib/mock/service" src/components/layout/admin-topbar.tsx` trả về `0`.
    - Với cookie owner Ngọc Phát và server dev đang chạy: `curl -b <cookie> -s "http://localhost:3000/api/employees" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(r.total!==28)process.exit(1);if(r.items.length!==r.pageSize&&r.items.length!==r.total)process.exit(1)})"` thoát 0 — đúng 28 nhân viên của Ngọc Phát.
    - Cùng cookie: `curl -b <cookie> -s "http://localhost:3000/api/employees?mode=all"` trả về mảng 28 phần tử.
    - Cùng cookie: `curl -b <cookie> -s "http://localhost:3000/api/employees?search=nguyen"` trả về `total` lớn hơn 0 — tìm không dấu ra được tên có dấu.
    - Cùng cookie: `curl -b <cookie> -s "http://localhost:3000/api/employees?page=9999"` trả về `page` bằng `totalPages` và `items` khác rỗng.
    - Với cookie owner Bình Minh: cùng URL trả về `total` bằng 12 — cùng endpoint, hai kết quả khác nhau, chứng minh không bị cache tĩnh giữa hai người dùng.
    - Với cookie owner Ngọc Phát nhưng thêm tham số truy vấn mang định danh doanh nghiệp Bình Minh: thân phản hồi **giống hệt** lời gọi không có tham số đó.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; node -e "const {execSync}=require('child_process');const fs=require('fs');const fl=execSync('git ls-files src/app/api',{encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean);if(fl.length<2)process.exit(1);for(const f of fl){const s=fs.readFileSync(f,'utf8');if(/export\s+(async\s+)?function\s+(POST|PUT|PATCH|DELETE|HEAD|OPTIONS)/.test(s))process.exit(1);if(!/force-dynamic/.test(s))process.exit(1)}"</automated>
  </verify>
  <done>`/api/employees` trả đúng 28 dòng cho Ngọc Phát và 12 dòng cho Bình Minh trên cùng URL; tìm kiếm không dấu ra kết quả có dấu; tham số truy vấn giả không đổi được phạm vi.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trọn lát cắt phòng ban — đọc, ghi, audit</name>
  <files>src/lib/validation/api/departments.ts, src/lib/data/departments.ts, src/lib/data/mutations/departments.ts, src/app/api/departments/route.ts, src/app/admin/departments/departments-view.tsx, src/lib/data/__tests__/departments.test.ts</files>
  <read_first>
    - `src/lib/mock/service.ts` dòng 109-160 — `DepartmentWithStats`, `listDepartments`,
      `createDepartment`, `updateDepartment`, `deleteDepartment`: chữ ký chính xác và hai quy tắc
      dẫn xuất (`employeeCount` chỉ đếm nhân viên chưa nghỉ việc; `managerName` tra từ `employees`
      theo `managerId`, trả `null` nếu không tra được)
    - `src/lib/data/mutations/companies.ts` (tạo ở 02-04) — khuôn Server Action: `"use server"`,
      `getSessionContext()` đầu tiên, `requireRole()`, đọc dòng trước, ghi, đọc dòng sau,
      `logMutation()` trong cùng hàm
    - `src/lib/data/audit.ts` (tạo ở 02-04) — chữ ký `logMutation`
    - `supabase/migrations/0004_core_entities.sql` dòng 20-27 và 139-147 — cột thật của `departments`
      và khóa ngoại `departments_manager_id_fkey` (deferrable) tới `employees`
    - `src/app/admin/departments/departments-view.tsx` — dòng import và mọi chỗ gọi bốn hàm; giao
      diện không đổi
  </read_first>
  <behavior>
    - `listDepartments` trả về `employeeCount` chỉ đếm nhân viên có trạng thái khác `terminated`.
    - `listDepartments` trả về `managerName` bằng `null` khi `managerId` là `null` **và** khi `managerId` trỏ tới một nhân viên không còn tồn tại.
    - `listDepartments` cho một doanh nghiệp không có phòng ban nào trả về `[]`, không phải lỗi.
    - `updateDepartment` với một patch không đổi giá trị nào vẫn ghi đúng một dòng `audit_log`, với `before` và `after` bằng nhau — không có nhánh nào bỏ qua ghi audit vì "không có gì đổi" (edge DATA-06 empty).
    - Hai thao tác ghi trên cùng một dòng trong cùng một giây tạo ra hai dòng `audit_log` riêng biệt, không dòng nào đè lên dòng nào (edge DATA-06 adjacency).
    - `createDepartment`/`updateDepartment`/`deleteDepartment` gọi bởi vai trò `employee` hoặc `manager` ném lỗi quyền và không đổi dữ liệu.
  </behavior>
  <action>
    **(a) `src/lib/validation/api/departments.ts`.** `departmentWithStatsSchema` khớp
    `DepartmentWithStats` (bao gồm hai trường dẫn xuất) và `departmentListResponseSchema` là mảng
    của nó. `departmentInputSchema` khớp `DepartmentInput` cho đường ghi.

    **(b) `src/app/api/departments/route.ts`.** `GET` theo đúng khuôn 02-04. Truy vấn `departments`
    lọc `company_id` từ session, kèm:
    - `employeeCount`: đếm `employees` theo `department_id` với `status <> 'terminated'`.
    - `managerName`: `full_name` của `employees` có `id = departments.manager_id`, `null` nếu không tra được.
    Sắp xếp xác định theo `name` tăng dần rồi `id` tăng dần. Trả `[]` cho doanh nghiệp không có dòng
    nào — đó là dữ liệu, không phải lỗi. Parse bằng `departmentListResponseSchema` trước khi trả.

    **(c) `src/lib/data/departments.ts`.** Xuất `DepartmentWithStats` (kiểu chuyển nhà từ tầng giả
    lập sang đây, giữ nguyên tên và hình dạng) và `listDepartments(companyId: string): Promise<DepartmentWithStats[]>`
    gọi qua `fetchJson`. Tham số `companyId` giữ trong chữ ký nhưng không gửi lên server, cùng lý do
    và cùng comment như `listAllEmployees` ở Task 1. Re-export ba hàm ghi từ module Server Action.

    **(d) `src/lib/data/mutations/departments.ts`** — `"use server"`. Ba hàm giữ nguyên chữ ký cũ:
    `createDepartment(companyId, input)`, `updateDepartment(id, patch)`, `deleteDepartment(id)`.
    Mỗi hàm:
    1. `const { companyId, userId, role } = await getSessionContext()` — **bỏ qua** tham số
      `companyId` do client truyền và dùng giá trị từ session; ghi comment nói rõ tham số kia chỉ
      còn để giữ chữ ký.
    2. `requireRole(role, ["owner", "admin"])`.
    3. Đọc nguyên dòng trước (`select("*")`), thực hiện ghi với `.eq("company_id", companyId)`, đọc
      nguyên dòng sau.
    4. `logMutation` với `action` tương ứng, `entityTable: "departments"`, `before`/`after` là
      **nguyên dòng** (D-18), `reason: null`.
    Với `deleteDepartment`, `after` là `null` và `before` là dòng vừa xóa. Nếu `logMutation` ném lỗi
    thì hàm ghi cũng ném — không nuốt lỗi audit.
    `updateDepartment` **không** được có nhánh "không có gì đổi thì bỏ qua": người dùng đã thực hiện
    thao tác, và việc quyết định có gì đổi hay không đòi hỏi tính delta, đúng thứ D-18 loại bỏ.

    **(e) Chuyển call site.** Đổi dòng import ở `src/app/admin/departments/departments-view.tsx`:
    bốn hàm phòng ban và kiểu `DepartmentWithStats` lấy từ `@/lib/data/departments`, `listAllEmployees`
    lấy từ `@/lib/data/employees`. Không đổi gì khác trong file.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -c "@/lib/mock/service" src/app/admin/departments/departments-view.tsx` trả về `0`.
    - Với cookie owner Ngọc Phát: `curl -b <cookie> -s http://localhost:3000/api/departments` trả về mảng 5 phần tử, mỗi phần tử có khóa `employeeCount` và `managerName`.
    - Với cookie owner Bình Minh: cùng URL trả về mảng 4 phần tử.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_table='departments'"` tăng đúng 1 sau mỗi thao tác thêm/sửa/xóa trên giao diện; ba lần đo ghi vào SUMMARY.
    - Sau một lần sửa phòng ban **không đổi giá trị nào**: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select (before = after) from audit_log where entity_table='departments' order by id desc limit 1"` trả về `t`, và số dòng audit vẫn tăng 1.
    - Sau hai lần sửa liên tiếp cùng một phòng ban: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(distinct id) from audit_log where entity_table='departments' order by 1"` cho thấy hai dòng riêng biệt.
    - `npx vitest run src/lib/data/__tests__/departments.test.ts` thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/departments.test.ts</automated>
  </verify>
  <done>Màn hình phòng ban chạy trọn trên Postgres; ba thao tác ghi đều để lại đúng một dòng audit mang nguyên dòng trước và sau; sửa không đổi gì vẫn có audit.</done>
</task>

<task type="auto">
  <name>Task 3: Test hình dạng lỗi và ranh giới rỗng cho tầng dữ liệu mới</name>
  <files>src/lib/data/employees.ts, src/lib/data/departments.ts, src/lib/data/__tests__/departments.test.ts</files>
  <read_first>
    - `src/lib/data/fetch-json.ts` (tạo ở 02-04) — ba nhánh lỗi phải được test phủ
    - `src/hooks/use-mock-query.ts` dòng 39-56 — hợp đồng `error: string | null` mà tầng dữ liệu
      phải giữ nguyên
    - `src/lib/data/departments.ts`, `src/lib/data/employees.ts` — hàm cần test
  </read_first>
  <action>
    Tạo `src/lib/data/__tests__/departments.test.ts` (đặt chung một file cho cả hai module để tránh
    trùng lặp mock). Dùng `vi.stubGlobal("fetch", ...)` để giả lập tầng mạng — không gọi mạng thật
    trong Vitest. Sáu nhóm khẳng định:
    1. Phản hồi 200 với mảng rỗng → hàm trả `[]` và **không** ném; đây là ranh giới "rỗng khác lỗi"
      (edge DATA-05 empty).
    2. Phản hồi 200 với `Paginated` rỗng → `items` rỗng, `total` bằng 0, `totalPages` bằng 1.
    3. Phản hồi 500 kèm thân `{ error: "<thông điệp tiếng Việt>" }` → hàm ném `Error` với `message`
      đúng bằng thông điệp đó, để `useMockQuery` hiển thị lại nguyên văn.
    4. Phản hồi 500 với thân không đúng hình dạng → hàm vẫn ném `Error` với một `message` tiếng Việt
      khác rỗng, không bao giờ ném một giá trị không phải `Error` (nếu ném thứ khác thì `useMockQuery`
      rơi vào nhánh "lỗi không xác định" và người dùng mất thông tin).
    5. `fetch` ném lỗi mạng → hàm ném `Error` với `message` đúng bằng chuỗi lỗi kết nối mà tầng giả
      lập V1 đang dùng, để giao diện báo lỗi không đổi hành vi (D-12e).
    6. Phản hồi 200 nhưng thân sai hình dạng so với schema → hàm ném `Error` với thông điệp về định
      dạng dữ liệu, **không** trả về giá trị chưa được kiểm (nếu lọt, ràng buộc "không dùng `any`"
      của dự án thủng đúng tại chỗ dữ liệu vào — D-12d).
    Mọi assertion so sánh `err instanceof Error` và `err.message`, không so sánh kiểu lỗi tùy biến.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/departments.test.ts` thoát 0 và báo ít nhất 6 test.
    - `npm run test` thoát 0.
    - `npm run typecheck` thoát 0 — không `any` trong file test hay trong mock.
    - Sau khi tạm sửa `fetch-json.ts` để trả về thẳng thân phản hồi mà không parse schema, `npx vitest run src/lib/data/__tests__/departments.test.ts` thoát **khác 0**; hoàn tác thì trở lại 0. Hai mã thoát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run test &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Sáu nhánh của hợp đồng lỗi và ranh giới rỗng đều có test; cổng đã được chứng minh có răng bằng một lần cố tình làm đỏ rồi hoàn tác.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt → `GET /api/departments`, `GET /api/employees` | URL công khai, xác thực bằng cookie |
| Trình duyệt → Server Action ghi phòng ban | Next.js kiểm nguồn gửi sẵn |
| Tầng ứng dụng → Postgres | Lọc `company_id` là lớp 1, RLS là lớp 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | Vượt ranh giới doanh nghiệp qua tham số truy vấn | high | mitigate | Schema truy vấn không khai trường định danh doanh nghiệp; `.eq("company_id", …)` lấy từ `getSessionContext()`; acceptance criteria có lời gọi đối kháng kèm tham số giả và so thân phản hồi |
| T-02-05-02 | Information Disclosure | Route Handler bị cache giữa hai doanh nghiệp | high | mitigate | `force-dynamic` tường minh; acceptance criteria gọi **cùng URL** bằng hai cookie khác nhau và đòi hai kết quả khác nhau (28 và 12) |
| T-02-05-03 | Elevation of Privilege | Vai trò thấp ghi được phòng ban | high | mitigate | `requireRole(role, ["owner","admin"])` ở đầu cả ba Server Action, trước mọi lời gọi database |
| T-02-05-04 | Tampering | Ghi thành công mà không có vết audit | medium | mitigate | `logMutation` gọi trong cùng hàm ghi và lỗi audit làm cả thao tác thất bại; acceptance criteria đo số dòng audit sau từng thao tác |
| T-02-05-05 | Repudiation | Sửa không đổi gì không để lại vết | medium | mitigate | Cấm nhánh "không có gì đổi thì bỏ qua"; acceptance criteria khẳng định `before = after` mà dòng audit vẫn tăng |
| T-02-05-06 | Tampering | Dữ liệu máy chủ sai hình dạng lọt vào giao diện | medium | mitigate | Zod parse ở cả nơi trả lẫn nơi nhận (D-12d); test nhóm 6 khẳng định hàm ném thay vì trả giá trị chưa kiểm |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test` thoát 0
- Cùng một URL `/api/employees` trả 28 dòng cho Ngọc Phát và 12 dòng cho Bình Minh
- Tham số truy vấn mang định danh doanh nghiệp khác không đổi thân phản hồi
- Ba thao tác ghi phòng ban tạo đúng ba dòng audit
- Không file nào dưới `src/app/api/` xuất hàm HTTP ngoài `GET`
</verification>

<success_criteria>
Hai màn hình đầu tiên ngoài lát cắt tracer chạy trên Postgres thật với cùng một khuôn; hợp đồng lỗi
và ranh giới rỗng có test riêng; ranh giới doanh nghiệp được kiểm chứng bằng một lời gọi đối kháng
chứ không bằng lời khẳng định.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-05-SUMMARY.md` when done
</output>
