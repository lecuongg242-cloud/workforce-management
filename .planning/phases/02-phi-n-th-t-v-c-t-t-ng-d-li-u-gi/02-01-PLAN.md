---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.mts
  - vitest.setup.ts
  - src/__tests__/env-contract.test.ts
  - .env.local
  - .env.example
autonomous: false
requirements: [DATA-05]

user_setup:
  - service: supabase
    why: "Trình duyệt phải cầm được publishable key để gọi signInWithPassword (AUTH-01). Khóa hiện đang nằm ở biến không có tiền tố NEXT_PUBLIC_, nên `check:secrets` coi giá trị của nó là chuỗi cấm và sẽ báo đỏ ngay khi khóa xuất hiện trong bundle — đúng như thiết kế của cổng Phase 1."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Đổi tên biến SUPABASE_PUBLISHABLE_KEY sẵn có trong .env.local — giữ nguyên giá trị, chỉ đổi tên biến. Xóa hẳn dòng tên cũ, không giữ cả hai."
    dashboard_config: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npm run test` chạy Vitest và thoát 0 trên một bộ test không rỗng (ít nhất một file test được thu thập)."
    - "`npm run typecheck` và `npm run lint` vẫn thoát 0 sau khi thêm toàn bộ gói mới."
    - "`.env.local` chứa đúng bốn biến: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SECRET_KEY, POSTGRES_URL_NON_POOLING — không còn biến tên cũ, không thêm biến thừa."
    - "`npm run check:secrets` thoát 0 sau `npm run build`, và danh sách chuỗi cấm của nó không còn chứa giá trị khóa publishable."
    - "Phiên bản gói được ghim chính xác: @supabase/supabase-js 2.111.0, @supabase/ssr 0.12.4 — không dải ^ hay ~."
  artifacts:
    - path: "vitest.config.mts"
      provides: "Cấu hình Vitest đầu tiên của dự án, hiểu alias @/ qua vite-tsconfig-paths"
      contains: "tsconfigPaths"
    - path: "src/__tests__/env-contract.test.ts"
      provides: "Test khẳng định hợp đồng biến môi trường sau khi đổi tên"
    - path: "package.json"
      provides: "Script test/check:assertions/seed:auth và toàn bộ dependency mới"
      contains: "\"test\": \"vitest run\""
  key_links:
    - from: "vitest.config.mts"
      to: "tsconfig.json"
      via: "vite-tsconfig-paths đọc paths @/* để test import được @/lib/*"
      pattern: "tsconfigPaths\\(\\)"
    - from: "scripts/check-bundle-secrets.mjs"
      to: ".env.local"
      via: "process.loadEnvFile — biến không có tiền tố NEXT_PUBLIC_ và dài hơn 12 ký tự trở thành chuỗi cấm"
      pattern: "NEXT_PUBLIC_"
  prohibitions:
    - statement: "MUST NOT giữ đồng thời cả hai tên biến cho cùng một giá trị khóa publishable — giữ tên cũ nghĩa là giá trị vẫn nằm trong danh sách chuỗi cấm và cổng check:secrets sẽ báo đỏ đúng lúc khóa làm đúng việc của nó, dạy người đọc rằng cổng hay báo sai."
      category: transparency
---

<objective>
Dựng đủ hạ tầng để mọi thứ sau đó **kiểm chứng được**: cài hai gói Supabase với phiên bản ghim
chính xác, dựng bộ chạy test JavaScript đầu tiên của dự án (repo hiện có 0 test JS — Phase 1 chỉ
để lại pgTAP), khai báo trọn bộ npm script mà các plan sau sẽ dùng, và sửa hợp đồng biến môi
trường để trình duyệt cầm được publishable key mà không làm cổng `check:secrets` báo đỏ oan.

Purpose: mọi plan sau plan này khẳng định hành vi runtime. Không có Vitest thì các khẳng định đó
là văn xuôi. Không đổi tên biến publishable key thì `createBrowserClient()` không có khóa để chạy.

Output: `vitest.config.mts`, `vitest.setup.ts`, test đầu tiên, `package.json` với đủ script và
dependency, `.env.local` đúng hợp đồng bốn biến.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-03-SUMMARY.md
</context>

## Multi-Source Coverage Audit (toàn phase 2)

Audit này bao trọn phase, đặt ở plan đầu tiên; các plan sau không lặp lại.

| # | Nguồn | Mục | Plan phủ |
|---|---|---|---|
| GOAL-1 | ROADMAP §Phase 2 tiêu chí 1 | Đăng nhập Supabase Auth, phiên ở cookie, sống qua đóng/mở trình duyệt; đăng xuất mất quyền ngay | 02-04 |
| GOAL-2 | ROADMAP §Phase 2 tiêu chí 2 | Khách gõ thẳng URL `/admin/*` `/employee/*` bị chặn ở `middleware.ts` trước khi render | 02-04 |
| GOAL-3 | ROADMAP §Phase 2 tiêu chí 3 | Bốn vai trò thấy/làm đúng phần mình; đổi doanh nghiệp đổi dữ liệu; company hiện hành từ phiên server | 02-02, 02-04 |
| GOAL-4 | ROADMAP §Phase 2 tiêu chí 4 | Quản trị tạo tài khoản kèm mật khẩu tạm; buộc đổi mật khẩu lần đầu | 02-03, 02-10 |
| GOAL-5 | ROADMAP §Phase 2 tiêu chí 5 | Mọi màn hình chạy trên Postgres theo ngày giờ thật; `mock/db.ts`+`mock/seed.ts` bị xóa; mọi ghi để lại audit | 02-04..02-09, 02-11 |
| REQ-AUTH-01 | REQUIREMENTS.md | Đăng nhập Supabase Auth, phiên cookie, hết localStorage | 02-04 |
| REQ-AUTH-02 | REQUIREMENTS.md | Route bảo vệ ở `middleware.ts` | 02-04 |
| REQ-AUTH-03 | REQUIREMENTS.md | Phân quyền bốn vai trò | 02-02 (platform_admins), 02-04 (requireRole) |
| REQ-AUTH-04 | REQUIREMENTS.md | Tạo tài khoản kèm mật khẩu tạm, buộc đổi lần đầu | 02-03, 02-10 |
| REQ-AUTH-05 | REQUIREMENTS.md | Chọn doanh nghiệp, company hiện hành từ phiên server | 02-04 |
| REQ-DATA-05 | REQUIREMENTS.md | Thay 24 hàm `service.ts`, xóa `mock/db.ts`+`mock/seed.ts` | 02-01, 02-04..02-09, 02-11 |
| REQ-DATA-06 | REQUIREMENTS.md | Mọi ghi vào audit log | 02-04 (audit.ts), 02-05..02-10 |
| REQ-DATA-08 | REQUIREMENTS.md | Gỡ hằng số ngày cố định, chạy thời gian thật không lỗi hydration | 02-08, 02-09, 02-11 |
| RES-1 | RESEARCH §Standard Stack | `@supabase/supabase-js` 2.111.0 + `@supabase/ssr` 0.12.4, Vitest 4.1.10 stack | 02-01 |
| RES-2 | RESEARCH Pattern 1 | Ba client instance (browser / server cookie-bound / middleware) | 02-04 |
| RES-3 | RESEARCH Pattern 2 + Pitfall 1 | Middleware trả về đúng response đã ghi cookie | 02-04 |
| RES-4 | RESEARCH Pattern 3 | `getClaims()` khả dụng (ký bất đối xứng đã đo, PROJECT.md:149) | 02-04 |
| RES-5 | RESEARCH Pattern 4 | `getSessionContext()` là điểm chặn danh tính duy nhất | 02-04 |
| RES-6 | RESEARCH Pattern 5 + Pitfall 2 | Route Handler `force-dynamic`, chỉ GET, Zod hai đầu | 02-04..02-09 |
| RES-7 | RESEARCH Pattern 6 | Server Action ghi, CSRF sẵn có, audit trong cùng hàm | 02-04..02-10 |
| RES-8 | RESEARCH Pattern 7 | `auth.admin.createUser()` thay vì INSERT thẳng `auth.users` | 02-03, 02-10 |
| RES-9 | RESEARCH Pattern 8 + Pitfall 3 | Ép refresh sau khi xóa cờ, xử lý race refresh-token-rotation | 02-10 |
| RES-10 | RESEARCH Pattern 9 | Tắt đăng ký ở GoTrue, kiểm chứng bằng probe HTTP | 02-03 |
| RES-11 | RESEARCH Pitfall 4 | Giữ `middleware.ts`, không đổi tên theo docs Next 16 | 02-04 |
| RES-12 | RESEARCH Code Examples §D-19a | Rule ESLint cấm giờ máy trong client component | 02-11 |
| RES-13 | RESEARCH Validation Architecture | Vitest + guard cơ học D-12c + pgTAP mới | 02-01, 02-02, 02-04, 02-07, 02-11 |
| CTX-D-11/11a/11b | CONTEXT.md | `platform_admins` tối thiểu + `tf_is_platform_admin()` + RLS chặn hết | 02-02 |
| CTX-D-12/12a-e | CONTEXT.md | Hai transport, điểm chặn dùng chung, GET-only, Zod hai đầu, giữ hình dạng lỗi | 02-04..02-09 |
| CTX-D-13/13a | CONTEXT.md | Tắt đăng ký công khai ở cấu hình GoTrue | 02-03 |
| CTX-D-14/14a | CONTEXT.md | ~10 credential thật, `email_confirm: true`, 30 người `user_id = null` | 02-03 |
| CTX-D-15/15a | CONTEXT.md | Tách fixture khỏi seed; số assertion không được giảm | 02-02 |
| CTX-D-16/16a | CONTEXT.md | Cờ ở `app_metadata`, ép refresh sau khi xóa cờ | 02-03, 02-10 |
| CTX-D-17/17a | CONTEXT.md | Audit ở tầng server, ghi rõ giới hạn | 02-04, 02-11 |
| CTX-D-18/18a | CONTEXT.md | Lưu nguyên dòng before/after, ghi trước hệ quả GPS | 02-04 |
| CTX-D-19/19a | CONTEXT.md | "Hôm nay" do server cấp + rule ESLint cưỡng chế | 02-08, 02-09, 02-11 |

**Kết quả:** 0 mục MISSING. Mọi mục nguồn đều có plan phủ.

## Bộ dò cạnh — 14 mục, đối chiếu số lượng cho toàn phase

Phase này không có SPEC.md, nên bộ dò cạnh chạy trên văn bản requirement và trả về **14 mục, tất cả
ở trạng thái chưa giải quyết**. Bảng dưới là kết luận của người lập kế hoạch cho từng mục. Đẳng thức
không-âm-thầm-bỏ-sót: **14 = 9 được viết vào `must_haves` + 5 được nêu thành giả định gắn cờ.**

### 9 mục đã giải quyết và đã viết vào `must_haves`

| # | Requirement | Loại cạnh | Cách đọc cụ thể trong phase này | Kết luận | Plan mang truth |
|---|---|---|---|---|---|
| 1 | AUTH-03 | adjacency | Người dùng có **hai** membership với **hai vai trò khác nhau** | covered | 02-02, 02-04 |
| 2 | AUTH-03 | empty | Người dùng có **không** membership nào (fixture `…0004`) | covered | 02-02, 02-04 |
| 3 | AUTH-03 | ordering | Thứ tự danh sách doanh nghiệp khi hai mục so sánh bằng nhau | covered | 02-04 |
| 4 | DATA-05 | empty | Mỗi hàm `list*` trả gì cho doanh nghiệp không có dòng nào, và có phân biệt được với lỗi không | covered | 02-05, 02-06, 02-08, 02-09 |
| 5 | DATA-05 | adjacency | `code` nhân viên trùng trong cùng doanh nghiệp so với trùng ở doanh nghiệp khác | covered | 02-07 |
| 6 | DATA-05 | ordering | Hai nhân viên **trùng tên** khi phân trang | covered | 02-07 |
| 7 | DATA-06 | empty | Một lần sửa **không đổi giá trị nào** có còn sinh dòng audit không | covered — **có**, `before` bằng `after` | 02-05 |
| 8 | DATA-06 | adjacency | Hai lần ghi trên cùng một dòng trong cùng một giây | covered | 02-05 |
| 9 | DATA-06 | ordering | Thứ tự đọc lại khi hai dòng audit có cùng `created_at` | **backstop** | 02-09 |

Mục 9 được viết dưới dạng đánh dấu có `verification: backstop` trong `must_haves.truths` của plan
02-09, vì Phase 2 chưa có màn hình nào đọc `audit_log` nên chưa có đường nào chứng minh hành vi đọc
lại; nó sẽ được xác nhận khi Phase 5 hoặc Phase 6 dựng đường đọc.

### 5 mục chưa phân loại được — nêu thành giả định gắn cờ, không âm thầm bỏ

Bộ dò cạnh trả về `unclassified` cho năm requirement dưới đây, nghĩa là bộ phân loại hình dạng dữ
liệu không tìm được trục cạnh nào áp dụng. Theo giao thức, chúng **giữ nguyên trạng thái chưa giải
quyết** và được nêu ra ở đây thay vì bị suy diễn thành một cạnh giả:

| # | Requirement | Giả định của người lập kế hoạch | Vì sao chưa thành một truth |
|---|---|---|---|
| 10 | AUTH-01 | Vòng đời phiên (sống qua đóng/mở trình duyệt, mất quyền ngay khi đăng xuất) là hành vi của cookie do thư viện quản lý, không có trục cạnh dữ liệu | Không tự động hóa được bằng Vitest hay pgTAP; nghiệm thu tay ở plan 02-04 bước 3 và 5 là bằng chứng duy nhất |
| 11 | AUTH-02 | Cổng chặn route là một hàm quyết định trên `(đường dẫn, có claims, cờ đổi mật khẩu)`, phủ hết bằng bảng chân trị chứ không bằng cạnh | Đã phủ bằng 14 test bảng chân trị ở plan 02-04 và 02-10, nhưng đó là độ phủ nhánh, không phải một cạnh dữ liệu |
| 12 | AUTH-04 | Rủi ro thật nằm ở **thứ tự thao tác** và **thời điểm làm mới JWT**, không ở hình dạng dữ liệu | Đã phủ bằng test thứ tự lời gọi ở plan 02-10, nhưng đó là bất biến về trình tự, không phải một cạnh |
| 13 | AUTH-05 | Rủi ro thật là **nguồn** của doanh nghiệp hiện hành (phiên server so với giá trị client khai), không phải một giá trị biên | Đã phủ bằng test đối kháng ở plan 02-04, nhưng đó là ranh giới tin cậy, không phải một cạnh |
| 14 | DATA-08 | Rủi ro thật là **lệch giữa hai lần vẽ** và **lệch múi giờ**, không phải một giá trị biên của ngày | Đã phủ bằng test hai múi giờ ở plan 02-08 và rule ESLint ở plan 02-11, nhưng cả hai là bất biến môi trường, không phải một cạnh |

Năm mục này được đưa vào SUMMARY của phase như giả định còn mở, để bước nghiệm thu sau đọc được
chúng thay vì phải suy ra từ sự vắng mặt.

## Điều cấm được nhớ lại (recall) — kết quả sau bước lọc

Phase không có SPEC.md nên không có mục `## Prohibitions` để nâng lên. Sau vòng nhớ lại rộng rồi
lọc theo giá trị/an toàn/đạo đức, **14 điều cấm bespoke** được giữ và phân bổ vào khối
`must_haves.prohibitions` của các plan tương ứng: 02-01 ×1, 02-02 ×1, 02-03 ×1, 02-04 ×1, 02-05 ×1,
02-06 ×1, 02-07 ×2, 02-08 ×2, 02-09 ×1, 02-10 ×2, 02-11 ×1. Tất cả được viết **không kèm mô tả cơ
chế kiểm** (không có khóa `check_*` nào), nên mỗi mục được xử lý là "chưa kiểm chứng, có gắn cờ"
thay vì được coi là đã xanh — đúng theo giao thức cho phase không có SPEC.

**Ba ứng viên bị loại theo quy tắc quy chiếu về chuẩn** (đã có công cụ chuyên trách, không đúc lại
thành điều cấm riêng):
- Đọc dữ liệu xuyên doanh nghiệp qua bảng audit — thuộc kiểm soát truy cập chuẩn; đã có trong
  `<threat_model>` của các plan và thuộc phạm vi `/gsd-secure-phase`.
- Thông điệp đăng nhập tiết lộ email có tồn tại hay không — chuẩn OWASP; thuộc `/gsd-secure-phase`.
- Nâng quyền qua nhánh metadata mà client sửa được — chuẩn; đã bị D-16 khóa và có grep chặn ở plan
  02-10, không cần đúc thành điều cấm riêng.

## Artifacts this phase produces (plan 02-01)

| Loại | Tên |
|---|---|
| File cấu hình | `vitest.config.mts`, `vitest.setup.ts` |
| npm script | `test`, `test:ui`(không), `check:assertions`, `seed:auth` |
| Test | `src/__tests__/env-contract.test.ts` |
| Biến môi trường | `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (đổi tên từ biến cũ) |
| Dependency | `@supabase/supabase-js@2.111.0`, `@supabase/ssr@0.12.4` |
| devDependency | `vitest@4.1.10`, `@vitejs/plugin-react@6.0.5`, `vite-tsconfig-paths@6.1.1`, `jsdom`, `@testing-library/react@16.3.2`, `@testing-library/dom` |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Cổng hợp lệ gói npm trước khi cài</name>
  <what-built>Chưa build gì — đây là cổng chặn trước `npm install`, theo Package Legitimacy Gate
  của `02-RESEARCH.md §Package Legitimacy Audit`. Ba gói bị gắn cờ `SUS (too-new)` và chưa được
  miễn: `vitest`, `@vitejs/plugin-react`, `jsdom`.

  Hai gói `@supabase/supabase-js` và `@supabase/ssr` đã được chủ dự án miễn khỏi cổng này
  (chúng là gói chính chủ Supabase, hàng chục triệu lượt tải mỗi tuần) — **không kiểm lại hai
  gói đó**.</what-built>
  <how-to-verify>
    1. Mở `https://www.npmjs.com/package/vitest` — xác nhận Repository trỏ `github.com/vitest-dev/vitest`
       và weekly downloads ở mức hàng chục triệu.
    2. Mở `https://www.npmjs.com/package/@vitejs/plugin-react` — Repository trỏ `github.com/vitejs/vite-plugin-react`.
    3. Mở `https://www.npmjs.com/package/jsdom` — Repository trỏ `github.com/jsdom/jsdom`.
    4. Nếu bất kỳ mục nào không khớp, KHÔNG duyệt — báo lại tên gói lệch.
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu cả ba khớp, hoặc nêu gói nào lệch</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Cài gói ghim phiên bản và dựng Vitest — bộ chạy test JS đầu tiên của dự án</name>
  <files>package.json, vitest.config.mts, vitest.setup.ts, .env.local</files>
  <read_first>
    - `package.json` — khối `scripts` và hai khối dependency hiện tại; chép đúng phong cách chuỗi
      lệnh trần (không cross-env, không wrapper) khi thêm script mới
    - `tsconfig.json` — alias `@/*` trỏ `./src/*`; đây là lý do bắt buộc phải có `vite-tsconfig-paths`
    - `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md` §Standard Stack — bảng
      phiên bản đã xác minh bằng `npm view` trong phiên nghiên cứu
    - `eslint.config.mjs` — mảng `ignores` hiện tại; file cấu hình Vitest mới không được lọt vào lint lỗi
  </read_first>
  <action>
    **(a) Cài dependency runtime, ghim tuyệt đối.** `npm install --save-exact @supabase/supabase-js@2.111.0 @supabase/ssr@0.12.4`.
    Ghim tuyệt đối (không `^`, không `~`) vì `02-RESEARCH.md` ghi rõ "Valid until: 14 ngày" — một
    bản minor tự trôi giữa lúc thực thi phase làm mọi pattern cookie adapter trong nghiên cứu mất
    hiệu lực âm thầm.

    **(b) Cài devDependency test.** `npm install --save-dev --save-exact vitest@4.1.10 @vitejs/plugin-react@6.0.5 vite-tsconfig-paths@6.1.1 @testing-library/react@16.3.2` cộng
    `jsdom` và `@testing-library/dom` (hai gói này lấy bản mới nhất, không ghim tuyệt đối vì chúng
    chỉ là môi trường test).

    **(c) `vitest.config.mts`.** Đuôi `.mts` bắt buộc — dự án không đặt `"type": "module"` trong
    `package.json`, nên `.ts` sẽ bị Node đọc như CommonJS. Nội dung: `defineConfig` từ `vitest/config`,
    `plugins: [react(), tsconfigPaths()]`, `test.environment: "jsdom"`, `test.globals: false`
    (dự án dùng TypeScript strict — import `describe`/`it`/`expect` tường minh từ `vitest`),
    `test.setupFiles: ["./vitest.setup.ts"]`, `test.include` phủ cả `src/**/*.test.ts`,
    `src/**/*.test.tsx` và `src/**/__tests__/**/*.test.ts`, `test.exclude` loại `node_modules` và `.next`.

    **(d) `vitest.setup.ts`.** Import `@testing-library/jest-dom` chỉ khi gói đó được cài — plan này
    KHÔNG cài `@testing-library/jest-dom`, nên file setup ở bước này chỉ chứa `import "@testing-library/react"`-free
    nội dung tối thiểu: đăng ký `afterEach(cleanup)` từ `@testing-library/react` để test component
    không rò DOM giữa các case.

    **(e) Bốn script mới trong `package.json`**, đặt cạnh nhóm `db:*` hiện có, đúng phong cách chuỗi
    lệnh trần:
    - `"test": "vitest run"` — chạy một lần, KHÔNG chế độ watch (chế độ watch treo executor).
    - `"check:assertions": "node scripts/check-pgtap-assertions.mjs"` — file script do plan 02-02 tạo.
    - `"seed:auth": "node --env-file=.env.local scripts/seed-auth.mjs"` — file script do plan 02-03 tạo,
      quy ước `--env-file` chép từ `db:seed`.
    Hai script sau trỏ tới file chưa tồn tại ở plan này; đó là chủ ý — `package.json` là file dùng
    chung nên mọi mục thêm vào nó gom hết về một plan để các plan sau chạy song song được mà không
    tranh file.

    **(f)** Thêm `coverage/` và `vitest.config.mts.timestamp*` vào mảng `ignores` của `eslint.config.mjs`
    nếu Vitest sinh ra chúng; nếu không sinh thì bỏ qua bước này.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};const want={'@supabase/supabase-js':'2.111.0','@supabase/ssr':'0.12.4','vitest':'4.1.10','@vitejs/plugin-react':'6.0.5','vite-tsconfig-paths':'6.1.1','@testing-library/react':'16.3.2'};for(const[k,v]of Object.entries(want)){if(d[k]!==v){console.error(k+' = '+d[k]+', muon '+v);process.exit(1)}}"` thoát 0 — sáu gói ghim đúng phiên bản, không dải.
    - `node -e "const s=require('./package.json').scripts;for(const k of ['test','check:assertions','seed:auth','db:push','db:seed','test:rls','test:db','check:secrets']){if(!s[k]){console.error('thieu script '+k);process.exit(1)}}"` thoát 0.
    - `node -e "const s=require('./package.json').scripts.test;if(/--watch|^vitest$/.test(s)){console.error('script test o che do watch');process.exit(1)}"` thoát 0.
    - `test -f vitest.config.mts` đúng, và `grep -c 'tsconfigPaths' vitest.config.mts` trả về ít nhất 1.
    - `npm run typecheck` thoát 0.
    - `npm run lint` thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};const want={'@supabase/supabase-js':'2.111.0','@supabase/ssr':'0.12.4','vitest':'4.1.10'};for(const[k,v]of Object.entries(want)){if(d[k]!==v)process.exit(1)}"</automated>
  </verify>
  <done>Sáu gói ghim đúng phiên bản; `vitest.config.mts` tồn tại và dùng `tsconfigPaths`; ba script mới có mặt; typecheck và lint xanh.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Đổi tên biến publishable key trong .env.local (người thực hiện)</name>
  <files>.env.local, .env.example</files>
  <action>
    Executor **không đọc và không ghi được** bất kỳ file `.env*` nào — bộ phân loại quyền của môi
    trường chặn đường dẫn đó (đã kiểm chứng trong phiên lập kế hoạch: lệnh đọc `.env.example` trả về
    "File is in a directory that is denied"). Đây là lý do bước này là thao tác của người, không
    phải lười tự động hóa. Executor chỉ trình bày hướng dẫn rồi dừng chờ.

    Vì sao phải đổi: `scripts/check-bundle-secrets.mjs` xây danh sách chuỗi cấm từ **giá trị** của
    mọi biến trong `.env.local` không mang tiền tố công khai và dài hơn 12 ký tự. Publishable key
    hiện đang nằm ở biến không có tiền tố đó. Nhưng `createBrowserClient()` bắt buộc phải có key này
    trong bundle trình duyệt — nên giữ nguyên tên cũ nghĩa là cổng `check:secrets` sẽ báo đỏ ngay khi
    khóa làm đúng việc của nó. Đổi tên là cách sửa đúng; nới cổng là cách sai.
  </action>
  <instructions>
    1. Mở `.env.local` ở gốc repo.
    2. Tìm dòng bắt đầu bằng `SUPABASE_PUBLISHABLE_KEY=`. Đổi **tên biến** thành
       `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, **giữ nguyên giá trị**. Xóa hẳn dòng tên cũ —
       không giữ cả hai dòng.
    3. File sau khi sửa phải có đúng bốn dòng biến: `NEXT_PUBLIC_SUPABASE_URL`,
       `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, `POSTGRES_URL_NON_POOLING`.
    4. Mở `.env.example` và đổi tên biến tương ứng để mẫu khớp thực tế.
    5. Báo "done" để executor chạy bước kiểm chứng tự động.
  </instructions>
  <verification>
    `node -e "process.loadEnvFile('.env.local');const need=['NEXT_PUBLIC_SUPABASE_URL','NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY','SUPABASE_SECRET_KEY','POSTGRES_URL_NON_POOLING'];const miss=need.filter(k=>!process.env[k]);if(miss.length){console.error('thieu: '+miss.join(','));process.exit(1)}if(process.env.SUPABASE_PUBLISHABLE_KEY){console.error('ten bien cu van con');process.exit(1)}"` thoát 0.
  </verification>
  <acceptance_criteria>
    - Lệnh kiểm ở `<verification>` thoát 0.
    - `.env.local` định nghĩa đúng bốn biến, trong đó đúng hai biến mang tiền tố công khai.
    - Tên biến publishable key cũ không còn xuất hiện trong `.env.local` lẫn `.env.example`.
  </acceptance_criteria>
  <resume-signal>Gõ "done" sau khi đã sửa cả `.env.local` lẫn `.env.example`</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Test hợp đồng biến môi trường và chứng minh cổng bí mật vẫn có răng</name>
  <precondition>`.env.local` chứa dòng `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=` khác rỗng (Task 3 đã xong) — kiểm bằng `node -e "process.loadEnvFile('.env.local');process.exit(process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY?0:1)"`</precondition>
  <files>vitest.config.mts, src/__tests__/env-contract.test.ts, .env.local</files>
  <read_first>
    - `scripts/check-bundle-secrets.mjs` — hàm `loadSecretValuesFromEnvLocal()` (dòng 47-67) là nơi
      quy tắc "biến không mang tiền tố công khai và dài hơn 12 ký tự thì giá trị của nó là chuỗi cấm"
      được cài đặt; test mới phải khẳng định đúng quy tắc này, không phải một quy tắc tưởng tượng
    - `vitest.config.mts` — vừa tạo ở Task 2; test mới phải nằm trong `test.include`
    - `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-03-SUMMARY.md` — ghi nhận cổng
      `check:secrets` quét cả `.next/static` lẫn `.next/server/app` và vì sao cả hai đều cần
  </read_first>
  <action>
    **(a) `src/__tests__/env-contract.test.ts`.** Test này KHÔNG đọc giá trị bí mật và không in ra
    bất cứ giá trị nào — nó chỉ khẳng định *hình dạng* hợp đồng. Dùng `process.loadEnvFile(".env.local")`
    rồi đọc lại nội dung thô để biết đúng tên biến nào được định nghĩa trong file (đúng cách
    `check-bundle-secrets.mjs` đang làm), và khẳng định bốn điều:
    1. Bốn tên biến bắt buộc đều có mặt và giá trị khác rỗng.
    2. Tên biến publishable key cũ (không có tiền tố công khai) không còn xuất hiện.
    3. Đúng hai biến mang tiền tố công khai, và cả hai đều thuộc danh sách cho phép
       (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`).
    4. `SUPABASE_SECRET_KEY` KHÔNG mang tiền tố công khai — nếu ai đó "sửa" nó cho tiện thì test đỏ.
    Mọi assertion so sánh tên biến, không bao giờ so sánh giá trị.

    **(b) Chứng minh cổng có răng.** Chạy `npm run build` rồi `npm run check:secrets` và xác nhận
    thoát 0. Sau đó chứng minh cổng không phải luôn xanh: tạm thêm vào một Client Component bất kỳ
    một biểu thức đọc `process.env.SUPABASE_SECRET_KEY` và render nó, chạy lại `npm run build` +
    `npm run check:secrets`, xác nhận **thoát khác 0**, rồi hoàn tác thay đổi tạm và chạy lại để
    xác nhận trở về 0. Ghi cả ba mã thoát vào SUMMARY. Không commit đoạn code tạm.
  </action>
  <acceptance_criteria>
    - `npm run test` thoát 0 và output báo ít nhất 1 test file, ít nhất 4 test đã chạy.
    - `npx vitest run src/__tests__/env-contract.test.ts` thoát 0.
    - `npm run build` thoát 0.
    - `npm run check:secrets` thoát 0 và output chứa chuỗi `check:secrets OK`.
    - Ba mã thoát của thủ tục phá hoại có kiểm soát ở bước (b) được ghi vào SUMMARY theo đúng thứ tự 0 → khác 0 → 0.
    - `git status --porcelain src/` không liệt kê file nào mang đoạn code tạm của bước (b).
  </acceptance_criteria>
  <verify>
    <automated>npm run test &amp;&amp; npm run build &amp;&amp; npm run check:secrets</automated>
  </verify>
  <done>Test hợp đồng biến môi trường xanh; `check:secrets` xanh trên build thật; cổng đã được chứng minh có răng bằng một lần cố tình làm đỏ rồi hoàn tác.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | Mã của bên thứ ba vào cây phụ thuộc lúc cài |
| `.env.local` → bundle client | Giá trị bí mật có thể lọt xuống trình duyệt qua một dòng code vô ý |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-SC | Tampering | `npm install` (vitest, @vitejs/plugin-react, jsdom) | high | mitigate | Task 1 là `checkpoint:human-verify` chặn cứng trước khi cài, đối chiếu repo GitHub chính chủ của từng gói `SUS` chưa được miễn |
| T-02-01-02 | Tampering | Ghim phiên bản | medium | mitigate | `--save-exact` cho sáu gói cốt lõi; acceptance criteria khẳng định không có dải `^`/`~`, chặn một bản minor trôi vào giữa phase |
| T-02-01-03 | Information Disclosure | `SUPABASE_SECRET_KEY` lọt bundle | high | mitigate | Task 4(b) chạy `check:secrets` trên build thật VÀ chứng minh cổng có răng bằng một lần cố tình làm đỏ; test hợp đồng chặn việc gắn tiền tố công khai vào biến bí mật |
| T-02-01-04 | Information Disclosure | Test in giá trị khóa ra log CI | medium | mitigate | `env-contract.test.ts` chỉ so sánh **tên** biến, không bao giờ so sánh hay in giá trị |
</threat_model>

<verification>
- `npm run typecheck` thoát 0
- `npm run lint` thoát 0
- `npm run test` thoát 0 với ít nhất 1 file test được thu thập
- `npm run build && npm run check:secrets` thoát 0
</verification>

<success_criteria>
Bộ chạy test JavaScript tồn tại và chạy được; sáu gói ghim đúng phiên bản đã xác minh; hợp đồng
biến môi trường đổi xong và được một test khẳng định; cổng `check:secrets` vẫn xanh trên build thật
và đã được chứng minh không phải luôn xanh.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-01-SUMMARY.md` when done
</output>
