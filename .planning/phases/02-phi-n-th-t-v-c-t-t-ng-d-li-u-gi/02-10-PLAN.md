---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 10
type: execute
wave: 9
depends_on: ["02-09"]
files_modified:
  - src/lib/supabase/admin.ts
  - src/lib/data/mutations/accounts.ts
  - src/middleware.ts
  - src/app/(auth)/doi-mat-khau/page.tsx
  - src/app/(auth)/doi-mat-khau/change-password-form.tsx
  - src/lib/validation/schemas.ts
  - src/app/admin/employees/[id]/employee-detail-view.tsx
  - src/lib/constants.ts
  - src/__tests__/middleware-gate.test.ts
  - src/lib/data/__tests__/accounts.test.ts
  - src/__tests__/admin-client-scope.test.ts
autonomous: false
requirements: [AUTH-03, AUTH-04, DATA-06]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Quản trị mở hồ sơ một nhân viên chưa có tài khoản, bấm tạo tài khoản, và nhận lại một mật khẩu tạm hiển thị đúng một lần trên màn hình (AUTH-04)."
    - "Tài khoản vừa tạo đăng nhập được ngay, không kẹt ở trạng thái chờ xác nhận email (D-14a)."
    - "Nhân viên đăng nhập lần đầu bị đưa tới trang đổi mật khẩu và **không** đi tiếp được tới bất kỳ trang nào khác trong khu vực đã đăng nhập cho tới khi đổi xong (AUTH-04)."
    - "Cờ buộc đổi mật khẩu đọc từ nhánh metadata phía server trong JWT; không lời gọi nào của client sửa được nó (D-16)."
    - "Sau khi đổi mật khẩu thành công, phiên được làm mới **trong cùng request** trước khi người dùng bị chuyển hướng, nên request kế tiếp mang token đã sạch cờ và người dùng không bị đá ngược lại (D-16a)."
    - "Nếu lần làm mới phiên đầu tiên thất bại vì token đã bị xoay, hệ thống thử lại một lần trước khi báo lỗi — và nếu vẫn thất bại thì thông điệp nói rõ phải đăng nhập lại, không để người dùng mắc kẹt im lặng."
    - "Khóa bí mật chỉ được import từ các module chạy phía server; `npm run check:secrets` vẫn thoát 0 sau khi build."
    - "Tạo tài khoản để lại đúng một dòng `audit_log`, và dòng đó **không** chứa mật khẩu tạm ở bất kỳ trường nào (DATA-06)."
    - "Vai trò `manager` và `employee` không tạo được tài khoản cho người khác (AUTH-03)."
  artifacts:
    - path: "src/lib/supabase/admin.ts"
      provides: "Client dùng khóa bí mật, chỉ import được từ module chạy phía server"
      exports: ["createAdminSupabase"]
    - path: "src/lib/data/mutations/accounts.ts"
      provides: "Server Action tạo tài khoản và hoàn tất đổi mật khẩu bắt buộc"
      exports: ["createEmployeeAccount", "completeForcedPasswordChange"]
    - path: "src/app/(auth)/doi-mat-khau/page.tsx"
      provides: "Trang đổi mật khẩu bắt buộc"
    - path: "src/__tests__/admin-client-scope.test.ts"
      provides: "Cổng cơ học chặn khóa bí mật rò sang module client"
  key_links:
    - from: "src/middleware.ts"
      to: "app_metadata trong JWT"
      via: "đọc cờ buộc đổi mật khẩu từ claims, không truy vấn database"
      pattern: "must_change_password"
    - from: "src/lib/data/mutations/accounts.ts"
      to: "src/lib/supabase/admin.ts"
      via: "auth.admin.createUser và auth.admin.updateUserById cần khóa bí mật"
      pattern: "createAdminSupabase"
  prohibitions:
    - statement: "MUST NOT lưu mật khẩu tạm ở bất kỳ nơi nào ngoài phản hồi một lần của Server Action — không vào `audit_log`, không vào cột nào của `employees`, không vào log server, không vào thông báo nào tồn tại lâu hơn phiên làm việc đang mở."
      category: privacy
    - statement: "MUST NOT để người dùng mắc kẹt giữa hai trạng thái sau khi đổi mật khẩu thành công. Nếu bước làm mới phiên thất bại, thông điệp phải nói rõ mật khẩu **đã** đổi và người dùng cần đăng nhập lại — im lặng ở đây tạo ra một người dùng tin rằng thao tác của mình thất bại và thử lại bằng mật khẩu cũ."
      category: safety
---

<objective>
Đóng vòng vận hành của tài khoản: quản trị tạo tài khoản kèm mật khẩu tạm, và người nhận bắt buộc
đổi mật khẩu trước khi làm được bất cứ việc gì.

Purpose: cho tới trước plan này, cờ buộc đổi mật khẩu do `scripts/seed-auth.mjs` đặt cho cả 10 tài
khoản là một cờ **không có ai đọc** — đúng loại khoảng trống mà D-16a cảnh báo. Plan này gắn người
đọc cho nó và làm cho luồng thoát ra khỏi cổng thật sự thoát được.

Output: client khóa bí mật giới hạn phạm vi, hai Server Action, cổng ở `middleware.ts`, trang đổi
mật khẩu, và ba cổng test.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-03-SUMMARY.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-04-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Client Supabase | `createAdminSupabase()` ở `src/lib/supabase/admin.ts` |
| Server Action | `createEmployeeAccount(employeeId)`, `completeForcedPasswordChange(newPassword)` |
| Route | `/doi-mat-khau` (`page.tsx` + `change-password-form.tsx`) |
| Schema Zod | `changePasswordSchema` trong `src/lib/validation/schemas.ts` |
| Nhãn | Nhóm nhãn tiếng Việt cho luồng tài khoản trong `src/lib/constants.ts` |
| Test | `admin-client-scope.test.ts`, `accounts.test.ts`, mở rộng `middleware-gate.test.ts` |

<tasks>

<task type="auto">
  <name>Task 1: Client khóa bí mật giới hạn phạm vi và Server Action tạo tài khoản nhân viên</name>
  <files>src/lib/supabase/admin.ts, src/lib/data/mutations/accounts.ts, src/app/admin/employees/[id]/employee-detail-view.tsx, src/lib/constants.ts, src/__tests__/admin-client-scope.test.ts</files>
  <read_first>
    - `scripts/seed-auth.mjs` (tạo ở 02-03) — lời gọi `auth.admin.createUser` đã chạy thật, gồm cờ
      xác nhận email và cách đặt cờ buộc đổi mật khẩu; Server Action ở đây làm cùng việc đó cho một
      người, và phải khớp hành vi để tài khoản tạo từ hai đường không khác nhau
    - `src/lib/supabase/server.ts` (tạo ở 02-04) — khuôn tạo client; client khóa bí mật khác ở chỗ
      **không** bind cookie và **không** giữ phiên
    - `scripts/check-bundle-secrets.mjs` dòng 47-82 — cách danh sách chuỗi cấm được dựng; đây là
      cổng sẽ đỏ nếu khóa bí mật lọt bundle
    - `src/lib/data/mutations/employees.ts` (tạo ở 02-07) — khuôn Server Action kèm audit và
      `requireRole`
    - `src/app/admin/employees/[id]/employee-detail-view.tsx` — nơi thêm nút và hộp thoại; đọc trước
      để dùng lại đúng thành phần giao diện và token thiết kế đang có, không tạo kiểu mới
    - `src/lib/constants.ts` — nơi mọi chuỗi hiển thị tiếng Việt phải nằm
  </read_first>
  <action>
    **(a) `src/lib/supabase/admin.ts`.** `createAdminSupabase()` gọi `createClient` từ
    `@supabase/supabase-js` (không phải `@supabase/ssr` — client này không dính cookie) với
    `NEXT_PUBLIC_SUPABASE_URL` và `SUPABASE_SECRET_KEY`, kèm `auth: { persistSession: false, autoRefreshToken: false }`.
    Thiếu biến khóa bí mật thì ném lỗi nêu đúng tên biến. Comment đầu file phải nói rõ: module này
    bỏ qua toàn bộ RLS, chỉ được import từ file có chỉ thị chạy phía server, và mọi lời gọi qua nó
    phải tự kiểm quyền trước vì database sẽ không kiểm hộ.

    **(b) `src/lib/data/mutations/accounts.ts`** — `"use server"`. Hàm
    `createEmployeeAccount(employeeId: string): Promise<{ email: string; temporaryPassword: string }>`:
    1. `getSessionContext()` → `requireRole(role, ["owner", "admin"])`.
    2. Đọc dòng `employees` theo `employeeId` **kèm** `.eq("company_id", companyId)` bằng client
      cookie-bound (để RLS vẫn là lớp hai). Không tìm thấy → ném `Error` tiếng Việt.
    3. Nếu dòng đó đã có tài khoản liên kết → ném `Error` tiếng Việt nói rõ nhân viên đã có tài
      khoản; không tạo tài khoản thứ hai và không đặt lại mật khẩu của tài khoản đang có.
    4. Sinh mật khẩu tạm bằng `randomBytes` của `node:crypto`, độ dài đủ mạnh.
    5. `createAdminSupabase().auth.admin.createUser({ email: <email của dòng employees>, password, email_confirm: true, app_metadata: { must_change_password: true } })`.
      Cờ xác nhận email là bắt buộc (D-14a). Cờ buộc đổi mật khẩu đặt ở nhánh metadata phía server,
      tuyệt đối không ở nhánh mà client sửa được (D-16).
    6. Chèn dòng `memberships` với vai trò suy từ `employees.system_role`, trạng thái active; cập
      nhật `employees.user_id`.
    7. `logMutation` `action: "update"`, `entityTable: "employees"`, `entityId: employeeId`,
      `before`/`after` là nguyên dòng trước và sau. **Trước khi ghi audit, loại bỏ mọi trường có
      thể mang giá trị nhạy cảm khỏi hai ảnh chụp** — ở bảng này không có cột mật khẩu nên hai ảnh
      chụp nguyên dòng là an toàn, nhưng phải khẳng định điều đó bằng một assertion chứ không bằng
      giả định (xem acceptance criteria).
    8. Trả về email và mật khẩu tạm. Đây là **lần duy nhất** mật khẩu tạm rời khỏi hàm.

    **(c) Giao diện kích hoạt.** Ở `employee-detail-view.tsx`, thêm một nút trong khu vực thao tác
    của hồ sơ, chỉ hiển thị khi vai trò phiên là quản trị **và** nhân viên chưa có tài khoản liên
    kết. Bấm nút gọi Server Action, rồi mở một hộp thoại dùng lại thành phần hộp thoại sẵn có của
    dự án, hiển thị email và mật khẩu tạm kèm một dòng cảnh báo rằng mật khẩu chỉ hiện một lần và
    cần chuyển cho nhân viên qua kênh an toàn. Mọi chuỗi hiển thị đặt trong `src/lib/constants.ts`
    theo quy ước dự án. Tuân thủ token thiết kế: mỗi khu vực chỉ một nút nền đặc — nếu khu vực thao
    tác đã có nút nền đặc thì nút mới dùng biến thể viền hoặc nhạt.

    **(d) `src/__tests__/admin-client-scope.test.ts`.** Cổng cơ học chặn rò khóa bí mật:
    1. Quét toàn bộ `src/` tìm mọi file import `@/lib/supabase/admin`; khẳng định số file **lớn hơn 0**.
    2. Với mỗi file đó, khẳng định file có chỉ thị `"use server"` ở đầu **hoặc** nằm dưới
      `src/lib/data/mutations/`.
    3. Khẳng định không file nào có chỉ thị `"use client"` mà import module đó.
    4. Khẳng định không file nào dưới `src/components/` hay khớp `*-view.tsx` import module đó.
    5. Chứng minh cổng có răng ngay trong test: chạy cùng hàm phân tích trên một chuỗi nguồn giả lập
      có chỉ thị client và import module đó, và khẳng định hàm **có** báo vi phạm.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/__tests__/admin-client-scope.test.ts` thoát 0 và báo ít nhất 5 test.
    - `npm run build` thoát 0 và `npm run check:secrets` thoát 0.
    - Với cookie owner Ngọc Phát: tạo tài khoản cho một nhân viên đang có `user_id` rỗng làm `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from employees where user_id is null"` giảm đúng 1.
    - Cùng thao tác làm `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from memberships"` tăng đúng 1.
    - Cùng thao tác làm `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_table='employees' and action='update'"` tăng đúng 1.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where before::text ~* 'password' or after::text ~* 'password'"` trả về `0` — không dòng audit nào mang dấu vết mật khẩu.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select raw_app_meta_data->>'must_change_password' from auth.users order by created_at desc limit 1"` trả về `true`.
    - Bấm tạo tài khoản lần thứ hai cho cùng nhân viên hiển thị thông báo lỗi tiếng Việt và không làm ba con số đếm ở trên thay đổi.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/__tests__/admin-client-scope.test.ts &amp;&amp; npm run build &amp;&amp; npm run check:secrets</automated>
  </verify>
  <done>Quản trị tạo được tài khoản cho một nhân viên và nhận mật khẩu tạm một lần; khóa bí mật chỉ sống trong module chạy phía server và có cổng cơ học canh; audit ghi thao tác nhưng không ghi mật khẩu.</done>
</task>

<task type="auto">
  <name>Task 2: Cổng buộc đổi mật khẩu ở middleware và trang đổi mật khẩu</name>
  <files>src/lib/data/mutations/accounts.ts, src/middleware.ts, src/app/(auth)/doi-mat-khau/page.tsx, src/app/(auth)/doi-mat-khau/change-password-form.tsx, src/lib/validation/schemas.ts, src/lib/constants.ts, src/__tests__/middleware-gate.test.ts</files>
  <read_first>
    - `src/middleware.ts` (tạo ở 02-04) — hàm thuần quyết định cổng đã tách sẵn ở Task 3 của plan đó;
      plan này mở rộng hàm thuần đó thêm một đầu vào, không viết lại middleware
    - `src/__tests__/middleware-gate.test.ts` (tạo ở 02-04) — tám test hiện có phải vẫn xanh sau khi
      hàm thuần đổi chữ ký
    - `src/lib/validation/schemas.ts` — quy ước đặt schema Zod và thông điệp lỗi tiếng Việt của dự án
    - `src/app/(auth)/login/login-form.tsx` — khuôn một form đã có: `useForm` + `zodResolver`,
      hiển thị lỗi gửi biểu mẫu qua state, nút có trạng thái đang gửi; trang mới dùng lại đúng khuôn
    - `src/app/(auth)/layout.tsx` — bố cục khu vực xác thực; trang mới nằm trong nhóm route đó nên
      thừa hưởng nền gradient sẵn có, không tự dựng nền mới
    - `src/lib/constants.ts` — nơi đặt mọi chuỗi hiển thị
  </read_first>
  <action>
    **(a) Mở rộng hàm thuần quyết định cổng trong `src/middleware.ts`.** Thêm đầu vào thứ ba
    `mustChangePassword: boolean`. Quy tắc mới, xếp trước quy tắc bảo vệ route hiện có:
    - Cờ bật **và** đường dẫn không phải `/doi-mat-khau` và không phải `/login` → chuyển hướng tới
      `/doi-mat-khau`. Bao gồm cả `/select-company` — người chưa đổi mật khẩu không được làm gì khác.
    - Cờ bật **và** đường dẫn là `/doi-mat-khau` → cho qua.
    - Cờ tắt **và** đường dẫn là `/doi-mat-khau` **và** có claims → chuyển hướng về `/admin/dashboard`;
      không có claims → chuyển hướng về `/login`.
    Trong `middleware()`, đọc cờ từ nhánh metadata phía server của claims mà `updateSession` đã trả
    về — **không** truy vấn database, đó chính là lý do cờ được đặt trong JWT (D-16). Xóa dòng
    comment ở 02-04 nói rằng cổng này thuộc plan sau.

    **(b) `src/lib/validation/schemas.ts`.** Thêm `changePasswordSchema` với hai trường mật khẩu mới
    và xác nhận, ràng buộc độ dài tối thiểu và điều kiện hai trường khớp nhau, thông điệp lỗi tiếng
    Việt theo đúng phong cách các schema đang có trong file.

    **(c) `src/app/(auth)/doi-mat-khau/page.tsx`** — Server Component, `export const metadata` với
    tiêu đề tiếng Việt, render `ChangePasswordForm`.

    **(d) `src/app/(auth)/doi-mat-khau/change-password-form.tsx`** — Client Component theo đúng
    khuôn của form đăng nhập. Gọi Server Action `completeForcedPasswordChange`. Ba trạng thái phải
    hiển thị khác nhau: đang gửi, thành công (chuyển hướng), và thất bại. Trong nhánh thất bại,
    phân biệt hai loại thông điệp: mật khẩu chưa đổi được (thử lại), và mật khẩu **đã** đổi nhưng
    phiên không làm mới được (phải đăng nhập lại) — gộp hai loại này thành một thông điệp là cách
    tạo ra người dùng thử lại bằng mật khẩu cũ. Mọi chuỗi đặt trong `src/lib/constants.ts`.

    **(e) `completeForcedPasswordChange(newPassword: string)` trong `src/lib/data/mutations/accounts.ts`**
    — `"use server"`. Trình tự bắt buộc, đúng thứ tự này (D-16a):
    1. `getSessionContext()` lấy `userId`; cờ buộc đổi mật khẩu phải đang bật, nếu không thì ném lỗi
      (ngăn dùng endpoint này như một đường đổi mật khẩu chung chưa được thiết kế).
    2. Trên client cookie-bound của chính người dùng: `auth.updateUser({ password: newPassword })`.
      Thất bại → ném `Error` thuộc loại "chưa đổi được".
    3. Trên client khóa bí mật: `auth.admin.updateUserById(userId, { app_metadata: { must_change_password: false } })`.
    4. **Vẫn trong cùng request này**, trên client cookie-bound: `auth.refreshSession()`. Nhờ cookie
      adapter đã đăng ký, access token mới (đã sạch cờ) được ghi lên response của chính request này.
      **Không** dựa vào middleware của request kế tiếp làm hộ.
    5. Nếu bước 4 trả lỗi về refresh token đã được dùng (project có bật xoay refresh token, nên hai
      lệnh làm mới gần nhau tranh nhau cùng một token), **thử lại đúng một lần** sau một khoảng chờ
      ngắn. Vẫn thất bại → ném `Error` thuộc loại "đã đổi nhưng phải đăng nhập lại", mang thông điệp
      tiếng Việt nói rõ mật khẩu mới **đã** có hiệu lực.
    6. `logMutation` `action: "update"`, `entityTable: "auth.users"`, `entityId: userId`,
      `before: { must_change_password: true }`, `after: { must_change_password: false }`,
      `reason: null`. Hai ảnh chụp ở đây **không** phải nguyên dòng — bảng người dùng của Supabase
      không thuộc quyền đọc nguyên dòng của tầng ứng dụng, và ghi nguyên dòng ở đây nghĩa là sao chép
      băm mật khẩu vào bảng audit. Ghi comment nêu rõ ngoại lệ này và lý do, để nó không trông như
      một chỗ quên áp dụng D-18.
    7. Chỉ chuyển hướng **sau khi** bước 4 hoàn tất.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npm run test` thoát 0 — tám test cổng route của plan 02-04 vẫn xanh sau khi hàm thuần đổi chữ ký.
    - `grep -c "must_change_password" src/middleware.ts` trả về ít nhất 1.
    - `grep -rEc "from\(.memberships.\)|from\(.employees.\)" src/middleware.ts` trả về `0` — cổng đọc cờ từ JWT, không truy vấn database.
    - `grep -c "user_metadata" src/middleware.ts src/lib/data/mutations/accounts.ts src/lib/auth/session-context.ts` trả về `0` cho cả ba file.
    - `npm run build` thoát 0 và `npm run check:secrets` thoát 0.
    - Với một tài khoản đang bật cờ và server dev đang chạy: `curl -b <cookie> -s -o /dev/null -w "%{http_code} %{redirect_url}" http://localhost:3000/admin/dashboard` trả về `307` và URL đích kết thúc bằng `/doi-mat-khau`.
    - Cùng cookie: `curl -b <cookie> -s -o /dev/null -w "%{http_code}" http://localhost:3000/select-company` trả về `307` — người chưa đổi mật khẩu không đi vòng qua màn hình chọn doanh nghiệp.
    - Cùng cookie: `curl -b <cookie> -s -o /dev/null -w "%{http_code}" http://localhost:3000/doi-mat-khau` trả về `200`.
    - Với một tài khoản đã tắt cờ: `curl -b <cookie> -s -o /dev/null -w "%{http_code} %{redirect_url}" http://localhost:3000/doi-mat-khau` trả về `307` với URL đích kết thúc bằng `/admin/dashboard`.
    - Sau một lần đổi mật khẩu thành công: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_table='auth.users'"` tăng đúng 1, và `select count(*) from audit_log where after::text ~* 'encrypted|hash|password'` vẫn trả về `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; npm run build &amp;&amp; npm run check:secrets</automated>
  </verify>
  <done>Cờ buộc đổi mật khẩu có người đọc; người chưa đổi không tới được trang nào khác; đổi xong thì phiên được làm mới ngay trong cùng request nên không có vòng lặp chuyển hướng.</done>
</task>

<task type="auto">
  <name>Task 3: Test cổng buộc đổi mật khẩu và chuỗi thao tác chống mắc kẹt</name>
  <files>src/lib/data/mutations/accounts.ts, src/__tests__/middleware-gate.test.ts, src/lib/data/__tests__/accounts.test.ts</files>
  <read_first>
    - `src/__tests__/middleware-gate.test.ts` (tạo ở 02-04) — tám test hiện có; plan này thêm nhóm mới
    - `src/lib/data/mutations/accounts.ts` — trình tự sáu bước cần test
    - `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md` §Pitfall 3 — mô tả cụ thể
      của lỗi tranh chấp refresh token và vì sao nó khó tái lập bằng tay
  </read_first>
  <action>
    **(a) Mở rộng `src/__tests__/middleware-gate.test.ts`** thêm sáu test cho hàm thuần quyết định cổng:
    1. Cờ bật, đường dẫn khu vực quản trị → chuyển hướng tới trang đổi mật khẩu.
    2. Cờ bật, đường dẫn khu vực nhân viên → chuyển hướng tới trang đổi mật khẩu.
    3. Cờ bật, đường dẫn chọn doanh nghiệp → chuyển hướng tới trang đổi mật khẩu. Đây là lối vòng dễ
      bị bỏ sót nhất vì trang đó không nằm trong hai tiền tố được bảo vệ.
    4. Cờ bật, đường dẫn trang đổi mật khẩu → cho qua.
    5. Cờ tắt, có claims, đường dẫn trang đổi mật khẩu → chuyển hướng về bảng điều khiển. Không có
      quy tắc này thì người đã đổi xong vẫn quay lại được trang đổi mật khẩu và bối rối.
    6. Cờ tắt, không claims, đường dẫn trang đổi mật khẩu → chuyển hướng về đăng nhập.

    **(b) `src/lib/data/__tests__/accounts.test.ts`** với client Supabase giả lập. Năm khẳng định
    tập trung vào **thứ tự** và **chống mắc kẹt**, không phải vào việc gọi được API:
    1. Trình tự thành công gọi ba API theo đúng thứ tự: đổi mật khẩu → xóa cờ → làm mới phiên. Ghi
      lại thứ tự lời gọi và so sánh với mảng kỳ vọng. Nếu ai đó đảo hai bước cuối, test đỏ.
    2. Bước đổi mật khẩu thất bại → hàm ném lỗi loại "chưa đổi được", và **không** gọi bước xóa cờ.
    3. Bước làm mới phiên thất bại lần đầu với lỗi refresh token đã dùng → hàm gọi lại đúng **một**
      lần nữa (đếm số lời gọi bằng 2, không phải 1 và không phải 3).
    4. Cả hai lần làm mới đều thất bại → hàm ném lỗi loại "đã đổi nhưng phải đăng nhập lại", với
      thông điệp khác hẳn thông điệp của trường hợp 2. Khẳng định hai chuỗi **không bằng nhau** —
      đây là điều kiện để người dùng không thử lại bằng mật khẩu cũ.
    5. `createEmployeeAccount` cho một nhân viên đã có tài khoản liên kết → ném lỗi và **không** gọi
      API tạo người dùng.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/middleware-gate.test.ts` thoát 0 và báo ít nhất 14 test (8 cũ + 6 mới).
    - `npx vitest run src/lib/data/__tests__/accounts.test.ts` thoát 0 và báo ít nhất 5 test.
    - `npm run test` thoát 0.
    - Sau khi tạm đảo thứ tự bước xóa cờ và bước làm mới phiên trong `accounts.ts`, `npx vitest run src/lib/data/__tests__/accounts.test.ts` thoát **khác 0**; hoàn tác thì trở lại 0. Hai mã thoát ghi vào SUMMARY.
    - Sau khi tạm bỏ quy tắc chặn đường dẫn chọn doanh nghiệp khỏi hàm thuần, `npx vitest run src/__tests__/middleware-gate.test.ts` thoát **khác 0**; hoàn tác thì trở lại 0. Hai mã thoát ghi vào SUMMARY.
    - `npm run typecheck` thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Cổng buộc đổi mật khẩu và trình tự sáu bước đều có test; hai cổng đã được chứng minh có răng; hai loại thông điệp lỗi được một test khẳng định là khác nhau.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Nghiệm thu tay — vòng đời tài khoản từ lúc tạo tới lúc đổi mật khẩu xong</name>
  <what-built>Quản trị tạo tài khoản cho nhân viên và nhận mật khẩu tạm một lần; cổng ở
  `middleware.ts` chặn người chưa đổi mật khẩu khỏi mọi trang khác; trang đổi mật khẩu hoàn tất
  trình tự sáu bước và làm mới phiên trong cùng request.

  Hai hạng mục dưới đây phụ thuộc thời điểm làm mới JWT thật, và `02-RESEARCH.md` §Pitfall 3 ghi rõ
  lỗi tranh chấp refresh token là loại lỗi ngẫu nhiên chỉ hiện ra khi thao tác nhanh liên tiếp — không
  test tự động nào tái lập trung thực được điều đó.</what-built>
  <how-to-verify>
    1. Chạy `npm run dev`. Đăng nhập bằng một tài khoản owner. Vào `/admin/employees`, mở hồ sơ một
       nhân viên **chưa có tài khoản** (30 người trong seed thuộc nhóm này).
    2. Bấm nút tạo tài khoản. Kỳ vọng: hộp thoại hiện email và một mật khẩu tạm; đóng hộp thoại rồi
       mở lại hồ sơ thì mật khẩu **không** hiện lại lần nữa.
    3. Ghi lại email và mật khẩu tạm. Đăng xuất.
    4. Đăng nhập bằng tài khoản vừa tạo. Kỳ vọng: bị đưa thẳng tới `/doi-mat-khau`.
    5. Trong khi đang ở trang đó, gõ thẳng `http://localhost:3000/admin/dashboard` rồi
       `http://localhost:3000/select-company`. Kỳ vọng: cả hai đều bị đá về `/doi-mat-khau`.
    6. Đổi mật khẩu. Kỳ vọng: **không phải tải lại trang bằng tay**, và không bị đá ngược về trang
       đổi mật khẩu. Đây là hạng mục quan trọng nhất của bước nghiệm thu này.
    7. Ngay sau khi vào được, bấm điều hướng qua lại vài trang thật nhanh. Kỳ vọng: không bị đăng
       xuất đột ngột, không màn hình lỗi nào.
    8. Đăng xuất rồi đăng nhập lại bằng **mật khẩu mới**. Kỳ vọng: vào thẳng khu vực quản trị hoặc
       màn hình chọn doanh nghiệp, không quay lại trang đổi mật khẩu.
    9. Thử đăng nhập bằng **mật khẩu tạm cũ**. Kỳ vọng: bị từ chối.
    Báo lại kết quả từng bước; nếu bước 6 hoặc 7 lệch kỳ vọng thì mô tả chính xác triệu chứng và số
    lần lặp lại được.
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu cả chín bước đúng kỳ vọng, hoặc mô tả bước nào lệch</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Supabase Admin API | Khóa bí mật bỏ qua toàn bộ RLS; mọi lời gọi phải tự kiểm quyền trước |
| JWT → `middleware.ts` | Cờ buộc đổi mật khẩu là dữ liệu có ký, đọc được mà không cần truy vấn database |
| Mật khẩu tạm → người vận hành | Giá trị đi qua đúng một lần, trên một màn hình |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10-01 | Elevation of Privilege | Khóa bí mật lọt vào bundle trình duyệt | critical | mitigate | Module khóa bí mật chỉ import được từ file chạy phía server, có cổng test cơ học quét toàn bộ `src/` và tự chứng minh nó không mù; `npm run check:secrets` chạy trên build thật ở cả Task 1 lẫn Task 2 |
| T-02-10-02 | Elevation of Privilege | Vai trò thấp tạo tài khoản cho người khác | high | mitigate | `requireRole(role, ["owner","admin"])` trước mọi lời gọi Admin API |
| T-02-10-03 | Elevation of Privilege | Cờ buộc đổi mật khẩu đặt ở nhánh metadata mà client sửa được | high | mitigate | Chỉ dùng nhánh metadata phía server; acceptance criteria grep chặn nhánh còn lại trong cả ba file liên quan |
| T-02-10-04 | Elevation of Privilege | Tạo tài khoản cho nhân viên thuộc doanh nghiệp khác | high | mitigate | Câu đọc dòng nhân viên kèm `.eq("company_id", <từ session>)` bằng client cookie-bound, nên RLS vẫn kiểm độc lập trước khi khóa bí mật được dùng |
| T-02-10-05 | Information Disclosure | Mật khẩu tạm lọt vào bảng audit hoặc log | high | mitigate | Ảnh chụp audit của bảng người dùng chỉ chứa đúng cờ, không phải nguyên dòng; acceptance criteria quét toàn bảng audit tìm dấu vết mật khẩu và đòi kết quả bằng 0 |
| T-02-10-06 | Denial of Service | Người dùng mắc kẹt trong vòng lặp chuyển hướng sau khi đổi mật khẩu | high | mitigate | Làm mới phiên trong cùng request trước khi chuyển hướng; thử lại một lần khi gặp tranh chấp xoay token; test khẳng định đúng thứ tự và đúng số lần thử lại; nghiệm thu tay bước 6 và 7 |
| T-02-10-07 | Repudiation | Người dùng tin rằng đổi mật khẩu thất bại và thử lại bằng mật khẩu cũ | medium | mitigate | Hai loại thông điệp lỗi tách biệt; một test khẳng định hai chuỗi không bằng nhau |
| T-02-10-08 | Spoofing | Endpoint đổi mật khẩu bị dùng như đường đổi mật khẩu chung chưa thiết kế | medium | mitigate | Server Action từ chối khi cờ buộc đổi mật khẩu không bật |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test && npm run build && npm run check:secrets` thoát 0
- Tài khoản đang bật cờ bị chuyển hướng khỏi cả khu vực quản trị, khu vực nhân viên, và màn hình chọn doanh nghiệp
- Tài khoản đã tắt cờ bị chuyển hướng khỏi trang đổi mật khẩu
- Bảng audit không chứa dấu vết mật khẩu ở bất kỳ dòng nào
- Nghiệm thu tay 9 bước được duyệt, đặc biệt bước 6 và 7
</verification>

<success_criteria>
Vòng đời tài khoản chạy trọn: quản trị tạo được, nhân viên nhận được, cổng chặn được, và người dùng
thoát ra khỏi cổng mà không phải tải lại trang bằng tay. Khóa bí mật có cổng cơ học canh phạm vi
thay vì chỉ có quy ước.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-10-SUMMARY.md` when done
</output>
