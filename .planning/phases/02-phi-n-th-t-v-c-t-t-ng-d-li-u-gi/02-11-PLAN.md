---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 11
type: execute
wave: 10
depends_on: ["02-10"]
files_modified:
  - eslint-rules/no-date-in-client.mjs
  - eslint-rules/__fixtures__/violating-client.tsx
  - eslint-rules/__fixtures__/clean-server.tsx
  - eslint.config.mjs
  - src/__tests__/eslint-no-date-in-client.test.ts
  - src/components/employees/employee-form.tsx
  - src/lib/constants.ts
  - src/lib/mock/db.ts
  - src/lib/mock/seed.ts
  - src/lib/mock/service.ts
  - src/lib/mock/store.tsx
  - src/lib/data/store.tsx
  - src/hooks/use-mock-query.ts
  - src/hooks/use-data-query.ts
  - src/app/providers.tsx
  - src/__tests__/no-mock-layer.test.ts
autonomous: false
requirements: [DATA-05, DATA-08]

estimate:
  tokens: 86000
  raw_tokens: 86000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`src/lib/mock/db.ts` và `src/lib/mock/seed.ts` không còn tồn tại trong cây mã nguồn, và không file nào import chúng (DATA-05)."
    - "`src/lib/mock/service.ts` không còn tồn tại; 24 hàm của nó đã có nhà mới dưới `src/lib/data/` với **chữ ký giữ nguyên** (DATA-05)."
    - "Hai hằng số ngày và tháng cố định không còn tồn tại trong `src/lib/constants.ts` và không file nào tham chiếu chúng (DATA-08)."
    - "Chạy `npm run lint` trên một file client component có chứa lời gọi lấy giờ máy thì lint **báo đỏ** với đúng mã rule của dự án; trên cùng nội dung nhưng không có chỉ thị client thì lint xanh (D-19a)."
    - "Rule ESLint đã được chứng minh bắt được vi phạm thật bằng một fixture cố ý vi phạm, không phải một rule chạy mà luôn xanh."
    - "Hook đọc dữ liệu và provider vô hiệu hóa cache giữ nguyên cơ chế và giữ nguyên hình dạng kết quả, chỉ đổi tên và đổi vị trí thư mục (D-12)."
    - "Toàn bộ bộ test JavaScript và bộ pgTAP đều xanh sau khi tầng dữ liệu giả biến mất, và số assertion pgTAP không nhỏ hơn mốc 170 (D-15a)."
    - "Sau plan này, `git grep` trên toàn bộ `src/` không tìm thấy chuỗi đường dẫn của thư mục dữ liệu giả ở bất kỳ đâu."
  artifacts:
    - path: "eslint-rules/no-date-in-client.mjs"
      provides: "Rule cấm đọc giờ máy trong client component (D-19a)"
    - path: "src/__tests__/eslint-no-date-in-client.test.ts"
      provides: "Chứng minh rule có răng — fixture vi phạm đỏ, fixture sạch xanh"
    - path: "src/__tests__/no-mock-layer.test.ts"
      provides: "Cổng cơ học chặn tầng dữ liệu giả quay lại"
    - path: "src/hooks/use-data-query.ts"
      provides: "Hook đọc dữ liệu, đổi tên từ bản cũ, cơ chế và hình dạng kết quả không đổi"
  key_links:
    - from: "eslint.config.mjs"
      to: "eslint-rules/no-date-in-client.mjs"
      via: "đăng ký rule cục bộ qua khóa plugins của flat config"
      pattern: "no-date-in-client"
    - from: "src/hooks/use-data-query.ts"
      to: "src/lib/data/store.tsx"
      via: "bộ đếm phiên bản kích hoạt tải lại — cơ chế giữ nguyên từ V1"
      pattern: "@/lib/data/store"
  prohibitions:
    - statement: "MUST NOT nới rule ESLint hay thêm ngoại lệ cho một file chỉ để lint xanh. Rule này tồn tại vì loại quy ước nó bảo vệ là loại âm thầm mục sau vài tháng, và triệu chứng của nó là lỗi lệch lần vẽ rất khó truy. Một ngoại lệ không kèm lý do viết ra là cách rule chết mà không ai nhận ra."
      category: transparency
---

<objective>
Dọn sạch: gỡ hằng số thời gian cố định cuối cùng, dựng rule ESLint cưỡng chế quy ước "hôm nay do
server cấp", xóa tầng dữ liệu giả khỏi cây mã nguồn, và đổi tên hook cùng provider cho khớp thực tế
mới.

Purpose: một quy ước không cưỡng chế được thì mục sau vài tháng (D-19a). Một thư mục tên "giả lập"
còn nằm đó sau khi dữ liệu đã thật thì người đọc tiếp theo sẽ mất thời gian tìm hiểu nó còn dùng
không. Cả hai đều là nợ mà phase này có trách nhiệm không để lại.

Output: rule ESLint kèm bằng chứng nó có răng, ba file tầng giả lập bị xóa, hai file đổi tên, một
cổng cơ học chặn tầng giả lập quay lại.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-09-SUMMARY.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-10-SUMMARY.md
</context>

<!-- planner-discipline-allow: REFERENCE_DATE -->
<!-- planner-discipline-allow: REFERENCE_MONTH -->
<!-- planner-discipline-allow: @/lib/mock/ -->
<!-- planner-discipline-allow: useMockQuery -->

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Rule ESLint | `timeflow/no-date-in-client` ở `eslint-rules/no-date-in-client.mjs` |
| Fixture | `eslint-rules/__fixtures__/violating-client.tsx`, `eslint-rules/__fixtures__/clean-server.tsx` |
| Hook (đổi tên) | `useDataQuery`, `DataQueryResult<T>` ở `src/hooks/use-data-query.ts` |
| Provider (đổi tên) | `DataStoreProvider`, `useDataStore` ở `src/lib/data/store.tsx` |
| Test | `src/__tests__/eslint-no-date-in-client.test.ts`, `src/__tests__/no-mock-layer.test.ts` |
| Xóa | `src/lib/mock/db.ts`, `src/lib/mock/seed.ts`, `src/lib/mock/service.ts`, `src/lib/mock/store.tsx`, `src/hooks/use-mock-query.ts` |

<tasks>

<task type="auto">
  <name>Task 1: Rule ESLint cấm đọc giờ máy trong client component, kèm bằng chứng rule có răng</name>
  <files>eslint-rules/no-date-in-client.mjs, eslint-rules/__fixtures__/violating-client.tsx, eslint-rules/__fixtures__/clean-server.tsx, eslint.config.mjs, src/__tests__/eslint-no-date-in-client.test.ts</files>
  <read_first>
    - `eslint.config.mjs` — toàn bộ 21 dòng; đây là file sẽ được nối thêm, không viết lại. Chú ý
      mảng `ignores` hiện tại và cách `FlatCompat` được dùng
    - `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-RESEARCH.md` §Code Examples §D-19a —
      hai lựa chọn hiện thực và giả định A3 chưa được chạy thử: cấu trúc AST của chỉ thị đầu file
      chưa được xác minh với parser thật của dự án
    - `src/lib/today.ts` (tạo ở 02-08) — nguồn thay thế mà thông điệp của rule phải trỏ tới
    - `src/app/admin/dashboard/dashboard-view.tsx` — một client component thật, dùng để xác nhận rule
      không báo nhầm trên mã đúng
  </read_first>
  <action>
    Chọn **lựa chọn B** (rule cục bộ đọc chỉ thị đầu file thật) chứ không phải lựa chọn A (chặn theo
    glob đường dẫn). Lý do: glob phải cập nhật thủ công mỗi khi thêm thư mục client component mới,
    và một quy ước bảo vệ bằng một danh sách phải nhớ cập nhật là quy ước sẽ mục — đúng thứ D-19a
    tồn tại để ngăn. Rủi ro của lựa chọn B là giả định về cấu trúc AST (A3 trong nhật ký giả định
    của nghiên cứu) chưa được chạy thử; bước (c) dưới đây biến rủi ro đó thành một test.

    **(a) `eslint-rules/no-date-in-client.mjs`.** Rule ESLint dạng flat config:
    - `meta.type` là `"problem"`, `meta.docs.description` bằng tiếng Việt, `meta.schema` rỗng.
    - Trong `create(context)`: lấy AST chương trình, kiểm chỉ thị đầu file có phải chỉ thị client
      hay không. **Không** giả định chỉ thị luôn nằm ở phần tử đầu tiên — quét toàn bộ phần mở đầu
      của chương trình cho tới phần tử đầu tiên không phải câu lệnh biểu thức chuỗi. Cách này miễn
      nhiễm với việc một chỉ thị khác hoặc một comment đứng trước.
    - Nếu không phải file client → trả về đối tượng rỗng (rule không làm gì).
    - Nếu là file client → báo lỗi tại mọi biểu thức khởi tạo đối tượng ngày không tham số và mọi
      lời gọi lấy dấu thời gian hiện tại của đối tượng ngày. Thông điệp tiếng Việt nêu đúng mã quyết
      định D-19a và chỉ sang `src/lib/today.ts` làm nguồn thay thế.
    - Cho phép biểu thức khởi tạo đối tượng ngày **có tham số** (ví dụ dựng lại một ngày từ chuỗi
      ISO nhận qua prop) — chỉ dạng không tham số mới là "đọc đồng hồ máy". Ghi comment nêu rõ ranh
      giới này, vì báo nhầm ở đây sẽ khiến người ta muốn tắt rule.

    **(b) Đăng ký trong `eslint.config.mjs`.** Nối thêm một mục vào mảng cấu hình: khóa `plugins`
    với một namespace của dự án trỏ tới đối tượng chứa rule vừa viết, và khóa `rules` bật rule ở
    mức lỗi cho phạm vi `src/**/*.{ts,tsx}`. Thêm `eslint-rules/__fixtures__/**` vào mảng `ignores`
    — fixture cố ý vi phạm, để nó lọt vào lượt lint thường thì `npm run lint` sẽ đỏ vĩnh viễn.

    **(c) Hai fixture và test chứng minh rule có răng.**
    - `eslint-rules/__fixtures__/violating-client.tsx`: có chỉ thị client ở đầu file và một biểu
      thức đọc đồng hồ máy trong thân component.
    - `eslint-rules/__fixtures__/clean-server.tsx`: **cùng nội dung** nhưng **không** có chỉ thị
      client. Đây là đối chứng: nó chứng minh rule đỏ vì *nội dung file* chứ không phải vì bất cứ
      thứ gì khác.
    - `src/__tests__/eslint-no-date-in-client.test.ts`: dùng API Node của ESLint để lint hai fixture
      với chính cấu hình của dự án. Ba khẳng định: fixture vi phạm có ít nhất một lỗi mang đúng mã
      rule; fixture đối chứng có **không** lỗi nào mang mã rule đó; và lint một file client component
      thật đang có trong dự án cũng không sinh lỗi nào mang mã rule đó (chống báo nhầm).

    **(d) Xử lý mọi vi phạm hiện có.** Chạy `npm run lint` và sửa mọi chỗ bị rule bắt. Với mỗi chỗ,
    thay bằng giá trị nhận qua prop từ Server Component, không thêm ngoại lệ cho file. Nếu gặp một
    chỗ thật sự không thay được, dừng lại và báo cáo thay vì thêm ngoại lệ — một ngoại lệ không kèm
    lý do viết ra là cách rule chết mà không ai nhận ra.
  </action>
  <acceptance_criteria>
    - `npm run lint` thoát 0 trên toàn bộ dự án.
    - `npx vitest run src/__tests__/eslint-no-date-in-client.test.ts` thoát 0 và báo ít nhất 3 test.
    - Lint riêng fixture vi phạm bằng `npx eslint --no-ignore eslint-rules/__fixtures__/violating-client.tsx` thoát **khác 0** và output chứa mã rule của dự án.
    - Lint riêng fixture đối chứng bằng `npx eslint --no-ignore eslint-rules/__fixtures__/clean-server.tsx` **không** báo lỗi nào mang mã rule đó.
    - `grep -c "no-date-in-client" eslint.config.mjs` trả về ít nhất 1.
    - `grep -c "__fixtures__" eslint.config.mjs` trả về ít nhất 1.
    - Sau khi tạm thêm một biểu thức đọc đồng hồ máy vào một client component thật, `npm run lint` thoát **khác 0**; hoàn tác thì trở lại 0. Hai mã thoát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint &amp;&amp; npx vitest run src/__tests__/eslint-no-date-in-client.test.ts</automated>
  </verify>
  <done>Quy ước "hôm nay do server cấp" được cưỡng chế bằng một rule chạy trong `npm run lint`, và rule đó đã được chứng minh bắt được vi phạm thật bằng một fixture cố ý vi phạm cùng một fixture đối chứng.</done>
</task>

<task type="auto">
  <name>Task 2: Gỡ hằng số thời gian cố định cuối cùng và xóa tầng dữ liệu giả</name>
  <files>src/components/employees/employee-form.tsx, src/lib/constants.ts, src/lib/mock/db.ts, src/lib/mock/seed.ts, src/lib/mock/service.ts, src/lib/mock/store.tsx, src/lib/data/store.tsx, src/hooks/use-mock-query.ts, src/hooks/use-data-query.ts, src/app/providers.tsx, src/__tests__/no-mock-layer.test.ts</files>
  <read_first>
    - `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-09-SUMMARY.md` — danh sách chính xác
      những gì còn sót, do plan đó ghi lại; dùng làm danh sách kiểm chứ không tự dò lại từ đầu
    - `src/components/employees/employee-form.tsx` dòng 29 và 108 — chỗ cuối cùng dùng hằng số ngày
      cố định, làm giá trị mặc định của ngày vào làm khi tạo nhân viên mới. `02-PATTERNS.md` gắn cờ
      chỗ này là **cần xác nhận ý định nghiệp vụ**, không phải xóa máy móc: ý nghĩa của nó là "hôm
      nay khi tạo mới", nên nó vẫn hợp lệ — chỉ nguồn giá trị phải đổi
    - `src/lib/constants.ts` dòng 22 và 31-34 — hằng số múi giờ (giữ lại), hai hằng số ngày/tháng
      cố định (xóa), và hằng số khóa lưu trữ phiên (xóa nếu không còn nơi nào dùng sau plan 02-04)
    - `src/app/admin/employees/new/new-employee-view.tsx` — nơi render biểu mẫu nhân viên; đây là
      nơi truyền giá trị ngày mặc định xuống
    - `src/lib/mock/service.ts` — đọc lần cuối để đối chiếu 24 chữ ký đã chuyển hết chưa trước khi xóa
  </read_first>
  <action>
    **(a) `employee-form.tsx`.** Thêm prop `defaultStartDate: string` và dùng nó làm giá trị mặc định
    của ngày vào làm khi tạo nhân viên mới. Xóa dòng import hằng số. Ý nghĩa nghiệp vụ **không đổi**
    (vẫn là "hôm nay khi tạo mới"), chỉ nguồn giá trị chuyển từ hằng số cố định sang giá trị do
    server cấp. `new-employee-view.tsx` nhận giá trị đó qua prop từ Server Component
    `src/app/admin/employees/new/page.tsx` — sửa cả hai file theo chuỗi truyền prop đó.
    Ở chỗ biểu mẫu dùng để **sửa** một nhân viên đang có, giá trị mặc định vẫn lấy từ dữ liệu nhân
    viên như hiện tại, không đụng.

    **(b) `src/lib/constants.ts`.** Xóa hai hằng số ngày và tháng cố định. Xóa hằng số khóa lưu trữ
    phiên nếu `git grep` xác nhận không còn nơi nào dùng (plan 02-04 đã gỡ đường lưu phiên ở trình
    duyệt). **Giữ nguyên** hằng số múi giờ — `src/lib/today.ts` đang dùng nó. Giữ nguyên mọi nhãn
    tiếng Việt và mọi hằng số khác.

    **(c) Đổi tên hook và provider.** D-12 ghi rõ hook đọc dữ liệu "chỉ đổi tên", và giữ một thư mục
    mang tên tầng giả lập sau khi dữ liệu đã thật là để lại một biển chỉ đường sai:
    - `src/hooks/use-mock-query.ts` → `src/hooks/use-data-query.ts`; đổi tên hàm xuất và tên kiểu kết
      quả sang tên tương ứng không mang chữ "mock". **Cơ chế và hình dạng kết quả giữ nguyên tuyệt
      đối**: bốn trường của kết quả, nhánh bắt lỗi chuyển sang chuỗi, cờ hủy chống kết quả cũ ghi
      đè kết quả mới, và mảng phụ thuộc gồm bộ đếm phiên bản cùng token tải lại thủ công — không
      dòng nào trong số đó được sửa.
    - `src/lib/mock/store.tsx` → `src/lib/data/store.tsx`; đổi tên provider và hook đọc bộ đếm sang
      tên tương ứng. Cơ chế bộ đếm phiên bản giữ nguyên.
    - Cập nhật mọi nơi import hai module đó: khoảng 10 file import provider và khoảng 13 file import
      hook. Mỗi file chỉ đổi dòng import và tên định danh, không đổi logic.
    - `src/app/providers.tsx` đổi tên provider trong cây lồng, giữ nguyên thứ tự lồng.
    Ghi vào SUMMARY: đây là **đổi tên**, không phải thay cơ chế; việc thay hook bằng một thư viện
    tải dữ liệu là mục đã hoãn có chủ ý trong CONTEXT.md và không thuộc phase này.

    **(d) Xóa ba file tầng giả lập.** Trước khi xóa, đối chiếu lần cuối 24 chữ ký trong
    `src/lib/mock/service.ts` với các module dưới `src/lib/data/`: mọi tên hàm phải có mặt với đúng
    chữ ký. Ghi bảng đối chiếu 24 dòng vào SUMMARY. Sau đó xóa `src/lib/mock/db.ts`,
    `src/lib/mock/seed.ts`, `src/lib/mock/service.ts` bằng `git rm`. Thư mục `src/lib/mock/` phải
    biến mất hoàn toàn.

    **(e) `src/__tests__/no-mock-layer.test.ts`** — cổng cơ học chặn tầng giả lập quay lại. Bốn
    khẳng định, dựa trên danh sách file mà git đang theo dõi:
    1. Không file nào trong `src/` nằm dưới thư mục tầng giả lập.
    2. Không file nào trong `src/` chứa chuỗi đường dẫn import của thư mục đó.
    3. Không file nào trong `src/` chứa tên hai hằng số ngày/tháng cố định.
    4. Số file được quét **lớn hơn 0** — một cổng quét 0 file không được coi là xanh.
  </action>
  <acceptance_criteria>
    - `test ! -d src/lib/mock` đúng — thư mục không còn tồn tại.
    - `git ls-files src/lib/mock` không trả về dòng nào.
    - `git grep -l "@/lib/mock/" -- src` không trả về file nào.
    - `git grep -lE "REFERENCE_DATE|REFERENCE_MONTH" -- src` không trả về file nào.
    - `git grep -l "useMockQuery\|MockDataProvider\|useMockData" -- src` không trả về file nào.
    - `npm run typecheck` thoát 0 — không import nào bị treo sau khi xóa và đổi tên.
    - `npm run lint` thoát 0.
    - `npm run test` thoát 0.
    - `npm run build` thoát 0.
    - `npx vitest run src/__tests__/no-mock-layer.test.ts` thoát 0 và báo ít nhất 4 test.
    - SUMMARY chứa bảng đối chiếu đủ 24 dòng, mỗi dòng ghi tên hàm cũ, module mới, và ghi chú "chữ ký giữ nguyên" hoặc mô tả chính xác điểm khác nếu có.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; npm run build &amp;&amp; node -e "const {execSync}=require('child_process');const out=execSync('git grep -lE \"REFERENCE_DATE|REFERENCE_MONTH|useMockQuery|MockDataProvider\" -- src || true',{encoding:'utf8'}).trim();if(out){console.error('con sot: '+out);process.exit(1)}const m=execSync('git ls-files src/lib/mock',{encoding:'utf8'}).trim();if(m){console.error('thu muc tang gia lap con file: '+m);process.exit(1)}"</automated>
  </verify>
  <done>Tầng dữ liệu giả không còn tồn tại trong cây mã nguồn; hằng số thời gian cố định đã biến mất khỏi toàn bộ `src/`; hook và provider đã đổi tên mà cơ chế không đổi; một cổng cơ học canh việc chúng không quay lại.</done>
</task>

<task type="auto">
  <name>Task 3: Cổng nghiệm thu toàn phase — chạy trọn bộ kiểm chứng trên trạng thái cuối</name>
  <precondition>`.env.local` chứa `POSTGRES_URL_NON_POOLING` khác rỗng và server dev khởi động được bằng `npm run dev`</precondition>
  <files>(xem <read_first> và <action>)</files>
  <read_first>
    - `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-VALIDATION.md` — bảng "Manual-Only
      Verifications" và mục "Validation Sign-Off"; task này điền trạng thái thật cho từng dòng
    - `.planning/ROADMAP.md` §"Phase 2" — năm tiêu chí nghiệm thu của phase; mỗi tiêu chí phải được
      đối chiếu với một bằng chứng cụ thể chứ không phải một lời khẳng định
    - `package.json` — danh sách script để dựng đúng chuỗi lệnh nghiệm thu
  </read_first>
  <action>
    Chạy trọn bộ kiểm chứng trên trạng thái cuối của phase và ghi **kết quả thật** vào SUMMARY, kể
    cả kết quả không như mong đợi:
    1. `npm run typecheck`
    2. `npm run lint`
    3. `npm run test`
    4. `npm run build`
    5. `npm run check:secrets`
    6. `npm run db:push` (xác nhận không còn migration nào chưa áp)
    7. `npm run test:db`
    8. `npm run check:assertions`
    9. `node --env-file=.env.local scripts/check-signup-disabled.mjs`

    Sau đó đối chiếu **từng** tiêu chí trong năm tiêu chí nghiệm thu của phase ở ROADMAP với một
    bằng chứng cụ thể: tên lệnh và mã thoát, hoặc mã HTTP quan sát được, hoặc số dòng đếm được trên
    database. Ghi bảng đối chiếu vào SUMMARY. Tiêu chí nào chưa có bằng chứng thì ghi là **chưa có
    bằng chứng**, không ghi là đạt.

    Cập nhật `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-VALIDATION.md`: điền bảng
    "Per-Task Verification Map" bằng dữ liệu thật của phase (một dòng cho mỗi task của 11 plan, với
    lệnh tự động và trạng thái), đánh dấu bốn dòng "Manual-Only Verifications" theo kết quả nghiệm
    thu tay đã thực hiện ở plan 02-04 và 02-10, và đặt `wave_0_complete` thành `true`. **Không** tự
    đặt `nyquist_compliant` thành `true` — trường đó do bước kiểm định riêng đặt.

    Ghi vào SUMMARY ba giới hạn đã biết của phase, nguyên văn, để chúng không bị đọc nhầm thành đã
    giải quyết:
    - Nhánh vai trò nền tảng chỉ kiểm chứng được tới mức hàm trả đúng true/false; phần "thấy đúng
      cái được phép thấy" thuộc Phase 6 (D-11).
    - Audit ở tầng server đúng cho *ai* và *vì sao* nhưng không đảm bảo *đủ*: một migration hay một
      phiên psql tay ghi thẳng vào bảng sẽ không để lại vết (D-17a).
    - Từ Phase 3, bảng audit thành bản sao thứ hai của dữ liệu cá nhân với vòng đời riêng, vì mỗi
      dòng chấm công sẽ mang toạ độ GPS (D-18a).
  </action>
  <acceptance_criteria>
    - Cả chín lệnh ở bước 1-9 đều thoát 0, và cả chín mã thoát được ghi vào SUMMARY.
    - `npm run check:assertions` báo tổng ≥ 170 và con số thật được ghi vào SUMMARY.
    - SUMMARY chứa bảng đối chiếu đủ 5 dòng cho 5 tiêu chí nghiệm thu của phase, mỗi dòng kèm bằng chứng cụ thể.
    - `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-VALIDATION.md` có `wave_0_complete: true` và bảng "Per-Task Verification Map" không còn dòng giữ chỗ.
    - SUMMARY chứa nguyên văn ba giới hạn đã biết.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; npm run build &amp;&amp; npm run check:secrets &amp;&amp; npm run db:push &amp;&amp; npm run test:db &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Chín cổng tự động đều xanh trên trạng thái cuối; năm tiêu chí nghiệm thu của phase được đối chiếu với bằng chứng cụ thể; ba giới hạn đã biết được ghi lại nguyên văn.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Nghiệm thu tay toàn phase — mọi màn hình V1 trên dữ liệu thật</name>
  <what-built>Toàn bộ phase: phiên thật trên cookie, cổng chặn route, 24 hàm dữ liệu chạy trên
  Postgres, audit log ở mọi đường ghi, thời gian thật, tầng dữ liệu giả đã bị xóa, và một rule
  ESLint canh quy ước thời gian.

  Chín cổng tự động đã xanh. Bước này kiểm thứ mà không cổng nào kiểm được: mười ba màn hình V1 có
  thật sự dùng được không.</what-built>
  <how-to-verify>
    Chạy `npm run dev`. Đăng nhập bằng một tài khoản owner (mật khẩu đã đổi ở plan 02-10). Đi qua
    từng màn hình dưới đây và xác nhận nó hiển thị **dữ liệu thật** (tên doanh nghiệp Ngọc Phát hoặc
    Bình Minh, 28 hoặc 12 nhân viên) và **ngày hôm nay thật**, không phải một ngày cố định trong
    tương lai:
    1. `/select-company` — danh sách doanh nghiệp
    2. `/admin/dashboard` — bốn chỉ số, biểu đồ 7 ngày kết thúc ở hôm nay, hoạt động hôm nay
    3. `/admin/departments` — thêm một phòng ban, sửa nó, rồi xóa nó
    4. `/admin/shifts` — nhân bản một ca, rồi sửa ca vừa nhân bản
    5. `/admin/employees` — tìm kiếm bằng chuỗi **không dấu** (ví dụ `nguyen`), lọc theo phòng ban,
       chuyển trang, chọn nhiều dòng và chuyển phòng ban hàng loạt
    6. `/admin/employees/new` — tạo một nhân viên mới; thử lại với **cùng mã nhân viên** và xác nhận
       bị từ chối kèm thông báo nêu đúng mã đó
    7. `/admin/employees/<id>` — mở hồ sơ, xem đủ năm tab
    8. Đăng xuất, đăng nhập bằng một tài khoản **nhân viên**
    9. `/employee` — chấm công vào, rồi chấm công ra; xác nhận giờ hiển thị là giờ thật lúc bạn bấm
    10. `/employee/history` — xem tháng hiện tại, lùi về tháng trước
    11. `/employee/requests` — tạo một yêu cầu nghỉ phép; xác nhận ngày mặc định của biểu mẫu là hôm nay
    12. `/employee/profile` — xem hồ sơ cá nhân
    13. `/onboarding` — tạo một doanh nghiệp mới và xác nhận vào được ngay sau đó
    Trong suốt quá trình, mở DevTools Console. Kỳ vọng: **không** cảnh báo lệch lần vẽ, **không** lỗi
    đỏ nào.
    Cuối cùng, mở lại `/admin/dashboard` bằng tài khoản owner và xác nhận thao tác ở bước 3, 4, 5, 6
    đã phản ánh vào số liệu.
    Báo lại kết quả từng màn hình; màn hình nào lệch thì mô tả cụ thể triệu chứng.
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu cả mười ba màn hình đúng kỳ vọng, hoặc liệt kê màn hình nào lệch</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client component → đồng hồ thiết bị | Ranh giới mà rule ESLint cưỡng chế; vượt qua nó gây lệch lần vẽ và số liệu sai theo múi giờ người xem |
| Cây mã nguồn → tầng dữ liệu giả | Sau plan này ranh giới là tuyệt đối: tầng đó không tồn tại |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11-01 | Tampering | Quy ước "hôm nay do server cấp" mục dần vì không ai cưỡng chế | medium | mitigate | Rule ESLint chạy trong `npm run lint`; hai fixture (vi phạm và đối chứng) chứng minh rule đỏ vì nội dung file chứ không vì lý do khác; một lần cố tình làm đỏ trên mã thật rồi hoàn tác |
| T-02-11-02 | Tampering | Rule bị vô hiệu bằng một ngoại lệ lặng lẽ | medium | accept | Không có cách cơ học nào chặn việc thêm ngoại lệ; ghi thành điều cấm tường minh trong plan và trong thông điệp của rule. Chấp nhận vì cưỡng chế mức cao hơn (cấm mọi ngoại lệ) sẽ chặn cả những trường hợp hợp lệ chưa lường trước |
| T-02-11-03 | Information Disclosure | Tầng dữ liệu giả quay lại và che mất một đường dữ liệu thật hỏng | medium | mitigate | Cổng cơ học `no-mock-layer.test.ts` quét danh sách file git đang theo dõi và khẳng định số file quét lớn hơn 0 |
| T-02-11-04 | Denial of Service | Đổi tên hook làm hỏng cơ chế tải lại sau khi ghi | medium | mitigate | Đổi tên thuần túy, cấm sửa bốn trường kết quả, nhánh bắt lỗi, cờ hủy và mảng phụ thuộc; nghiệm thu tay bước 3-6 kiểm chính hành vi tải lại sau khi ghi |
| T-02-11-05 | Repudiation | Tiêu chí nghiệm thu được ghi là đạt mà không có bằng chứng | medium | mitigate | Task 3 buộc mỗi tiêu chí đi kèm một bằng chứng cụ thể, và cho phép ghi "chưa có bằng chứng" thay vì buộc ghi đạt |
</threat_model>

<verification>
- Chín cổng tự động ở Task 3 đều thoát 0
- `git ls-files src/lib/mock` không trả về dòng nào
- `git grep` không tìm thấy hằng số thời gian cố định, đường dẫn tầng giả lập, hay tên hook cũ trong `src/`
- Rule ESLint đỏ trên fixture vi phạm và xanh trên fixture đối chứng
- Nghiệm thu tay 13 màn hình được duyệt
</verification>

<success_criteria>
Phase kết thúc ở đúng trạng thái mà mục tiêu của nó mô tả: người dùng đăng nhập bằng tài khoản thật,
mọi màn hình V1 đọc và ghi Postgres theo thời gian thật, mỗi thao tác ghi để lại một dòng truy ngược
được, và tầng dữ liệu giả đã biến mất khỏi mã nguồn — với ba giới hạn đã biết được ghi lại nguyên
văn thay vì bị làm mờ.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-11-SUMMARY.md` when done
</output>
