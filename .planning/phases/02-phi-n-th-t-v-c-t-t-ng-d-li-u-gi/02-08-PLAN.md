---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 08
type: execute
wave: 7
depends_on: ["02-07"]
files_modified:
  - src/lib/today.ts
  - src/lib/validation/api/attendance.ts
  - src/lib/data/attendance.ts
  - src/lib/data/mutations/attendance.ts
  - src/app/api/attendance/route.ts
  - src/app/api/attendance/summary/route.ts
  - src/lib/validation/api/dashboard.ts
  - src/lib/data/dashboard.ts
  - src/app/api/dashboard/route.ts
  - src/app/admin/dashboard/page.tsx
  - src/app/admin/dashboard/dashboard-view.tsx
  - src/app/employee/page.tsx
  - src/app/employee/employee-home-view.tsx
  - src/app/employee/history/page.tsx
  - src/app/employee/history/history-view.tsx
  - src/lib/data/__tests__/attendance.test.ts
  - src/lib/__tests__/today.test.ts
autonomous: true
requirements: [AUTH-03, DATA-05, DATA-06, DATA-08]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Bảng điều khiển quản trị, trang chủ nhân viên và lịch sử chấm công đều chạy trên dữ liệu Postgres thật theo ngày giờ thật — không màn hình nào còn seed ngày cố định (DATA-08)."
    - "Giá trị \"hôm nay\" do Server Component tính đúng một lần theo múi giờ Việt Nam rồi truyền xuống làm prop; không client component nào tự tính lại, nên không có lệch giữa lần vẽ ở server và lần vẽ ở client (D-19)."
    - "`getServerToday()` trả về cùng ngày với `public.tf_work_date(now())` của database, kể cả khi tiến trình Node chạy ở múi giờ khác — hai nguồn không được phép lệch nhau."
    - "Dấu thời gian của một lần chấm công là giờ server: `checkIn` ghi `now()` của Postgres, không ghi giá trị giờ do client gửi lên."
    - "`work_date` của bản ghi chấm công luôn bằng `tf_work_date(check_in_at)` — ràng buộc `check` của database là lớp hai, tầng ứng dụng không được tự đặt một ngày khác."
    - "Biểu đồ 7 ngày của bảng điều khiển tính từ bản ghi chấm công thật, không còn dãy số mẫu cố định của V1."
    - "`checkIn` và `checkOut` mỗi lần để lại đúng một dòng `audit_log` (DATA-06)."
    - "Nhân viên chỉ chấm công được cho chính mình: `checkIn`/`checkOut` với một `employeeId` khác `employeeId` của phiên bị từ chối trừ khi người gọi có vai trò quản trị (AUTH-03)."
    - "`listAttendance` và `getMonthlySummary` cho một tháng không có bản ghi nào trả về mảng rỗng và một bản tổng hợp toàn số 0 — không phải lỗi, không phải `null` (edge DATA-05 empty)."
  artifacts:
    - path: "src/lib/today.ts"
      provides: "getServerToday()/getServerMonth() — nguồn duy nhất của 'hôm nay', chỉ chạy phía server"
      exports: ["getServerToday", "getServerMonth"]
    - path: "src/app/api/dashboard/route.ts"
      provides: "Route Handler tổng hợp bảng điều khiển"
      exports: ["GET", "dynamic"]
    - path: "src/lib/data/mutations/attendance.ts"
      provides: "Server Action chấm công vào/ra kèm audit"
      exports: ["checkIn", "checkOut"]
  key_links:
    - from: "src/app/admin/dashboard/page.tsx"
      to: "src/lib/today.ts"
      via: "Server Component tính today một lần rồi truyền xuống DashboardView làm prop"
      pattern: "getServerToday"
    - from: "src/lib/data/mutations/attendance.ts"
      to: "public.tf_work_date"
      via: "work_date suy từ dấu thời gian server, không từ tham số client"
      pattern: "tf_work_date"
  prohibitions:
    - statement: "MUST NOT lấy dấu thời gian chấm công từ đồng hồ của thiết bị người dùng. Một bản ghi vào/ra là bằng chứng về thời điểm; nhận giờ do client khai biến bằng chứng thành lời khai, và đó là chính điều mà giá trị cốt lõi của sản phẩm nói rằng doanh nghiệp phải tin được."
      category: safety
    - statement: "MUST NOT giữ lại dãy số mẫu cố định cho biểu đồ 7 ngày khi đã có dữ liệu thật. Một biểu đồ trông có vẻ đúng nhưng vẽ từ hằng số là thứ người quản lý sẽ ra quyết định dựa trên đó mà không biết mình đang nhìn cái gì."
      category: transparency
---

<objective>
Chuyển ba màn hình mang tính thời gian nhất sang dữ liệu và thời gian thật cùng lúc: chấm công,
lịch sử tháng, và bảng điều khiển. Đây cũng là nơi hằng số ngày cố định của V1 bị gỡ khỏi ba file
đầu tiên và thay bằng giá trị do server cấp (D-19).

Purpose: dữ liệu chấm công thật mà vẫn seed ngày cố định thì màn hình sẽ hiển thị một ngày không có
dữ liệu và trông như hỏng. Hai việc này phải đi cùng nhau, không tách được.

Output: `src/lib/today.ts`, ba Route Handler, một module Server Action, ba màn hình chạy dữ liệu và
thời gian thật.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-PATTERNS.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-07-SUMMARY.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-02-SUMMARY.md
</context>

<!-- planner-discipline-allow: REFERENCE_DATE -->
<!-- planner-discipline-allow: REFERENCE_MONTH -->

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Route Handler | `GET /api/attendance`, `GET /api/attendance/summary`, `GET /api/dashboard` |
| Hàm dữ liệu | `listAttendance()`, `getMonthlySummary()`, `getDashboardSummary()` |
| Server Action | `checkIn()`, `checkOut()` |
| Tiện ích server | `getServerToday()`, `getServerMonth()` ở `src/lib/today.ts` |
| Schema Zod | `attendanceRecordSchema`, `attendanceQuerySchema`, `monthlySummarySchema`, `dashboardSummarySchema` |
| Test | `src/lib/data/__tests__/attendance.test.ts`, `src/lib/__tests__/today.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: "Hôm nay" do server cấp, và đường đọc/ghi chấm công theo giờ server</name>
  <files>src/lib/today.ts, src/lib/validation/api/attendance.ts, src/lib/data/attendance.ts, src/lib/data/mutations/attendance.ts, src/app/api/attendance/route.ts, src/app/api/attendance/summary/route.ts, src/lib/__tests__/today.test.ts</files>
  <read_first>
    - `src/lib/constants.ts` dòng 22 và 31-32 — hằng số múi giờ đã có (`Asia/Ho_Chi_Minh`) và hai
      hằng số ngày cố định sẽ bị gỡ dần; plan này gỡ chúng khỏi ba file, `constants.ts` bị dọn ở
      plan cuối
    - `supabase/migrations/0003_enums_time.sql` — `tf_tz()`, `tf_work_date()`, `tf_overnight()`,
      `tf_shift_minutes()`, `tf_worked_minutes()`; đây là quy ước thời gian đã chốt ở Phase 1 và
      tầng ứng dụng không được dựng quy ước thứ hai
    - `supabase/migrations/0004_core_entities.sql` dòng 90-110 — cột thật của `attendance_records`,
      ràng buộc `unique (employee_id, work_date, shift_id)` và ràng buộc
      `check (check_in_at is null or work_date = public.tf_work_date(check_in_at))`
    - `src/lib/mock/service.ts` dòng 343-463 — chữ ký chính xác của `listAttendance`,
      `getMonthlySummary`, `checkIn`, `checkOut`, cùng quy tắc tính `lateMinutes`,
      `earlyLeaveMinutes`, `workedMinutes` và trạng thái dẫn xuất; ngưỡng chặn 720 phút chống giá
      trị âm quanh nửa đêm cần được thay bằng hàm thời gian của Phase 1 chứ không chép lại
    - `src/lib/types/domain.ts` dòng 139-158, 218-225, 287-295 — `AttendanceRecord`,
      `AttendanceQuery`, `MonthlySummary`
    - `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-02-SUMMARY.md` — quyết định D-08:
      ca qua đêm tính trọn vào ngày bắt đầu
  </read_first>
  <behavior>
    - `getServerToday()` trả về chuỗi `YYYY-MM-DD` bằng đúng kết quả của `public.tf_work_date(now())` trên database, kể cả khi biến môi trường múi giờ của tiến trình Node là `UTC` hoặc `America/New_York`.
    - `getServerMonth()` trả về `YYYY-MM` là tháng của `getServerToday()`.
    - `checkIn` ghi `check_in_at` bằng dấu thời gian của database (`now()`), không bằng tham số giờ mà nơi gọi truyền vào.
    - `checkIn` đặt `work_date` bằng `tf_work_date(now())`; nếu tham số ngày mà nơi gọi truyền vào khác giá trị đó thì hàm ném `Error` mang thông điệp tiếng Việt thay vì ghi một ngày sai.
    - `checkIn` lần thứ hai trong cùng ngày cho cùng nhân viên và cùng ca cập nhật bản ghi đang có thay vì tạo dòng thứ hai — ràng buộc duy nhất của database là lớp hai.
    - `checkOut` trên một bản ghi thuộc doanh nghiệp khác ném lỗi và không đổi dữ liệu.
    - `listAttendance` cho một tháng không có bản ghi trả về `[]`.
    - `getMonthlySummary` cho một tháng không có bản ghi trả về `{ month, workedDays: 0, totalMinutes: 0, lateCount: 0, leaveDays: 0 }`.
  </behavior>
  <action>
    **(a) `src/lib/today.ts`** — module **chỉ chạy phía server**. Comment đầu file phải nói rõ: đây
    là nguồn duy nhất của khái niệm "hôm nay" trong ứng dụng; mọi client component nhận giá trị này
    qua prop và không bao giờ tự tính (D-19).
    - `getServerToday(): Promise<string>` trả `YYYY-MM-DD`. Hiện thực bằng `Intl.DateTimeFormat`
      với locale `en-CA` (cho ra sẵn định dạng `YYYY-MM-DD`) và `timeZone` lấy từ hằng số múi giờ
      đã có trong `constants.ts` — **không** viết một phép cộng offset `+7` thủ công, vì đó là quy
      ước thứ hai và nó sẽ lệch với `tf_tz()` vào một ngày nào đó.
    - `getServerMonth(): Promise<string>` trả `YYYY-MM`, cắt từ kết quả trên.
    - Hai hàm là `async` dù không cần `await`: chúng được gọi từ Server Component và giữ chữ ký bất
      đồng bộ để về sau đổi sang một nguồn cần I/O mà không phải sửa nơi gọi.

    **(b) `src/lib/validation/api/attendance.ts`.** `attendanceRecordSchema` khớp `AttendanceRecord`,
    với ba phép biến đổi nằm trong schema: `work_date` (kiểu `date` của Postgres) → `date`;
    `check_in_at`/`check_out_at` (kiểu `timestamptz`) → `checkIn`/`checkOut` dạng `HH:mm` **theo múi
    giờ Việt Nam**, `null` giữ nguyên `null`; snake_case → camelCase. `attendanceQuerySchema` khai
    `employeeId`, `month`, `date` — **không** khai định danh doanh nghiệp. `monthlySummarySchema`
    khớp `MonthlySummary`.

    **(c) `src/app/api/attendance/route.ts`** và **`src/app/api/attendance/summary/route.ts`** theo
    khuôn 02-04. Cả hai lọc `.eq("company_id", <từ session>)`.
    - `/api/attendance`: áp ba bộ lọc tùy chọn, sắp xếp theo `work_date` **giảm dần** rồi `id` tăng
      dần (V1 sắp giảm dần theo ngày; thêm `id` làm tiebreaker để thứ tự ổn định khi một nhân viên
      có nhiều bản ghi cùng ngày ở các ca khác nhau).
    - `/api/attendance/summary`: nhận `employeeId` và `month`, trả `MonthlySummary` tính bằng phép
      tổng hợp trên database. Tháng không có bản ghi trả về bản tổng hợp toàn số 0 với `month` đúng
      bằng tham số — không trả `null`, không trả lỗi.
    - Thêm một lớp quyền: nếu vai trò phiên là `employee` hoặc `manager` và `employeeId` trong truy
      vấn khác `employeeId` của phiên thì trả 403. Quản trị đọc được mọi nhân viên trong doanh nghiệp.

    **(d) `src/lib/data/attendance.ts`.** `listAttendance(query)` và
    `getMonthlySummary(companyId, employeeId, month)` giữ nguyên chữ ký, gọi qua `fetchJson`.
    Tham số `companyId` giữ để không phải sửa call site nhưng không gửi lên server, kèm comment như
    các module trước. Re-export `checkIn`/`checkOut`.

    **(e) `src/lib/data/mutations/attendance.ts`** — `"use server"`. Hai hàm giữ nguyên chữ ký
    `checkIn(companyId, employeeId, date, time)` và `checkOut(recordId, time)`:
    - Lấy `companyId`, `role`, `employeeId` của phiên từ `getSessionContext()`.
    - Quyền: vai trò `employee`/`manager` chỉ chấm được cho chính mình (`employeeId` tham số phải
      bằng `employeeId` của phiên); `owner`/`admin` chấm được cho mọi nhân viên trong doanh nghiệp.
    - **Dấu thời gian và ngày công đều do server cấp.** `check_in_at` ghi bằng `now()` của Postgres;
      `work_date` ghi bằng `public.tf_work_date(now())`. Hai tham số `date` và `time` do nơi gọi
      truyền vào **không** được dùng làm giá trị lưu; `date` chỉ dùng để đối chiếu — lệch thì ném
      `Error` mang thông điệp tiếng Việt nói rõ ngày trên máy người dùng không khớp ngày của hệ
      thống. Comment tại chỗ ghi rõ hai tham số đó còn trong chữ ký chỉ để không phải sửa call site
      ở phase này, và ATT-06 ở Phase 3 sẽ bỏ hẳn chúng.
    - Ca của nhân viên tra từ `employees.shift_id`; `lateMinutes`, `earlyLeaveMinutes`,
      `workedMinutes` tính bằng các hàm thời gian của Phase 1 (`tf_shift_minutes`,
      `tf_worked_minutes`) chứ không chép lại phép trừ phút của tầng giả lập — quy ước ca qua đêm
      (D-08) đã nằm trong các hàm đó.
    - Chấm vào lần thứ hai trong cùng `(employee_id, work_date, shift_id)` cập nhật bản ghi đang có.
    - `checkOut` đọc bản ghi kèm `.eq("company_id", companyId)`; không tìm thấy thì ném `Error` với
      **đúng** thông điệp tiếng Việt mà tầng giả lập đang dùng.
    - Mỗi hàm đọc nguyên dòng trước, ghi, đọc nguyên dòng sau, rồi `logMutation` với
      `entityTable: "attendance_records"`.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/` trả về `0` trên mọi file.
    - `node -e "const {getServerToday}=require('...')"` không áp dụng; thay bằng: `npx vitest run src/lib/__tests__/today.test.ts` thoát 0.
    - `TZ=UTC node --input-type=module -e "import('./src/lib/today.ts')"` không áp dụng trực tiếp; thay bằng khẳng định trong test: `today.test.ts` chạy với `process.env.TZ` đặt lần lượt là `UTC` và `America/New_York` và cả hai lần đều cho cùng kết quả với chuỗi kỳ vọng.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select public.tf_work_date(now())"` và giá trị `getServerToday()` in ra bởi một script kiểm tạm thời **bằng nhau**; hai giá trị ghi vào SUMMARY.
    - Sau một lần chấm công vào trên giao diện: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from attendance_records where work_date <> public.tf_work_date(check_in_at) and check_in_at is not null"` trả về `0`.
    - Sau lần chấm công đó: số dòng `audit_log` với `entity_table='attendance_records'` tăng đúng 1.
    - Chấm công vào lần thứ hai trong cùng ngày không làm `select count(*) from attendance_records` tăng.
    - Với cookie của một nhân viên thường: `curl -b <cookie> -s -o /dev/null -w "%{http_code}" "http://localhost:3000/api/attendance?employeeId=<id nguoi khac>"` trả về `403`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/__tests__/today.test.ts</automated>
  </verify>
  <done>"Hôm nay" có đúng một nguồn và nguồn đó khớp database; chấm công ghi giờ server; ngày công luôn suy từ dấu thời gian server; nhân viên thường không đọc và không chấm hộ người khác.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bảng điều khiển tính từ dữ liệu thật, bỏ dãy số mẫu cố định</name>
  <files>src/app/api/attendance/route.ts, src/lib/validation/api/dashboard.ts, src/lib/data/dashboard.ts, src/app/api/dashboard/route.ts</files>
  <read_first>
    - `src/lib/mock/service.ts` dòng 510-668 — `getDashboardSummary` đầy đủ: bốn chỉ số KPI kèm
      chênh lệch so với hôm trước, biểu đồ 7 ngày, danh sách hoạt động hôm nay, danh sách chưa chấm
      công (tối đa 6 người), và bảng đếm yêu cầu chờ duyệt theo bốn loại. Hai hằng số dãy số mẫu
      (`CHART_PRESENT_OFFSET`, `CHART_LATE_VALUES`) là dữ liệu giả và **không** được mang sang
    - `src/lib/types/domain.ts` dòng 237-285 — `KpiValue`, `AttendanceChartPoint`,
      `TodayActivityItem`, `PendingRequestSummary`, `DashboardSummary`
    - `src/lib/format.ts` — `addDays`, `getWeekday`, `formatDayMonth`; nhãn trục biểu đồ giữ nguyên
      cách dựng của V1 (viết tắt thứ trong tuần + ngày/tháng)
    - `src/app/api/attendance/route.ts` (Task 1) — khuôn handler và cách lọc theo doanh nghiệp
  </read_first>
  <behavior>
    - Bốn chỉ số KPI đếm từ bản ghi thật của ngày được truyền vào; `delta` là chênh lệch so với ngày liền trước, không phải hằng số 0.
    - Biểu đồ trả về đúng 7 điểm, điểm cuối là ngày được truyền vào, mỗi điểm mang số liệu thật của đúng ngày đó.
    - Không giá trị nào trong biểu đồ đến từ một dãy số cố định trong mã nguồn.
    - Danh sách hoạt động hôm nay sắp theo giờ chấm vào tăng dần, và mỗi mục có tên phòng ban tra được từ dữ liệu thật.
    - Danh sách "chưa chấm công" loại người đã nghỉ việc và người đang nghỉ phép, và cắt ở tối đa 6 người, đúng như V1.
    - Bảng đếm yêu cầu chờ duyệt trả về đủ bốn loại kể cả loại có số đếm bằng 0 — mảng luôn có 4 phần tử.
    - Một ngày không có bản ghi chấm công nào trả về `DashboardSummary` với các số đếm bằng 0 và biểu đồ đủ 7 điểm, không phải lỗi (edge DATA-05 empty).
  </behavior>
  <action>
    **(a) `src/lib/validation/api/dashboard.ts`.** `dashboardSummarySchema` khớp `DashboardSummary`
    đầy đủ gồm cả bốn cấu trúc con. `dashboardQuerySchema` khai đúng một tham số `date` — không
    khai định danh doanh nghiệp.

    **(b) `src/app/api/dashboard/route.ts`.** `GET` theo khuôn 02-04. Tính toàn bộ `DashboardSummary`
    từ dữ liệu thật của doanh nghiệp trong phiên:
    - Bốn KPI: tổng nhân sự (kể cả người chưa kích hoạt tài khoản, đúng như comment của V1), số
      người đã chấm vào trong ngày, số người đi muộn, số người nghỉ phép. Mỗi KPI kèm `delta` so
      với ngày liền trước tính từ dữ liệu thật.
    - Biểu đồ 7 ngày: sáu ngày trước cộng ngày được truyền vào; mỗi điểm mang số có mặt, số muộn,
      số vắng đếm từ `attendance_records` của đúng ngày đó. **Xóa hẳn** hai dãy số mẫu của V1 —
      chúng là dữ liệu giả và tồn tại chỉ vì tầng giả lập không có lịch sử thật.
    - Hoạt động hôm nay: mỗi bản ghi có giờ vào, kèm tên nhân viên, tên phòng ban, ảnh đại diện,
      trạng thái, địa điểm; sắp theo giờ vào tăng dần.
    - Chưa chấm công: nhân viên còn làm việc, không nghỉ phép, chưa có bản ghi trong ngày; tối đa 6
      người; kèm tên ca. Sắp xếp xác định theo `(full_name, id)` để danh sách không nhảy giữa hai
      lần tải.
    - Yêu cầu chờ duyệt: đếm theo bốn loại; loại không có yêu cầu nào vẫn xuất hiện với số đếm 0.
    Nếu số truy vấn cần thiết vượt quá mức hợp lý cho một handler, gom lại thành một hàm RPC trong
    một migration mới và kèm một bước `npm run db:push` trong task này; ghi lựa chọn vào SUMMARY.

    **(c) `src/lib/data/dashboard.ts`.** `getDashboardSummary(companyId, date)` giữ nguyên chữ ký,
    gọi qua `fetchJson` với `date` là tham số truy vấn duy nhất. Tham số `companyId` giữ để không
    phải sửa call site nhưng không gửi lên server.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -rc "CHART_PRESENT_OFFSET\|CHART_LATE_VALUES" src/lib/data/ src/app/api/` trả về `0` — không dãy số mẫu nào được mang sang tầng dữ liệu mới.
    - Với cookie owner Ngọc Phát và `date` là ngày hôm nay của server: `curl -b <cookie> -s "http://localhost:3000/api/dashboard?date=<today>"` trả 200 với `chart` có đúng 7 phần tử và `pendingRequests` có đúng 4 phần tử.
    - Trong phản hồi đó, `chart[6].date` bằng đúng giá trị `date` truyền vào.
    - Trong phản hồi đó, `totalEmployees.value` bằng `28` cho Ngọc Phát; với cookie Bình Minh bằng `12`.
    - Với `date` là một ngày trong tương lai xa (không có bản ghi nào): phản hồi vẫn là 200, `chart` vẫn 7 phần tử, mọi số đếm bằng 0.
    - `curl -b <cookie> -s "http://localhost:3000/api/dashboard?date=<today>"` gọi hai lần liên tiếp trả về `notCheckedIn` với cùng thứ tự phần tử.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; node -e "const {execSync}=require('child_process');const out=execSync('git grep -l \"CHART_PRESENT_OFFSET\\|CHART_LATE_VALUES\" -- src/lib/data src/app/api || true',{encoding:'utf8'}).trim();if(out){console.error('day so mau con sot: '+out);process.exit(1)}"</automated>
  </verify>
  <done>Bảng điều khiển vẽ từ bản ghi chấm công thật; biểu đồ 7 ngày không còn một con số nào đến từ hằng số; ngày không có dữ liệu ra số 0 chứ không ra lỗi.</done>
</task>

<task type="auto">
  <name>Task 3: Ba màn hình nhận "hôm nay" từ Server Component và chuyển sang nguồn dữ liệu thật</name>
  <files>src/lib/today.ts, src/app/admin/dashboard/page.tsx, src/app/admin/dashboard/dashboard-view.tsx, src/app/employee/page.tsx, src/app/employee/employee-home-view.tsx, src/app/employee/history/page.tsx, src/app/employee/history/history-view.tsx, src/lib/data/__tests__/attendance.test.ts, src/lib/__tests__/today.test.ts</files>
  <read_first>
    - `src/app/admin/dashboard/page.tsx`, `src/app/employee/page.tsx`,
      `src/app/employee/history/page.tsx` — cả ba hiện là Server Component chỉ render View, chưa
      truyền prop nào
    - `src/app/admin/dashboard/dashboard-view.tsx` dòng 19 và 25 — nơi hằng số ngày cố định được
      dùng làm giá trị khởi tạo của state
    - `src/app/employee/employee-home-view.tsx` dòng 14, 48, 50, 83, 111, 156 — sáu chỗ dùng hằng số
      ngày/tháng cố định, mỗi chỗ một vai trò khác nhau (tham số truy vấn, tham số chấm công, nhãn
      hiển thị)
    - `src/app/employee/history/history-view.tsx` dòng 16, 45, 87 — hằng số tháng dùng làm giá trị
      khởi tạo và làm chặn trên của bộ chọn tháng
    - `src/lib/today.ts` (Task 1) — hai hàm sẽ được gọi
  </read_first>
  <action>
    **(a) Ba Server Component `page.tsx`.** Mỗi file `await` hàm tương ứng từ `src/lib/today.ts` rồi
    truyền xuống View làm prop:
    - `src/app/admin/dashboard/page.tsx` → `<DashboardView today={today} />`.
    - `src/app/employee/page.tsx` → `<EmployeeHomeView today={today} month={month} />`.
    - `src/app/employee/history/page.tsx` → `<HistoryView month={month} />`.
    Giữ nguyên `export const metadata` của cả ba file.

    **(b) `dashboard-view.tsx`.** Thêm prop `today: string`; giá trị khởi tạo của state ngày lấy từ
    prop đó thay vì hằng số. Xóa dòng import hằng số. Không đổi logic đổi ngày — người dùng vẫn xem
    được ngày khác. Đổi dòng import hàm dữ liệu sang `@/lib/data/dashboard`.

    **(c) `employee-home-view.tsx`.** Thêm prop `today: string` và `month: string`. Thay từng chỗ
    dùng hằng số bằng prop tương ứng, xử lý đúng vai trò của từng chỗ: tham số truy vấn dữ liệu,
    tham số truyền cho hàm chấm công, và nhãn hiển thị "Hôm nay, …". Xóa dòng import hằng số. Đổi
    dòng import hàm dữ liệu: `getEmployee` từ `@/lib/data/employees`, `listShifts` từ
    `@/lib/data/shifts`, bốn hàm còn lại từ `@/lib/data/attendance`.

    **(d) `history-view.tsx`.** Thêm prop `month: string`; giá trị khởi tạo của state tháng và chặn
    trên của bộ chọn tháng đều lấy từ prop đó. Xóa dòng import hằng số. Đổi dòng import hàm dữ liệu:
    `listShifts` từ `@/lib/data/shifts`, hai hàm còn lại từ `@/lib/data/attendance`.

    **(e) `src/lib/__tests__/today.test.ts`.** Khẳng định `getServerToday()` cho cùng kết quả khi
    biến múi giờ của tiến trình được đặt lần lượt là `UTC` và `America/New_York`, và kết quả đó khớp
    biểu thức `^\d{4}-\d{2}-\d{2}$`. Thêm một trường hợp biên: một khoảnh khắc thuộc ngày hôm sau
    theo giờ Việt Nam nhưng vẫn là hôm nay theo giờ UTC (buổi tối muộn theo UTC) phải cho ra ngày
    Việt Nam, không phải ngày UTC. Dùng thời gian giả của Vitest để cố định khoảnh khắc.

    **(f) `src/lib/data/__tests__/attendance.test.ts`.** Bốn nhóm khẳng định trên `fetch` giả lập:
    tháng rỗng trả `[]`; bản tổng hợp tháng rỗng trả toàn số 0 với `month` đúng bằng tham số; giá
    trị `timestamptz` trả về được chuyển đúng sang `HH:mm` theo giờ Việt Nam; giá trị `null` của giờ
    ra giữ nguyên `null` chứ không thành chuỗi rỗng.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -rEc "REFERENCE_DATE|REFERENCE_MONTH" src/app/admin/dashboard/ src/app/employee/employee-home-view.tsx src/app/employee/history/` trả về `0` cho mọi file.
    - `grep -rc "@/lib/mock/service" src/app/admin/dashboard/ src/app/employee/employee-home-view.tsx src/app/employee/history/` trả về `0` cho mọi file.
    - `grep -c "getServerToday" src/app/admin/dashboard/page.tsx` trả về ít nhất 1; tương tự `src/app/employee/page.tsx`.
    - `grep -rEc "new Date\(\)|Date\.now\(\)" src/app/admin/dashboard/dashboard-view.tsx src/app/employee/employee-home-view.tsx src/app/employee/history/history-view.tsx` trả về `0` cho cả ba file.
    - `npm run build` thoát 0.
    - Với server dev đang chạy và một phiên hợp lệ, mở `/admin/dashboard` và `/employee` trong trình duyệt: DevTools Console **không** có cảnh báo hydration mismatch. Kết quả quan sát ghi vào SUMMARY.
    - `npx vitest run src/lib/__tests__/today.test.ts src/lib/data/__tests__/attendance.test.ts` thoát 0 và báo ít nhất 7 test.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run src/lib/__tests__/today.test.ts src/lib/data/__tests__/attendance.test.ts</automated>
  </verify>
  <done>Ba màn hình nhận "hôm nay" từ Server Component làm prop và chạy trên dữ liệu Postgres thật; không client component nào trong ba màn hình đó còn tự đọc đồng hồ; build sạch và không có cảnh báo lệch giữa lần vẽ ở server và ở client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Đồng hồ thiết bị người dùng → bản ghi chấm công | Giá trị thời gian do client khai không phải bằng chứng |
| Nhân viên → dữ liệu chấm công của người khác | Ranh giới quyền riêng tư trong nội bộ một doanh nghiệp |
| Trình duyệt → `GET /api/attendance`, `/api/attendance/summary`, `/api/dashboard` | URL công khai, xác thực bằng cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08-01 | Tampering | Người dùng đổi đồng hồ thiết bị để chấm công lệch giờ | high | mitigate | `check_in_at` ghi bằng `now()` của Postgres; `work_date` suy từ `tf_work_date(now())`; tham số giờ và ngày do nơi gọi truyền chỉ dùng để đối chiếu, lệch thì ném lỗi; acceptance criteria đếm số dòng có `work_date` lệch phải bằng 0 |
| T-02-08-02 | Spoofing | Nhân viên chấm công hộ người khác | high | mitigate | `checkIn`/`checkOut` so `employeeId` tham số với `employeeId` của phiên cho vai trò không phải quản trị |
| T-02-08-03 | Information Disclosure | Nhân viên đọc lịch sử chấm công của người khác | high | mitigate | Handler đọc trả 403 khi vai trò không phải quản trị hỏi một `employeeId` khác của mình; acceptance criteria gọi thật và đòi mã 403 |
| T-02-08-04 | Information Disclosure | Route Handler bảng điều khiển bị cache giữa hai doanh nghiệp | high | mitigate | `force-dynamic` tường minh; acceptance criteria gọi cùng URL bằng hai cookie và đòi 28 với 12 |
| T-02-08-05 | Repudiation | Chấm công không để lại vết | medium | mitigate | `logMutation` trong cùng hàm ghi; acceptance criteria đo số dòng audit tăng đúng 1 |
| T-02-08-06 | Tampering | Bảng điều khiển hiển thị số liệu bịa | medium | mitigate | Hai dãy số mẫu của tầng giả lập bị xóa hẳn; acceptance criteria có một lệnh grep chặn chúng quay lại tầng dữ liệu mới |
| T-02-08-07 | Denial of Service | Lệch múi giờ làm "hôm nay" ở ứng dụng khác "hôm nay" ở database, người dùng không chấm công được | medium | mitigate | `getServerToday()` dùng chung hằng số múi giờ với `tf_tz()`; acceptance criteria so trực tiếp hai giá trị và ghi cả hai vào SUMMARY; test chạy dưới hai biến múi giờ khác nhau |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test && npm run build` thoát 0
- `getServerToday()` bằng đúng `public.tf_work_date(now())` của database
- Không dòng `attendance_records` nào có `work_date` lệch với `tf_work_date(check_in_at)`
- `/api/dashboard` trả 7 điểm biểu đồ và 4 loại yêu cầu, kể cả cho ngày không có dữ liệu
- Nhân viên thường nhận 403 khi hỏi dữ liệu chấm công của người khác
- Ba màn hình không còn tham chiếu hằng số ngày cố định và không có cảnh báo lệch lần vẽ
</verification>

<success_criteria>
Ba màn hình mang tính thời gian nhất của sản phẩm chạy trên dữ liệu thật theo giờ thật; dấu thời
gian chấm công là bằng chứng do server cấp chứ không phải lời khai của thiết bị; bảng điều khiển
không còn một con số nào đến từ hằng số.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-08-SUMMARY.md` when done
</output>