---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - supabase/migrations/0006_platform_admins.sql
  - supabase/migrations/0007_search_normalize.sql
  - supabase/tests/00_fixture_users.sql
  - supabase/tests/06_platform_admins.sql
  - supabase/tests/07_search_normalize.sql
  - supabase/tests/run-all.sql
  - supabase/tests/05_seed_fixture.sql
  - supabase/seed.sql
  - scripts/check-pgtap-assertions.mjs
autonomous: true
requirements: [AUTH-03, DATA-05]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Bảng `platform_admins` tồn tại với đúng hai cột `user_id uuid not null references auth.users(id) on delete cascade` và `created_at timestamptz not null default now()`, bật RLS, và có ít nhất một policy (D-11, D-11a)."
    - "Không tài khoản nào đọc được trực tiếp một dòng nào của `platform_admins` — kể cả chính người đang có tên trong bảng đó; mọi câu trả lời đi qua `tf_is_platform_admin()` (D-11a)."
    - "`tf_is_platform_admin()` trả `true` cho người có tên trong bảng, `false` cho người không có tên, và `false` khi không có phiên đăng nhập — hàm tự lọc theo `auth.uid()` bên trong, không nhận tham số user (D-11b)."
    - "`supabase/seed.sql` không còn chứa chuỗi uuid tổng hợp nào và không còn câu lệnh nào ghi vào `auth.users` (D-15)."
    - "Bộ test pgTAP tự dựng fixture của mình: chạy `npm run test:db` trên một Postgres chỉ có migration + seed vẫn xanh (D-15a)."
    - "`npm run check:assertions` báo tổng số assertion của bộ pgTAP theo đúng thứ tự thực thi của `scripts/db.mjs`, và thoát khác 0 nếu tổng đó nhỏ hơn mốc 170 đo được ngày 2026-07-31 (D-15a)."
    - "`supabase/tests/00_rls_coverage.sql` vẫn xanh sau khi thêm bảng mới — bảng mới không rơi vào cả hai nhánh vi phạm của cổng (thiếu RLS, hoặc bật RLS mà 0 policy)."
    - "Người dùng là thành viên active của hai doanh nghiệp với hai vai trò khác nhau đọc được dữ liệu của cả hai, và `tf_is_member()` trả `true` cho cả hai `company_id` — vai trò khác nhau không làm mất tư cách thành viên ở bên nào (edge AUTH-03 adjacency)."
    - "Người dùng đã tồn tại trong `auth.users` nhưng không có membership active nào đọc được 0 dòng ở mọi bảng thuộc phạm vi doanh nghiệp, và điều đó phân biệt được với lỗi truy vấn (edge AUTH-03 empty)."
    - "`public.tf_normalize(text)` trả về cùng kết quả với `normalizeText()` ở `src/lib/format.ts` cho mọi chuỗi tiếng Việt: bỏ dấu, đổi đ/Đ thành d/D, hạ chữ thường, cắt khoảng trắng hai đầu — nhờ vậy ô tìm kiếm nhân viên giữ nguyên hành vi V1 sau khi chuyển sang truy vấn phía server."
  artifacts:
    - path: "supabase/migrations/0006_platform_admins.sql"
      provides: "Bảng platform_admins + hàm tf_is_platform_admin() + RLS chặn hết"
      contains: "tf_is_platform_admin"
    - path: "supabase/tests/00_fixture_users.sql"
      provides: "Bốn uuid tổng hợp + memberships + gán employees.user_id + actor audit — chỉ tồn tại trong supabase/tests/"
    - path: "supabase/tests/06_platform_admins.sql"
      provides: "pgTAP cho D-11/D-11a/D-11b"
      contains: "select plan("
    - path: "supabase/migrations/0007_search_normalize.sql"
      provides: "public.tf_normalize(text) — chuẩn hóa chuỗi tiếng Việt phía server, khớp normalizeText() của V1"
      contains: "tf_normalize"
    - path: "supabase/tests/07_search_normalize.sql"
      provides: "pgTAP đối chiếu tf_normalize với các cặp giá trị lấy từ dữ liệu seed thật"
    - path: "scripts/check-pgtap-assertions.mjs"
      provides: "Cổng cơ học D-15a — tổng assertion không được tụt dưới mốc Phase 1"
  key_links:
    - from: "supabase/tests/run-all.sql"
      to: "supabase/tests/00_fixture_users.sql"
      via: "\\ir nạp fixture ngay sau helpers.sql, trước mọi file test dùng tới bốn uuid"
      pattern: "\\\\ir 00_fixture_users\\.sql"
    - from: "supabase/migrations/0006_platform_admins.sql"
      to: "supabase/migrations/0002_tenancy.sql"
      via: "nhân bản khuôn security definer + revoke execute from public của tf_is_member"
      pattern: "security definer"
    - from: "scripts/check-pgtap-assertions.mjs"
      to: "supabase/tests/run-all.sql"
      via: "đọc danh sách \\ir để dựng lại đúng thứ tự thực thi của scripts/db.mjs"
      pattern: "run-all\\.sql"
  prohibitions:
    - statement: "MUST NOT hạ mốc assertion trong `scripts/check-pgtap-assertions.mjs` để làm cổng xanh — mốc chỉ được nâng lên. Hạ mốc là cách âm thầm biến một bộ test đang co lại thành một cổng vẫn báo xanh."
      category: transparency
---

<objective>
Dựng vai trò nền tảng thứ tư ở mức tối thiểu (D-11) và cắt bốn uuid tổng hợp ra khỏi `seed.sql`
để chúng không bao giờ chạm cloud (D-15), kèm một cổng cơ học chặn việc bộ test âm thầm co lại
(D-15a). Kết thúc bằng bước đẩy schema thật.

Purpose: `platform_admins` phải có mặt **trước** khi tầng kiểm quyền của phase này được viết —
thêm vai trò thứ tư sau khi mọi guard đã viết xong nghĩa là mở lại từng guard (D-11, costly).
Việc tách fixture phải xong **trước** khi có tài khoản thật, vì chèn thẳng vào `auth.users` mà
thiếu `auth.identities` là chính xác lý do không tài khoản nào trong seed hiện tại đăng nhập được.

Output: migration 0006, fixture pgTAP tách riêng, bộ test mới cho D-11, cổng đếm assertion, schema
đã đẩy lên Postgres thật.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-PATTERNS.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-06-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Bảng SQL | `public.platform_admins` |
| Hàm SQL | `public.tf_is_platform_admin()`, `public.tf_normalize(text)` |
| Policy SQL | `platform_admins_select_none` |
| File migration | `supabase/migrations/0006_platform_admins.sql`, `supabase/migrations/0007_search_normalize.sql` |
| File test | `supabase/tests/00_fixture_users.sql`, `supabase/tests/06_platform_admins.sql`, `supabase/tests/07_search_normalize.sql` |
| Script | `scripts/check-pgtap-assertions.mjs` (chạy qua `npm run check:assertions`, script đã khai ở 02-01) |

<tasks>

<task type="auto">
  <name>Task 1: Hai migration — platform_admins + tf_is_platform_admin() (D-11), và tf_normalize() cho tìm kiếm tiếng Việt</name>
  <reversibility rating="costly">Tầng kiểm quyền viết ở phase này được gọi từ mọi màn hình; thêm vai trò thứ tư ở Phase 6 nghĩa là mở lại từng guard đã viết (D-11).</reversibility>
  <files>supabase/migrations/0006_platform_admins.sql, supabase/migrations/0007_search_normalize.sql, supabase/tests/07_search_normalize.sql</files>
  <read_first>
    - `supabase/migrations/0002_tenancy.sql` dòng 56-77 — khuôn `tf_is_member()`: `language sql`,
      `stable`, `security definer`, `set search_path = public, pg_temp`, rồi
      `revoke execute ... from public` + `grant execute ... to authenticated`. D-11b bắt buộc nhân
      bản đúng khuôn này. Đọc cả khối comment giải thích vì sao hàm phải tự lọc theo `auth.uid()`
      bên trong thay vì nhận `user_id` làm tham số (threat T-01-03)
    - `supabase/migrations/0005_v2_tables.sql` dòng 88-99 — kiểu cột `actor_user_id uuid null references auth.users (id) on delete set null`;
      `platform_admins.user_id` theo quy ước `memberships.user_id` chứ không theo quy ước này
    - `supabase/migrations/0002_tenancy.sql` dòng 38-47 — `memberships.user_id uuid not null references auth.users (id) on delete cascade`, đây là quy ước phải theo
    - `supabase/tests/00_rls_coverage.sql` — hai nhánh vi phạm mà bảng mới phải thoát: `relrowsecurity = false`, và RLS bật mà `pg_policies` đếm 0
  </read_first>
  <action>
    Tạo `supabase/migrations/0006_platform_admins.sql`. Bốn phần, theo đúng thứ tự và đúng phong cách
    comment của các migration Phase 1 (comment tiếng Việt không dấu ở đầu file giải thích *vì sao*,
    kèm mã quyết định `D-11` / `D-11a` / `D-11b`).

    **(a) Bảng.** `create table platform_admins (user_id uuid primary key references auth.users (id) on delete cascade, created_at timestamptz not null default now());`
    Đúng hai cột theo D-11 — không thêm cột `note`, `granted_by`, `is_active` hay bất cứ thứ gì
    "để sau này dùng". `user_id` làm khóa chính vì một người chỉ có thể là platform admin một lần.

    **(b) Hàm.** `create function public.tf_is_platform_admin() returns boolean` —
    **không tham số** (khác `tf_is_member(text)`), `language sql`, `stable`, `security definer`,
    `set search_path = public, pg_temp`, thân hàm là
    `select exists (select 1 from platform_admins where user_id = (select auth.uid()))`.
    Bọc `auth.uid()` trong `(select ...)` đúng như `tf_is_member` để planner cache được giá trị.

    **(c) Quyền thực thi.** `revoke execute on function public.tf_is_platform_admin() from public;`
    rồi `grant execute on function public.tf_is_platform_admin() to authenticated;` — D-11b.

    **(d) RLS chặn hết.** `alter table platform_admins enable row level security;` cộng đúng một
    policy `create policy platform_admins_select_none on platform_admins for select using (false);`.
    Không policy cho INSERT/UPDATE/DELETE — RLS bật mà không có policy cho một lệnh nghĩa là lệnh đó
    bị từ chối mặc định, đó chính là điều D-11a muốn. Policy SELECT `using (false)` tồn tại **chỉ**
    để `00_rls_coverage.sql` không đỏ ở nhánh "bật RLS mà 0 policy" — comment trong file phải nói
    rõ điều đó, để người đọc sau không tưởng đây là policy cho phép đọc.

    **Không đụng bất kỳ policy nào của Phase 1** (D-11). Không thêm giá trị vào enum `company_role`
    (`<specifics>` mục 2 đã xác nhận enum đang đúng).

    **Giới hạn phải ghi vào SUMMARY (D-11):** ở phase này nhánh platform admin chỉ kiểm chứng được
    tới mức "hàm trả đúng true/false". Việc "platform admin thấy đúng cái được phép thấy" chưa có
    đường đọc nào để chứng minh và chỉ chứng minh được ở Phase 6. SUMMARY không được viết như thể
    đã kiểm chứng đầy đủ.

    **(e) Migration thứ hai — `supabase/migrations/0007_search_normalize.sql`.** Ô tìm kiếm nhân viên
    của V1 lọc bằng `normalizeText()` (`src/lib/format.ts` dòng 200-208): bỏ dấu bằng `NFD` +
    xóa dấu tổ hợp, đổi `đ`→`d` và `Đ`→`D`, hạ chữ thường, cắt khoảng trắng. Khi phép lọc chuyển
    sang phía server ở plan 02-05, Postgres phải làm đúng phép biến đổi đó, nếu không tìm "nguyen"
    sẽ không ra "Nguyễn" và người dùng mất một hành vi đang có.
    Tạo `public.tf_normalize(p text) returns text`, `language sql`, **`immutable`**,
    `set search_path = public, pg_temp`, thân hàm là `lower(trim(translate(p, <chuỗi ký tự có dấu>, <chuỗi ký tự ASCII tương ứng>)))`.
    Dùng `translate()` với bảng ký tự tiếng Việt đầy đủ (a/ă/â, e/ê, i, o/ô/ơ, u/ư, y, d/đ và toàn bộ
    biến thể dấu thanh, cả chữ hoa lẫn chữ thường) — **không** dùng extension `unaccent`: extension
    phải có mặt trên cả Postgres CI lẫn Supabase cloud, `unaccent()` là `stable` chứ không `immutable`,
    và bảng ký tự tiếng Việt là tập đóng nên `translate()` cho kết quả xác định trên mọi môi trường.
    Hai chuỗi truyền cho `translate()` phải dài bằng nhau — viết comment nhắc điều đó ngay trên hàm.
    Không tạo index nào ở migration này: bộ dữ liệu ở quy mô hai doanh nghiệp pilot chưa cần, và
    index trên biểu thức sẽ khóa cứng định nghĩa hàm.

    **(f) `supabase/tests/07_search_normalize.sql`** theo khuôn `02_time_overnight.sql`
    (`begin; select plan(N); ... rollback;`), tối thiểu 6 assertion đối chiếu `tf_normalize` với kết
    quả mà `normalizeText()` trả về cho cùng đầu vào — dùng chính chuỗi có thật trong seed:
    `'Nguyễn Minh Anh'` → `'nguyen minh anh'`; `'Đoàn Minh Trí'` → `'doan minh tri'`;
    `'  Trần Hoàng Nam  '` → `'tran hoang nam'`; `'NV001'` → `'nv001'`; chuỗi rỗng → chuỗi rỗng;
    và một assertion khẳng định `tf_normalize` là `immutable` (`provolatile = 'i'` trong `pg_proc`).
    Thêm `\ir 07_search_normalize.sql` vào `run-all.sql` ở Task 2.
  </action>
  <acceptance_criteria>
    - `grep -c 'security definer' supabase/migrations/0006_platform_admins.sql` trả về ít nhất 1.
    - `grep -c 'revoke execute on function public.tf_is_platform_admin' supabase/migrations/0006_platform_admins.sql` trả về 1.
    - `grep -c 'set search_path = public, pg_temp' supabase/migrations/0006_platform_admins.sql` trả về 1.
    - `grep -vE '^\s*--' supabase/migrations/0006_platform_admins.sql | grep -cE 'company_role|alter type' ` trả về 0 — migration không đụng enum vai trò doanh nghiệp.
    - `grep -vE '^\s*--' supabase/migrations/0006_platform_admins.sql | grep -cE 'drop policy|alter policy'` trả về 0 — không đụng policy của Phase 1.
    - `grep -c 'enable row level security' supabase/migrations/0006_platform_admins.sql` trả về 1.
    - `grep -c 'immutable' supabase/migrations/0007_search_normalize.sql` trả về ít nhất 1, và `grep -c 'create extension' supabase/migrations/0007_search_normalize.sql` trả về `0`.
    - `node -e "const s=require('fs').readFileSync('supabase/migrations/0007_search_normalize.sql','utf8');const m=s.match(/translate\(\s*p\s*,\s*'([^']*)'\s*,\s*'([^']*)'\s*\)/s);if(!m){console.error('khong tim thay translate 2 doi so');process.exit(1)}if([...m[1]].length!==[...m[2]].length){console.error('hai chuoi translate lech do dai: '+[...m[1]].length+' vs '+[...m[2]].length);process.exit(1)}"` thoát 0.
    - `grep -c 'select plan(' supabase/tests/07_search_normalize.sql` trả về 1.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0006_platform_admins.sql','utf8');const body=s.split(/\r?\n/).filter(l=>!/^\s*--/.test(l)).join('\n');const need=['security definer','set search_path = public, pg_temp','revoke execute on function public.tf_is_platform_admin','enable row level security','create policy platform_admins_select_none'];const miss=need.filter(n=>!body.includes(n));const bad=['drop policy','alter policy','alter type company_role'].filter(n=>body.includes(n));if(miss.length||bad.length){console.error({miss,bad});process.exit(1)}"</automated>
  </verify>
  <done>Migration 0006 tồn tại, nhân bản đúng khuôn bảo vệ của `tf_is_member`, bật RLS kèm đúng một policy chặn đọc, và không chạm gì của Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Tách bốn uuid tổng hợp khỏi seed.sql sang fixture pgTAP, thêm test D-11, dựng cổng đếm assertion</name>
  <files>supabase/tests/00_fixture_users.sql, supabase/tests/06_platform_admins.sql, supabase/tests/run-all.sql, supabase/tests/05_seed_fixture.sql, supabase/seed.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `supabase/seed.sql` dòng 20-45 (khối `truncate` + khối `insert into auth.users`), dòng 78-90
      (khối `memberships`), dòng 140-148 và 308-316 (hai dòng `employees` mang `user_id` cứng),
      dòng 686-700 (khối `audit_log` mang `actor_user_id` cứng)
    - `supabase/tests/run-all.sql` — thứ tự `\ir` hiện tại; fixture mới phải nạp ngay sau `helpers.sql`
    - `supabase/tests/helpers.sql` — `tf_test_login(uuid)` / `tf_test_logout()`, dùng lại nguyên xi
    - `supabase/tests/01_isolation_companies.sql` dòng 1-40 — khuôn `begin; select plan(N); ... select * from finish(); rollback;` và phong cách mô tả assertion
    - `supabase/tests/05_seed_fixture.sql` dòng 44-55 — hai assertion về `memberships` sẽ đổi ý nghĩa sau khi tách
    - `scripts/db.mjs` hàm `cmdTest` (dòng 108-130) — thứ tự thực thi thật: `00_install_pgtap.sql`,
      rồi `00_rls_coverage.sql` **đứng riêng**, rồi `run-all.sql` (bản thân run-all lại `\ir` một lần
      nữa `00_rls_coverage.sql`). Cổng đếm assertion phải dựng lại đúng thứ tự này, nếu không con số
      sẽ lệch 4
  </read_first>
  <action>
    **(a) Gỡ uuid khỏi `supabase/seed.sql`.** Bốn chỗ, mỗi chỗ một cách xử lý khác nhau:
    1. Xóa toàn bộ khối chèn vào `auth.users` cùng comment header của nó. `seed.sql` sau bước này
       không được còn câu lệnh nào ghi vào schema `auth`.
    2. Xóa toàn bộ khối chèn `memberships`. Giữ `memberships` trong danh sách `truncate` ở đầu file.
    3. Hai dòng `employees` `nv-01a` và `nv-02a`: đổi giá trị cột cuối (`user_id`) thành `null`.
       Không đổi bất cứ cột nào khác của hai dòng đó.
    4. Khối `audit_log`: giữ nguyên hai dòng nhưng đổi `actor_user_id` thành `null`. Giữ nguyên
       `company_id`, `action`, `entity_table`, `entity_id`, `after` — hai assertion ở
       `05_seed_fixture.sql` và bốn assertion ở `04_isolation_v2.sql` đọc theo `company_id`, không
       theo actor.
    Cập nhật khối comment đầu file `seed.sql` để nói rõ: dữ liệu nghiệp vụ không mang danh tính; danh
    tính đến từ `supabase/tests/00_fixture_users.sql` (Postgres CI) hoặc `scripts/seed-auth.mjs` (cloud).

    **(b) Tạo `supabase/tests/00_fixture_users.sql`.** File này **không** bọc `begin/rollback` và
    **không** có `plan()` — nó là fixture, không phải test, nên các file test chạy sau vẫn thấy dữ
    liệu của nó. Nội dung, theo đúng thứ tự:
    1. Comment header giải thích: bốn uuid tổng hợp ở lại `supabase/tests/` vì trên Postgres CI
       `auth.users` chỉ là bảng compat (`0001_supabase_compat.sql`) nên không có vấn đề
       `auth.identities`; trên cloud thật thì `scripts/seed-auth.mjs` mới là đường tạo tài khoản
       (D-15). Uuid tổng hợp không bao giờ chạm cloud.
    2. Chèn 4 dòng `auth.users` — chép nguyên xi bốn dòng đã gỡ ở bước (a), giữ nguyên cả
       `instance_id`, `aud`, `role`, `email` và mệnh đề `on conflict (id) do nothing`.
    3. Chèn 4 dòng `memberships` — chép nguyên xi khối đã gỡ ở bước (a): `…0001`→`cty-01` owner,
       `…0002`→`cty-02` owner, `…0003`→`cty-01` admin **và** `…0003`→`cty-02` admin. Giữ nguyên
       chú thích rằng `…0004` cố ý không có membership nào.
    4. `update employees set user_id = '…0001' where id = 'nv-01a'` và tương tự `…0002`/`nv-02a`.
    5. `update audit_log set actor_user_id = '…0001' where company_id = 'cty-01' and entity_id = 'nv-01a'`
       và tương tự cho `cty-02`/`nv-02a` — khôi phục ràng buộc khóa ngoại tới `auth.users` để pgTAP
       vẫn kiểm được đường đó.
    6. `insert into platform_admins (user_id) values ('…0004') on conflict (user_id) do nothing;` —
       chọn `…0004` có chủ ý: người này **không** là thành viên doanh nghiệp nào, nên fixture thể
       hiện đúng mô hình "platform admin không phải một giá trị thứ năm của vai trò doanh nghiệp".

    **(c) Nối fixture và hai file test mới vào `supabase/tests/run-all.sql`.** Thêm
    `\ir 00_fixture_users.sql` ngay sau dòng `\ir helpers.sql` và trước `\ir 00_rls_coverage.sql`.
    Thêm `\ir 06_platform_admins.sql` và `\ir 07_search_normalize.sql` vào cuối danh sách. Không đổi
    thứ tự các dòng còn lại.

    **(d) Sửa hai assertion đã đổi ý nghĩa ở `supabase/tests/05_seed_fixture.sql`.** Hai assertion
    về `memberships` giờ đo fixture của test chứ không còn đo `seed.sql`. Không xóa chúng (D-15a cấm
    giảm số assertion) — sửa **chuỗi mô tả** để nói đúng nó đang đo cái gì, ví dụ nêu rõ nguồn dữ
    liệu là fixture của bộ test. Giữ nguyên `select plan(35)`.

    **(e) Tạo `supabase/tests/06_platform_admins.sql`** theo khuôn `01_isolation_companies.sql`:
    `begin; select plan(8); ... select * from finish(); rollback;`. Tám assertion:
    1. `tf_is_platform_admin()` trả `true` khi đăng nhập bằng `…0004`.
    2. `tf_is_platform_admin()` trả `false` khi đăng nhập bằng `…0001` (owner, không phải platform admin).
    3. `tf_is_platform_admin()` trả `false` khi không có phiên (sau `tf_test_logout()`, dùng
       vai trò mặc định của kết nối — `auth.uid()` là NULL).
    4. `platform_admins` có `relrowsecurity = true`.
    5. `platform_admins` có ít nhất một dòng trong `pg_policies`.
    6. Đăng nhập bằng `…0004` rồi `select count(*) from platform_admins` trả về `0` — chính người
       có tên trong bảng cũng không đọc trực tiếp được (đây là khẳng định cốt lõi của D-11a).
    7. Đăng nhập bằng `…0001` rồi `insert into platform_admins (user_id) values ('…0001')` bị từ chối —
       dùng `throws_ok` với thông điệp `new row violates row-level security policy for table "platform_admins"`
       theo đúng khuôn `04_isolation_v2.sql`.
    8. `has_function_privilege('public', 'tf_is_platform_admin()', 'execute')` trả `false` cho vai
       trò `public` — chứng minh `revoke` đã có hiệu lực, không chỉ có mặt trong file migration.
    Mỗi assertion phải nêu đích danh `platform_admins` hoặc `tf_is_platform_admin` trong chuỗi mô tả
    để output TAP trả lời được "cái gì đỏ".

    **(f) Tạo `scripts/check-pgtap-assertions.mjs`** — chỉ dùng built-in Node 22 (`node:fs`,
    `node:path`, `node:process`), không thêm dependency, theo đúng ràng buộc D-03 mà `scripts/db.mjs`
    đã ghi. Logic:
    1. Hằng số `MIN_ASSERTIONS = 170`, kèm comment ghi rõ đây là số đo được ngày 2026-07-31 trên
       cây Phase 1: 4 (lần chạy riêng của cổng RLS) + 4 + 18 + 53 + 41 + 15 + 35 (bảy lần `plan()`
       theo thứ tự `run-all.sql`). Ghi rõ mốc này **chỉ được nâng lên**, không bao giờ hạ xuống.
    2. Đọc `supabase/tests/run-all.sql`, lấy danh sách tên file từ các dòng `\ir` theo đúng thứ tự.
    3. Dựng lại thứ tự thực thi thật của `scripts/db.mjs` lệnh `test`: một lần `00_rls_coverage.sql`
       đứng riêng, rồi toàn bộ danh sách `\ir` ở bước 2.
    4. Với mỗi file, đọc và trích số trong `select plan(N)`; file không có `plan()` (helpers, fixture)
       đóng góp 0 và được liệt kê rõ là fixture chứ không im lặng bỏ qua.
    5. In bảng breakdown từng file kèm tổng, rồi so tổng với `MIN_ASSERTIONS`.
    6. Thoát 1 kèm thông điệp nêu đúng số hiện tại và số tối thiểu nếu tổng nhỏ hơn mốc; thoát 1
       kèm thông điệp riêng nếu không đọc được `run-all.sql` hoặc danh sách `\ir` rỗng — một cổng
       đếm được 0 file không được coi là xanh.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('fs').readFileSync('supabase/seed.sql','utf8');if(/00000000-0000-0000-0000-0000000000/.test(s)){console.error('seed.sql van con uuid tong hop');process.exit(1)}if(/auth\.users/.test(s.split(/\r?\n/).filter(l=>!/^\s*--/.test(l)).join('\n'))){console.error('seed.sql van ghi vao auth.users');process.exit(1)}"` thoát 0.
    - `grep -c '\\\\ir 00_fixture_users.sql' supabase/tests/run-all.sql` trả về 1.
    - `node -e "const s=require('fs').readFileSync('supabase/tests/run-all.sql','utf8').split(/\r?\n/).filter(l=>l.startsWith('\\\\ir'));if(s.indexOf('\\\\ir 00_fixture_users.sql')!==1)process.exit(1)"` thoát 0 — fixture đứng thứ hai, ngay sau helpers.
    - `grep -c 'select plan(8)' supabase/tests/06_platform_admins.sql` trả về 1.
    - `npm run check:assertions` thoát 0 và output chứa tổng ≥ 170.
    - `npm run check:assertions` thoát khác 0 sau khi tạm hạ một `select plan(N)` bất kỳ xuống đủ để tổng tụt dưới 170, rồi trở lại 0 sau khi hoàn tác — ba mã thoát ghi vào SUMMARY.
    - `npm run typecheck` và `npm run lint` vẫn thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:assertions &amp;&amp; node -e "const s=require('fs').readFileSync('supabase/seed.sql','utf8');if(/00000000-0000-0000-0000-0000000000/.test(s))process.exit(1);const body=s.split(/\r?\n/).filter(l=>!/^\s*--/.test(l)).join('\n');if(/auth\.users/.test(body))process.exit(1)"</automated>
  </verify>
  <done>`seed.sql` sạch uuid tổng hợp và không còn ghi vào `auth.users`; fixture pgTAP mới đứng thứ hai trong `run-all.sql`; 8 assertion mới cho D-11; cổng đếm assertion tồn tại và đã được chứng minh có răng.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Đẩy schema lên Postgres thật rồi chạy trọn bộ pgTAP</name>
  <precondition>`.env.local` chứa `POSTGRES_URL_NON_POOLING` khác rỗng và `psql` gọi được — kiểm bằng `node -e "process.loadEnvFile('.env.local');process.exit(process.env.POSTGRES_URL_NON_POOLING?0:1)"`</precondition>
  <files>(xem <read_first> và <action>)</files>
  <read_first>
    - `scripts/db.mjs` — `cmdPush` gọi `npx supabase db push --db-url <url> --include-all --yes`;
      `cmdTest` chạy `00_install_pgtap.sql`, `00_rls_coverage.sql`, `run-all.sql`; `testdb` chạy
      `seed` rồi `test`. Đây là đường duy nhất được dùng — không gọi `supabase db push` trần
    - `.github/workflows/db-ci.yml` — thứ tự CI: migration → pgTAP → seed → cổng RLS → `run-all.sql`.
      Vì fixture nằm trong `run-all.sql`, CI không phải sửa; xác nhận lại điều đó thay vì giả định
  </read_first>
  <action>
    Chạy `npm run db:push` để áp hai migration `0006_platform_admins.sql` và
    `0007_search_normalize.sql` lên Postgres thật. Bước này
    bắt buộc và không thay thế được bằng `npm run typecheck` hay `npm run build` — cả hai đều xanh
    mà không cần schema, nên bỏ qua push sẽ tạo ra một trạng thái "xanh giả".

    Sau khi push xong, chạy `npm run test:db` (seed rồi test) và xác nhận toàn bộ bộ pgTAP xanh trên
    schema mới. Nếu bất kỳ assertion nào đỏ, sửa file SQL rồi push lại — không được nới assertion.

    Cuối cùng đối chiếu trực tiếp trên database, không qua file:
    - `platform_admins` tồn tại, `relrowsecurity = true`, đếm được đúng 1 policy.
    - `tf_is_platform_admin` tồn tại và `prosecdef = true`.
    - Vai trò `public` không có quyền `execute` trên hàm đó.

    Ghi vào SUMMARY: mã thoát của `db:push`, mã thoát của `test:db`, tổng số assertion mà
    `npm run check:assertions` báo sau khi thêm 8 assertion mới, và giới hạn kiểm chứng của D-11
    (chỉ chứng minh được hàm trả đúng true/false; "platform admin thấy đúng cái được phép thấy"
    phải đợi Phase 6).
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0.
    - `npm run test:db` thoát 0 và output TAP không chứa dòng nào bắt đầu bằng `not ok`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select relrowsecurity from pg_class where oid='public.platform_admins'::regclass"` trả về `t`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_policies where schemaname='public' and tablename='platform_admins'"` trả về `1`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select prosecdef from pg_proc where proname='tf_is_platform_admin'"` trả về `t`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select has_function_privilege('public','public.tf_is_platform_admin()','execute')"` trả về `f`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from auth.users where id::text like '00000000-0000-0000-0000-0000000000%'"` trả về `0` sau khi chỉ chạy `npm run db:seed` (chưa chạy test) — chứng minh uuid tổng hợp không chạm database qua đường seed.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select public.tf_normalize('Nguyễn Minh Anh')"` trả về `nguyen minh anh`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select public.tf_normalize('Đoàn Minh Trí')"` trả về `doan minh tri`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select provolatile from pg_proc where proname='tf_normalize'"` trả về `i`.
    - `npm run check:assertions` thoát 0 và tổng báo về ≥ 184.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run test:db &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Schema đã lên Postgres thật; bộ pgTAP xanh trên schema mới; sáu khẳng định đối chiếu trực tiếp trên database đều đúng; giới hạn kiểm chứng của D-11 đã ghi vào SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Người dùng đã xác thực → Postgres | RLS là ranh giới duy nhất giữa hai doanh nghiệp ở tầng dữ liệu |
| `supabase/tests/` → schema sản phẩm | File test không bao giờ được lọt vào `migrations/` (threat T-01-04 của Phase 1) |
| Máy phát triển (`POSTGRES_URL_NON_POOLING`) → Postgres cloud | Chuỗi kết nối mang quyền `postgres`, bỏ qua toàn bộ RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Elevation of Privilege | `tf_is_platform_admin()` | high | mitigate | Hàm không nhận tham số user và tự lọc theo `auth.uid()` bên trong; `revoke execute from public` + `grant to authenticated`; assertion 8 kiểm quyền thực thi thật trên database chứ không đọc file |
| T-02-02-02 | Information Disclosure | Đọc trực tiếp `platform_admins` | high | mitigate | RLS bật + đúng một policy `using (false)`; assertion 6 khẳng định chính người có tên trong bảng cũng đọc được 0 dòng |
| T-02-02-03 | Tampering | Ghi vào `platform_admins` từ phía người dùng | high | mitigate | Không policy nào cho INSERT/UPDATE/DELETE — mặc định từ chối; assertion 7 dùng `throws_ok` chứng minh |
| T-02-02-04 | Repudiation | Bộ test co lại âm thầm | medium | mitigate | `npm run check:assertions` là cổng cơ học so tổng với mốc 170 đo được; cổng được chứng minh có răng bằng một lần cố tình làm đỏ |
| T-02-02-05 | Spoofing | Uuid tổng hợp lọt lên cloud tạo "tài khoản" không đăng nhập được | medium | mitigate | Acceptance criteria đếm `auth.users` sau khi chỉ seed, phải bằng 0; `seed.sql` bị grep chặn cả chuỗi uuid lẫn mọi câu lệnh chạm schema `auth` |
| T-02-02-06 | Elevation of Privilege | Migration vô ý đụng policy Phase 1 | high | mitigate | Acceptance criteria grep chặn `drop policy` / `alter policy` / `alter type company_role` trong file migration mới |
</threat_model>

<verification>
- `npm run db:push` thoát 0
- `npm run test:db` thoát 0, không có dòng `not ok`
- `npm run check:assertions` thoát 0 với tổng ≥ 178
- `npm run typecheck && npm run lint` thoát 0
</verification>

<success_criteria>
Vai trò nền tảng thứ tư có nhà (bảng + hàm + nhánh RLS) và được kiểm chứng tới đúng giới hạn mà
Phase 2 cho phép; bốn uuid tổng hợp đã ra khỏi `seed.sql` và không thể chạm cloud qua đường seed;
bộ pgTAP tự dựng fixture của mình, xanh, và có cổng cơ học chặn việc co lại.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-02-SUMMARY.md` when done
</output>
