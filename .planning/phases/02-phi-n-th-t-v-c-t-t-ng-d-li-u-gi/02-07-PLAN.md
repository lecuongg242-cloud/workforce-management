---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 07
type: execute
wave: 6
depends_on: ["02-05", "02-06"]
files_modified:
  - src/lib/data/employees.ts
  - src/lib/data/mutations/employees.ts
  - src/lib/validation/api/employees.ts
  - src/app/api/employees/[id]/route.ts
  - src/app/admin/employees/employees-view.tsx
  - src/app/admin/employees/new/new-employee-view.tsx
  - src/components/employees/employee-form.tsx
  - src/app/employee/profile/profile-view.tsx
  - src/components/layout/employee-shell.tsx
  - src/lib/data/__tests__/employees.test.ts
autonomous: true
requirements: [AUTH-03, DATA-05, DATA-06]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Danh sách nhân viên, trang tạo mới, biểu mẫu nhân viên, hồ sơ cá nhân và khung ứng dụng nhân viên đều chạy trên Postgres thật."
    - "`createEmployee` với `code` trùng trong cùng doanh nghiệp bị từ chối và ném `Error` mang thông điệp tiếng Việt nêu đúng mã trùng; cùng `code` đó ở doanh nghiệp khác vẫn tạo được (edge DATA-05 adjacency)."
    - "`listEmployees` phân trang theo `(full_name, id)`: hai nhân viên trùng tên xuất hiện theo cùng thứ tự ở mọi lần gọi, và hợp của trang 1 với trang 2 không có dòng nào lặp và không thiếu dòng nào (edge DATA-05 ordering)."
    - "`getEmployee` với một `id` thuộc doanh nghiệp khác trả về `null`, không phải dữ liệu của doanh nghiệp đó và không phải lỗi 500."
    - "`bulkMoveDepartment` chỉ chuyển những nhân viên thuộc doanh nghiệp hiện hành và trả về đúng số dòng thật sự đã đổi; id lạ trong danh sách bị bỏ qua chứ không làm cả thao tác đổ."
    - "Ba thao tác ghi nhân viên (tạo, sửa, chuyển hàng loạt) đều để lại vết trong `audit_log`; chuyển hàng loạt N nhân viên để lại đúng N dòng, mỗi dòng một nhân viên (DATA-06)."
    - "Vai trò `employee` sửa được hồ sơ của chính mình nhưng không sửa được hồ sơ người khác; vai trò `manager` không tạo được nhân viên mới (AUTH-03)."
  artifacts:
    - path: "src/lib/data/mutations/employees.ts"
      provides: "Server Action ghi nhân viên kèm audit"
      exports: ["createEmployee", "updateEmployee", "bulkMoveDepartment"]
    - path: "src/app/api/employees/[id]/route.ts"
      provides: "Route Handler đọc một nhân viên"
      exports: ["GET", "dynamic"]
    - path: "src/lib/data/__tests__/employees.test.ts"
      provides: "Test biên trùng mã, thứ tự phân trang, và ranh giới doanh nghiệp"
  key_links:
    - from: "src/components/employees/employee-form.tsx"
      to: "src/lib/data/mutations/employees.ts"
      via: "đổi đường dẫn import, giữ nguyên tên hàm và chữ ký"
      pattern: "@/lib/data/employees"
    - from: "src/lib/data/mutations/employees.ts"
      to: "src/lib/data/audit.ts"
      via: "logMutation một dòng cho mỗi nhân viên bị ảnh hưởng, kể cả trong thao tác hàng loạt"
      pattern: "logMutation"
  prohibitions:
    - statement: "MUST NOT gộp một thao tác hàng loạt thành một dòng audit duy nhất. Mỗi nhân viên bị đổi phòng ban là một thay đổi riêng với giá trị trước và sau riêng; gộp lại làm mất khả năng trả lời 'ai đã đổi phòng ban của người này' — đúng câu hỏi mà bảng audit sinh ra để trả lời."
      category: transparency
    - statement: "MUST NOT cho phép một người sửa hồ sơ của người khác chỉ vì cả hai cùng doanh nghiệp. Quyền sửa hồ sơ người khác thuộc về vai trò quản trị; nhân viên thường chỉ sửa được hồ sơ gắn với chính tài khoản mình."
      category: privacy
---

<objective>
Hoàn tất lát cắt nhân viên: đường đọc một hồ sơ, ba đường ghi, và năm màn hình chuyển sang dữ liệu
thật. Đây là nhóm dữ liệu có nhiều ràng buộc nghiệp vụ nhất (mã duy nhất trong doanh nghiệp, thao
tác hàng loạt, quyền sửa hồ sơ của chính mình).

Purpose: ba biên mà bộ dò cạnh nêu ra cho DATA-05 (trùng khớp, rỗng, thứ tự) đều rơi vào đúng nhóm
dữ liệu này, nên đây là nơi chúng được giải quyết bằng test chứ không bằng lời.

Output: một Route Handler, một module Server Action, năm màn hình chạy dữ liệu thật, một bộ test biên.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-PATTERNS.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-05-SUMMARY.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-06-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Route Handler | `GET /api/employees/[id]` |
| Hàm dữ liệu | `getEmployee()` |
| Server Action | `createEmployee()`, `updateEmployee()`, `bulkMoveDepartment()` |
| Schema Zod | `employeeInputSchema`, `employeeDetailResponseSchema` |
| Test | `src/lib/data/__tests__/employees.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Đọc một hồ sơ nhân viên và ba đường ghi kèm audit</name>
  <files>src/lib/data/employees.ts, src/lib/data/mutations/employees.ts, src/lib/validation/api/employees.ts, src/app/api/employees/[id]/route.ts, src/app/admin/employees/employees-view.tsx, src/app/admin/employees/new/new-employee-view.tsx, src/components/employees/employee-form.tsx, src/app/employee/profile/profile-view.tsx, src/components/layout/employee-shell.tsx, src/lib/data/__tests__/employees.test.ts</files>
  <read_first>
    - `src/lib/data/employees.ts` và `src/lib/validation/api/employees.ts` (tạo ở plan 02-05) —
      module sẽ được mở rộng; giữ nguyên mọi thứ đã có
    - `src/lib/data/mutations/departments.ts` (tạo ở plan 02-05) — khuôn Server Action kèm audit
    - `src/lib/mock/service.ts` dòng 284-337 — `getEmployee`, `createEmployee`, `updateEmployee`,
      `bulkMoveDepartment`: chữ ký chính xác, quy tắc kiểm mã trùng (so sánh không phân biệt hoa
      thường, trong phạm vi một doanh nghiệp), thông điệp lỗi tiếng Việt nêu đích danh mã trùng, và
      giá trị trả về của thao tác hàng loạt là **số dòng đã đổi**
    - `supabase/migrations/0004_core_entities.sql` dòng 58-84 — cột thật của `employees`, ràng buộc
      `unique (company_id, code)`, và `user_id uuid null` (dòng 82) — cột này **không** thuộc
      `EmployeeInput` và không được ghi qua đường biểu mẫu
    - `src/lib/auth/session-context.ts` (tạo ở 02-04) — `SessionContext.employeeId` là căn cứ để
      biết "hồ sơ của chính mình"
    - `src/app/admin/employees/employees-view.tsx`, `src/app/admin/employees/new/new-employee-view.tsx`,
      `src/components/employees/employee-form.tsx`, `src/app/employee/profile/profile-view.tsx`,
      `src/components/layout/employee-shell.tsx` — năm call site; chỉ đổi dòng import
  </read_first>
  <behavior>
    - `getEmployee` với `id` của một nhân viên trong doanh nghiệp hiện hành trả về đối tượng `Employee` đầy đủ.
    - `getEmployee` với `id` của một nhân viên thuộc doanh nghiệp khác trả về `null` — không lỗi, không dữ liệu.
    - `getEmployee` với `id` không tồn tại trả về `null`.
    - `createEmployee` với `code` đã tồn tại trong cùng doanh nghiệp (so sánh không phân biệt hoa thường) ném `Error` có `message` chứa đúng mã đó.
    - `createEmployee` với cùng `code` đó nhưng ở doanh nghiệp khác thành công.
    - `updateEmployee` bởi vai trò `employee` trên `id` bằng chính `employeeId` của phiên thành công.
    - `updateEmployee` bởi vai trò `employee` trên `id` của người khác ném lỗi quyền.
    - `bulkMoveDepartment` với danh sách chứa 3 id hợp lệ và 2 id thuộc doanh nghiệp khác trả về `3`.
    - `bulkMoveDepartment` với danh sách rỗng trả về `0` và không ghi dòng audit nào.
  </behavior>
  <action>
    **(a) Mở rộng `src/lib/validation/api/employees.ts`.** Thêm `employeeInputSchema` khớp
    `EmployeeInput` (là `Employee` bỏ `id` và định danh doanh nghiệp) và
    `employeeDetailResponseSchema` cho phản hồi một hồ sơ, cho phép giá trị `null`. Không đưa
    `user_id` vào `employeeInputSchema` — cột đó do đường tạo tài khoản ở plan 02-10 quản lý, không
    do biểu mẫu nhân viên.

    **(b) `src/app/api/employees/[id]/route.ts`.** `GET` theo khuôn 02-04, đọc `id` từ tham số
    đường dẫn (đây là định danh **thực thể**, không phải định danh doanh nghiệp — D-12b cấm nhận
    định danh doanh nghiệp từ client, không cấm nhận id thực thể, vì id thực thể luôn được lọc lại
    theo `company_id` của session). Truy vấn kèm `.eq("company_id", companyId)` và
    `.maybeSingle()`. Trả `null` với mã 200 khi không tìm thấy — không phải 404, để hình dạng trả
    về khớp `Promise<Employee | null>` của chữ ký cũ và giao diện không phải sửa.

    **(c) Mở rộng `src/lib/data/employees.ts`.** Thêm `getEmployee(id: string): Promise<Employee | null>`
    gọi `fetchJson` tới `/api/employees/<id>`. Re-export ba hàm ghi từ module Server Action.

    **(d) `src/lib/data/mutations/employees.ts`** — `"use server"`. Ba hàm giữ nguyên chữ ký:
    - `createEmployee(companyId, input)`: `getSessionContext()` → `requireRole(role, ["owner","admin"])`
      → kiểm mã trùng bằng một truy vấn `employees` lọc `company_id` từ session và
      `public.tf_normalize(code)` bằng `public.tf_normalize(<mã mới>)` (dùng lại hàm chuẩn hóa của
      migration 0007 để so sánh không phân biệt hoa thường và không phụ thuộc collation) → nếu có
      thì ném `Error` với **đúng thông điệp tiếng Việt** mà tầng giả lập đang dùng, nêu đích danh mã
      → chèn dòng mới → `logMutation` `action: "insert"`, `entityTable: "employees"`, `before: null`,
      `after` là nguyên dòng.
      Ràng buộc `unique (company_id, code)` của database là lớp hai: nếu chèn vẫn vi phạm (đua giữa
      hai request), bắt lỗi vi phạm ràng buộc và chuyển thành **cùng** thông điệp tiếng Việt đó,
      không để lỗi Postgres thô lọt lên giao diện.
    - `updateEmployee(id, patch)`: `getSessionContext()` → quy tắc quyền hai nhánh:
      `owner`/`admin` sửa được mọi hồ sơ trong doanh nghiệp; `manager`/`employee` chỉ sửa được hồ sơ
      có `id` bằng `employeeId` của chính phiên, ngược lại ném `ForbiddenError`. → đọc nguyên dòng
      trước với `.eq("company_id", companyId)` → cập nhật → đọc nguyên dòng sau → `logMutation`
      `action: "update"`.
    - `bulkMoveDepartment(ids, departmentId)`: `getSessionContext()` → `requireRole(role, ["owner","admin"])`
      → xác minh `departmentId` thuộc doanh nghiệp hiện hành, không thuộc thì ném lỗi → đọc nguyên
      các dòng `employees` có `id` nằm trong `ids` **và** `company_id` bằng session (id lạ tự bị
      loại, không làm cả thao tác đổ) → cập nhật chúng → đọc lại các dòng sau → gọi `logMutation`
      **một lần cho mỗi nhân viên** với `before`/`after` là nguyên dòng của chính nhân viên đó →
      trả về số dòng thật sự đã đổi. Danh sách rỗng trả `0` và không ghi dòng audit nào.

    **(e) Chuyển năm call site.** Chỉ đổi dòng import:
    `employees-view.tsx` (bulkMoveDepartment, listEmployees, updateEmployee từ `@/lib/data/employees`;
    listDepartments từ `@/lib/data/departments`), `new-employee-view.tsx` (listAllEmployees từ
    `@/lib/data/employees`, listDepartments từ `@/lib/data/departments`, listShifts từ
    `@/lib/data/shifts`), `employee-form.tsx` (createEmployee, updateEmployee từ `@/lib/data/employees`),
    `profile-view.tsx` (getEmployee từ `@/lib/data/employees`, listDepartments, listShifts từ hai
    module tương ứng), `employee-shell.tsx` (getEmployee). Không đổi gì khác trong năm file.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -rc "@/lib/mock/service" src/app/admin/employees/ src/components/employees/ src/app/employee/profile/ src/components/layout/employee-shell.tsx` trả về `0` cho mọi file.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/` trả về `0` trên mọi file.
    - `grep -c 'export const dynamic = "force-dynamic"' src/app/api/employees/\[id\]/route.ts` trả về 1.
    - Với cookie owner Ngọc Phát: `curl -b <cookie> -s http://localhost:3000/api/employees/nv-01a` trả 200 với đối tượng có `id` bằng `nv-01a`.
    - Với cookie owner Ngọc Phát: `curl -b <cookie> -s -w " %{http_code}" http://localhost:3000/api/employees/nv-02a` trả 200 với thân `null` — nhân viên đó thuộc Bình Minh.
    - Sau một lần chuyển hàng loạt 3 nhân viên trên giao diện: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_table='employees' and action='update'"` tăng đúng `3`, không phải `1`.
    - Sau một lần tạo nhân viên mới: số dòng `audit_log` với `action='insert'` và `entity_table='employees'` tăng đúng 1, dòng mới có `before` bằng NULL và `after` khác NULL.
    - `npx vitest run src/lib/data/__tests__/employees.test.ts` thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/employees.test.ts</automated>
  </verify>
  <done>Năm màn hình nhân viên chạy trên Postgres; đọc hồ sơ ngoài doanh nghiệp trả `null`; chuyển hàng loạt để lại một dòng audit cho mỗi người; mã trùng bị chặn ở cả tầng ứng dụng lẫn tầng ràng buộc database.</done>
</task>

<task type="auto">
  <name>Task 2: Test ba biên mà bộ dò cạnh nêu — trùng khớp, thứ tự, ranh giới doanh nghiệp</name>
  <files>src/lib/data/employees.ts, src/lib/data/mutations/employees.ts, src/lib/data/__tests__/employees.test.ts</files>
  <read_first>
    - `src/lib/data/employees.ts`, `src/lib/data/mutations/employees.ts` — hàm cần test
    - `src/lib/data/__tests__/departments.test.ts` (plan 02-05) — khuôn giả lập `fetch`
    - `supabase/migrations/0004_core_entities.sql` dòng 156-159 — index
      `employees_company_id_full_name_id_idx` dựng sẵn cho đúng phép sắp xếp mà test khẳng định
  </read_first>
  <action>
    Tạo `src/lib/data/__tests__/employees.test.ts`. Ba nhóm, tổng ít nhất 9 test.

    **Nhóm A — trùng khớp (edge DATA-05 adjacency).** Giả lập `fetch` cho `createEmployee`:
    1. Server trả lỗi trùng mã → hàm ném `Error` có `message` chứa đúng chuỗi mã bị trùng, để giao
      diện hiển thị lại nguyên văn.
    2. Server trả lỗi vi phạm ràng buộc duy nhất ở tầng database (thông điệp Postgres thô) → hàm
      vẫn ném `Error` với **cùng** thông điệp tiếng Việt như trường hợp 1, không để chuỗi tiếng Anh
      của Postgres lọt lên giao diện.
    3. Cùng mã ở doanh nghiệp khác → thành công (giả lập server trả 200); khẳng định hàm không tự
      chặn dựa trên trạng thái phía client.

    **Nhóm B — thứ tự (edge DATA-05 ordering).** Giả lập `fetch` trả hai trang liên tiếp của một tập
    có hai nhân viên **trùng tên**:
    4. Thứ tự trong `items` khớp đúng thứ tự server trả — hàm không sắp xếp lại ở client.
    5. Gọi hai lần liên tiếp trên cùng dữ liệu trả về cùng thứ tự.
    6. Hợp của trang 1 và trang 2 có số phần tử bằng `total` và không có `id` nào xuất hiện hai lần —
      đây là khẳng định chống cả hai kiểu hỏng của phân trang không ổn định (lặp dòng và mất dòng).
    7. Khẳng định tĩnh: `src/app/api/employees/route.ts` sắp xếp theo cả hai cột `full_name` và `id`,
      không chỉ theo tên — nếu chỉ theo tên thì hai người trùng tên có thứ tự do database quyết định
      và phân trang mất ổn định.

    **Nhóm C — ranh giới doanh nghiệp.**
    8. `getEmployee` khi server trả thân `null` → hàm trả `null`, **không** ném.
    9. `bulkMoveDepartment` với mảng `ids` rỗng → trả `0` và không thực hiện lời gọi mạng nào
      (khẳng định bằng số lần `fetch` được gọi).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/employees.test.ts` thoát 0 và báo ít nhất 9 test.
    - `npm run test` thoát 0.
    - `npm run typecheck` thoát 0.
    - Sau khi tạm sửa `src/app/api/employees/route.ts` để chỉ sắp xếp theo tên, `npx vitest run src/lib/data/__tests__/employees.test.ts` thoát **khác 0** (test 7 đỏ); hoàn tác thì trở lại 0. Hai mã thoát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run test &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Ba biên mà bộ dò cạnh nêu cho DATA-05 đều có test cụ thể; test thứ tự đã được chứng minh có răng.</done>
</task>

<task type="auto">
  <name>Task 3: pgTAP cho quyền ghi theo vai trò trên employees</name>
  <files>(xem <read_first> và <action>)</files>
  <read_first>
    - `supabase/tests/06_platform_admins.sql` (tạo ở 02-02) — khuôn file test mới và cách nối vào
      `run-all.sql`
    - `supabase/tests/00_fixture_users.sql` (tạo ở 02-02) — bốn uuid tổng hợp và membership tương ứng
    - `supabase/tests/helpers.sql` — `tf_test_login` / `tf_test_logout`
    - `scripts/check-pgtap-assertions.mjs` (tạo ở 02-02) — cổng đếm; thêm file test mới làm tổng tăng,
      đúng chiều mà D-15a cho phép
  </read_first>
  <action>
    Quy tắc quyền ở plan này nằm ở tầng Server Action (`requireRole` và nhánh "hồ sơ của chính mình").
    RLS của Phase 1 chỉ phân biệt **thành viên hay không**, không phân biệt vai trò — nên nếu tầng
    Server Action bị bỏ qua (một Server Action tương lai quên gọi `requireRole`), database sẽ **không**
    chặn. Đây là một giới hạn có thật của mô hình hiện tại và phải được ghi lại chứ không giả vờ
    không tồn tại.

    Tạo `supabase/tests/08_role_write_scope.sql` theo khuôn quen thuộc, tối thiểu 5 assertion, ghi
    lại **trạng thái thật** chứ không phải trạng thái mong muốn:
    1. Người dùng `…0003` (admin ở cả hai doanh nghiệp) cập nhật được một dòng `employees` của
      `cty-01`.
    2. Người dùng `…0003` **không** cập nhật được dòng `employees` của một doanh nghiệp họ không
      thuộc — dùng `throws_ok` hoặc khẳng định số dòng bị ảnh hưởng bằng 0.
    3. Người dùng `…0004` (không membership nào) không đọc và không ghi được dòng `employees` nào.
    4. Người dùng `…0001` (owner `cty-01`) đọc được đúng 28 dòng `employees` và 0 dòng của `cty-02`.
    5. Một assertion **ghi nhận giới hạn**: với hai người cùng thuộc `cty-01` nhưng khác vai trò
      trong `memberships`, RLS cho phép cả hai cùng cập nhật một dòng `employees` — khẳng định điều
      đó là **đúng như thiết kế hiện tại**, kèm mô tả nêu rõ rằng phân biệt vai trò nằm ở tầng
      Server Action chứ không ở RLS, và rằng thêm điều kiện vai trò vào RLS là việc của một phase
      sau nếu được quyết định. Assertion này tồn tại để một thay đổi RLS trong tương lai làm nó đỏ,
      buộc người sửa phải đọc lại ghi chú.
    Thêm `\ir 08_role_write_scope.sql` vào cuối `run-all.sql`.

    Ghi vào SUMMARY: giới hạn ở mục 5, nguyên văn, để không ai đọc bộ test này rồi tin rằng vai trò
    đã được cưỡng chế ở tầng database.
  </action>
  <acceptance_criteria>
    - `grep -c '\\\\ir 08_role_write_scope.sql' supabase/tests/run-all.sql` trả về 1.
    - `grep -c 'select plan(' supabase/tests/08_role_write_scope.sql` trả về 1.
    - `npm run test:db` thoát 0, output TAP không chứa dòng nào bắt đầu bằng `not ok`.
    - `npm run check:assertions` thoát 0 và tổng báo về lớn hơn con số mà plan 02-02 ghi trong SUMMARY của nó.
    - SUMMARY chứa nguyên văn ghi chú giới hạn ở assertion 5.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:db &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Phạm vi ghi theo doanh nghiệp được kiểm chứng ở tầng database; giới hạn "RLS không phân biệt vai trò" được ghi lại bằng một assertion sẽ đỏ nếu ai đó đổi mô hình mà không đọc ghi chú.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt → `GET /api/employees/[id]` | Id thực thể đến từ client; phạm vi doanh nghiệp thì không |
| Trình duyệt → Server Action ghi nhân viên | Next.js kiểm nguồn gửi sẵn |
| Nhân viên thường → hồ sơ người khác | Ranh giới quyền riêng tư trong nội bộ một doanh nghiệp |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Information Disclosure | Đọc hồ sơ nhân viên của doanh nghiệp khác qua id đoán được | high | mitigate | Mọi truy vấn theo id đều kèm `.eq("company_id", <từ session>)`; trả `null` chứ không trả dữ liệu; acceptance criteria gọi thật `nv-02a` bằng cookie Ngọc Phát và đòi thân `null` |
| T-02-07-02 | Elevation of Privilege | Nhân viên thường sửa hồ sơ người khác | high | mitigate | Nhánh quyền hai lớp trong `updateEmployee`: vai trò quản trị sửa mọi hồ sơ, vai trò còn lại chỉ sửa hồ sơ có id bằng `employeeId` của chính phiên |
| T-02-07-03 | Elevation of Privilege | Vai trò thấp tạo nhân viên hoặc chuyển hàng loạt | high | mitigate | `requireRole(role, ["owner","admin"])` đầu cả hai hàm |
| T-02-07-04 | Tampering | Chuyển hàng loạt kéo theo nhân viên của doanh nghiệp khác | high | mitigate | Câu đọc và câu cập nhật đều kèm `company_id` từ session; id lạ tự bị loại; giá trị trả về là số dòng thật sự đổi, không phải độ dài danh sách đầu vào |
| T-02-07-05 | Repudiation | Thao tác hàng loạt chỉ để lại một dòng audit | medium | mitigate | `logMutation` gọi một lần cho mỗi nhân viên; acceptance criteria đo số dòng audit tăng đúng bằng số nhân viên bị đổi |
| T-02-07-06 | Tampering | Cột liên kết tài khoản bị ghi qua biểu mẫu nhân viên | high | mitigate | `employeeInputSchema` không khai cột đó; đường ghi duy nhất là luồng tạo tài khoản ở plan 02-10 |
| T-02-07-07 | Information Disclosure | Thông điệp lỗi thô của Postgres lọt lên giao diện | low | mitigate | Nhánh bắt lỗi vi phạm ràng buộc duy nhất chuyển thành thông điệp tiếng Việt đã định; test nhóm A mục 2 canh điều đó |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test` thoát 0
- `npm run test:db && npm run check:assertions` thoát 0
- `/api/employees/nv-02a` bằng cookie Ngọc Phát trả 200 với thân `null`
- Chuyển hàng loạt 3 nhân viên làm `audit_log` tăng đúng 3 dòng
- Không file nào dưới `src/app/api/` xuất hàm HTTP ngoài `GET`
</verification>

<success_criteria>
Nhóm dữ liệu nhiều ràng buộc nhất chạy trên Postgres với đúng khuôn; ba biên mà bộ dò cạnh nêu cho
DATA-05 được giải quyết bằng test cụ thể; giới hạn "RLS không phân biệt vai trò" được ghi lại bằng
một assertion thay vì bằng im lặng.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-07-SUMMARY.md` when done
</output>
