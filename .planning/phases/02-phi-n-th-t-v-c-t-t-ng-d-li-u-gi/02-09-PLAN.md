---
phase: 02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi
plan: 09
type: execute
wave: 8
depends_on: ["02-08"]
files_modified:
  - src/lib/validation/api/requests.ts
  - src/lib/data/requests.ts
  - src/lib/data/mutations/requests.ts
  - src/app/api/requests/route.ts
  - src/app/employee/requests/page.tsx
  - src/app/employee/requests/requests-view.tsx
  - src/components/employee-app/request-form-sheet.tsx
  - src/app/admin/employees/[id]/page.tsx
  - src/app/admin/employees/[id]/employee-detail-view.tsx
  - src/lib/data/__tests__/requests.test.ts
autonomous: true
requirements: [AUTH-03, DATA-05, DATA-06, DATA-08]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Màn hình yêu cầu của nhân viên và trang chi tiết nhân viên của quản trị chạy trọn trên Postgres thật — đây là hai màn hình cuối cùng còn dùng tầng dữ liệu giả."
    - "Sau plan này, không file nào dưới `src/app/` và `src/components/` còn import từ tầng dữ liệu giả."
    - "Biểu mẫu tạo yêu cầu lấy giá trị ngày mặc định từ giá trị do server cấp, không từ hằng số ngày cố định và không từ đồng hồ thiết bị (DATA-08, D-19)."
    - "Trang chi tiết nhân viên lấy tháng cần tổng hợp từ giá trị do server cấp, không từ hằng số tháng cố định (DATA-08)."
    - "`createRequest` gắn yêu cầu vào doanh nghiệp và nhân viên lấy từ phiên phía server; nhân viên thường không tạo được yêu cầu thay người khác (AUTH-03)."
    - "Mỗi yêu cầu được tạo để lại đúng một dòng `audit_log` (DATA-06)."
    - "`listRequests` cho một doanh nghiệp hoặc một nhân viên không có yêu cầu nào trả về `[]` với `error: null` (edge DATA-05 empty)."
    - "`listRequests` sắp xếp xác định theo `created_at` giảm dần rồi `id` tăng dần; hai yêu cầu tạo trong cùng một giây vẫn ra cùng thứ tự ở mọi lần gọi (edge DATA-05 ordering)."
    - "Khi hai dòng `audit_log` có `created_at` bằng nhau, thứ tự đọc lại được quyết định bởi khóa chính tăng dần chứ không bởi thứ tự vật lý của bảng."
      verification: backstop
  artifacts:
    - path: "src/app/api/requests/route.ts"
      provides: "Route Handler đọc yêu cầu"
      exports: ["GET", "dynamic"]
    - path: "src/lib/data/mutations/requests.ts"
      provides: "Server Action tạo yêu cầu kèm audit"
      exports: ["createRequest"]
  key_links:
    - from: "src/app/admin/employees/[id]/employee-detail-view.tsx"
      to: "src/lib/data/"
      via: "tám hàm đổi sang bốn module dữ liệu mới, chữ ký giữ nguyên"
      pattern: "@/lib/data/"
    - from: "src/app/employee/requests/page.tsx"
      to: "src/lib/today.ts"
      via: "Server Component cấp ngày mặc định cho biểu mẫu tạo yêu cầu"
      pattern: "getServerToday"
  prohibitions:
    - statement: "MUST NOT cho nhân viên tạo yêu cầu đứng tên người khác. Yêu cầu nghỉ phép hay bổ sung công là lời khai gắn với một cá nhân; cho phép khai hộ mà không có dấu vết ai khai làm hỏng chính thứ mà luồng duyệt ở phase sau dựa vào."
      category: safety
---

<objective>
Hai màn hình cuối cùng còn chạy trên tầng dữ liệu giả: yêu cầu của nhân viên và trang chi tiết nhân
viên. Sau plan này, mọi `*-view.tsx` đã đứng trên Postgres thật và tầng giả lập không còn người dùng
nào.

Purpose: trang chi tiết nhân viên gọi tám hàm thuộc năm nhóm dữ liệu khác nhau — nó là phép thử
tổng hợp cho toàn bộ khuôn đã dựng, và là lý do nó được để lại sau cùng.

Output: một Route Handler, một module Server Action, hai màn hình chuyển nguồn, hai giá trị thời
gian cuối cùng chuyển sang nguồn server.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-PATTERNS.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-08-SUMMARY.md
</context>

<!-- planner-discipline-allow: REFERENCE_DATE -->
<!-- planner-discipline-allow: REFERENCE_MONTH -->

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Route Handler | `GET /api/requests` |
| Hàm dữ liệu | `listRequests()` |
| Server Action | `createRequest()` |
| Schema Zod | `workRequestSchema`, `requestQuerySchema`, `workRequestInputSchema` |
| Test | `src/lib/data/__tests__/requests.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trọn lát cắt yêu cầu — đọc, tạo, audit, và ngày mặc định do server cấp</name>
  <files>src/lib/validation/api/requests.ts, src/lib/data/requests.ts, src/lib/data/mutations/requests.ts, src/app/api/requests/route.ts, src/app/employee/requests/page.tsx, src/app/employee/requests/requests-view.tsx, src/components/employee-app/request-form-sheet.tsx</files>
  <read_first>
    - `src/lib/mock/service.ts` dòng 469-504 — `listRequests` và `createRequest`: chữ ký chính xác,
      ba bộ lọc tùy chọn (với `"all"` nghĩa là không lọc), phép sắp giảm dần theo thời điểm tạo, và
      các giá trị mặc định của một yêu cầu mới (trạng thái chờ, người duyệt và ghi chú duyệt đều rỗng)
    - `src/lib/types/domain.ts` dòng 160-177, 227-231, 307-310 — `WorkRequest`, `RequestQuery`,
      `WorkRequestInput`
    - `supabase/migrations/0004_core_entities.sql` dòng 116-133 — cột thật của `work_requests`, gồm
      `reviewed_at` (chưa có trong domain V1, thuộc Phase 5) và ràng buộc `check (to_date >= from_date)`
    - `src/components/employee-app/request-form-sheet.tsx` dòng 26, 59-60, 71-72 — bốn chỗ dùng hằng
      số ngày cố định làm giá trị mặc định của biểu mẫu
    - `src/app/employee/requests/requests-view.tsx` — dòng import và nơi render biểu mẫu; đây là nơi
      prop ngày mặc định được truyền xuống
    - `src/lib/data/mutations/attendance.ts` (tạo ở 02-08) — khuôn Server Action gần nhất, gồm cách
      lấy `employeeId` từ phiên
    - `src/lib/today.ts` (tạo ở 02-08) — hàm cấp ngày
  </read_first>
  <behavior>
    - `listRequests` với `status` bằng `"all"` không lọc theo trạng thái.
    - `listRequests` cho một nhân viên chưa có yêu cầu nào trả về `[]`.
    - `listRequests` sắp giảm dần theo thời điểm tạo; hai yêu cầu có cùng thời điểm tạo vẫn ra cùng thứ tự ở mọi lần gọi nhờ khóa chính làm tiebreaker.
    - `createRequest` gắn yêu cầu vào `company_id` và `employee_id` lấy từ phiên; tham số `companyId` và `employeeId` mà nơi gọi truyền vào không quyết định giá trị lưu.
    - `createRequest` bởi vai trò `employee` với `employeeId` khác `employeeId` của phiên ném lỗi quyền.
    - `createRequest` với `toDate` nhỏ hơn `fromDate` bị từ chối với thông điệp tiếng Việt, không để lỗi ràng buộc thô của database lọt lên giao diện.
    - Yêu cầu mới luôn ở trạng thái chờ duyệt, không người duyệt, không ghi chú duyệt — bất kể nơi gọi truyền gì.
  </behavior>
  <action>
    **(a) `src/lib/validation/api/requests.ts`.** `workRequestSchema` khớp `WorkRequest` với biến đổi
    snake_case → camelCase và `created_at` (timestamptz) → chuỗi ISO. `requestQuerySchema` khai
    `employeeId` và `status` — **không** khai định danh doanh nghiệp. `workRequestInputSchema` khớp
    `WorkRequestInput` (đúng sáu trường), **không** khai trạng thái, người duyệt, ghi chú duyệt hay
    thời điểm tạo: bốn giá trị đó do server quyết định, và nhận chúng từ client là mở đường cho một
    yêu cầu tự duyệt chính mình.

    **(b) `src/app/api/requests/route.ts`.** `GET` theo khuôn 02-04, lọc `.eq("company_id", <từ session>)`.
    Áp hai bộ lọc tùy chọn. Sắp xếp theo `created_at` giảm dần rồi `id` tăng dần — tiebreaker là bắt
    buộc vì seed tạo nhiều yêu cầu trong cùng một lần chạy nên thời điểm tạo trùng nhau là chuyện
    thường. Thêm lớp quyền giống `/api/attendance`: vai trò `employee`/`manager` hỏi một `employeeId`
    khác của mình thì trả 403; không truyền `employeeId` thì mặc định giới hạn về `employeeId` của
    chính phiên cho hai vai trò đó, còn quản trị thấy toàn bộ doanh nghiệp.

    **(c) `src/lib/data/requests.ts`.** `listRequests(query: RequestQuery): Promise<WorkRequest[]>`
    giữ nguyên chữ ký, gọi qua `fetchJson`. Tham số định danh doanh nghiệp trong `query` giữ để
    không phải sửa call site nhưng không gửi lên server, kèm comment như các module trước.
    Re-export `createRequest`.

    **(d) `src/lib/data/mutations/requests.ts`** — `"use server"`. `createRequest(companyId, employeeId, input)`
    giữ nguyên chữ ký:
    - `getSessionContext()` cho `companyId`, `role`, `employeeId` của phiên.
    - Quyền: `owner`/`admin` tạo được yêu cầu cho mọi nhân viên trong doanh nghiệp; `manager`/`employee`
      chỉ tạo được cho chính mình. Tham số `employeeId` được đối chiếu chứ không được tin.
    - Kiểm `toDate >= fromDate` ở tầng ứng dụng và ném `Error` tiếng Việt nếu sai; ràng buộc `check`
      của database là lớp hai, và nếu nó bắn ra thì bắt lại và chuyển thành **cùng** thông điệp đó.
    - Chèn dòng mới với trạng thái chờ duyệt, người duyệt và ghi chú duyệt để rỗng, `created_at` do
      database đặt bằng giá trị mặc định `now()` — không truyền từ client.
    - Đọc nguyên dòng vừa chèn, rồi `logMutation` `action: "insert"`, `entityTable: "work_requests"`,
      `before: null`, `after` là nguyên dòng.

    **(e) Ngày mặc định của biểu mẫu.** `src/app/employee/requests/page.tsx` `await getServerToday()`
    và truyền xuống `RequestsView` làm prop; `requests-view.tsx` chuyển tiếp prop đó xuống
    `RequestFormSheet`; `request-form-sheet.tsx` nhận prop `today: string` và dùng nó cho cả bốn chỗ
    hiện đang dùng hằng số ngày cố định. Xóa dòng import hằng số khỏi `request-form-sheet.tsx`.
    Không đổi bất cứ phần giao diện nào của ba file.

    **(f) Chuyển call site.** Đổi dòng import ở `requests-view.tsx`: `createRequest` và
    `listRequests` từ `@/lib/data/requests`, `listAllEmployees` từ `@/lib/data/employees`.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -rEc "REFERENCE_DATE|REFERENCE_MONTH" src/components/employee-app/request-form-sheet.tsx src/app/employee/requests/` trả về `0` cho mọi file.
    - `grep -rc "@/lib/mock/service" src/app/employee/requests/` trả về `0` cho mọi file.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/` trả về `0` trên mọi file.
    - Với cookie owner Ngọc Phát: `curl -b <cookie> -s http://localhost:3000/api/requests` trả về mảng 9 phần tử; với cookie owner Bình Minh trả về mảng 3 phần tử.
    - Gọi cùng URL hai lần liên tiếp trả về các phần tử theo **cùng thứ tự** (so sánh mảng `id`).
    - Với cookie một nhân viên thường: `curl -b <cookie> -s -o /dev/null -w "%{http_code}" "http://localhost:3000/api/requests?employeeId=<id nguoi khac>"` trả về `403`.
    - Sau một lần tạo yêu cầu trên giao diện: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_table='work_requests'"` tăng đúng 1, và dòng mới có `before` bằng NULL.
    - Sau lần tạo đó: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select status from work_requests order by created_at desc limit 1"` trả về giá trị trạng thái chờ duyệt.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; node -e "const {execSync}=require('child_process');const fs=require('fs');for(const f of execSync('git ls-files src/app/api',{encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean)){const s=fs.readFileSync(f,'utf8');if(/export\s+(async\s+)?function\s+(POST|PUT|PATCH|DELETE|HEAD|OPTIONS)/.test(s))process.exit(1)}"</automated>
  </verify>
  <done>Màn hình yêu cầu chạy trọn trên Postgres; ngày mặc định của biểu mẫu do server cấp; nhân viên thường không đọc và không tạo được yêu cầu đứng tên người khác; mỗi yêu cầu để lại một dòng audit.</done>
</task>

<task type="auto">
  <name>Task 2: Trang chi tiết nhân viên — tám hàm, năm nhóm dữ liệu, một màn hình cuối</name>
  <files>src/app/api/requests/route.ts, src/app/admin/employees/[id]/page.tsx, src/app/admin/employees/[id]/employee-detail-view.tsx, src/lib/data/__tests__/requests.test.ts</files>
  <read_first>
    - `src/app/admin/employees/[id]/employee-detail-view.tsx` dòng 57, 70-79, 95-125, 317, 374 —
      danh sách tám hàm được import, ba chỗ dùng hằng số tháng cố định, và toàn bộ khối tải dữ liệu
      song song ở đầu component
    - `src/app/admin/employees/[id]/page.tsx` — Server Component hiện chỉ đọc tham số đường dẫn và
      render View
    - `src/lib/today.ts` (tạo ở 02-08) — `getServerMonth()`
    - Năm module dữ liệu đã tạo ở các plan trước: `@/lib/data/employees`, `@/lib/data/departments`,
      `@/lib/data/shifts`, `@/lib/data/attendance`, `@/lib/data/requests` — tám hàm cần dùng đều đã
      có ở đó với **chữ ký y hệt** bản cũ
  </read_first>
  <action>
    **(a) `src/app/admin/employees/[id]/page.tsx`.** Thêm `await getServerMonth()` và truyền xuống
    `EmployeeDetailView` làm prop `month: string`, bên cạnh `employeeId` đang có. Giữ nguyên cách
    đọc tham số đường dẫn và `export const metadata` nếu có.

    **(b) `src/app/admin/employees/[id]/employee-detail-view.tsx`.** Hai loại thay đổi, không có
    loại thứ ba:
    1. **Đổi dòng import.** Tám hàm chia về năm module: `getEmployee`/`listAllEmployees`/`updateEmployee`
      từ `@/lib/data/employees`; `listDepartments` từ `@/lib/data/departments`; `listShifts` từ
      `@/lib/data/shifts`; `getMonthlySummary`/`listAttendance` từ `@/lib/data/attendance`;
      `listRequests` từ `@/lib/data/requests`.
    2. **Thay hằng số tháng bằng prop.** Ba chỗ dùng hằng số tháng cố định (tham số truyền cho hàm
      tổng hợp, và hai chỗ dựng nhãn hiển thị) đều lấy từ prop `month`. Xóa dòng import hằng số.
    Không đổi cấu trúc khối tải dữ liệu song song, không đổi giao diện, không đổi mảng phụ thuộc của
    hook ngoài việc thêm `month`.

    **(c) `src/lib/data/__tests__/requests.test.ts`.** Năm khẳng định trên `fetch` giả lập:
    1. Danh sách rỗng → trả `[]`, không ném (edge DATA-05 empty).
    2. Server trả các phần tử theo một thứ tự nhất định → hàm giữ nguyên thứ tự đó, không sắp lại ở client.
    3. Gọi hai lần trên cùng dữ liệu → cùng thứ tự.
    4. Server trả 403 → hàm ném `Error` với thông điệp tiếng Việt về quyền, không ném giá trị không phải `Error`.
    5. Khẳng định tĩnh: `src/app/api/requests/route.ts` sắp xếp theo cả `created_at` và một cột
      tiebreaker, không chỉ theo `created_at` — nếu chỉ theo một cột thì các yêu cầu trong seed
      (tạo cùng lúc) sẽ đổi thứ tự giữa hai lần tải và người dùng thấy danh sách nhảy.

    **(d) Chốt sổ.** Chạy một lệnh quét toàn bộ `src/app/` và `src/components/` tìm mọi tham chiếu
    còn lại tới tầng dữ liệu giả và tới hai hằng số ngày/tháng cố định. Ghi kết quả vào SUMMARY:
    danh sách file còn sót (nếu có) và lý do — plan 02-10 và 02-11 phải biết chính xác còn gì.
    Ở thời điểm này kỳ vọng là chỉ còn `src/components/employees/employee-form.tsx` (hằng số ngày,
    xử lý ở plan cuối) và bản thân `src/lib/constants.ts` cùng ba file thuộc tầng giả lập.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -rEc "REFERENCE_DATE|REFERENCE_MONTH" src/app/admin/employees/` trả về `0` cho mọi file.
    - `git grep -l "@/lib/mock/service" -- src/app src/components` không trả về file nào.
    - `git grep -lE "REFERENCE_DATE|REFERENCE_MONTH" -- src/app src/components` chỉ trả về `src/components/employees/employee-form.tsx` và không file nào khác.
    - Với cookie owner Ngọc Phát và server dev đang chạy: mở `/admin/employees/nv-01a`, trang render đầy đủ năm tab với dữ liệu thật; không lỗi trong Console. Kết quả quan sát ghi vào SUMMARY.
    - `npx vitest run src/lib/data/__tests__/requests.test.ts` thoát 0 và báo ít nhất 5 test.
    - `npm run build` thoát 0.
    - `npm run test` thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; npm run build &amp;&amp; node -e "const {execSync}=require('child_process');const out=execSync('git grep -l \"@/lib/mock/service\" -- src/app src/components || true',{encoding:'utf8'}).trim();if(out){console.error('con call site tang gia lap: '+out);process.exit(1)}"</automated>
  </verify>
  <done>Không màn hình nào trong `src/app/` hay `src/components/` còn gọi tầng dữ liệu giả; trang chi tiết nhân viên render đầy đủ trên dữ liệu thật; danh sách những gì còn sót đã ghi vào SUMMARY cho hai plan cuối.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nhân viên → yêu cầu đứng tên người khác | Ranh giới danh tính bên trong một doanh nghiệp |
| Client → trạng thái yêu cầu | Trạng thái duyệt là quyết định của server, không phải trường biểu mẫu |
| Trình duyệt → `GET /api/requests` | URL công khai, xác thực bằng cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09-01 | Spoofing | Tạo yêu cầu đứng tên nhân viên khác | high | mitigate | `createRequest` lấy `employee_id` từ phiên; tham số cùng tên chỉ được đối chiếu; vai trò không phải quản trị bị từ chối khi hai giá trị lệch |
| T-02-09-02 | Elevation of Privilege | Client gửi kèm trạng thái đã duyệt cho một yêu cầu mới | high | mitigate | `workRequestInputSchema` chỉ khai sáu trường nghiệp vụ; trạng thái, người duyệt, ghi chú duyệt và thời điểm tạo đều do server và database đặt |
| T-02-09-03 | Information Disclosure | Nhân viên đọc yêu cầu của người khác | high | mitigate | Handler trả 403 khi vai trò không phải quản trị hỏi `employeeId` khác của mình, và mặc định giới hạn về chính mình khi không truyền tham số |
| T-02-09-04 | Information Disclosure | Route Handler bị cache giữa hai doanh nghiệp | high | mitigate | `force-dynamic` tường minh; acceptance criteria gọi cùng URL bằng hai cookie và đòi 9 với 3 phần tử |
| T-02-09-05 | Repudiation | Tạo yêu cầu không để lại vết | medium | mitigate | `logMutation` trong cùng hàm ghi; acceptance criteria đo số dòng audit tăng đúng 1 |
| T-02-09-06 | Tampering | Thứ tự danh sách không ổn định làm người dùng thao tác nhầm dòng | low | mitigate | Sắp xếp hai cột với khóa chính làm tiebreaker; test tĩnh mục 5 canh việc tiebreaker không bị gỡ |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test && npm run build` thoát 0
- `git grep -l "@/lib/mock/service" -- src/app src/components` không trả về file nào
- `/api/requests` trả 9 dòng cho Ngọc Phát và 3 dòng cho Bình Minh, thứ tự ổn định giữa hai lần gọi
- Nhân viên thường nhận 403 khi hỏi yêu cầu của người khác
- Tạo một yêu cầu làm `audit_log` tăng đúng 1 dòng
</verification>

<success_criteria>
Toàn bộ giao diện V1 đã đứng trên Postgres thật. Tầng dữ liệu giả không còn người dùng nào trong
`src/app/` và `src/components/`, và danh sách chính xác những gì còn phải dọn đã được ghi lại cho
hai plan cuối.
</success_criteria>

<output>
Create `.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-09-SUMMARY.md` when done
</output>
