---
phase: 05-duy-t-y-u-c-u-v-ch-t-k-c-ng
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0017_request_reviews.sql
  - supabase/tests/12_request_reviews.sql
  - supabase/tests/run-all.sql
  - scripts/check-pgtap-assertions.mjs
  - src/lib/types/domain.ts
  - src/lib/validation/api/requests.ts
  - src/lib/data/requests.ts
  - src/lib/data/mutations/requests.ts
  - src/app/api/requests/route.ts
  - src/app/admin/requests/page.tsx
  - src/app/admin/requests/requests-review-view.tsx
  - src/components/requests/review-dialog.tsx
  - src/components/requests/request-history.tsx
  - src/lib/nav.ts
  - src/lib/constants.ts
  - src/components/dashboard/pending-requests-card.tsx
  - src/lib/data/__tests__/request-review.test.ts
autonomous: true
requirements: [APRV-01, APRV-02, APRV-04]

estimate:
  tokens: 82000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "Người có quyền duyệt (`owner`/`admin`) thấy danh sách yêu cầu **chờ xử lý của đúng doanh nghiệp mình** tại `/admin/requests` và xử lý ngay tại đó (APRV-01)."
    - "Từ chối **bắt buộc nhập lý do**: gửi không có lý do thì Server Action từ chối, không chỉ giao diện chặn — client sửa được, server thì không."
    - "Duyệt cũng cho phép ghi chú, nhưng không bắt buộc: bắt buộc lý do khi từ chối là để người bị từ chối biết vì sao; duyệt thì không có ai cần lời giải thích."
    - "Mỗi lần xử lý ghi một dòng `request_reviews` (append-only): ai, lúc nào, quyết định gì, lý do gì. Mở lại một yêu cầu đã xử lý thấy **đủ lịch sử**, không chỉ lần cuối (APRV-04, D-33)."
    - "Ba cột `reviewer_id`/`review_note`/`reviewed_at` trên `work_requests` vẫn được cập nhật làm ảnh chụp trạng thái hiện tại — mọi màn hình đang đọc chúng không phải sửa."
    - "Yêu cầu **không còn ở trạng thái `pending`** thì không xử lý lại được; thử lần hai trả thông điệp tiếng Việt rõ ràng, không im lặng ghi đè."
    - "Vai trò `employee`/`manager` gọi thẳng Server Action duyệt bị từ chối (403), không chỉ bị ẩn nút."
    - "Mỗi lần xử lý để lại một dòng `audit_log` mang nguyên dòng yêu cầu trước và sau."
    - "Thẻ 'Yêu cầu chờ duyệt' trên dashboard quản trị trỏ tới `/admin/requests`, không còn trỏ sang giao diện nhân viên."
  artifacts:
    - path: "supabase/migrations/0017_request_reviews.sql"
      provides: "Bảng lịch sử xử lý append-only kèm RLS"
      exports: ["request_reviews"]
    - path: "src/lib/data/mutations/requests.ts"
      provides: "Server Action duyệt / từ chối"
      exports: ["reviewRequest"]
    - path: "src/app/admin/requests/requests-review-view.tsx"
      provides: "Màn hình duyệt của quản trị"
      min_lines: 120
  key_links:
    - from: "src/lib/data/mutations/requests.ts"
      to: "supabase/migrations/0017_request_reviews.sql"
      via: "mỗi lần xử lý chèn một dòng lịch sử"
      pattern: "request_reviews"
    - from: "src/app/admin/requests/requests-review-view.tsx"
      to: "src/components/requests/request-history.tsx"
      via: "mở một yêu cầu đã xử lý thấy đủ lịch sử"
      pattern: "RequestHistory"
  prohibitions:
    - statement: "MUST NOT để việc bắt buộc nhập lý do chỉ nằm ở form. Người bị từ chối mà không biết vì sao là một lỗi nghiệp vụ, và một client sửa được thì bỏ qua được form. Kiểm ở Server Action là chỗ duy nhất tính."
      category: correctness
    - statement: "MUST NOT thay ba cột review hiện có bằng bảng mới trong cùng một plan. Chúng đang được đọc ở màn hình nhân viên và thẻ dashboard; đổi cả hai thứ cùng lúc làm không phân biệt được lỗi đến từ đâu."
      category: architecture
    - statement: "MUST NOT cho phép xử lý một yêu cầu đã xử lý rồi. Duyệt hai lần sẽ sinh hai lần tác động lên dữ liệu công khi 05-02 nối tác động vào — và lúc đó lỗi sẽ hiện ra dưới dạng số liệu sai, không phải một thông báo lỗi."
      category: correctness
---

<objective>
Lát cắt xuyên suốt của phase: một yêu cầu đi từ danh sách chờ → được duyệt hoặc từ chối kèm lý
do → để lại lịch sử đọc được.

Purpose: đây là tracer. Nó **chưa** chạm vào dữ liệu công (việc của 05-02) và **chưa** gửi
thông báo (05-04) — nhưng nó dựng đủ ba thứ mà bốn plan sau đều đứng lên: bảng lịch sử, Server
Action xử lý có kiểm trạng thái, và màn hình duyệt. Nếu ba thứ này đúng, phần còn lại của phase
là nối thêm hệ quả vào một điểm đã có tên.

Output: migration 0017, `reviewRequest()`, `/admin/requests`, thành phần lịch sử xử lý.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-09-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bảng lịch sử xử lý</name>
  <files>supabase/migrations/0017_request_reviews.sql, supabase/tests/12_request_reviews.sql, supabase/tests/run-all.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `supabase/migrations/0015_company_settings.sql` (04-01) — khuôn bảng mới gần nhất: bốn
      policy `<bảng>_<lệnh>_member`, index, khối comment giải thích **vì sao**.
    - `supabase/migrations/0016_overtime_rules_append_only.sql` (04-04) — khuôn trigger cưỡng
      chế append-only kèm thủ tục gỡ có ý thức; bảng này dùng lại đúng cách đó.
    - `supabase/migrations/0004_core_entities.sql` khối `work_requests` — cột thật, gồm ba cột
      review sẽ **giữ nguyên**.
    - `supabase/tests/10_company_settings.sql` — khuôn test cô lập rút gọn.
    - `scripts/check-pgtap-assertions.mjs` — sàn hiện tại 212, chỉ được nâng.
  </read_first>
  <behavior>
    - `request_reviews`: `id`, `company_id`, `request_id` (FK `work_requests`, cascade),
      `decision` (`approved`/`rejected`), `note` (null được khi duyệt), `reviewer_user_id`,
      `reviewer_employee_id` (null được), `created_at` mặc định `now()`.
    - `UPDATE` và `DELETE` trên bảng này bị trigger từ chối — lịch sử không sửa được.
    - Ràng buộc: `decision = 'rejected'` thì `note` phải khác null và khác rỗng sau khi cắt
      khoảng trắng. Đây là **lớp thứ hai**; lớp thứ nhất ở Server Action.
    - Bốn policy RLS điều kiện duy nhất `tf_is_member(company_id)`.
    - Người thuộc doanh nghiệp A không đọc và không chèn được dòng của doanh nghiệp B.
  </behavior>
  <action>
    Viết migration + test pgTAP (ít nhất 6 khẳng định: cô lập đọc, cô lập ghi, `UPDATE` bị chặn,
    `DELETE` bị chặn, từ chối thiếu lý do bị chặn, duyệt không ghi chú vẫn chèn được), khai
    trong `run-all.sql`, nâng sàn assertion.

    Khối comment đầu file nói rõ **vì sao có bảng này trong khi đã có ba cột review**: ba cột
    giữ được đúng lần xử lý cuối, còn APRV-04 hỏi *lịch sử*. Và nói rõ ba cột kia **không bị
    bỏ** — chúng là ảnh chụp trạng thái hiện tại cho các màn hình đang đọc.
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `npm run check:assertions` thoát 0 với sàn mới.
    - `psql ... -c "update request_reviews set note='x'"` thất bại với thông điệp của trigger (kiểm qua test tích hợp nếu không có `psql` — xem §Ghi chú của CONTEXT).
    - `psql ... -tAc "select count(*) from pg_policies where tablename='request_reviews'"` trả `4`.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Lịch sử xử lý có chỗ để sống, và nó không sửa được — cùng cách mà hệ số tăng ca được bảo vệ ở Phase 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Đường đọc danh sách chờ và lịch sử</name>
  <files>src/lib/types/domain.ts, src/lib/validation/api/requests.ts, src/lib/data/requests.ts, src/app/api/requests/route.ts</files>
  <read_first>
    - `src/app/api/requests/route.ts` — tầng quyền hiện có: `employee`/`manager` không truyền
      `employeeId` thì bị giới hạn về chính mình; `owner`/`admin` thấy cả doanh nghiệp.
    - `src/lib/validation/api/requests.ts` — cặp schema hai đầu đang có.
    - `src/app/api/overtime-rules/route.ts` (04-04) — khuôn Route Handler mới nhất.
  </read_first>
  <behavior>
    - `GET /api/requests?status=pending` lọc theo trạng thái; không tham số nào khai định danh
      doanh nghiệp.
    - Mỗi phần tử mang thêm **tên nhân viên** và **tên phòng ban** — danh sách duyệt mà chỉ có
      `employeeId` thì người duyệt phải tự tra, và họ sẽ duyệt mà không tra.
    - `GET /api/requests/[id]/reviews` trả lịch sử xử lý của một yêu cầu, mới nhất trước; vai
      trò không phải `owner`/`admin` chỉ đọc được lịch sử của **yêu cầu của chính mình**.
    - Sắp xếp xác định: yêu cầu chờ xử lý lâu nhất lên trước (`created_at` tăng dần) — người
      chờ lâu nhất được xử lý trước, và hai lần gọi cho cùng thứ tự.
    - Doanh nghiệp không có yêu cầu chờ nào trả mảng rỗng với mã 200.
  </behavior>
  <action>
    Mở rộng schema + Route Handler theo `<behavior>`; thêm `RequestReview` vào `domain.ts` và
    `listRequestReviews()` vào `src/lib/data/requests.ts`.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint` thoát 0.
    - `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0 (route mới cũng chỉ `GET`).
    - `grep -c "companyId" src/lib/validation/api/requests.ts` — không xuất hiện trong schema đầu vào hay tham số truy vấn; kiểm bằng mắt, ghi kết luận vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/__tests__/route-handlers-get-only.test.ts</automated>
  </verify>
  <done>Người duyệt đọc được đúng danh sách của doanh nghiệp mình, đủ ngữ cảnh để quyết định, và đọc lại được lịch sử.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Server Action duyệt / từ chối</name>
  <files>src/lib/data/mutations/requests.ts, src/lib/data/__tests__/request-review.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/requests.ts` — `createRequest()` đã có; chú ý cách nó **không bao
      giờ** đọc trạng thái/người duyệt từ `input` (T-02-09-02). `reviewRequest` giữ đúng tinh
      thần đó theo chiều ngược lại.
    - `src/lib/data/mutations/holidays.ts` (04-03) — khuôn Server Action mới nhất kèm
      `logMutation` và bắt riêng mã lỗi Postgres.
    - `src/lib/data/__tests__/holidays-mutations.test.ts` (04-03) — khuôn test tích hợp trên
      database dev thật; file này dùng lại nguyên khuôn đó.
  </read_first>
  <behavior>
    - `reviewRequest(requestId, { decision, note })` đòi `owner`/`admin`; vai trò khác nhận
      `ForbiddenError`.
    - `decision === 'rejected'` mà `note` rỗng hoặc chỉ khoảng trắng → ném lỗi tiếng Việt
      **trước khi** chạm database.
    - Yêu cầu không thuộc doanh nghiệp của phiên → "Không tìm thấy yêu cầu", và dòng đó **không
      đổi** — không im lặng thành công.
    - Yêu cầu đã `approved`/`rejected` → ném lỗi nói rõ yêu cầu đã được xử lý, kèm quyết định
      trước đó.
    - Thành công: cập nhật `work_requests` (status + ba cột review) **và** chèn một dòng
      `request_reviews` **và** ghi một dòng `audit_log` — cả ba đều xảy ra hoặc thao tác coi như
      thất bại.
    - Gọi hai lần liên tiếp với cùng `requestId`: lần hai bị từ chối, và lịch sử vẫn đúng một dòng.
  </behavior>
  <action>
    Viết `reviewRequest()` theo khuôn `mutations/holidays.ts`. Comment tại chỗ ghi rõ: đây là
    điểm mà 05-02 sẽ nối tác động lên dữ liệu công vào, nên **kiểm trạng thái phải đứng trước
    mọi thứ khác** — một yêu cầu xử lý hai lần sẽ thành hai lần trừ công.

    Test tích hợp trên database dev thật, phủ đủ sáu hành vi ở `<behavior>`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/request-review.test.ts` thoát 0, ít nhất 6 test.
    - `grep -c "logMutation" src/lib/data/mutations/requests.ts` tăng đúng 1 so với trước plan.
    - `grep -c "requireRole" src/lib/data/mutations/requests.ts` trả ít nhất 1.
    - Test khẳng định: gọi `reviewRequest` hai lần → lần hai ném lỗi và `select count(*) from request_reviews where request_id = ...` vẫn bằng 1.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/request-review.test.ts</automated>
  </verify>
  <done>Một yêu cầu chỉ xử lý được một lần, bởi đúng người, và mọi lần xử lý đều để lại vết ở hai nơi.</done>
</task>

<task type="auto">
  <name>Task 4: Màn hình duyệt của quản trị</name>
  <files>src/app/admin/requests/page.tsx, src/app/admin/requests/requests-review-view.tsx, src/components/requests/review-dialog.tsx, src/components/requests/request-history.tsx, src/lib/nav.ts, src/lib/constants.ts, src/components/dashboard/pending-requests-card.tsx</files>
  <read_first>
    - `src/app/employee/requests/requests-view.tsx` — màn hình yêu cầu phía nhân viên: nhãn
      loại yêu cầu, `StatusBadge`, cách hiển thị khoảng ngày. Màn hình quản trị dùng **cùng**
      nhãn và **cùng** quy ước trạng thái.
    - `src/app/admin/settings/settings-view.tsx` (04-01) — khuôn màn hình quản trị mới nhất.
    - `src/components/common/confirm-dialog.tsx`, `empty-state.tsx`, `error-state.tsx`,
      `data-table-skeleton.tsx`.
    - `src/components/dashboard/pending-requests-card.tsx` — hai liên kết đang trỏ tới
      `/employee/requests`; với vai trò quản trị đó là đích **sai**.
    - `src/lib/nav.ts` — `ADMIN_NAV_ITEMS`; mục mới đặt cạnh "Chấm công".
  </read_first>
  <action>
    **(a) `/admin/requests`** — Server Component + View:
    - Bộ lọc trạng thái, mặc định `pending`. Bảng: nhân viên (kèm phòng ban), loại yêu cầu,
      khoảng ngày, lý do, thời điểm gửi, trạng thái, hành động.
    - Hai hành động: **Duyệt** (nút chính) và **Từ chối** (nút phụ) — mở `ReviewDialog`.
    - `ReviewDialog`: ô lý do **bắt buộc khi từ chối** (`zodResolver` với schema khác nhau theo
      quyết định), tuỳ chọn khi duyệt; nút xác nhận nói rõ đang làm gì.
    - Yêu cầu đã xử lý hiện `RequestHistory` (ai / lúc nào / quyết định / lý do), mới nhất trước.
    - Trạng thái rỗng nói đúng sự thật: không có yêu cầu nào chờ xử lý là **tin tốt**, không
      phải một màn hình hỏng.
    - Trạng thái phân biệt bằng biểu tượng + nhãn chữ, không bằng màu đơn thuần.

    **(b) `nav.ts`** — thêm mục "Yêu cầu" cho khu quản trị; `BREADCRUMB_LABELS` thêm `requests`.

    **(c) `pending-requests-card.tsx`** — hai liên kết đổi sang `/admin/requests` (thẻ này chỉ
    render trong dashboard quản trị).

    **(d) `constants.ts`** — khối `REQUEST_REVIEW_LABEL`.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -c "/admin/requests" src/lib/nav.ts src/components/dashboard/pending-requests-card.tsx` — cả hai file đều có; `grep -c "/employee/requests" src/components/dashboard/pending-requests-card.tsx` trả `0`.
    - `grep -c "RequestHistory" src/app/admin/requests/requests-review-view.tsx` trả ít nhất 1.
    - Với phiên owner và một yêu cầu chờ thật: từ chối **không nhập lý do** → không gửi được; nhập lý do → gửi được, dòng biến khỏi danh sách chờ, mở lại thấy lịch sử đúng một dòng. Ghi cả ba quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Người duyệt có một chỗ để làm việc, và mọi quyết định của họ đọc lại được sau này.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt quản trị → `reviewRequest` | Quyết định ảnh hưởng tới công và (từ 05-02) tới số liệu kỳ |
| `requestId` do client gửi → dòng dữ liệu | Định danh của doanh nghiệp khác phải bị chặn |
| Lịch sử xử lý → bằng chứng sau này | Sửa được lịch sử là mất luôn giá trị của việc ghi nó |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Elevation of Privilege | `employee`/`manager` gọi thẳng Server Action duyệt yêu cầu của người khác | high | mitigate | `requireRole(role, ["owner","admin"])`; test tích hợp gọi bằng phiên `employee` thật |
| T-05-01-02 | Spoofing | `requestId` của doanh nghiệp khác | high | mitigate | Mọi lệnh ghi kèm `.eq("company_id", companyId)` từ phiên; test kiểm bằng id chéo thật |
| T-05-01-03 | Tampering | Xử lý lại một yêu cầu đã xử lý → (từ 05-02) tác động nhân đôi lên dữ liệu công | high | mitigate | Kiểm trạng thái đứng trước mọi thao tác; test gọi hai lần liên tiếp |
| T-05-01-04 | Repudiation | Sửa lịch sử xử lý để che một quyết định | high | mitigate | Trigger chặn `UPDATE`/`DELETE` trên `request_reviews` (khuôn D-25a) |
| T-05-01-05 | Repudiation | Từ chối mà không nêu lý do — người bị từ chối không biết vì sao | medium | mitigate | Kiểm ở Server Action (lớp quyết định) + ràng buộc CHECK ở database (lớp hai) |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run db:push && npm run check:assertions` thoát 0
- Lịch sử xử lý không sửa và không xoá được
- Từ chối không lý do bị chặn ở tầng server, không chỉ ở form
- Một yêu cầu chỉ xử lý được một lần
- Vai trò không có quyền nhận 403; id chéo doanh nghiệp không chạm được dòng nào
</verification>

<success_criteria>
Một yêu cầu đi hết vòng đời xử lý ở đúng nơi người duyệt làm việc, và sáu tháng sau vẫn trả lời
được câu "ai duyệt cái này, lúc nào, vì sao".
</success_criteria>

<output>
Create `.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-01-SUMMARY.md` when done
</output>
