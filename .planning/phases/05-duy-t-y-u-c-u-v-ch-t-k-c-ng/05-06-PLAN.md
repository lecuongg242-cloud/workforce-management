---
phase: 05-duy-t-y-u-c-u-v-ch-t-k-c-ng
plan: 06
type: execute
wave: 5
depends_on: ["05-03", "05-04", "05-05"]
files_modified:
  - src/__tests__/no-silent-period-write.test.ts
  - scripts/e2e-approval.mjs
  - package.json
  - .planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-UAT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [APRV-01, APRV-02, APRV-03, APRV-04, APRV-05, SET-05, PERD-01, PERD-02]

estimate:
  tokens: 60000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Có một cổng tự động chạy trong `npm test` chặn việc mở một đường ghi mới vào `attendance_records` mà không đi qua `tf_apply_approved_request()` — đây là lời hứa PERD-02 và nó phải sống sót sau khi phase kết thúc."
    - "Cổng ấy được chứng minh **có răng** bằng một lần phá hoại có kiểm soát: thêm một lệnh ghi thẳng vào bảng chấm công ở tầng ứng dụng → đỏ; hoàn tác → xanh; `git status` sạch."
    - "Tám yêu cầu của phase (APRV-01…05, SET-05, PERD-01, PERD-02) được nghiệm thu bằng quan sát trên hệ thống chạy thật, không phải bằng suy luận từ test đơn vị."
    - "Vòng đời đầy đủ chạy được đầu-cuối một lần qua HTTP thật: nhân viên gửi yêu cầu → quản trị duyệt → số liệu kỳ đổi → nhân viên nhận thông báo → quản trị chốt kỳ → ghi thẳng vào kỳ bị chặn."
    - "ROADMAP, REQUIREMENTS và STATE phản ánh đúng trạng thái sau phase; Phase 6 sẵn sàng lập kế hoạch."
  artifacts:
    - path: "src/__tests__/no-silent-period-write.test.ts"
      provides: "Cổng chặn đường ghi thẳng vào dữ liệu chấm công ngoài luồng đã duyệt"
    - path: "scripts/e2e-approval.mjs"
      provides: "Vòng đời yêu cầu đầu-cuối qua HTTP thật"
    - path: ".planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-UAT.md"
      provides: "Biên bản nghiệm thu tám yêu cầu kèm giới hạn đã biết"
  prohibitions:
    - statement: "MUST NOT đánh dấu phase hoàn thành khi một yêu cầu chỉ được chứng minh bằng test đơn vị. Tám yêu cầu của phase nói về hành vi của hệ thống với người dùng thật; chúng phải được quan sát trên hệ thống chạy thật ít nhất một lần."
      category: process
    - statement: "MUST NOT nới lỏng cổng để nó xanh. Nếu cổng bắt nhầm một dòng hợp lệ thì thu hẹp bằng NGỮ NGHĨA và ghi lý do ngay trong mã — không miễn trừ cả một file, vì miễn trừ đường dẫn làm mù toàn bộ file đó về sau."
      category: integrity
    - statement: "MUST NOT bỏ qua việc ghi ra những gì phase KHÔNG làm được. Ba thứ đã biết: duyệt một cấp, không có đường mở lại kỳ, `manager` không duyệt được. Người đọc sau phải thấy chúng trong biên bản chứ không phải tự vấp phải."
      category: transparency
---

<objective>
Đóng phase: dựng cổng tự động giữ cho lời hứa PERD-02 không bị xói mòn, chạy một vòng đời đầy đủ
qua HTTP thật, và nghiệm thu tám yêu cầu bằng quan sát.

Purpose: năm plan trước mỗi plan có grep gate riêng trong acceptance criteria, nhưng acceptance
criteria chỉ chạy một lần. Điều dễ mất nhất của phase này là bất biến "mọi thay đổi vào dữ liệu
công đi qua một đường có kiểm soát" — nó chết lặng lẽ, dưới dạng một `insert` tiện tay ở một
plan tương lai.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-CONTEXT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-01-SUMMARY.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-05-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cổng chặn đường ghi thẳng vào dữ liệu chấm công</name>
  <files>src/__tests__/no-silent-period-write.test.ts</files>
  <read_first>
    - `src/__tests__/lib/work-rule-scan.ts` và `src/__tests__/no-hardcoded-work-rules.test.ts`
      (04-06) — khuôn cổng quét mã nguồn của dự án: tách bộ quét ra khỏi test để chính nó kiểm
      được bằng mẫu vi phạm giả lập; quét trên mã **đã bỏ comment và nội dung chuỗi**; mỗi quy
      tắc đòi một **ngữ cảnh**, không chỉ một chuỗi.
    - `src/lib/data/mutations/attendance.ts` — `checkIn`/`checkOut` **là** đường ghi hợp lệ vào
      bảng chấm công cho kỳ đang mở; cổng phải cho chúng qua.
  </read_first>
  <behavior>
    - Báo đỏ khi một file **mới** dưới `src/lib/data/mutations/` chèn/sửa/xoá `attendance_records`
      mà không phải hai file đã được biết (`attendance.ts` cho chấm công thường; đường yêu cầu
      thì đi qua RPC nên không có lệnh ghi trực tiếp nào).
    - Không báo nhầm: `checkIn`/`checkOut` hiện có vẫn xanh; các lệnh **đọc** (`select`) không bị
      coi là ghi; comment và chuỗi hiển thị không tính.
    - Danh sách file được biết là tường minh, mỗi mục có lý do, tối đa 3 mục.
    - Cổng quét được ít nhất 5 file trong `mutations/` — một cổng đếm 0 file không phải bằng
      chứng không vi phạm.
  </behavior>
  <action>
    Viết cổng theo khuôn 04-06 (dùng lại `stripCommentsAndStrings` nếu phù hợp; nếu phải sửa nó
    thì test cũ của 04-06 phải vẫn xanh).

    **Kiểm răng bắt buộc:** (1) thêm tạm một `from("attendance_records").insert(...)` vào một
    file mutation khác (ví dụ `requests.ts`) → cổng **đỏ**; (2) hoàn tác → **xanh**; (3)
    `git status` sạch. Ghi cả ba bước vào SUMMARY — một cổng chưa từng báo đỏ là một cổng chưa
    được chứng minh là còn thức.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/no-silent-period-write.test.ts` thoát 0.
    - `npx vitest run src/__tests__/no-hardcoded-work-rules.test.ts` thoát 0 (cổng của Phase 4 không hồi quy).
    - `npm test` chạy cả hai cổng.
    - Kết quả ba bước kiểm răng ghi vào SUMMARY kèm `git status` sạch.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/no-silent-period-write.test.ts src/__tests__/no-hardcoded-work-rules.test.ts</automated>
  </verify>
  <done>Bất biến trung tâm của PERD-02 có một người canh gác chạy tự động, và người canh gác ấy đã được chứng minh là thức.</done>
</task>

<task type="auto">
  <name>Task 2: Vòng đời yêu cầu đầu-cuối qua HTTP thật</name>
  <files>scripts/e2e-approval.mjs, package.json</files>
  <read_first>
    - `scripts/e2e-settings.mjs` (04-06) — khuôn kịch bản e2e của dự án: tự tạo tài khoản +
      doanh nghiệp qua Admin API, đăng nhập thật, dựng cookie phiên đúng khuôn `@supabase/ssr`,
      gọi HTTP thật, dọn fixture ở cuối. Đọc kỹ khối comment nói **điều nó không chứng minh được**
      và giữ đúng sự trung thực đó.
    - `.planning/phases/04-.../04-06-SUMMARY.md` — giới hạn đã biết: thao tác GHI là Server
      Action nên script ngoài không gọi được. Phần ghi của kịch bản này đi qua Admin API hoặc
      RPC, và điều đó phải được nói rõ.
  </read_first>
  <action>
    Viết `scripts/e2e-approval.mjs` đi hết vòng đời, mỗi bước một khẳng định:
    1. Dựng doanh nghiệp + nhân viên có tài khoản + một tháng dữ liệu chấm công.
    2. Nhân viên gửi một yêu cầu nghỉ phép 3 ngày (trong đó có 1 ngày cuối tuần).
    3. Đọc `/api/requests?status=pending` bằng cookie quản trị — yêu cầu có mặt, kèm tên nhân viên.
    4. Xem trước tác động: **2 ngày công** (bỏ ngày cuối tuần).
    5. Duyệt → số ngày nghỉ phép của kỳ tăng đúng 2; lịch sử xử lý có đúng 1 dòng.
    6. Đọc `/api/notifications` bằng cookie **nhân viên** — có đúng 1 thông báo, nội dung nói
       đúng quyết định.
    7. Chốt kỳ tháng đó → trạng thái `closed`, có `closed_by`.
    8. `insert` thẳng vào `attendance_records` cho một ngày trong kỳ → **bị chặn**.
    9. Duyệt một yêu cầu bổ sung công cho ngày trong kỳ đã chốt → **thành công**.
    10. Dọn sạch; in một dòng tổng kết mỗi bước.

    Khai `test:e2e-approval` trong `package.json`.
  </action>
  <acceptance_criteria>
    - `npm run test:e2e-approval` thoát 0 trên môi trường dev thật (cần `npm run dev`).
    - Bước 8 và bước 9 nằm **liền nhau** và khẳng định hai kết quả **ngược nhau** — đó là bằng chứng cờ không rò.
    - `git status` sạch sau khi chạy; dữ liệu tạo ra đã dọn (trừ những gì trigger append-only chặn xoá — nếu có, ghi rõ vào SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e-approval</automated>
  </verify>
  <done>Vòng đời của một yêu cầu chạy được một lần đầu-cuối trên hệ thống thật, gồm cả vế khó nhất: kỳ đã chốt.</done>
</task>

<task type="checkpoint" gate="blocking">
  <name>Task 3: Nghiệm thu tám yêu cầu và đóng phase</name>
  <files>.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-UAT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 5 — năm tiêu chí, nguyên văn.
    - `.planning/phases/04-.../04-UAT.md` — khuôn biên bản nghiệm thu, gồm cả cách nói rõ
      **điều biên bản không nhận là đã làm**.
    - Năm file SUMMARY của 05-01…05-05.
  </read_first>
  <action>
    **(a) `05-UAT.md`** — với chủ dự án, chạy qua năm tiêu chí của phase trên hệ thống thật, mỗi
    tiêu chí ghi **quan sát cụ thể** (con số, thông điệp thật), không ghi "đạt":

    1. Danh sách chờ đúng doanh nghiệp; từ chối bắt buộc lý do.
    2. Duyệt xong số liệu kỳ đổi đúng theo loại yêu cầu.
    3. Lịch sử xử lý đầy đủ; nhân viên nhận thông báo trong ứng dụng.
    4. Duyệt vượt trần hiện cảnh báo nhưng vẫn duyệt được.
    5. Chốt kỳ hiển thị rõ; sau khi chốt, thay đổi chỉ đi được qua yêu cầu được duyệt, và đều có
       vết audit.

    Ghi rõ **giới hạn đã biết**: duyệt một cấp (D-30 — `manager` không duyệt được); không có
    đường mở lại kỳ đã chốt (D-32b); duyệt tăng ca không ghi giờ (D-31) nên doanh nghiệp không
    chấm công ngoài giờ sẽ thấy 0; và bất kỳ điều gì chỉ chạy được bằng máy chứ chưa ai bấm tay.

    **(b)** cập nhật `ROADMAP.md` (Phase 5 → Complete, 6/6, bảng Progress), `REQUIREMENTS.md`
    (APRV-01…05, SET-05, PERD-01, PERD-02 → Complete), `STATE.md` (chuyển sang Phase 6, cập nhật
    Blockers và khối Decisions với D-30…D-35).
  </action>
  <acceptance_criteria>
    - `05-UAT.md` tồn tại với năm mục, mỗi mục có quan sát cụ thể chứ không phải chữ "đạt".
    - Bốn giới hạn đã biết được liệt kê tường minh.
    - `ROADMAP.md` Phase 5 đánh dấu `[x]` đủ 6 plan; bảng Progress cập nhật.
    - `REQUIREMENTS.md` tám dòng chuyển Complete.
    - `STATE.md` chuyển sang Phase 6 với Blockers còn lại được ghi đúng.
    - `npm run typecheck && npm run lint && npm test && npm run build` thoát 0 lần cuối trước khi đóng phase.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>
  <done>Phase đóng bằng quan sát trên hệ thống thật, với giới hạn đã biết được viết ra chứ không để người sau tự phát hiện.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cổng tự động → mã nguồn tương lai | Thứ duy nhất của phase còn hiệu lực sau khi phase kết thúc |
| Biên bản nghiệm thu → trạng thái thật | Một biên bản ghi "đạt" mà không có quan sát là một lời khẳng định trống |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Tampering | Cổng bị nới lỏng dần bằng miễn trừ cho tới khi không bắt được gì | high | mitigate | Tối đa 3 mục miễn trừ, mỗi mục có lý do; prohibition tường minh; thu hẹp bằng ngữ nghĩa chứ không bằng đường dẫn |
| T-05-06-02 | Repudiation | Phase được đánh dấu hoàn thành khi một yêu cầu chưa từng được quan sát trên hệ thống thật | high | mitigate | Task 3 là checkpoint chặn; biên bản đòi quan sát cụ thể |
| T-05-06-03 | Denial of Service | Kịch bản e2e tạo dữ liệu rác trên môi trường dev và không dọn | medium | mitigate | Bước dọn cuối; acceptance criteria kiểm `git status` và trạng thái database |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Cổng chặn ghi thẳng vào dữ liệu chấm công chạy trong `npm test` và đã được chứng minh có răng
- `npm run test:e2e-approval` thoát 0, gồm cặp khẳng định ngược nhau ở kỳ đã chốt
- Năm tiêu chí của phase có biên bản nghiệm thu với quan sát cụ thể
- ROADMAP / REQUIREMENTS / STATE phản ánh đúng trạng thái sau phase
</verification>

<success_criteria>
Phase kết thúc với một người canh gác tự động cho bất biến trung tâm của nó, một biên bản nghiệm
thu dựa trên quan sát thật, và một danh sách giới hạn đã biết được viết ra thay vì để người sau
tự vấp phải.
</success_criteria>

<output>
Create `.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-06-SUMMARY.md` when done
</output>
