---
phase: 05-duy-t-y-u-c-u-v-ch-t-k-c-ng
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - supabase/migrations/0018_apply_approved_request.sql
  - supabase/tests/13_apply_approved_request.sql
  - supabase/tests/run-all.sql
  - scripts/check-pgtap-assertions.mjs
  - src/lib/data/mutations/requests.ts
  - src/lib/validation/api/requests.ts
  - src/app/admin/requests/requests-review-view.tsx
  - src/lib/constants.ts
  - src/lib/data/__tests__/request-effect.test.ts
autonomous: true
requirements: [APRV-03]

estimate:
  tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Duyệt một yêu cầu **nghỉ phép** sinh bản ghi công cho từng NGÀY LÀM VIỆC trong khoảng — bỏ qua ngày ngoài `working_days` của ca và ngày lễ đã khai (D-35). Số ngày công của kỳ giảm đúng bằng số ngày sinh ra."
    - "Duyệt một yêu cầu **bổ sung công** tạo một bản ghi chấm công cho ngày đó với giờ vào/ra lấy từ chính yêu cầu; bản ghi mang dấu vết là nó đến từ một yêu cầu được duyệt, không phải từ một lần chấm công thật."
    - "Duyệt một yêu cầu **điều chỉnh giờ** sửa giờ vào/ra của bản ghi tương ứng; giá trị trước khi sửa nằm trong `audit_log`."
    - "Duyệt một yêu cầu **tăng ca** KHÔNG ghi giờ vào dữ liệu công (D-31): nó chỉ đánh dấu đã được phép. Giờ tăng ca vẫn do dữ liệu chấm công thật quyết định qua mô-đun của Phase 4."
    - "Toàn bộ phần GHI nằm trong hàm SQL `tf_apply_approved_request()`, không nằm ở JavaScript (D-32a) — đây là điều kiện để 05-05 dựng được trigger bảo vệ kỳ đã chốt."
    - "Hàm chỉ xử lý yêu cầu ở trạng thái đã duyệt và **chưa từng được áp dụng**; gọi hai lần không sinh tác động thứ hai."
    - "Yêu cầu nghỉ phép chồng lên ngày ĐÃ CÓ bản ghi chấm công thật không ghi đè bản ghi đó — người đã đi làm hôm đó thì dữ liệu chấm công là sự thật, không phải đơn nghỉ."
    - "Màn hình duyệt hiện TRƯỚC KHI bấm: yêu cầu này sẽ tác động vào bao nhiêu ngày công. Người duyệt biết mình đang làm gì trước khi làm."
  artifacts:
    - path: "supabase/migrations/0018_apply_approved_request.sql"
      provides: "Hàm áp dụng tác động của một yêu cầu đã duyệt, chạy trong một transaction"
      exports: ["tf_apply_approved_request", "tf_preview_request_effect"]
    - path: "src/lib/data/__tests__/request-effect.test.ts"
      provides: "Đối chiếu số liệu kỳ trước và sau khi duyệt, trên database thật"
  key_links:
    - from: "src/lib/data/mutations/requests.ts"
      to: "supabase/migrations/0018_apply_approved_request.sql"
      via: "Server Action gọi đúng một RPC, không tự chèn từng dòng"
      pattern: "tf_apply_approved_request"
  prohibitions:
    - statement: "MUST NOT viết phần ghi ở JavaScript rồi hẹn 05-05 chuyển vào SQL. Cờ bỏ qua trigger là transaction-local; một chuỗi lệnh PostgREST rời rạc không có transaction chung, nên phần ghi ở JS sẽ KHÔNG bao giờ đi qua được kỳ đã chốt. Làm sai thứ tự này là làm lại từ đầu ở 05-05."
      category: architecture
    - statement: "MUST NOT ghi đè một bản ghi chấm công đã có bằng dữ liệu từ đơn nghỉ phép. Nhân viên đã chấm công hôm đó nghĩa là họ đã đi làm; một đơn nghỉ được duyệt muộn không xoá được sự thật đó, nó chỉ nêu ra một mâu thuẫn cần người xem."
      category: correctness
    - statement: "MUST NOT ghi số giờ tăng ca từ yêu cầu vào dữ liệu công (D-31). Phase 4 vừa dựng xong nguồn sự thật duy nhất cho con số đó; thêm nguồn thứ hai là tạo ra hai câu trả lời cho cùng một câu hỏi."
      category: correctness
---

<objective>
Nối hệ quả vào điểm đã có tên: một yêu cầu được duyệt **tác động đúng** vào dữ liệu công của kỳ.

Purpose: đây là plan làm cho cả phase có nghĩa. Không có nó, màn hình duyệt của 05-01 là một cái
nút đổi trạng thái. Và nó phải được viết **ở tầng SQL ngay từ đầu** — không phải vì SQL đẹp hơn,
mà vì cơ chế bảo vệ kỳ đã chốt của 05-05 dựa vào một cờ transaction-local, thứ không tồn tại khi
tầng ứng dụng gửi từng lệnh PostgREST rời rạc (D-32a).

Output: `tf_apply_approved_request()`, `tf_preview_request_effect()`, `reviewRequest` gọi RPC,
màn hình duyệt hiện trước tác động sắp xảy ra.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-CONTEXT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-01-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hàm áp dụng tác động, viết ở tầng SQL</name>
  <files>supabase/migrations/0018_apply_approved_request.sql, supabase/tests/13_apply_approved_request.sql, supabase/tests/run-all.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `.planning/phases/05-.../05-CONTEXT.md` D-31, D-32, D-32a, D-35 — nguyên văn.
    - `supabase/migrations/0016_overtime_rules_append_only.sql` (04-04) — khuôn hàm + trigger
      của dự án, gồm cách viết `comment on function`.
    - `supabase/migrations/0004_core_entities.sql` — cột thật của `attendance_records` và
      `work_requests`; chú ý `check (check_in_at is null or work_date = public.tf_work_date(check_in_at))`.
    - `supabase/migrations/0013_multi_punch_per_day.sql` — index `attendance_records_open_punch_uidx`
      **cố ý** loại dòng hai cột null; đó chính là hình dạng của dòng nghỉ phép.
    - `supabase/migrations/0003_enums_time.sql` — `tf_tz()`, `tf_work_date()`, `tf_shift_minutes()`.
    - `src/lib/attendance/classification.ts` (04-05) — quy tắc "ngày nào là ngày làm việc" ở
      tầng ứng dụng; hàm SQL phải cho **cùng** kết quả, nếu không hai nơi sẽ nói hai chuyện.
  </read_first>
  <behavior>
    - `tf_apply_approved_request(p_request_id)` chỉ chạy khi yêu cầu ở trạng thái đã duyệt và
      chưa từng áp dụng; ngược lại ném lỗi có thông điệp tiếng Việt.
    - Đặt cờ `set_config('tf.applying_approved_request','on', true)` ở **đầu** hàm — cờ
      transaction-local, sống đúng trong lần gọi này (D-32/D-32a).
    - `leave`: với mỗi ngày trong `[from_date, to_date]` mà **thuộc `working_days` của ca nhân
      viên** và **không nằm trong `holidays`** của doanh nghiệp, chèn một dòng
      `attendance_records` với `check_in_at`/`check_out_at` null, `worked_minutes` 0, trạng thái
      `leave_paid`. Ngày đã có bản ghi chấm công thật thì **bỏ qua** và đếm vào danh sách xung
      đột trả về.
    - `attendance_supplement`: chèn một dòng cho `from_date` với giờ vào/ra dựng từ `from_time`/
      `to_time` qua `tf_local_instant()`; `worked_minutes` tính bằng `tf_worked_minutes()`.
    - `time_adjustment`: sửa giờ vào/ra của bản ghi tương ứng ngày đó; nếu không tìm thấy bản
      ghi nào thì ném lỗi nói rõ, **không** tự tạo mới.
    - `overtime`: **không ghi gì** vào `attendance_records` (D-31); hàm trả về đúng thông tin
      "đã cho phép", số ngày tác động bằng 0.
    - Trả về một bản ghi tổng kết: số dòng đã chèn, số dòng đã sửa, số ngày bị bỏ qua vì xung đột.
    - Gọi lần thứ hai cho cùng yêu cầu: ném lỗi "đã được áp dụng", không sinh dòng nào thêm.
    - `tf_preview_request_effect(p_request_id)` trả **cùng phép đếm** nhưng **không ghi gì** —
      dùng cho màn hình duyệt hiện trước tác động.
  </behavior>
  <action>
    Viết migration 0018. Cột đánh dấu "đã áp dụng" thêm vào `work_requests`
    (`applied_at timestamptz null`) — đây là bất biến chống áp dụng hai lần, và nó phải nằm ở
    dòng yêu cầu chứ không phải ở một bảng phụ, để điều kiện kiểm nằm ngay cạnh dữ liệu.

    Khối comment đầu file ghi ba việc: (1) vì sao phần ghi nằm ở SQL chứ không ở JS (D-32a, cờ
    transaction-local); (2) vì sao nghỉ phép bỏ qua ngày nghỉ và ngày lễ (D-35 — sinh cả sẽ làm
    nhân viên mất phép cho ngày họ vốn được nghỉ); (3) vì sao `overtime` không ghi gì (D-31).

    Test pgTAP: ít nhất 8 khẳng định phủ bốn loại yêu cầu, chống áp dụng hai lần, bỏ qua ngày
    nghỉ/lễ, và không ghi đè bản ghi đã có.
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `npm run check:assertions` thoát 0 với sàn mới.
    - Hàm khai `security invoker` (mặc định) — `grep -c "security definer" supabase/migrations/0018_apply_approved_request.sql` trả `0`.
    - `grep -c "tf.applying_approved_request" supabase/migrations/0018_apply_approved_request.sql` trả ít nhất `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Tác động của một yêu cầu được duyệt nằm gọn trong một transaction, một chỗ, và không chạy được hai lần.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server Action gọi RPC, và bằng chứng số liệu đổi đúng</name>
  <files>src/lib/data/mutations/requests.ts, src/lib/validation/api/requests.ts, src/lib/data/__tests__/request-effect.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/requests.ts` — `reviewRequest()` của 05-01; điểm nối nằm **sau**
      khi trạng thái đã đổi và **trong cùng** lần gọi.
    - `src/lib/data/mutations/attendance.ts` — khuôn gọi `supabase.rpc()` và xử lý lỗi RPC.
    - `src/lib/data/__tests__/attendance-classification.test.ts` (04-05) — khuôn test tích hợp
      dựng cả một doanh nghiệp riêng; file này dùng lại để số liệu không lẫn với dữ liệu demo.
  </read_first>
  <behavior>
    - `reviewRequest` với `decision === 'approved'` gọi `tf_apply_approved_request` **sau** khi
      cập nhật trạng thái; lỗi từ RPC làm cả thao tác thất bại và trả thông điệp tiếng Việt.
    - `decision === 'rejected'` **không** gọi RPC.
    - Kết quả trả về mang phần tổng kết tác động để giao diện hiện toast nói rõ đã tạo bao nhiêu
      ngày công.
    - Duyệt nghỉ phép 3 ngày trong đó có 1 ngày cuối tuần: sinh **2** bản ghi, tổng hợp tháng
      tăng đúng 2 ngày nghỉ phép.
    - Duyệt nghỉ phép chồng lên một ngày đã có chấm công thật: bản ghi cũ **nguyên vẹn**, ngày
      đó vào danh sách xung đột.
    - Duyệt bổ sung công: `GET /api/attendance/summary` của tháng đó tăng đúng số phút của bản
      ghi mới.
    - Duyệt tăng ca: **không** bản ghi nào được tạo, và `convertedOvertimeHours` của tháng
      **không đổi** — con số đó vẫn đến từ chấm công thật (D-31).
  </behavior>
  <action>
    Nối RPC vào `reviewRequest()`; viết test tích hợp đối chiếu **số liệu kỳ trước và sau** cho
    cả bốn loại yêu cầu. Bài kiểm quan trọng nhất là hai bài cuối: xung đột không ghi đè, và
    tăng ca không đổi số liệu.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/request-effect.test.ts` thoát 0, ít nhất 7 test.
    - `grep -nE "from\(\"attendance_records\"\).*(insert|update)" src/lib/data/mutations/requests.ts` không trả dòng nào — tầng ứng dụng KHÔNG tự ghi vào bảng chấm công.
    - `npx vitest run` thoát 0 toàn bộ (đường đọc của Phase 4 không vỡ).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Duyệt xong thì số liệu kỳ đổi đúng theo loại yêu cầu — và có bằng chứng chạy được cho từng loại.</done>
</task>

<task type="auto">
  <name>Task 3: Người duyệt thấy trước tác động</name>
  <files>src/app/admin/requests/requests-review-view.tsx, src/lib/constants.ts</files>
  <read_first>
    - `src/components/settings/holidays-tab.tsx` (04-03) — tiền lệ "đếm ảnh hưởng trước khi
      hỏi": con số do server đếm, và chỉ hỏi khi thực sự có ảnh hưởng.
    - `src/components/requests/review-dialog.tsx` (05-01).
  </read_first>
  <action>
    Trước khi mở hộp duyệt, gọi `tf_preview_request_effect` qua một đường đọc và hiện trong hộp:
    yêu cầu này sẽ tạo bao nhiêu ngày công, sửa bao nhiêu bản ghi, bỏ qua bao nhiêu ngày vì đã
    có chấm công thật. Ngày bị bỏ qua hiện **nổi bật** kèm giải thích — đó là mâu thuẫn giữa đơn
    nghỉ và dữ liệu chấm công, và người duyệt là người duy nhất giải quyết được.

    Với yêu cầu tăng ca, hộp duyệt nói rõ: duyệt là **cho phép làm thêm**, giờ tăng ca vẫn tính
    từ chấm công thật (D-31) — nếu không, người duyệt sẽ tưởng mình vừa ghi nhận 3 giờ.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0.
    - `grep -c "tf_preview_request_effect\|previewRequestEffect" src/app/admin/requests/requests-review-view.tsx src/lib/data/requests.ts` — tổng ít nhất 2.
    - Với một đơn nghỉ 3 ngày có 1 ngày cuối tuần: hộp duyệt hiện "2 ngày công". Ghi quan sát vào SUMMARY.
    - Với một đơn nghỉ chồng ngày đã chấm công: hộp duyệt nêu rõ ngày bị bỏ qua. Ghi quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Không ai bấm duyệt mà không biết mình sắp đổi bao nhiêu ngày công.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yêu cầu được duyệt → dữ liệu công | Đây là đường duy nhất mà một thao tác của người dùng đổi được số liệu kỳ |
| Tầng ứng dụng → hàm SQL | Cờ bảo vệ kỳ đã chốt chỉ sống bên trong hàm |
| Đơn nghỉ ↔ dữ liệu chấm công thật | Hai nguồn nói khác nhau về cùng một ngày |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering | Áp dụng hai lần → trừ công hai lần, và lỗi hiện ra dưới dạng số liệu sai chứ không phải thông báo lỗi | high | mitigate | `applied_at` trên `work_requests` + kiểm ngay đầu hàm SQL; test gọi hai lần liên tiếp |
| T-05-02-02 | Tampering | Đơn nghỉ ghi đè bản ghi chấm công thật, xoá bằng chứng người đó đã đi làm | high | mitigate | Bỏ qua ngày đã có bản ghi và trả về danh sách xung đột; test khẳng định dòng cũ nguyên vẹn |
| T-05-02-03 | Tampering | Giờ tăng ca khai trong đơn được ghi vào dữ liệu công, tạo nguồn sự thật thứ hai | high | mitigate | D-31: `overtime` không ghi gì; test khẳng định `convertedOvertimeHours` không đổi |
| T-05-02-04 | Elevation of Privilege | Gọi thẳng RPC `tf_apply_approved_request` cho một yêu cầu chưa duyệt | high | mitigate | Hàm tự kiểm trạng thái, không tin người gọi; `security invoker` nên RLS vẫn áp |
| T-05-02-05 | Denial of Service | Đơn nghỉ khoảng ngày rất dài sinh hàng nghìn dòng | medium | mitigate | Giới hạn độ dài khoảng ngày ở schema đầu vào (đã có `to_date >= from_date`; thêm trần hợp lý) và nêu số ngày trong bản xem trước |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run db:push && npm run check:assertions` thoát 0
- Bốn loại yêu cầu tác động đúng như mô tả, kiểm bằng số liệu kỳ trước/sau trên database thật
- Áp dụng hai lần bị chặn; ngày đã có chấm công không bị ghi đè
- Duyệt tăng ca không đổi giờ quy đổi của tháng
- Tầng ứng dụng không có lệnh ghi trực tiếp nào vào `attendance_records` trong đường duyệt
</verification>

<success_criteria>
Duyệt một yêu cầu là một thao tác có hệ quả thật và đo được trên số liệu kỳ — và người duyệt
thấy hệ quả đó trước khi bấm.
</success_criteria>

<output>
Create `.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-02-SUMMARY.md` when done
</output>
