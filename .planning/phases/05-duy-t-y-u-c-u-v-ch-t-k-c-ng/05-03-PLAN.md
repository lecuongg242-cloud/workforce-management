---
phase: 05-duy-t-y-u-c-u-v-ch-t-k-c-ng
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - supabase/migrations/0019_overtime_cap.sql
  - src/lib/types/domain.ts
  - src/lib/validation/api/settings.ts
  - src/lib/data/mutations/settings.ts
  - src/lib/settings/company-settings.ts
  - src/components/settings/general-settings-form.tsx
  - src/lib/attendance/overtime-cap.ts
  - src/lib/attendance/__tests__/overtime-cap.test.ts
  - src/app/api/requests/overtime-usage/route.ts
  - src/app/admin/requests/requests-review-view.tsx
  - src/components/requests/review-dialog.tsx
  - src/lib/constants.ts
  - src/lib/data/__tests__/overtime-cap.test.ts
autonomous: true
requirements: [SET-05]

estimate:
  tokens: 62000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Chủ doanh nghiệp đặt **trần tăng ca của riêng mình** ở tab Chung của `/admin/settings` (SET-05); hệ thống không áp cứng con số nào và trần để trống nghĩa là **không giới hạn**, không phải bằng 0."
    - "Duyệt một yêu cầu tăng ca làm tổng giờ tăng ca của nhân viên trong tháng vượt trần thì **hiện cảnh báo** cho người duyệt — và **vẫn cho duyệt tiếp** (SET-05: cảnh báo, không chặn cứng)."
    - "Cảnh báo nói ba con số thật: đã dùng bao nhiêu giờ trong tháng, yêu cầu này thêm bao nhiêu, trần là bao nhiêu. Một cảnh báo không có số là một cảnh báo người ta bấm qua."
    - "Giờ đã dùng tính từ **dữ liệu chấm công thật** qua mô-đun của Phase 4 cộng giờ đã đăng ký được duyệt còn hiệu lực — không có cột nào lưu sẵn con số này."
    - "Trần để trống (null) thì không cảnh báo gì; không có ngưỡng thì không có gì để vượt."
    - "Trần là số giờ **mỗi nhân viên mỗi tháng** — đơn vị được nói rõ ngay trên nhãn, vì 'trần tăng ca' có thể hiểu là mỗi tuần, mỗi tháng hay mỗi năm."
  artifacts:
    - path: "src/lib/attendance/overtime-cap.ts"
      provides: "Mô-đun thuần quyết định có cảnh báo vượt trần hay không"
      exports: ["isOverCap", "capUsageSummary"]
    - path: "src/app/api/requests/overtime-usage/route.ts"
      provides: "Đường đọc giờ tăng ca đã dùng trong tháng của một nhân viên"
      exports: ["GET", "dynamic"]
  key_links:
    - from: "src/components/requests/review-dialog.tsx"
      to: "src/lib/attendance/overtime-cap.ts"
      via: "cảnh báo vượt trần dùng một hàm quyết định duy nhất"
      pattern: "isOverCap"
  prohibitions:
    - statement: "MUST NOT chặn duyệt khi vượt trần. SET-05 nói rõ: cảnh báo, không chặn cứng. Doanh nghiệp có lý do chính đáng để vượt trần trong một tháng cụ thể, và một cái chặn cứng sẽ được vượt qua bằng cách nâng trần — tức là mất luôn ngưỡng."
      category: correctness
    - statement: "MUST NOT coi trần để trống là 0. Doanh nghiệp chưa khai trần nghĩa là họ chưa đặt giới hạn, không phải họ cấm tăng ca — hiểu nhầm này biến mọi lần duyệt thành một cảnh báo và người duyệt sẽ ngừng đọc."
      category: correctness
    - statement: "MUST NOT lưu 'giờ tăng ca đã dùng' vào một cột. Phase 4 đã chọn tính-lúc-truy-vấn cho toàn bộ số liệu tăng ca; một cột đếm sẵn ở đây sẽ lệch với con số kia ngay lần đầu ai đó sửa dữ liệu chấm công."
      category: architecture
---

<objective>
Doanh nghiệp đặt trần tăng ca của riêng mình, và người duyệt được cảnh báo — chứ không bị chặn —
khi một lần duyệt đưa nhân viên vượt trần đó.

Purpose: SET-05 là yêu cầu duy nhất của nhóm SET còn lại sau Phase 4, và nó chỉ có nghĩa trong
luồng duyệt nên nằm ở đây. Giá trị thật của nó không phải con số trần, mà là **người duyệt biết
mình đang làm gì tại đúng lúc bấm** — nên phần khó là ba con số trong cảnh báo, không phải ô nhập.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-CONTEXT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-01-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-01-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trần tăng ca trong cấu hình doanh nghiệp</name>
  <files>supabase/migrations/0019_overtime_cap.sql, src/lib/types/domain.ts, src/lib/validation/api/settings.ts, src/lib/data/mutations/settings.ts, src/lib/settings/company-settings.ts, src/components/settings/general-settings-form.tsx, src/lib/constants.ts</files>
  <read_first>
    - `supabase/migrations/0015_company_settings.sql` (04-01) — bảng cấu hình; cột mới thêm vào
      đây, **không** tạo bảng mới.
    - `src/lib/data/mutations/settings.ts` (04-01) — khối comment nói rõ: plan nào thêm cột thì
      thêm vào **hàm này**, không mở đường ghi thứ hai. Đây chính là lần đầu lời hứa đó được dùng.
    - `src/components/settings/general-settings-form.tsx` — form bốn trường hiện có; trường mới
      là trường thứ năm, khác ba trường kia ở chỗ nó **được phép để trống**.
  </read_first>
  <behavior>
    - Cột `overtime_cap_hours_per_month numeric null` với `check (... is null or ... > 0)` —
      `null` là "không giới hạn", 0 không phải giá trị hợp lệ.
    - Không có giá trị mặc định (khác bốn cột kia): đây là **giá trị nghiệp vụ doanh nghiệp tự
      quyết**, đúng nhóm mà D-26 cấm áp cứng.
    - Form: ô để trống hợp lệ và lưu ra `null`; nhập số âm hoặc 0 bị từ chối; nhãn nói rõ đơn vị
      "giờ / nhân viên / tháng"; chữ trợ giúp nói rõ vượt trần **chỉ cảnh báo**, không chặn.
    - Ghi vẫn đi qua đúng `updateCompanySettings()`, để lại đúng một dòng audit.
  </behavior>
  <action>
    Migration 0019 + mở rộng schema/kiểu/form. Khối comment của migration ghi rõ vì sao cột này
    **không có mặc định** trong khi bốn cột cùng bảng thì có: bốn cột kia là ngưỡng vận hành và
    định nghĩa pháp lý (D-27/D-29), cột này là con số nghiệp vụ doanh nghiệp tự quyết (D-26).
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `npm run typecheck`, `npm run lint` thoát 0.
    - `psql ... -tAc "select column_default from information_schema.columns where table_name='company_settings' and column_name='overtime_cap_hours_per_month'"` trả rỗng (kiểm qua test tích hợp nếu không có `psql`).
    - `npx vitest run src/lib/data/__tests__/settings.test.ts` thoát 0 với test mới cho trường để trống.
    - `grep -c "logMutation" src/lib/data/mutations/settings.ts` vẫn trả `1` — không đường ghi thứ hai.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/settings.test.ts</automated>
  </verify>
  <done>Doanh nghiệp có chỗ đặt trần của mình, và để trống là một câu trả lời hợp lệ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ba con số của cảnh báo</name>
  <files>src/lib/attendance/overtime-cap.ts, src/lib/attendance/__tests__/overtime-cap.test.ts, src/app/api/requests/overtime-usage/route.ts, src/lib/data/__tests__/overtime-cap.test.ts</files>
  <read_first>
    - `src/lib/attendance/classification-context.ts` (04-05) — nguồn duy nhất của giờ tăng ca;
      đường đọc mới dùng lại nó, không tính lại.
    - `src/lib/attendance/suspicious.ts` — khuôn mô-đun thuần: một hàm quyết định, hằng số có tên.
    - `src/app/api/attendance/classification/route.ts` (04-05) — khuôn Route Handler đọc theo
      nhân viên + tháng, gồm cả tầng quyền.
  </read_first>
  <behavior>
    - `isOverCap({ usedHours, requestedHours, capHours })` trả `false` khi `capHours` là `null`;
      trả `true` khi `usedHours + requestedHours > capHours`; biên **bằng đúng trần** trả `false`
      (vượt là lớn hơn, không phải bằng).
    - `capUsageSummary(...)` trả ba con số để hiển thị, và phần vượt (nếu có).
    - `GET /api/requests/overtime-usage?employeeId&month` trả giờ tăng ca đã dùng của nhân viên
      đó trong tháng, tính từ `classification-context.ts`, cộng giờ đã đăng ký được duyệt của
      các yêu cầu tăng ca khác trong cùng tháng.
    - Tháng chưa có dữ liệu trả 0, không trả lỗi.
    - Tầng quyền giống `GET /api/attendance/classification`: `employee`/`manager` chỉ hỏi được
      chính mình.
  </behavior>
  <action>
    Viết mô-đun thuần + đường đọc + hai bộ test (thuần và tích hợp). Khối comment của mô-đun ghi
    rõ vì sao **không** có nhánh chặn: SET-05 nói cảnh báo, và một cái chặn cứng sẽ bị vượt qua
    bằng cách nâng trần — tức là mất luôn ngưỡng.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/attendance/__tests__/overtime-cap.test.ts` thoát 0, ít nhất 6 test (gồm biên bằng đúng trần và trần `null`).
    - `npx vitest run src/lib/data/__tests__/overtime-cap.test.ts` thoát 0, ít nhất 4 test trên database thật.
    - `grep -nE "throw|Forbidden|block" src/lib/attendance/overtime-cap.ts` không trả dòng nào — mô-đun này chỉ trả lời, không chặn.
    - `grep -c "classification-context\|listAttendanceClassification" src/app/api/requests/overtime-usage/route.ts` trả ít nhất `1` — giờ đã dùng đến từ nguồn của Phase 4.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/attendance/__tests__/overtime-cap.test.ts src/lib/data/__tests__/overtime-cap.test.ts</automated>
  </verify>
  <done>Ba con số của cảnh báo là số thật, lấy từ đúng một nguồn, và không có cột nào đếm sẵn để lệch.</done>
</task>

<task type="auto">
  <name>Task 3: Cảnh báo tại đúng lúc bấm</name>
  <files>src/components/requests/review-dialog.tsx, src/app/admin/requests/requests-review-view.tsx, src/lib/constants.ts</files>
  <read_first>
    - `src/components/requests/review-dialog.tsx` (05-01) và phần xem trước tác động của 05-02 —
      cảnh báo này nằm **cùng chỗ** đó, không phải một hộp thoại thứ hai.
    - `src/components/settings/overtime-rule-dialog.tsx` (04-04) — khuôn khối cảnh báo hổ phách
      trong hộp thoại.
  </read_first>
  <action>
    Với yêu cầu loại `overtime`, hộp duyệt hiện khối cảnh báo khi vượt trần: đã dùng X giờ trong
    tháng, yêu cầu này thêm Y giờ, trần là Z giờ, vượt W giờ. Nút "Duyệt" **vẫn bấm được** —
    nhưng chữ trên nút nói rõ đang duyệt một yêu cầu vượt trần, để cái bấm là một quyết định chứ
    không phải một phản xạ.

    Trần để trống thì không có khối nào — không nói gì cả, thay vì nói "không giới hạn" ở mọi
    lần duyệt.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -c "isOverCap" src/components/requests/review-dialog.tsx` trả ít nhất `1`.
    - `grep -ciE "không thể duyệt|bị chặn|không cho phép" src/components/requests/review-dialog.tsx src/lib/constants.ts` — mọi kết quả phải nằm ngoài đường cảnh báo trần; kiểm bằng mắt và ghi kết luận vào SUMMARY.
    - Với trần 20 giờ, nhân viên đã dùng 18 giờ, yêu cầu thêm 4 giờ: hộp duyệt hiện cảnh báo với đủ bốn con số **và** vẫn duyệt được. Ghi quan sát vào SUMMARY.
    - Với trần để trống: không khối cảnh báo nào xuất hiện. Ghi quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Người duyệt được nói cho biết, đúng lúc, bằng số — và vẫn là người quyết định.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trần doanh nghiệp → quyết định duyệt | Một cảnh báo sai làm người duyệt ngừng đọc mọi cảnh báo |
| Giờ đã dùng → hai nguồn có thể lệch | Phase 4 tính từ chấm công; đăng ký đã duyệt là một đại lượng khác |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | Trần để trống bị coi là 0 → mọi lần duyệt đều cảnh báo → người duyệt ngừng đọc, và cảnh báo thật chìm theo | high | mitigate | `null` là "không giới hạn", có test riêng cho biên này; prohibition tường minh |
| T-05-03-02 | Tampering | Một cột đếm sẵn "giờ đã dùng" lệch với con số Phase 4 tính ra | medium | mitigate | Không lưu cột nào; đường đọc dùng lại `classification-context.ts` |
| T-05-03-03 | Repudiation | Duyệt vượt trần mà không còn dấu vết là đã có cảnh báo | medium | mitigate | Ghi phần vượt vào `request_reviews.note` khi người duyệt bấm tiếp — dấu vết nằm trong lịch sử xử lý của 05-01 |
| T-05-03-04 | Elevation of Privilege | Đường đọc giờ đã dùng lộ số liệu của nhân viên khác | medium | mitigate | Tầng quyền giống `GET /api/attendance/classification`; test bằng phiên `employee` thật |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run db:push` thoát 0
- Trần để trống không sinh cảnh báo nào; biên bằng đúng trần không phải là vượt
- Vượt trần vẫn duyệt được, và phần vượt để lại dấu vết trong lịch sử xử lý
- Giờ đã dùng đến từ đúng nguồn của Phase 4, không từ một cột đếm sẵn
</verification>

<success_criteria>
Doanh nghiệp đặt được giới hạn của riêng mình, và giới hạn đó lên tiếng đúng lúc bằng những con
số thật — mà không tước quyền quyết định của người duyệt.
</success_criteria>

<output>
Create `.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-03-SUMMARY.md` when done
</output>
