---
phase: 05-duy-t-y-u-c-u-v-ch-t-k-c-ng
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-03"]
files_modified:
  - supabase/migrations/0021_period_close.sql
  - supabase/tests/15_period_close.sql
  - supabase/tests/run-all.sql
  - scripts/check-pgtap-assertions.mjs
  - src/lib/types/domain.ts
  - src/lib/validation/api/periods.ts
  - src/lib/data/periods.ts
  - src/lib/data/mutations/periods.ts
  - src/app/api/periods/route.ts
  - src/app/admin/periods/page.tsx
  - src/app/admin/periods/periods-view.tsx
  - src/lib/data/mutations/attendance.ts
  - src/lib/nav.ts
  - src/lib/constants.ts
  - src/lib/data/__tests__/period-close.test.ts
autonomous: true
requirements: [PERD-01, PERD-02]

estimate:
  tokens: 88000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Quản trị chốt được kỳ công (một tháng dương lịch — D-09) và trạng thái đã chốt hiển thị rõ ở màn hình kỳ công (PERD-01)."
    - "Sau khi chốt, MỌI ghi vào `attendance_records` của ngày thuộc kỳ đó bị **trigger của database** từ chối — kể cả chấm công bình thường, kể cả một Server Action mới viết sau này quên kiểm (PERD-02, D-32)."
    - "Đường ghi hợp lệ DUY NHẤT vào một kỳ đã chốt là `tf_apply_approved_request()` của 05-02: nó tự đặt cờ transaction-local mà trigger nhận ra. Gọi thẳng `insert`/`update` với cùng dữ liệu vẫn bị chặn — chứng minh bằng test."
    - "Chốt kỳ để lại một dòng `audit_log`; `closed_at` và `closed_by` ghi ai chốt lúc nào, và `closed_at` lấy từ đồng hồ database (D-19)."
    - "Kỳ chưa có trong bảng thì thao tác chốt tự tạo dòng kỳ đó — quản trị không phải 'khởi tạo kỳ' như một bước riêng mà không ai hiểu để làm gì."
    - "Chốt một kỳ **chưa kết thúc** bị từ chối kèm lý do: chốt một tháng chưa qua là chốt một con số còn đang thay đổi."
    - "Chấm công vào một ngày thuộc kỳ đã chốt trả về thông điệp tiếng Việt nói rõ kỳ đã chốt và cách đi tiếp (gửi yêu cầu bổ sung công), không phải một lỗi database thô."
    - "KHÔNG có đường mở lại kỳ đã chốt trong phase này (D-32b) — và điều đó được nói rõ trên màn hình, không để người dùng đi tìm một nút không tồn tại."
  artifacts:
    - path: "supabase/migrations/0021_period_close.sql"
      provides: "Trigger bảo vệ kỳ đã chốt + hàm chốt kỳ"
      exports: ["tf_attendance_period_guard", "tf_close_period"]
    - path: "src/app/admin/periods/periods-view.tsx"
      provides: "Màn hình kỳ công và thao tác chốt"
      min_lines: 100
  key_links:
    - from: "supabase/migrations/0021_period_close.sql"
      to: "supabase/migrations/0018_apply_approved_request.sql"
      via: "trigger nhận ra cờ mà hàm áp dụng yêu cầu đặt"
      pattern: "tf.applying_approved_request"
  prohibitions:
    - statement: "MUST NOT dựng bảo vệ kỳ đã chốt chỉ ở tầng Server Action. Một đường ghi mới quên kiểm là kỳ đã chốt bị sửa lặng lẽ, và không ai phát hiện — số liệu chỉ đơn giản là khác đi. Đây đúng là loại rủi ro mà PERD-02 tồn tại để chặn (D-32)."
      category: correctness
    - statement: "MUST NOT thêm một đường mở lại kỳ đã chốt vào phase này (D-32b). Một nút 'mở lại' làm rỗng nghĩa của việc chốt; khi cần, nó phải có tên riêng, lý do bắt buộc và audit riêng — thiết kế chưa được làm."
      category: architecture
    - statement: "MUST NOT để lỗi từ trigger nổi lên giao diện dưới dạng chuỗi lỗi Postgres. Nhân viên chấm công vào kỳ đã chốt cần biết PHẢI LÀM GÌ tiếp, không cần biết tên ràng buộc nào vừa nổ."
      category: values
---

<objective>
Chốt kỳ công, và làm cho việc chốt có nghĩa: sau đó số liệu của kỳ chỉ đổi được qua một yêu cầu
được duyệt.

Purpose: đây là lời hứa cuối cùng của phase và là lời hứa nặng nhất — nó nói rằng một con số đã
chốt thì không đổi sau lưng ai. Một lời hứa như vậy không thể sống ở tầng ứng dụng: nó phải được
cưỡng chế ở nơi mà **mọi** đường ghi đều đi qua. Đó là lý do 05-02 đã viết phần ghi ở tầng SQL —
plan này chỉ lắp cái khoá vào ổ đã chừa sẵn.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-CONTEXT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-02-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trigger bảo vệ kỳ đã chốt và hàm chốt kỳ</name>
  <files>supabase/migrations/0021_period_close.sql, supabase/tests/15_period_close.sql, supabase/tests/run-all.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `.planning/phases/05-.../05-CONTEXT.md` D-32, D-32a, D-32b — nguyên văn.
    - `supabase/migrations/0016_overtime_rules_append_only.sql` (04-04) — khuôn trigger cưỡng
      chế của dự án, gồm **thủ tục gỡ có ý thức** ghi ngay trong comment. Trigger ở đây phải có
      một khối tương đương.
    - `supabase/migrations/0018_apply_approved_request.sql` (05-02) — nơi cờ
      `tf.applying_approved_request` được đặt; trigger này là bên đọc cờ đó.
    - `supabase/migrations/0005_v2_tables.sql` khối `periods` — ràng buộc tròn tháng đã có sẵn.
    - `supabase/migrations/0010_check_in_time_functions.sql` — `tf_server_now()`.
  </read_first>
  <behavior>
    - Trigger `attendance_period_guard` trên `attendance_records` (`before insert or update or
      delete`): từ chối khi `work_date` của dòng rơi vào một kỳ có `status='closed'` của cùng
      doanh nghiệp, **trừ khi** `current_setting('tf.applying_approved_request', true) = 'on'`.
    - Kỳ **đang mở** hoặc chưa tồn tại: ghi bình thường, không ảnh hưởng gì tới chấm công hằng
      ngày.
    - Thông điệp lỗi nói **phải làm gì tiếp**: kỳ tháng nào đã chốt, và thay đổi phải đi qua một
      yêu cầu được duyệt.
    - `tf_close_period(p_company_id, p_start_date)`: tạo dòng kỳ nếu chưa có, đặt `status='closed'`,
      `closed_at = now()`, `closed_by = auth.uid()`. Từ chối nếu kỳ chưa kết thúc (ngày cuối kỳ
      còn ở tương lai theo `tf_work_date(now())`), hoặc nếu kỳ đã chốt rồi.
    - Không có hàm mở lại kỳ (D-32b).
  </behavior>
  <action>
    Viết migration 0021 + test pgTAP (ít nhất 8 khẳng định): ghi vào kỳ mở được; ghi vào kỳ chốt
    bị chặn ở cả ba lệnh insert/update/delete; cờ bật thì qua được; chốt kỳ chưa kết thúc bị từ
    chối; chốt hai lần bị từ chối; `closed_by` đúng người.

    Khối comment đầu file ghi ba việc: (1) vì sao bảo vệ nằm ở database chứ không ở ứng dụng
    (D-32); (2) cờ là transaction-local và **chỉ** được đặt bên trong `tf_apply_approved_request`,
    kèm cảnh báo rằng đặt cờ ở bất kỳ chỗ nào khác là mở một cửa hậu; (3) thủ tục gỡ trigger có
    ý thức cho trường hợp phải sửa dữ liệu hỏng thật.
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `npm run check:assertions` thoát 0 với sàn mới.
    - `grep -c "tf.applying_approved_request" supabase/migrations/0021_period_close.sql` trả ít nhất `1`.
    - `grep -ci "reopen\|mo_lai\|mo lai" supabase/migrations/0021_period_close.sql` — mọi kết quả phải nằm trong comment giải thích **vì sao không có**; kiểm bằng mắt, ghi kết luận vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Kỳ đã chốt được khoá ở nơi mọi đường ghi đều phải đi qua, và chìa khoá chỉ nằm trong tay một hàm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bằng chứng cái khoá có răng</name>
  <files>src/lib/data/__tests__/period-close.test.ts</files>
  <read_first>
    - `src/lib/data/__tests__/overtime-rules.test.ts` (04-04) — khuôn test tích hợp chứng minh
      một trigger có răng trên database thật.
    - `src/lib/data/__tests__/request-effect.test.ts` (05-02) — fixture doanh nghiệp riêng.
  </read_first>
  <behavior>
    - Kỳ đang mở: `checkIn`/`checkOut` chạy bình thường (không hồi quy chấm công hằng ngày).
    - Chốt kỳ xong: `insert` thẳng vào `attendance_records` cho một ngày trong kỳ **bị từ chối**
      bởi database, thông điệp nhắc tới kỳ đã chốt.
    - Cũng sau khi chốt: `update` một dòng cũ của kỳ đó **bị từ chối**.
    - Duyệt một yêu cầu bổ sung công cho một ngày trong kỳ đã chốt: **thành công**, và dòng mới
      xuất hiện — đường hợp lệ vẫn đi được (PERD-02 vế sau).
    - Ngay sau lần gọi hợp lệ đó, `insert` thẳng lại **vẫn bị chặn** — cờ không rò ra ngoài
      transaction của hàm.
    - Kỳ tháng khác (chưa chốt) không bị ảnh hưởng.
    - Mọi thay đổi vào kỳ đã chốt đều để lại vết trong `audit_log`.
  </behavior>
  <action>
    Viết test tích hợp trên database dev thật, phủ đủ bảy hành vi. Bài quan trọng nhất là hai bài
    liền nhau: **đường hợp lệ đi được** rồi **ngay sau đó đường thẳng vẫn bị chặn** — đó là bằng
    chứng cờ không rò.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/period-close.test.ts` thoát 0, ít nhất 7 test.
    - `npx vitest run` thoát 0 toàn bộ — không test nào của Phase 3/4 vỡ vì trigger mới.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Cái khoá được chứng minh trên database thật, cả chiều chặn lẫn chiều cho qua.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Đường đọc, Server Action chốt kỳ, và thông điệp cho nhân viên</name>
  <files>src/lib/types/domain.ts, src/lib/validation/api/periods.ts, src/lib/data/periods.ts, src/lib/data/mutations/periods.ts, src/app/api/periods/route.ts, src/lib/data/mutations/attendance.ts, src/lib/constants.ts</files>
  <read_first>
    - `src/lib/data/mutations/settings.ts` (04-01) — khuôn Server Action + audit.
    - `src/lib/data/mutations/attendance.ts` — `checkIn`/`checkOut`; đây là nơi lỗi từ trigger sẽ
      nổi lên, và là nơi phải dịch nó thành một câu nói được.
    - `src/lib/attendance/rejection.ts` (03-04) — `AttendanceRejectedError` và ba lý do của
      D-20b. **Cân nhắc**: kỳ đã chốt có phải lý do thứ tư không, hay là một loại lỗi khác? Ghi
      lựa chọn và lý do vào SUMMARY — D-20b khoá đúng ba lý do, nên thêm cái thứ tư là một quyết
      định có hệ quả, không phải một tiện tay.
  </read_first>
  <behavior>
    - `GET /api/periods` trả các kỳ của doanh nghiệp trong phiên, mới nhất trước, kèm số liệu
      tóm tắt đủ để quyết định có chốt hay không (số nhân viên có công, số yêu cầu còn chờ xử lý).
    - `closePeriod(month)` đòi `owner`/`admin`, gọi `tf_close_period`, ghi audit.
    - Chốt kỳ **còn yêu cầu chờ xử lý** không bị chặn, nhưng trả về cảnh báo để giao diện hỏi lại
      — chốt xong thì những yêu cầu đó chỉ còn đi được qua đường duyệt-vào-kỳ-đã-chốt.
    - `checkIn`/`checkOut` gặp lỗi trigger → thông điệp tiếng Việt nói rõ kỳ nào đã chốt và cách
      đi tiếp; không để lộ chuỗi lỗi Postgres.
  </behavior>
  <action>
    Viết đường đọc + Server Action + lớp dịch lỗi. Bắt riêng mã lỗi mà trigger ném ra (cùng khuôn
    `mutations/holidays.ts` bắt `23505`).
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint` thoát 0; `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0.
    - `grep -c "logMutation" src/lib/data/mutations/periods.ts` trả `1`.
    - Test: `checkIn` vào ngày thuộc kỳ đã chốt ném lỗi có chứa chữ "đã chốt" và **không** chứa chuỗi lỗi Postgres thô.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Chốt kỳ là một thao tác có tên, có vết, và người bị chặn sau đó biết phải làm gì tiếp.</done>
</task>

<task type="auto">
  <name>Task 4: Màn hình kỳ công</name>
  <files>src/app/admin/periods/page.tsx, src/app/admin/periods/periods-view.tsx, src/lib/nav.ts, src/lib/constants.ts</files>
  <read_first>
    - `src/app/admin/settings/settings-view.tsx` (04-01) — khuôn màn hình quản trị.
    - `src/components/common/confirm-dialog.tsx` — hộp xác nhận; chốt kỳ là thao tác **không
      hoàn tác được**, nên nó phải đi qua đây.
    - `src/lib/nav.ts` — `ADMIN_NAV_ITEMS`.
  </read_first>
  <action>
    Danh sách kỳ theo tháng: trạng thái (đang mở / đã chốt, phân biệt bằng biểu tượng + nhãn
    chữ), số nhân viên có công, số yêu cầu còn chờ, và với kỳ đã chốt thì ai chốt lúc nào.

    Nút "Chốt kỳ" chỉ hiện cho kỳ đã kết thúc và đang mở. Hộp xác nhận nói đủ ba điều: sau khi
    chốt, số liệu kỳ chỉ đổi được qua một yêu cầu được duyệt; **không có đường mở lại** (D-32b);
    và số yêu cầu còn chờ xử lý nếu có.

    Chữ trên màn hình không hứa một nút mở lại không tồn tại.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -c "ConfirmDialog" src/app/admin/periods/periods-view.tsx` trả ít nhất `1`.
    - `grep -ciE "mở lại|hoàn tác|undo" src/app/admin/periods/periods-view.tsx src/lib/constants.ts` — mọi kết quả phải nằm trong câu nói rằng **không có** đường đó; kiểm bằng mắt và ghi kết luận vào SUMMARY.
    - Với phiên owner: chốt một kỳ đã kết thúc → trạng thái đổi, hiện ai chốt lúc nào; thử chấm công vào ngày thuộc kỳ đó → bị từ chối với thông điệp đọc được. Ghi cả hai quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Quản trị chốt được kỳ, thấy rõ hệ quả trước khi bấm, và không đi tìm một nút mở lại không có.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kỳ đã chốt → mọi đường ghi | Đây là bất biến nặng nhất của phase; một lỗ ở đây làm rỗng nghĩa việc chốt |
| Cờ bỏ qua trigger → cửa hậu tiềm năng | Đặt được cờ ở ngoài hàm là mở khoá cho mọi thứ |
| Thao tác chốt → không hoàn tác được | Không có đường lùi ở phase này (D-32b) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05-01 | Tampering | Một đường ghi mới (viết ở phase sau) quên kiểm kỳ đã chốt, sửa lặng lẽ số liệu đã chốt | high | mitigate | D-32: cưỡng chế bằng trigger, không bằng kiểm ở tầng ứng dụng; test chứng minh `insert` thẳng bị chặn |
| T-05-05-02 | Elevation of Privilege | Cờ `tf.applying_approved_request` bị đặt từ ngoài, thành cửa hậu dùng chung | high | mitigate | Cờ transaction-local, chỉ đặt bên trong `tf_apply_approved_request`; test khẳng định ngay sau một lần áp dụng hợp lệ thì `insert` thẳng **vẫn** bị chặn |
| T-05-05-03 | Denial of Service | Trigger chặn nhầm chấm công của kỳ đang mở → cả doanh nghiệp không chấm công được | high | mitigate | Điều kiện chỉ kích hoạt với kỳ `status='closed'`; test phủ cả hai chiều; toàn bộ bộ test Phase 3/4 phải vẫn xanh |
| T-05-05-04 | Repudiation | Chốt kỳ mà không biết ai chốt lúc nào | medium | mitigate | `closed_by`/`closed_at` từ đồng hồ database + một dòng audit |
| T-05-05-05 | Tampering | Chốt một kỳ chưa kết thúc để đóng băng số liệu còn đang thay đổi | medium | mitigate | `tf_close_period` từ chối kỳ chưa kết thúc; test riêng |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run db:push && npm run check:assertions` thoát 0
- Kỳ đang mở ghi bình thường; kỳ đã chốt bị chặn ở cả insert/update/delete
- Đường hợp lệ (yêu cầu được duyệt) đi được vào kỳ đã chốt, và ngay sau đó đường thẳng vẫn bị chặn
- Chốt kỳ có audit, có người chốt, có thời điểm từ đồng hồ database
- Chấm công vào kỳ đã chốt trả thông điệp đọc được, không phải lỗi Postgres thô
</verification>

<success_criteria>
Một kỳ đã chốt là một con số đứng yên — và cách duy nhất làm nó đổi là một yêu cầu được duyệt,
để lại vết đầy đủ.
</success_criteria>

<output>
Create `.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-05-SUMMARY.md` when done
</output>
