---
phase: 05-duy-t-y-u-c-u-v-ch-t-k-c-ng
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - supabase/migrations/0020_notifications.sql
  - supabase/tests/14_notifications.sql
  - supabase/tests/run-all.sql
  - scripts/check-pgtap-assertions.mjs
  - src/lib/types/domain.ts
  - src/lib/validation/api/notifications.ts
  - src/lib/data/notifications.ts
  - src/lib/data/mutations/notifications.ts
  - src/app/api/notifications/route.ts
  - src/lib/data/mutations/requests.ts
  - src/components/employee-app/notification-bell.tsx
  - src/components/layout/mobile-header.tsx
  - src/app/employee/notifications/page.tsx
  - src/app/employee/notifications/notifications-view.tsx
  - src/lib/nav.ts
  - src/lib/constants.ts
  - src/lib/data/__tests__/notifications.test.ts
autonomous: true
requirements: [APRV-05]

estimate:
  tokens: 74000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Nhân viên nhận được thông báo **trong ứng dụng** ngay khi yêu cầu của mình được xử lý (APRV-05) — không phải chờ tự vào đúng màn hình mới biết."
    - "Thông báo sinh ra **trong cùng thao tác** duyệt/từ chối: xử lý xong mà không có thông báo là một trạng thái không được phép tồn tại."
    - "Thông báo chỉ người nhận đọc được. RLS điều kiện trên `user_id` của chính người đó, KHÔNG chỉ trên `company_id` — đồng nghiệp cùng doanh nghiệp không đọc được thông báo của nhau."
    - "Chuông trên giao diện nhân viên hiện số chưa đọc; mở danh sách thì đánh dấu đã đọc, và số về 0."
    - "Nội dung thông báo nói đủ để hiểu mà không phải bấm vào: loại yêu cầu, khoảng ngày, quyết định, và lý do nếu bị từ chối."
    - "Nhân viên chưa có tài khoản đăng nhập (`employees.user_id` null) thì KHÔNG sinh thông báo — không có ai để nhận, và một dòng mồ côi sẽ không bao giờ được đọc."
    - "Đánh dấu đã đọc chỉ tác động lên thông báo của chính mình; gọi với id của người khác không đổi được dòng nào."
  artifacts:
    - path: "supabase/migrations/0020_notifications.sql"
      provides: "Bảng thông báo kèm RLS theo người nhận"
      exports: ["notifications"]
    - path: "src/components/employee-app/notification-bell.tsx"
      provides: "Chuông kèm số chưa đọc trên giao diện nhân viên"
  key_links:
    - from: "src/lib/data/mutations/requests.ts"
      to: "supabase/migrations/0020_notifications.sql"
      via: "xử lý yêu cầu sinh thông báo trong cùng thao tác"
      pattern: "notifications"
  prohibitions:
    - statement: "MUST NOT dựng RLS của bảng thông báo chỉ trên `company_id`. Thông báo mang lý do từ chối — một câu nhận xét riêng về một người. Cho cả doanh nghiệp đọc được là biến một cơ chế thông báo thành một bảng tin nội bộ về đời tư của nhau."
      category: security
    - statement: "MUST NOT để việc sinh thông báo là một bước 'best effort' bỏ qua được khi lỗi. Nếu không thông báo được thì người bị ảnh hưởng không biết công của mình vừa đổi — thao tác phải thất bại và người duyệt phải biết."
      category: correctness
    - statement: "MUST NOT gửi thông báo qua email/SMS/push ở phase này. APRV-05 nói 'trong ứng dụng'; mở kênh ngoài kéo theo nhà cung cấp mới, thứ mà Constraints của dự án cấm."
      category: architecture
---

<objective>
Nhân viên biết yêu cầu của mình vừa được xử lý mà không phải đi tìm.

Purpose: bốn plan trước làm cho quyết định của người duyệt trở nên đúng và có vết. Plan này làm
cho nó **đến được người bị ảnh hưởng**. Đây là vế mà một hệ thống chấm công dễ bỏ quên nhất:
số liệu đúng nhưng người lao động là người cuối cùng biết công của mình vừa đổi.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-CONTEXT.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-01-SUMMARY.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bảng thông báo, cô lập theo NGƯỜI NHẬN</name>
  <files>supabase/migrations/0020_notifications.sql, supabase/tests/14_notifications.sql, supabase/tests/run-all.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `supabase/migrations/0002_tenancy.sql` — `tf_is_member()`; bảng này cần một điều kiện
      **chặt hơn** thế: chính chủ sở hữu dòng.
    - `supabase/migrations/0015_company_settings.sql` (04-01) — khuôn bảng mới gần nhất.
    - `supabase/tests/04_isolation_v2.sql` — khuôn test cô lập; bảng này cần thêm một khẳng định
      mà các bảng khác không có: **hai người CÙNG doanh nghiệp** không đọc được của nhau.
  </read_first>
  <behavior>
    - `notifications`: `id`, `company_id`, `user_id` (FK `auth.users`, cascade), `kind`,
      `title`, `body`, `request_id` (null được), `read_at` (null = chưa đọc), `created_at`.
    - Policy `select`/`update` điều kiện `user_id = auth.uid()` **và** `tf_is_member(company_id)`;
      `insert` cho thành viên doanh nghiệp (đường ghi đi qua Server Action đã kiểm quyền).
    - Người dùng A không đọc được dòng của người dùng B **dù cùng doanh nghiệp**.
    - Không policy `delete` nào — thông báo không xoá được từ ứng dụng ở phase này; đánh dấu đã
      đọc là đủ, và một danh sách xoá được sẽ cần một câu chuyện về "ai được xoá của ai".
    - Index `(user_id, read_at)` phục vụ đúng truy vấn đếm chưa đọc.
  </behavior>
  <action>
    Migration + test pgTAP (ít nhất 5 khẳng định, trong đó **hai người cùng doanh nghiệp không
    đọc được của nhau** là khẳng định quan trọng nhất), khai vào `run-all.sql`, nâng sàn.

    Khối comment ghi rõ vì sao RLS ở đây chặt hơn mọi bảng khác của dự án: thông báo mang lý do
    từ chối — một câu nhận xét riêng về một người.
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `npm run check:assertions` thoát 0 với sàn mới.
    - `psql ... -tAc "select count(*) from pg_policies where tablename='notifications'"` trả `3` (select/insert/update, không có delete).
    - Test tích hợp: hai người dùng thật cùng `cty-01`, mỗi người một thông báo — mỗi người đọc được đúng 1 dòng.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Thông báo có chỗ sống, và ranh giới của nó là con người chứ không phải doanh nghiệp.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sinh thông báo trong cùng thao tác xử lý</name>
  <files>src/lib/types/domain.ts, src/lib/validation/api/notifications.ts, src/lib/data/notifications.ts, src/lib/data/mutations/notifications.ts, src/app/api/notifications/route.ts, src/lib/data/mutations/requests.ts, src/lib/data/__tests__/notifications.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/requests.ts` — `reviewRequest()` sau 05-02: cập nhật trạng thái →
      ghi lịch sử → gọi RPC áp dụng → (thêm ở đây) sinh thông báo → audit.
    - `src/lib/data/mutations/holidays.ts` (04-03) — khuôn Server Action.
    - `src/app/api/settings/route.ts` (04-01) — khuôn Route Handler đọc một thứ của chính phiên.
    - `src/lib/constants.ts` — nhãn loại yêu cầu, dùng lại để dựng nội dung thông báo.
  </read_first>
  <behavior>
    - Xử lý một yêu cầu sinh **đúng một** dòng thông báo cho chủ yêu cầu; nội dung mang loại yêu
      cầu, khoảng ngày, quyết định, và lý do nếu bị từ chối.
    - Nhân viên chưa có `user_id` → **không** sinh dòng nào, và thao tác duyệt vẫn thành công
      (không có ai để nhận thì không phải một lỗi).
    - Không sinh được thông báo vì lỗi database (trong khi nhân viên CÓ tài khoản) → cả thao tác
      thất bại; người duyệt biết và làm lại.
    - `GET /api/notifications` trả thông báo của **chính phiên**, mới nhất trước, kèm số chưa đọc.
    - `markNotificationsRead(ids)` chỉ đánh dấu dòng của chính mình; truyền id của người khác thì
      số dòng bị tác động là 0 và hàm không ném lỗi giả vờ thành công — nó trả về đúng số đã đổi.
    - Vai trò nào cũng đọc được thông báo của chính mình (kể cả `owner`) — chủ doanh nghiệp cũng
      có thể là người gửi yêu cầu.
  </behavior>
  <action>
    Viết đường đọc + hai Server Action; nối vào `reviewRequest()`. Comment tại chỗ trong
    `reviewRequest` ghi rõ thứ tự và **vì sao thông báo không phải bước "best effort"**.

    Test tích hợp trên database thật: 6 hành vi ở `<behavior>`, trong đó "nhân viên không có tài
    khoản" và "đánh dấu đã đọc của người khác" là hai bài quan trọng nhất.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/notifications.test.ts` thoát 0, ít nhất 6 test.
    - `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0.
    - `grep -c "notifications" src/lib/data/mutations/requests.ts` trả ít nhất `1`.
    - Test khẳng định: duyệt một yêu cầu của nhân viên có tài khoản → đúng 1 dòng thông báo; của nhân viên không có tài khoản → 0 dòng và thao tác vẫn thành công.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/notifications.test.ts</automated>
  </verify>
  <done>Không có đường nào để một yêu cầu được xử lý mà người gửi không được báo.</done>
</task>

<task type="auto">
  <name>Task 3: Chuông và danh sách thông báo</name>
  <files>src/components/employee-app/notification-bell.tsx, src/components/layout/mobile-header.tsx, src/app/employee/notifications/page.tsx, src/app/employee/notifications/notifications-view.tsx, src/lib/nav.ts, src/lib/constants.ts</files>
  <read_first>
    - `src/components/layout/mobile-header.tsx` — nơi chuông sẽ đứng; đọc kỹ bố cục hiện có
      trước khi chèn.
    - `src/app/employee/requests/requests-view.tsx` — khuôn màn hình nhân viên (mobile-first).
    - `src/components/common/empty-state.tsx`, `error-state.tsx`.
    - `src/lib/nav.ts` — `EMPLOYEE_NAV_ITEMS` bốn mục; **cân nhắc kỹ** trước khi thêm mục thứ
      năm vào thanh dưới — chuông ở header có thể là chỗ đúng hơn cho giao diện mobile.
  </read_first>
  <action>
    **(a) `NotificationBell`** — số chưa đọc; 0 thì không hiện chấm. Bấm mở
    `/employee/notifications`.

    **(b) `/employee/notifications`** — danh sách mới nhất trước; dòng chưa đọc phân biệt bằng
    **dấu chấm + nhãn chữ**, không bằng màu đơn thuần. Mở màn hình đánh dấu đã đọc các dòng đang
    hiển thị. Trạng thái rỗng nói đúng sự thật: chưa có thông báo nào.

    **(c)** dòng thông báo bấm được để mở thẳng yêu cầu tương ứng ở `/employee/requests`.

    **(d) `constants.ts`** — khối `NOTIFICATION_LABEL` gồm mẫu câu cho từng loại quyết định.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -c "NotificationBell" src/components/layout/mobile-header.tsx` trả ít nhất `1`.
    - `grep -rn "new Date()\|Date.now()" src/components/employee-app/notification-bell.tsx src/app/employee/notifications/` không trả dòng nào (rule D-19a).
    - Với phiên nhân viên thật: duyệt một yêu cầu của họ ở màn quản trị → chuông hiện 1 → mở danh sách thấy đúng nội dung → số về 0 sau khi mở. Ghi cả bốn quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Nhân viên mở ứng dụng là thấy ngay yêu cầu của mình đã được xử lý.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Thông báo → người nhận | Nội dung mang lý do từ chối, là nhận xét riêng về một người |
| `notificationId` do client gửi → dòng dữ liệu | Đánh dấu đã đọc của người khác phải là bất khả |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | Đồng nghiệp cùng doanh nghiệp đọc được lý do từ chối của nhau | high | mitigate | RLS điều kiện `user_id = auth.uid()`, chặt hơn `tf_is_member`; test pgTAP có khẳng định riêng cho hai người cùng doanh nghiệp |
| T-05-04-02 | Tampering | Đánh dấu đã đọc thông báo của người khác để che một quyết định | medium | mitigate | Lệnh ghi kèm điều kiện `user_id` từ phiên; test bằng id chéo thật |
| T-05-04-03 | Repudiation | Yêu cầu được xử lý nhưng thông báo lặng lẽ thất bại — người bị ảnh hưởng không bao giờ biết | high | mitigate | Sinh thông báo không phải bước "best effort": lỗi làm cả thao tác thất bại |
| T-05-04-04 | Information Disclosure | Thông báo mồ côi cho nhân viên chưa có tài khoản nằm lại trong bảng | low | mitigate | Không sinh dòng khi `user_id` null |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run db:push && npm run check:assertions` thoát 0
- Hai người cùng doanh nghiệp không đọc được thông báo của nhau
- Xử lý yêu cầu sinh đúng một thông báo; nhân viên không có tài khoản thì không sinh và không lỗi
- Đánh dấu đã đọc không chạm được dòng của người khác
</verification>

<success_criteria>
Người lao động không còn là người cuối cùng biết công của mình vừa đổi.
</success_criteria>

<output>
Create `.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-04-SUMMARY.md` when done
</output>
