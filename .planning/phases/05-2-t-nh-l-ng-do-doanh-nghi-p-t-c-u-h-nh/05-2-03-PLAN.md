---
phase: 05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh
plan: 03
type: execute
wave: 2
depends_on: ["05-2-01"]
files_modified:
  - supabase/migrations/0023_pay_adjustments.sql
  - supabase/tests/17_pay_adjustments.sql
  - supabase/tests/run-all.sql
  - scripts/check-pgtap-assertions.mjs
  - src/lib/types/domain.ts
  - src/lib/validation/api/pay-adjustments.ts
  - src/lib/payroll/scope.ts
  - src/lib/payroll/__tests__/scope.test.ts
  - src/lib/data/pay-adjustments.ts
  - src/lib/data/mutations/pay-adjustments.ts
  - src/app/api/pay-adjustments/route.ts
  - src/components/settings/pay-adjustments-tab.tsx
  - src/components/settings/pay-adjustment-dialog.tsx
  - src/app/admin/settings/settings-view.tsx
  - src/lib/constants.ts
  - src/lib/data/__tests__/pay-adjustments.test.ts
autonomous: true
requirements: [PAY-04]

estimate:
  tokens: 94000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "Doanh nghiệp khai được **khoản** phụ cấp (cộng) và khấu trừ (trừ), giá trị là **số tiền cố định** hoặc **phần trăm lương ngày** (D-40)."
    - "Mỗi khoản có **phạm vi áp dụng**: toàn công ty / phòng ban / chức vụ / nhân viên cụ thể — và một **danh sách loại trừ** riêng ('toàn công ty trừ mấy người')."
    - "Phạm vi và loại trừ là **hai chiều khác nhau**: 'toàn công ty trừ 3 người' khai bằng một dòng include `company` cộng ba dòng exclude, KHÔNG bằng cách liệt kê 37 người."
    - "Thêm một nhân viên mới vào doanh nghiệp thì họ **tự động** nhận khoản có phạm vi toàn công ty — đây chính là điều mà cách khai 'liệt kê 37 người' sẽ làm sai lặng lẽ."
    - "Phạt đi muộn là một khoản khấu trừ có `basis = per_late`: giá trị được nhân với **số lần đi muộn** mà hệ thống đã đếm (D-41)."
    - "Mọi khoản áp cho **mọi kỳ lương** — không có khoản 'chỉ kỳ này' (D-40a). Bảng không có cột tháng."
    - "Màn hình khai hiện **danh sách người thực sự bị áp** trước khi lưu; người khai không phải tự suy ra từ bốn ô cấu hình."
    - "Tắt một khoản (`is_active = false`) không xoá nó — bảng lương kỳ đã chốt vẫn giữ khoản đó trong bản chốt (05-2-05)."
  artifacts:
    - path: "supabase/migrations/0023_pay_adjustments.sql"
      provides: "Danh mục khoản + bảng phạm vi"
      exports: ["pay_adjustments", "pay_adjustment_scopes"]
    - path: "src/lib/payroll/scope.ts"
      provides: "Mô-đun thuần giải phạm vi"
      exports: ["resolveTargets", "isTargeted"]
    - path: "src/components/settings/pay-adjustments-tab.tsx"
      provides: "Tab khai phụ cấp/khấu trừ"
      min_lines: 120
  key_links:
    - from: "src/components/settings/pay-adjustment-dialog.tsx"
      to: "src/lib/payroll/scope.ts"
      via: "hiện trước danh sách người bị áp"
      pattern: "resolveTargets"
  prohibitions:
    - statement: "MUST NOT gộp phạm vi và loại trừ thành một danh sách. 'Toàn công ty trừ 3 người' mà khai bằng cách liệt kê 37 người thì lần tuyển người thứ 41 sẽ sai — và sai lặng lẽ, vì không có gì báo động khi một người KHÔNG nhận được phụ cấp đáng ra phải có."
      category: correctness
    - statement: "MUST NOT cho phép XOÁ một khoản đã từng vào một bản chốt lương. Tắt (`is_active`) thì được; xoá làm bản chốt của kỳ đã trả mất mất phần giải thích 'vì sao ra con số đó'."
      category: correctness
    - statement: "MUST NOT tính một con số tiền nào ở plan này. `percent_of_daily_wage` chỉ là một CÁCH KHAI giá trị; quy nó ra tiền là việc của 05-2-04."
      category: architecture
---

<objective>
Danh mục phụ cấp và khấu trừ do doanh nghiệp tự khai, với phạm vi áp dụng đủ mềm để diễn đạt
được "toàn công ty trừ mấy người".

Purpose: đây là phần mà chủ dự án mô tả kỹ nhất, và cũng là phần dễ sai lặng lẽ nhất — một
phạm vi tính sai người thì có người mất phụ cấp đáng có mà **không ai báo động**, vì hệ thống
không biết đáng ra họ phải có.

Output: migration 0023, `scope.ts`, tab "Phụ cấp & khấu trừ" ở `/admin/settings`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-CONTEXT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-01-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hai bảng — khoản và phạm vi</name>
  <files>supabase/migrations/0023_pay_adjustments.sql, supabase/tests/17_pay_adjustments.sql, supabase/tests/run-all.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `supabase/migrations/0015_company_settings.sql` (04-01) — khuôn bảng mới: bốn policy
      `<bảng>_<lệnh>_member`, index, khối comment giải thích **vì sao**.
    - `supabase/migrations/0004_core_entities.sql` khối `employees` — `position` là **text tự
      do**, không phải bảng tra cứu. Phạm vi "theo chức vụ" vì thế so khớp theo chuỗi, và điều
      đó phải được ghi rõ trong comment kèm hệ quả (gõ sai chính tả là không khớp ai).
    - `scripts/check-pgtap-assertions.mjs` — sàn sau 05-2-01, chỉ được nâng.
  </read_first>
  <behavior>
    - `pay_adjustments`: `id`, `company_id`, `name`, `kind` (`allowance`/`deduction`),
      `value_type` (`fixed_amount`/`percent_of_daily_wage`), `value` (numeric > 0),
      `basis` (`per_period`/`per_late`, mặc định `per_period`), `is_active` (mặc định true),
      `created_at`.
    - `pay_adjustment_scopes`: `id`, `company_id`, `adjustment_id` (cascade),
      `mode` (`include`/`exclude`), `scope_type` (`company`/`department`/`position`/`employee`),
      `scope_value` (null **khi và chỉ khi** `scope_type = 'company'` — ép bằng `CHECK`).
    - `basis = 'per_late'` chỉ hợp lệ với `kind = 'deduction'` — ép bằng `CHECK`; phạt đi muộn
      không thể là một khoản cộng.
    - `percent_of_daily_wage` với `value > 100` bị chặn ở `CHECK` — một khoản trừ 300% lương
      ngày gần như chắc chắn là gõ nhầm.
    - Bốn policy RLS mỗi bảng, điều kiện duy nhất `tf_is_member(company_id)`.
  </behavior>
  <action>
    Viết migration 0023 + test pgTAP (ít nhất 8 khẳng định: `scope_value` null với
    `scope_type='company'` hợp lệ, không null với `company` bị chặn, null với `department` bị
    chặn, `per_late` + `allowance` bị chặn, phần trăm > 100 bị chặn, cascade xoá scope khi xoá
    khoản, cô lập đọc chéo, cô lập ghi chéo). Khai vào `run-all.sql`, nâng sàn.

    Khối comment ghi rõ vì sao **hai bảng** chứ không phải một cột mảng: phạm vi và loại trừ là
    hai chiều, và một mảng `text[]` không diễn đạt được "gồm cả phòng ban X, trừ hai người".

    Migration làm chạy-lại-được (khuôn 0018/0021).
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `npm run check:assertions` thoát 0 với sàn mới.
    - `psql ... -tAc "select count(*) from pg_policies where tablename in ('pay_adjustments','pay_adjustment_scopes')"` trả `8` (kiểm qua test tích hợp nếu không có `psql`).
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Khoản và phạm vi có chỗ để sống, và những cách khai vô nghĩa bị database từ chối.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mô-đun thuần giải phạm vi</name>
  <files>src/lib/payroll/scope.ts, src/lib/payroll/__tests__/scope.test.ts, src/lib/types/domain.ts</files>
  <read_first>
    - `src/lib/attendance/overtime-cap.ts` (05-03) — khuôn mô-đun thuần gần nhất: một hàm quyết
      định, không import Supabase, comment nói rõ điều gì **không** có ở đây và vì sao.
  </read_first>
  <behavior>
    - `isTargeted({ employee, scopes })` trả `true` khi: **có ít nhất một** dòng `include` khớp
      **và không** dòng `exclude` nào khớp.
    - Không dòng `include` nào → `false`. Một khoản không khai phạm vi thì không áp cho ai —
      **không** mặc định thành toàn công ty, vì im lặng không phải là "tất cả".
    - `exclude` **luôn thắng** `include`, kể cả khi `include` cụ thể hơn (`employee`) còn
      `exclude` rộng hơn (`department`). Quy tắc đơn giản và đoán được quan trọng hơn quy tắc
      thông minh.
    - So khớp `position` là so khớp **chuỗi chính xác sau khi cắt khoảng trắng**, không
      chuẩn hoá dấu — và hệ quả (gõ sai là không khớp ai) được ghi trong comment.
    - `resolveTargets({ employees, scopes })` trả danh sách nhân viên bị áp, dùng cho màn hình
      xem trước.
  </behavior>
  <action>
    Viết mô-đun + test, **ít nhất 10 khẳng định**, bắt buộc có:
    - "toàn công ty trừ 2 người" → 38/40 người;
    - thêm một nhân viên mới vào danh sách → **39/41**, không cần sửa cấu hình (đây là bài chứng
      minh vì sao không liệt kê tay);
    - `exclude` cụ thể thắng `include` rộng, **và** `exclude` rộng thắng `include` cụ thể;
    - không `include` nào → không ai bị áp.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/payroll/__tests__/scope.test.ts` thoát 0, ít nhất 10 test.
    - `grep -cE "supabase|process\.env|new Date" src/lib/payroll/scope.ts` trả `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/payroll/__tests__/scope.test.ts</automated>
  </verify>
  <done>Phép giải phạm vi có một chỗ duy nhất, kiểm được từng nhánh, và đoán được.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Đường đọc và đường ghi khoản</name>
  <files>src/lib/validation/api/pay-adjustments.ts, src/lib/data/pay-adjustments.ts, src/lib/data/mutations/pay-adjustments.ts, src/app/api/pay-adjustments/route.ts, src/lib/data/__tests__/pay-adjustments.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/holidays.ts` (04-03) — khuôn ba đường ghi kèm `logMutation`.
    - `src/lib/data/mutations/work-sites.ts` (03-02) — khuôn "tắt thay vì xoá" (`archiveWorkSite`).
  </read_first>
  <behavior>
    - `GET /api/pay-adjustments` trả mọi khoản của doanh nghiệp **kèm phạm vi**, khoản đang bật
      trước; chỉ `owner`/`admin`.
    - `createPayAdjustment` / `updatePayAdjustment` ghi khoản **và** phạm vi trong cùng thao
      tác; xoá hết phạm vi cũ rồi ghi lại danh sách mới (phạm vi là một tập, không phải một
      chuỗi lịch sử).
    - `deactivatePayAdjustment` đặt `is_active = false` — **không có** hàm xoá cứng.
    - Mỗi thao tác để lại một dòng `audit_log` mang nguyên khoản trước và sau.
    - `id` của doanh nghiệp khác → "Không tìm thấy khoản", dòng đó **không đổi**.
  </behavior>
  <action>
    Viết cặp schema hai đầu, đường đọc, ba Server Action; test tích hợp trên database dev thật
    phủ đủ năm hành vi, cộng một bài khẳng định vai trò `employee` nhận 403.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/pay-adjustments.test.ts` thoát 0, ít nhất 6 test.
    - `grep -ciE "\.delete\(\)" src/lib/data/mutations/pay-adjustments.ts` — mọi kết quả phải nằm ở đường xoá **phạm vi** khi ghi lại, không ở đường xoá khoản; kiểm bằng mắt, ghi kết luận vào SUMMARY.
    - `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/pay-adjustments.test.ts</automated>
  </verify>
  <done>Khoản khai được qua đúng một đường, tắt được nhưng không mất, và mỗi lần khai để lại vết.</done>
</task>

<task type="auto">
  <name>Task 4: Tab "Phụ cấp & khấu trừ" với xem trước người bị áp</name>
  <files>src/components/settings/pay-adjustments-tab.tsx, src/components/settings/pay-adjustment-dialog.tsx, src/app/admin/settings/settings-view.tsx, src/lib/constants.ts</files>
  <read_first>
    - `src/app/admin/settings/settings-view.tsx` (04-01) — khung bốn tab cố định; tab thứ năm
      thêm vào đây.
    - `src/components/settings/holidays-tab.tsx` (04-03) — khuôn tab có bảng + dialog + xác nhận.
    - `src/components/requests/review-dialog.tsx` (05-02/05-03) — tiền lệ "hiện trước hệ quả
      bằng con số thật trước khi bấm".
  </read_first>
  <action>
    **(a) Tab** — bảng các khoản: tên, loại (phụ cấp/khấu trừ, phân biệt bằng **biểu tượng +
    nhãn chữ**), giá trị (số tiền hoặc "% lương ngày"), cách áp (`mỗi kỳ` / `mỗi lần đi muộn`),
    phạm vi tóm tắt ("Toàn công ty, trừ 2 người"), số người bị áp, trạng thái bật/tắt.

    **(b) Dialog khai khoản** — bốn phần: thông tin khoản, giá trị, **phạm vi** (chọn kiểu rồi
    chọn giá trị), **loại trừ** (chọn nhân viên). Dưới cùng là khối **xem trước**: danh sách
    người thực sự bị áp, đếm bằng `resolveTargets()`, cập nhật ngay khi đổi phạm vi.

    Khối xem trước là phần quan trọng nhất của màn hình: người khai không có cách nào tự suy ra
    "ai bị áp" từ bốn ô cấu hình, và nếu họ đoán sai thì không có gì báo động.

    **(c) `constants.ts`** — khối `PAY_ADJUSTMENT_LABEL`, gồm câu giải thích cho
    `percent_of_daily_wage` (tính trên lương ngày, không phải lương tháng).
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -c "resolveTargets" src/components/settings/pay-adjustment-dialog.tsx` trả ít nhất `1`.
    - Với phiên owner: khai một khoản phụ cấp phạm vi toàn công ty rồi loại trừ 2 người → khối xem trước hiện đúng `tổng − 2` người, và bảng hiện "Toàn công ty, trừ 2 người". Ghi cả hai quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Người khai thấy được ai sẽ nhận khoản đó trước khi bấm lưu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phạm vi khoản → ai nhận tiền | Sai phạm vi là sai tiền của một nhóm người, và không có gì báo động |
| Khoản đã vào bản chốt → bằng chứng sau này | Xoá khoản làm bản chốt mất phần giải thích |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-2-03-01 | Tampering | Phạm vi khai bằng cách liệt kê tay → người mới không nhận được phụ cấp đáng có, sai lặng lẽ | high | mitigate | Hai chiều include/exclude; test khẳng định thêm người mới thì họ tự động vào phạm vi công ty |
| T-05-2-03-02 | Tampering | Không có `include` nào bị hiểu là "toàn công ty" → mọi người nhận một khoản chưa khai xong | high | mitigate | `isTargeted` trả `false` khi không có include; test riêng |
| T-05-2-03-03 | Repudiation | Xoá một khoản đã dùng → bản chốt lương mất phần giải thích | medium | mitigate | Chỉ có `deactivate`, không có xoá cứng |
| T-05-2-03-04 | Elevation of Privilege | Vai trò không phải quản trị đọc được danh mục khoản (suy ra được lương của nhau) | medium | mitigate | `requireRole` ở đường đọc; test bằng phiên `employee` thật |
| T-05-2-03-05 | Tampering | Gõ nhầm 300% lương ngày | low | mitigate | `CHECK value <= 100` cho `percent_of_daily_wage` |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run db:push && npm run check:assertions` thoát 0
- "Toàn công ty trừ 2 người" khai được bằng một include + hai exclude
- Thêm nhân viên mới thì họ tự vào phạm vi toàn công ty
- Không khai phạm vi thì không ai bị áp
- Khoản tắt được nhưng không xoá được
</verification>

<success_criteria>
Doanh nghiệp diễn đạt được đúng chính sách của mình — kể cả những chính sách có ngoại lệ — và
thấy trước ai bị áp trước khi lưu.
</success_criteria>

<output>
Create `.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-03-SUMMARY.md` when done
</output>
