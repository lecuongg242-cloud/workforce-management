---
phase: 05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh
plan: 05
type: execute
wave: 4
depends_on: ["05-2-04"]
files_modified:
  - supabase/migrations/0024_payroll_runs.sql
  - supabase/tests/18_payroll_runs.sql
  - supabase/tests/run-all.sql
  - scripts/check-pgtap-assertions.mjs
  - src/lib/types/domain.ts
  - src/lib/validation/api/payroll.ts
  - src/lib/data/payroll.ts
  - src/lib/data/mutations/payroll.ts
  - src/app/api/payroll/summary/route.ts
  - src/app/admin/payroll/payroll-view.tsx
  - src/lib/constants.ts
  - src/lib/data/__tests__/payroll-run.test.ts
autonomous: true
requirements: [PAY-01]

estimate:
  tokens: 90000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "Chốt lương một kỳ lưu lại **bản chốt tự chứa**: chế độ tính công, hai mẫu số, mức lương đã áp cho từng người, từng khoản cộng/trừ, và con số cuối (D-42)."
    - "Kỳ **đã chốt lương** thì bảng lương đọc từ **bản chốt**, không tính lại từ cấu hình hiện tại — đổi mức lương hôm nay không làm đổi con số của kỳ đã chốt."
    - "Kỳ **chưa chốt lương** thì bảng lương vẫn tính lúc truy vấn như 05-2-04."
    - "Bản chốt **không sửa được**: `UPDATE` trên bảng dòng lương bị trigger từ chối (khuôn D-25a)."
    - "**Huỷ chốt lương** được phép và xoá cả bản chốt, không sửa từng dòng; để lại một dòng `audit_log` (D-45) — khác kỳ công, vốn không có đường mở lại (D-32b)."
    - "Không chốt lương được một kỳ mà **kỳ công chưa chốt** — số liệu công còn đổi được thì con số tiền chưa đáng để đóng khung."
    - "Không chốt lương được khi còn dòng thiếu dữ kiện; thông điệp nói rõ **thiếu gì, của ai**."
    - "Mở lại một kỳ đã chốt sáu tháng sau vẫn ra **đúng con số đã trả**, kể cả khi mức lương và danh mục khoản đã đổi hết."
  artifacts:
    - path: "supabase/migrations/0024_payroll_runs.sql"
      provides: "Bản chốt lương tự chứa"
      exports: ["payroll_runs", "payroll_lines", "payroll_line_items"]
    - path: "src/lib/data/mutations/payroll.ts"
      provides: "Server Action chốt và huỷ chốt lương"
      exports: ["closePayroll", "reopenPayroll"]
  key_links:
    - from: "src/lib/data/mutations/payroll.ts"
      to: "src/lib/payroll/compute.ts"
      via: "chốt lương ghi lại đúng kết quả mà màn hình đang hiện"
      pattern: "computePayrollLine"
  prohibitions:
    - statement: "MUST NOT tính lại bản chốt từ cấu hình hiện tại khi đọc. Đó là toàn bộ lý do bản chốt tồn tại: một khi đã trả tiền, con số là một sự kiện đã xảy ra, không phải một phép tính có thể ra kết quả khác vào tháng sau (D-42)."
      category: correctness
    - statement: "MUST NOT cho phép sửa từng dòng của bản chốt. Sai thì huỷ cả bản chốt rồi chốt lại — có audit, có dấu vết. Sửa lẻ một dòng là mở đường cho một con số không ai giải thích được."
      category: correctness
    - statement: "MUST NOT chốt lương một kỳ mà kỳ công chưa chốt. Số liệu công còn đổi được (một yêu cầu chờ duyệt, một lần chấm công bù) thì con số tiền chưa đáng để đóng khung."
      category: correctness
---

<objective>
Chốt lương một kỳ, và làm cho việc chốt có nghĩa: con số đã chốt không đổi theo cấu hình của
tương lai.

Purpose: đây là lời hứa cuối của phase. Mọi thứ khác trong TimeFlow tính lúc truy vấn — và đó
là lựa chọn đúng cho số liệu công, vì nó cho phép sửa quy tắc mà không phải ghi đè lịch sử.
Tiền lương thì ngược lại: một khi đã trả cho người lao động, con số đó là **một sự kiện đã xảy
ra**. Plan này là chỗ hai nguyên tắc đó gặp nhau, và bản chốt là ranh giới.

Output: migration 0024, `closePayroll()`, `reopenPayroll()`, bảng lương đọc từ bản chốt.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-CONTEXT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-04-SUMMARY.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ba bảng của bản chốt</name>
  <files>supabase/migrations/0024_payroll_runs.sql, supabase/tests/18_payroll_runs.sql, supabase/tests/run-all.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `supabase/migrations/0021_period_close.sql` (05-05) — khuôn chốt kỳ, gồm cách ghi
      `closed_at`/`closed_by` từ đồng hồ database và thủ tục gỡ trigger có ý thức.
    - `supabase/migrations/0017_request_reviews.sql` (05-01) — khuôn trigger chặn `UPDATE` mà
      **vẫn cho** `DELETE` không phải là khuôn ở đây; đọc để thấy khác biệt và ghi rõ vì sao.
    - `supabase/migrations/0005_v2_tables.sql` khối `periods` — `unique (company_id, start_date)`.
  </read_first>
  <behavior>
    - `payroll_runs`: `id`, `company_id`, `period_start` (khớp `periods.start_date`),
      `work_mode`, `standard_hours_per_day`, `standard_days_per_month`, `closed_at`,
      `closed_by`, `unique (company_id, period_start)`.
      **Dòng tồn tại nghĩa là đã chốt lương** — không cột trạng thái, không bản nháp.
    - `payroll_lines`: một dòng mỗi nhân viên, mang **ảnh chụp** danh tính (mã, tên, phòng ban),
      mức lương đã áp (`pay_unit`, `pay_amount`), số liệu công, và các con số tiền.
    - `payroll_line_items`: từng khoản cộng/trừ của một dòng — `name` và `amount` **chép lại**,
      không tham chiếu sống tới `pay_adjustments` (khoản có thể bị tắt hoặc đổi tên sau).
    - Trigger chặn `UPDATE` trên cả ba bảng; **cho phép `DELETE`** (D-45) — khác 0017, và khối
      comment phải nói rõ vì sao hai bảng giống nhau về hình dạng lại khác nhau ở điểm này.
    - Bốn policy RLS mỗi bảng, điều kiện duy nhất `tf_is_member(company_id)`.
  </behavior>
  <action>
    Viết migration 0024 + test pgTAP (ít nhất 8 khẳng định: `UPDATE` bị chặn trên cả ba bảng,
    `DELETE` **được phép**, cascade xoá dòng và khoản khi xoá bản chốt, `unique` chặn chốt hai
    lần, cô lập đọc chéo, cô lập ghi chéo). Khai vào `run-all.sql`, nâng sàn.

    Khối comment ghi rõ **vì sao bản chốt tự chứa** (D-42) và **vì sao huỷ được** (D-45) — hai
    thứ trông mâu thuẫn nếu không giải thích: bản chốt bất biến để không ai sửa lén, nhưng huỷ
    được cả bản để sửa một sai lầm phát hiện trước khi trả tiền.

    Migration làm chạy-lại-được (khuôn 0018/0021).
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `npm run check:assertions` thoát 0 với sàn mới.
    - `grep -c "payroll_line_items" supabase/migrations/0024_payroll_runs.sql` trả ít nhất `1`.
    - Test khẳng định `UPDATE` bị chặn **và** `DELETE` chạy được — hai chiều, không chỉ một.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Bản chốt có chỗ để sống, sửa lén không được, nhưng huỷ cả bản thì được.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chốt lương và huỷ chốt lương</name>
  <files>src/lib/data/mutations/payroll.ts, src/lib/validation/api/payroll.ts, src/lib/types/domain.ts, src/lib/data/__tests__/payroll-run.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/periods.ts` (05-05) — khuôn Server Action chốt kỳ: `requireRole`,
      đọc dòng trước cho audit, gọi phần quyết định, `logMutation`.
    - `src/lib/payroll/payroll-context.ts` + `compute.ts` (05-2-04) — nguồn của các con số được
      chốt.
  </read_first>
  <behavior>
    - `closePayroll(month)` đòi `owner`/`admin` (D-44):
      1. **Kỳ công phải đã chốt** — chưa thì từ chối kèm lý do và chỉ đường sang `/admin/periods`;
      2. **Không dòng nào thiếu dữ kiện** — có thì từ chối, liệt kê **ai thiếu gì** (tối đa vài
         người rồi "và N người khác");
      3. Tính lại lần cuối bằng `computePayrollLine()` rồi ghi bản chốt;
      4. Một dòng `audit_log`.
    - Chốt lần hai → từ chối ("kỳ này đã chốt lương").
    - `reopenPayroll(month)` (D-45) xoá cả bản chốt, đòi `owner`/`admin`, ghi một dòng
      `audit_log` với `reason` bắt buộc — huỷ mà không nói vì sao thì không huỷ được.
    - Con số ghi vào bản chốt phải **bằng đúng** con số mà `GET /api/payroll/summary` đang hiện
      ngay trước khi chốt — không tính theo một đường khác.
  </behavior>
  <action>
    Viết hai Server Action + test tích hợp trên database dev thật, **ít nhất 8 test**, bắt buộc có:
    - chốt khi kỳ công chưa chốt → từ chối;
    - chốt khi còn dòng thiếu mức lương → từ chối, thông điệp **nêu tên người thiếu**;
    - chốt thành công → số dòng bản chốt bằng số nhân viên, tổng bằng tổng màn hình;
    - **sau khi chốt, đổi mức lương của một người rồi đọc lại → con số của kỳ KHÔNG đổi**;
    - huỷ chốt thiếu lý do → từ chối; có lý do → xoá được và audit ghi lý do;
    - huỷ rồi chốt lại → ra bản chốt mới.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/payroll-run.test.ts` thoát 0, ít nhất 8 test.
    - `grep -c "await logMutation(" src/lib/data/mutations/payroll.ts` trả `2` — một cho chốt, một cho huỷ.
    - Test khẳng định: đổi mức lương sau khi chốt → con số kỳ đã chốt **không đổi**. Đây là bài kiểm chính của D-42.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/payroll-run.test.ts</automated>
  </verify>
  <done>Chốt lương là một thao tác có điều kiện, có vết, và con số sau đó đứng yên.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Đường đọc phân nhánh theo trạng thái chốt</name>
  <files>src/app/api/payroll/summary/route.ts, src/lib/data/payroll.ts, src/lib/validation/api/payroll.ts</files>
  <read_first>
    - `src/app/api/payroll/summary/route.ts` (sau 05-2-04) — đường đọc hiện tính lúc truy vấn.
  </read_first>
  <behavior>
    - Kỳ **có** bản chốt → trả dữ liệu **từ bản chốt**, kèm `payrollStatus: "closed"`,
      `closedAt`, `closedBy`. Không chạm `computePayrollLine()`.
    - Kỳ **không có** bản chốt → tính lúc truy vấn như cũ, `payrollStatus: "open"`.
    - Hình dạng phản hồi **giống nhau ở cả hai nhánh** — màn hình không phải biết mình đang đọc
      cái nào để render.
    - Nhân viên đã nghỉ việc sau khi chốt vẫn **còn nguyên** trong bản chốt (ảnh chụp danh tính).
  </behavior>
  <action>
    Thêm nhánh đọc bản chốt. Chú ý giữ **một hình dạng phản hồi duy nhất**: nếu hai nhánh trả
    hai hình dạng khác nhau thì màn hình sẽ mọc hai đường render, và một trong hai sẽ mục dần.

    Test: một kỳ trước khi chốt và sau khi chốt trả **cùng bộ số**, chỉ khác `payrollStatus`.
  </action>
  <acceptance_criteria>
    - `npx vitest run` thoát 0 toàn bộ.
    - Test khẳng định hai nhánh cho cùng bộ số ở thời điểm ngay trước/ngay sau khi chốt.
    - `grep -c "computePayrollLine" src/app/api/payroll/summary/route.ts` trả đúng `1` — chỉ nhánh chưa chốt gọi nó.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Một hình dạng phản hồi, hai nguồn — và màn hình không cần biết khác biệt.</done>
</task>

<task type="auto">
  <name>Task 4: Chốt lương trên màn hình</name>
  <files>src/app/admin/payroll/payroll-view.tsx, src/lib/constants.ts</files>
  <read_first>
    - `src/app/admin/periods/periods-view.tsx` (05-05) — khuôn `ConfirmDialog` cho một thao tác
      nặng, và cách nói rõ hệ quả trước khi bấm.
  </read_first>
  <action>
    - Huy hiệu trạng thái: **đã chốt lương** (kèm ai chốt lúc nào) / **chưa chốt** — phân biệt
      bằng biểu tượng + nhãn chữ.
    - Nút **"Chốt lương kỳ"** là nút chính của màn hình. Hộp xác nhận nói đủ ba điều: con số sẽ
      được đóng khung; đổi mức lương sau đó **không** làm đổi kỳ này; và huỷ chốt được nhưng
      phải nêu lý do.
    - Nút bị vô hiệu khi kỳ công chưa chốt hoặc còn dòng thiếu dữ kiện — kèm **câu nói rõ vì
      sao**, không phải một nút xám im lặng.
    - Kỳ đã chốt lương: nút đổi thành **"Huỷ chốt lương"** (phụ, tông cảnh báo), hộp xác nhận
      **bắt buộc nhập lý do**.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -c "ConfirmDialog" src/app/admin/payroll/payroll-view.tsx` trả ít nhất `1`.
    - Với một doanh nghiệp thật: chốt kỳ công → chốt lương → đổi mức lương một người → mở lại bảng, con số **không đổi**; huỷ chốt kèm lý do → bảng tính lại theo mức mới. Ghi cả bốn quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Người chốt lương biết mình đang đóng khung cái gì, và biết đường lùi nếu sai.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bản chốt → con số đã trả | Bản chốt sửa được là mất luôn khả năng trả lời "tháng 7 đã trả bao nhiêu" |
| Cấu hình hiện tại → kỳ đã chốt | Một đường tính lại làm con số quá khứ đổi theo tương lai |
| Huỷ chốt → dấu vết | Huỷ mà không nêu lý do là xoá một sự kiện tài chính trong im lặng |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-2-05-01 | Tampering | Đọc kỳ đã chốt bằng cách tính lại → con số quá khứ đổi theo cấu hình hiện tại | high | mitigate | D-42; test đổi mức lương sau khi chốt rồi khẳng định con số không đổi |
| T-05-2-05-02 | Tampering | Sửa lẻ một dòng bản chốt | high | mitigate | Trigger chặn `UPDATE` trên cả ba bảng; test bằng khoá `service_role` |
| T-05-2-05-03 | Tampering | Chốt lương khi kỳ công còn mở → đóng khung một con số còn đổi được | high | mitigate | Điều kiện kiểm ở Server Action; test riêng |
| T-05-2-05-04 | Repudiation | Huỷ bản chốt trong im lặng | medium | mitigate | D-45: lý do bắt buộc + một dòng `audit_log` |
| T-05-2-05-05 | Tampering | Chốt lương khi còn dòng thiếu dữ kiện → đóng khung một bảng có lỗ | medium | mitigate | Từ chối kèm danh sách ai thiếu gì |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run db:push && npm run check:assertions` thoát 0
- Đổi mức lương sau khi chốt không làm đổi con số của kỳ đã chốt
- Bản chốt không sửa được, kể cả bằng khoá secret; huỷ cả bản thì được, kèm lý do
- Không chốt được khi kỳ công chưa chốt hoặc còn dòng thiếu dữ kiện
- Hai nhánh đọc trả cùng một hình dạng phản hồi
</verification>

<success_criteria>
Một kỳ lương đã chốt là một con số đứng yên — và sáu tháng sau vẫn trả lời được "đã trả bao
nhiêu, cho ai, và vì sao ra con số đó".
</success_criteria>

<output>
Create `.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-05-SUMMARY.md` when done
</output>
