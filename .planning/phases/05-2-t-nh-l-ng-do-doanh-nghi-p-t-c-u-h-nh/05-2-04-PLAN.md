---
phase: 05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh
plan: 04
type: execute
wave: 3
depends_on: ["05-2-02", "05-2-03"]
files_modified:
  - src/lib/payroll/rate.ts
  - src/lib/payroll/__tests__/rate.test.ts
  - src/lib/payroll/compute.ts
  - src/lib/payroll/__tests__/compute.test.ts
  - src/lib/payroll/payroll-context.ts
  - src/lib/types/domain.ts
  - src/lib/validation/api/payroll.ts
  - src/app/api/payroll/summary/route.ts
  - src/lib/payroll/csv.ts
  - src/lib/payroll/__tests__/csv.test.ts
  - src/app/admin/payroll/payroll-view.tsx
  - src/lib/constants.ts
  - src/lib/data/__tests__/payroll-compute.test.ts
autonomous: true
requirements: [PAY-01]

estimate:
  tokens: 98000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Bảng lương ra **số tiền** cho từng nhân viên: lương gốc + tiền tăng ca + phụ cấp − khấu trừ − phạt."
    - "Tiền tăng ca = **giờ quy đổi của Phase 4** × đơn giá giờ — không tính lại giờ, không mở nguồn thứ hai (D-31)."
    - "Quy đổi đơn vị đi qua **mẫu số doanh nghiệp khai** (D-38); thiếu mẫu số thì số tiền trả `null` kèm lý do, **không** đoán."
    - "Thiếu hệ số tăng ca (D-26) → tiền tăng ca `null` kèm lý do, và **tổng cũng `null`** — không cộng phần đã biết rồi trình bày như một con số đầy đủ."
    - "Mọi số tiền cuối cùng làm tròn **tới đồng, nửa lên**; các bước trung gian giữ nguyên độ chính xác (D-42a)."
    - "**Tổng bằng đúng tổng các dòng** — cộng rồi mới làm tròn, không làm tròn từng dòng rồi cộng."
    - "Nhân viên **chưa khai mức lương** hiện rõ là chưa khai, không hiện 0 đồng — 0 đồng đọc như 'người này không được trả gì'."
    - "Bảng và tệp CSV nói rõ con số **chưa gồm** thuế TNCN và bảo hiểm."
  artifacts:
    - path: "src/lib/payroll/rate.ts"
      provides: "Quy đổi đơn vị lương sang đơn giá ngày/giờ"
      exports: ["toDailyRate", "toHourlyRate"]
    - path: "src/lib/payroll/compute.ts"
      provides: "Phép tính tiền lương của một người trong một kỳ"
      exports: ["computePayrollLine"]
    - path: "src/lib/data/__tests__/payroll-compute.test.ts"
      provides: "Đối chiếu SỐ TIỀN trên database thật cho cả ba chế độ"
  key_links:
    - from: "src/lib/payroll/compute.ts"
      to: "src/lib/payroll/scope.ts"
      via: "khoản nào áp cho người nào"
      pattern: "isTargeted"
    - from: "src/lib/payroll/compute.ts"
      to: "src/lib/payroll/rate.ts"
      via: "đơn giá giờ để tính tiền tăng ca"
      pattern: "toHourlyRate"
  prohibitions:
    - statement: "MUST NOT tính lại giờ tăng ca ở đây. `convertedOvertimeHours` của Phase 4 là nguồn duy nhất; nhân nó với đơn giá là việc của plan này. Tính lại tạo nguồn thứ hai cho cùng một con số, và khi hai nguồn lệch thì không ai biết tin cái nào (D-31)."
      category: correctness
    - statement: "MUST NOT thay một giá trị thiếu bằng 0 ở bất kỳ đâu trong phép tính. Thiếu mức lương, thiếu mẫu số, thiếu hệ số — cả ba đều phải nổi lên thành `null` kèm lý do. Một con số 0 trong bảng lương đọc như một sự thật, và người ký duyệt sẽ ký."
      category: correctness
    - statement: "MUST NOT làm tròn ở các bước trung gian. Làm tròn từng dòng rồi cộng sẽ lệch vài đồng so với tổng — và kế toán sẽ phát hiện ra, rồi mất lòng tin vào mọi con số còn lại."
      category: correctness
---

<objective>
Phép tính ra tiền: từ số liệu công của một người trong một kỳ, cộng với mức lương và các khoản
doanh nghiệp đã khai, ra một con số.

Purpose: đây là plan mà cả phase tồn tại vì nó, và là plan mà sai thì **ra tiền sai**. Bốn plan
trước dựng đủ đầu vào: số liệu công theo chế độ (05-2-02), mức lương có phiên bản (05-2-01),
danh mục khoản có phạm vi (05-2-03). Plan này chỉ làm một việc — nhân và cộng cho đúng, và
**nói thật khi thiếu dữ kiện** thay vì bịa một con số.

Output: `rate.ts`, `compute.ts`, `GET /api/payroll/summary` có cột tiền, `/admin/payroll` hiện
tiền, CSV có tiền.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-CONTEXT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-02-SUMMARY.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-03-SUMMARY.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-1-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Quy đổi đơn vị lương</name>
  <files>src/lib/payroll/rate.ts, src/lib/payroll/__tests__/rate.test.ts, src/lib/types/domain.ts</files>
  <read_first>
    - `.planning/phases/05-2-.../05-2-CONTEXT.md` D-38 — hai mẫu số và vì sao chúng không có mặc định.
    - `src/lib/attendance/overtime-cap.ts` (05-03) — khuôn mô-đun thuần.
  </read_first>
  <behavior>
    - `toDailyRate({ unit, amount, standardDaysPerMonth, standardHoursPerDay })`:
      `month` → `amount / standardDaysPerMonth`; `day` → `amount`;
      `hour` → `amount × standardHoursPerDay`.
    - `toHourlyRate(...)`: `hour` → `amount`; `day` → `amount / standardHoursPerDay`;
      `month` → `amount / (standardDaysPerMonth × standardHoursPerDay)`.
    - **Thiếu mẫu số cần thiết → trả `null` kèm khoá thiếu**, không trả một con số. Người ăn
      lương giờ không cần `standardDaysPerMonth` để ra đơn giá giờ — chỉ đòi mẫu số **thực sự
      dùng tới**, để một doanh nghiệp trả lương giờ không bị chặn vì thiếu một con số vô can.
    - Không làm tròn: đơn giá là bước **trung gian** (D-42a).
  </behavior>
  <action>
    Viết mô-đun + test, ít nhất 10 khẳng định, phủ cả ba đơn vị × cả hai chiều quy đổi, cộng ba
    bài thiếu mẫu số (thiếu cái cần → `null`; thiếu cái không cần → vẫn tính được).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/payroll/__tests__/rate.test.ts` thoát 0, ít nhất 10 test.
    - `grep -cE "\?\? 0|\?\? 1|\?\? 8|\?\? 22|\?\? 26" src/lib/payroll/rate.ts` trả `0`.
    - `grep -cE "Math\.round|toFixed" src/lib/payroll/rate.ts` trả `0` — không làm tròn ở bước trung gian.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/payroll/__tests__/rate.test.ts</automated>
  </verify>
  <done>Ba đơn vị lương quy đổi qua lại được, và thiếu mẫu số thì nói thiếu chứ không đoán.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phép tính tiền của một dòng lương</name>
  <files>src/lib/payroll/compute.ts, src/lib/payroll/__tests__/compute.test.ts, src/lib/types/domain.ts</files>
  <read_first>
    - `src/lib/payroll/scope.ts` (05-2-03) — `isTargeted()`.
    - `src/lib/attendance/work-mode.ts` (05-2-02) — `creditedDays`, `regularMinutes`,
      `hourDeltaMinutes`.
    - `src/lib/attendance/classification.ts` (04-05) — `convertedOvertimeHours` và vì sao nó
      `null` khi thiếu hệ số (D-26). Cách xử lý `null` ở đây phải **giống hệt**.
  </read_first>
  <behavior>
    - `computePayrollLine({ summary, payRate, rates, adjustments, employee })` trả:
      `basePay`, `overtimePay`, `hourAdjustment`, `allowanceItems[]`, `deductionItems[]`,
      `allowanceTotal`, `deductionTotal`, `netPay`, `missing[]`.
    - **Lương gốc** theo chế độ:
      - `shift`: `đơn giá ngày × creditedDays`;
      - `daily_hours`: `đơn giá giờ × regularMinutes / 60`;
      - `shift_hourly`: `đơn giá ngày × creditedDays`, cộng `hourAdjustment =
        đơn giá giờ × hourDeltaMinutes / 60` (âm khi thiếu giờ).
    - **Tiền tăng ca** = `convertedOvertimeHours × đơn giá giờ`. `convertedOvertimeHours = null`
      → `overtimePay = null` **và** `netPay = null`, `missing` mang khoá hệ số còn thiếu.
    - **Khoản**: chỉ khoản `is_active` và `isTargeted()` mới áp.
      `fixed_amount` → đúng số tiền; `percent_of_daily_wage` → `value% × đơn giá ngày`.
      `basis = 'per_late'` → nhân thêm `summary.lateCount`.
    - **Chưa khai mức lương** → mọi con số tiền `null`, `missing` mang `pay_rate`.
    - Làm tròn **chỉ ở cuối**: từng dòng khoản làm tròn để hiển thị, nhưng `netPay` tính từ giá
      trị **chưa làm tròn** rồi mới làm tròn — và test phải khẳng định hai cách cho cùng kết quả
      ở một bộ số cố ý gây lệch.
  </behavior>
  <action>
    Viết mô-đun thuần + test, **ít nhất 14 khẳng định**, bắt buộc có:
    - một bài cho mỗi chế độ với số tiền tính tay ghi sẵn trong test;
    - `shift_hourly` thiếu giờ → `hourAdjustment` **âm**;
    - thiếu hệ số → `netPay` là `null`, **không phải** tổng của phần đã biết;
    - chưa khai lương → `null`, không phải 0;
    - phạt `per_late` với `lateCount = 3` ra đúng ba lần;
    - `percent_of_daily_wage` tính trên **lương ngày**, không phải lương tháng;
    - một bộ số cố ý gây lệch làm tròn, khẳng định tổng bằng tổng các dòng.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/payroll/__tests__/compute.test.ts` thoát 0, ít nhất 14 test.
    - `grep -cE "supabase|process\.env|new Date" src/lib/payroll/compute.ts` trả `0`.
    - `grep -c "convertedOvertimeHours" src/lib/payroll/compute.ts` trả ít nhất `1`; `grep -cE "overtimeMinutes \*|scheduledMinutes" src/lib/payroll/compute.ts` trả `0` — không tính lại giờ tăng ca.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/payroll/__tests__/compute.test.ts</automated>
  </verify>
  <done>Phép tính tiền nằm ở một chỗ, kiểm được từng nhánh, và thiếu dữ kiện thì nó nói thiếu.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Đường đọc bảng lương có tiền</name>
  <files>src/lib/payroll/payroll-context.ts, src/lib/validation/api/payroll.ts, src/app/api/payroll/summary/route.ts, src/lib/data/__tests__/payroll-compute.test.ts</files>
  <read_first>
    - `src/app/api/payroll/summary/route.ts` (5.1) — bảng chuẩn bị lương **không tiền** hiện có;
      plan này thêm cột tiền vào đúng đó, **không** dựng đường đọc thứ hai.
    - `src/lib/attendance/month-context.ts` — khuôn "đọc ngữ cảnh một lần, dùng lại cho hàng
      chục người".
  </read_first>
  <behavior>
    - `payroll-context.ts` đọc **một lần** cho cả kỳ: mức lương hiệu lực của từng nhân viên tại
      ngày cuối kỳ, danh mục khoản + phạm vi, hai mẫu số. Không đọc lại theo từng người.
    - `GET /api/payroll/summary` giữ nguyên mọi trường cũ và thêm phần tiền cho mỗi dòng, cộng
      `missing[]` của từng dòng.
    - Mức lương tra tại **ngày cuối kỳ**, không phải hôm nay — và ghi rõ lựa chọn đó trong
      comment kèm hệ quả (tăng lương giữa kỳ thì cả kỳ ăn mức mới).
    - Dòng nào thiếu dữ kiện thì thiếu **một mình nó** — không làm hỏng cả bảng.
  </behavior>
  <action>
    Nối `compute.ts` vào đường đọc. Test tích hợp trên database dev thật, **ít nhất 8 test**,
    trong đó bài quan trọng nhất là: **cùng một tập chấm công + cùng một mức lương, chạy qua ba
    chế độ, ra ba số tiền khác nhau và cả ba khớp con số tính tay ghi trong test**.

    Cộng thêm: một nhân viên chưa khai lương nằm cùng bảng với người đã khai → dòng của họ
    `null`, dòng của người kia vẫn ra số.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/payroll-compute.test.ts` thoát 0, ít nhất 8 test.
    - `npx vitest run` thoát 0 toàn bộ.
    - `grep -c "computePayrollLine" src/app/api/payroll/summary/route.ts` trả ít nhất `1`; không file nào khác dưới `src/app/` gọi nó.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Bảng lương ra tiền thật, và con số đó tái lập được bằng một phép tính tay.</done>
</task>

<task type="auto">
  <name>Task 4: Màn hình và tệp xuất có tiền</name>
  <files>src/app/admin/payroll/payroll-view.tsx, src/lib/payroll/csv.ts, src/lib/payroll/__tests__/csv.test.ts, src/lib/constants.ts</files>
  <read_first>
    - `src/app/admin/payroll/payroll-view.tsx` (5.1) — bảng hiện có; cột tiền thêm vào bên phải.
    - `src/lib/payroll/csv.ts` (5.1) — **ba quyết định định dạng cho Excel vi-VN** (dấu chấm
      phẩy, BOM, số thập phân dấu phẩy). Cột tiền phải theo đúng ba quyết định đó.
  </read_first>
  <action>
    **(a) Bảng** — thêm các cột: lương gốc, tiền tăng ca, phụ cấp, khấu trừ, **thực nhận**.
    Cột thực nhận nổi bật hơn các cột khác. Dòng thiếu dữ kiện hiện chữ nói **thiếu gì**
    ("chưa khai mức lương", "chưa khai hệ số tăng ca") thay vì một con số.

    **(b) Bấm một dòng** mở khối chi tiết: từng khoản phụ cấp và khấu trừ với tên và số tiền —
    người xem phải trả lời được "vì sao ra con số này" mà không cần hỏi ai.

    **(c) Chú thích cố định trên bảng và trong CSV**: con số **chưa gồm** thuế TNCN và
    BHXH/BHYT/BHTN. Đây không phải một dòng nhỏ ở góc — nó là điều dễ hiểu nhầm nhất của cả
    màn hình.

    **(d) CSV** thêm các cột tiền, giữ đúng ba quyết định định dạng của 5.1; ô thiếu dữ kiện
    xuất ra **chữ**, không xuất 0 (cùng lý do với "chưa khai hệ số" của 5.1).
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `npx vitest run src/lib/payroll/__tests__/csv.test.ts` thoát 0, có test mới cho cột tiền và cho ô thiếu dữ kiện.
    - `grep -ciE "chưa gồm|chưa bao gồm" src/lib/constants.ts` trả ít nhất `1`.
    - Với một doanh nghiệp thật đã khai đủ: bảng ra số tiền, bấm một dòng thấy từng khoản, tổng cột thực nhận bằng tổng các dòng. Ghi cả ba quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Kế toán mở màn hình là thấy con số, và mở một dòng là hiểu vì sao ra con số đó.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phép tính → tiền trả cho người lao động | Đây là điểm mà mọi sai sót của phase hội tụ lại |
| Dữ kiện thiếu → con số hiển thị | Một số 0 thay cho "chưa khai" là một lời nói dối có chữ ký |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-2-04-01 | Tampering | Thiếu dữ kiện được thay bằng 0 → người lao động bị trả thiếu, bảng trông bình thường | high | mitigate | `null` + `missing[]` xuyên suốt; test riêng cho từng loại thiếu; CSV xuất chữ |
| T-05-2-04-02 | Tampering | Tính lại giờ tăng ca ở tầng tiền → hai nguồn cho cùng một con số | high | mitigate | D-31; grep chặn `overtimeMinutes *` và `scheduledMinutes` trong `compute.ts` |
| T-05-2-04-03 | Tampering | Làm tròn từng bước → tổng lệch tổng các dòng | medium | mitigate | D-42a; test với bộ số cố ý gây lệch |
| T-05-2-04-04 | Repudiation | Không giải thích được vì sao ra con số | medium | mitigate | Khối chi tiết từng khoản trên màn hình; `missing[]` nói rõ thiếu gì |
| T-05-2-04-05 | Information Disclosure | Người dùng tưởng con số đã trừ thuế và bảo hiểm rồi | medium | mitigate | Chú thích cố định trên bảng **và** trong CSV |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Cùng một tập chấm công + mức lương, ba chế độ ra ba số tiền, cả ba khớp phép tính tay
- Thiếu mức lương / mẫu số / hệ số đều ra `null` kèm lý do, không ra 0
- Tổng bằng đúng tổng các dòng trên một bộ số cố ý gây lệch làm tròn
- Giờ tăng ca không được tính lại ở tầng tiền
</verification>

<success_criteria>
Bảng lương ra một con số mà người xem tái lập được bằng giấy bút — và khi hệ thống chưa đủ dữ
kiện, nó nói ra thay vì đưa một con số trông có vẻ đúng.
</success_criteria>

<output>
Create `.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-04-SUMMARY.md` when done
</output>
