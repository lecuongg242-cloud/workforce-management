---
phase: 05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0022_employee_pay_rates.sql
  - supabase/tests/16_employee_pay_rates.sql
  - supabase/tests/run-all.sql
  - scripts/check-pgtap-assertions.mjs
  - src/lib/types/domain.ts
  - src/lib/validation/api/settings.ts
  - src/lib/validation/api/pay-rates.ts
  - src/lib/settings/company-settings.ts
  - src/lib/data/mutations/settings.ts
  - src/lib/data/pay-rates.ts
  - src/lib/data/mutations/pay-rates.ts
  - src/app/api/pay-rates/route.ts
  - src/components/settings/general-settings-form.tsx
  - src/components/employees/pay-rate-panel.tsx
  - src/app/admin/employees/[id]/employee-detail-view.tsx
  - src/lib/constants.ts
  - src/lib/data/__tests__/pay-rates.test.ts
autonomous: true
requirements: [PAY-06]

estimate:
  tokens: 86000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "Mỗi nhân viên khai được **đơn vị lương** (tháng / ngày / giờ) và **số tiền**; hai nhân viên trong cùng doanh nghiệp khai được hai đơn vị khác nhau (D-37)."
    - "Mức lương là **append-only theo `effective_from`**: sửa lương nghĩa là khai một phiên bản mới, và `UPDATE`/`DELETE` bị **trigger của database** từ chối (D-37a, khuôn D-25a)."
    - "Tra mức lương của một nhân viên **tại một ngày** trả đúng phiên bản đang hiệu lực tại ngày đó — không phải phiên bản mới nhất."
    - "Ngày nằm **trước mọi phiên bản** trả `null` — tuyệt đối không lùi về phiên bản gần nhất, và không bịa ra 0 (cùng lý do với D-26)."
    - "Doanh nghiệp chọn được `work_mode` (một trong ba) ở tab Chung của `/admin/settings`; doanh nghiệp đang chạy giữ nguyên hành vi cũ (`shift`)."
    - "Hai mẫu số quy đổi (`standard_hours_per_day`, `standard_days_per_month`) **không có mặc định** — để trống nghĩa là chưa khai, và mọi nơi cần chúng phải nói 'chưa khai' chứ không đoán (D-38)."
    - "Tab 'Thông tin lương' ở `/admin/employees/[id]` không còn là `EmptyState` hứa hẹn — nó hiện lịch sử mức lương và cho khai phiên bản mới."
    - "Mỗi lần khai mức lương để lại một dòng `audit_log`; `owner` và `admin` đều làm được (D-44), vai trò khác nhận 403."
  artifacts:
    - path: "supabase/migrations/0022_employee_pay_rates.sql"
      provides: "Bảng mức lương append-only + work_mode + hai mẫu số"
      exports: ["employee_pay_rates", "tf_pay_rate_at"]
    - path: "src/lib/data/mutations/pay-rates.ts"
      provides: "Server Action khai mức lương"
      exports: ["createPayRate"]
    - path: "src/components/employees/pay-rate-panel.tsx"
      provides: "Tab Thông tin lương chạy thật"
      min_lines: 100
  key_links:
    - from: "src/components/employees/pay-rate-panel.tsx"
      to: "src/lib/data/mutations/pay-rates.ts"
      via: "khai một phiên bản mức lương mới"
      pattern: "createPayRate"
    - from: "supabase/migrations/0022_employee_pay_rates.sql"
      to: "supabase/migrations/0016_overtime_rules_append_only.sql"
      via: "nhân lại khuôn trigger append-only của D-25a"
      pattern: "append_only"
  prohibitions:
    - statement: "MUST NOT cho phép `UPDATE`/`DELETE` trên `employee_pay_rates`. Sửa đè một mức lương cũ làm bảng lương của kỳ đã trả tính lại ra số khác — và không ai phát hiện, vì số liệu chỉ đơn giản là khác đi. Đây đúng là lý do đã dẫn tới D-25a, với hậu quả nặng hơn vì nó là tiền."
      category: correctness
    - statement: "MUST NOT đặt giá trị mặc định cho hai mẫu số quy đổi. 22 hay 26 ngày công chuẩn là chuyện của từng doanh nghiệp; một mặc định làm màn hình trông như đã tính đúng trong khi chưa ai khai gì — đúng loại sai lặng lẽ mà D-26 tồn tại để chặn."
      category: correctness
    - statement: "MUST NOT tính một con số tiền nào ở plan này. Đây là plan dựng CHỖ ĐỂ KHAI; phép tính là 05-2-04. Trộn hai việc làm không phân biệt được lỗi đến từ dữ liệu khai sai hay từ công thức sai."
      category: architecture
---

<objective>
Chỗ để khai lương: mức lương của từng nhân viên (có phiên bản, không sửa đè được) và ba lựa
chọn cách tính công của doanh nghiệp.

Purpose: đây là tracer của phase. Nó **chưa** tính ra đồng nào — nhưng nó dựng đủ ba thứ mà năm
plan sau đều đứng lên: bảng mức lương append-only, hàm tra mức lương tại một ngày, và cấu hình
chế độ tính công. Nếu ba thứ này đúng, phần còn lại của phase là nối phép tính vào những điểm
đã có tên.

Output: migration 0022, `tf_pay_rate_at()`, `createPayRate()`, tab "Thông tin lương" chạy thật,
tab Chung có thêm chế độ tính công.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-CONTEXT.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-04-SUMMARY.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bảng mức lương append-only và hàm tra theo ngày</name>
  <files>supabase/migrations/0022_employee_pay_rates.sql, supabase/tests/16_employee_pay_rates.sql, supabase/tests/run-all.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `supabase/migrations/0016_overtime_rules_append_only.sql` (04-04) — khuôn **chính xác**
      mà bảng này nhân lại: trigger append-only, thủ tục gỡ có ý thức ghi trong comment, và
      hàm `tf_overtime_multiplier()` tra phiên bản hiệu lực tại một ngày.
    - `supabase/migrations/0019_overtime_cap.sql` (05-03) — khuôn thêm cột vào
      `company_settings` kèm lý do vì sao **không** có mặc định.
    - `supabase/tests/11_overtime_rules_append_only.sql` — khuôn test append-only, 7 khẳng định.
    - `scripts/check-pgtap-assertions.mjs` — sàn hiện tại **250**, chỉ được nâng.
  </read_first>
  <behavior>
    - `employee_pay_rates`: `id`, `company_id`, `employee_id`, `unit` (`month`/`day`/`hour`),
      `amount` (numeric, > 0), `effective_from` (date), `created_at`, `created_by`.
      `unique (employee_id, effective_from)` — một người một mức cho một mốc hiệu lực.
    - `UPDATE` và `DELETE` bị trigger từ chối, thông điệp nói **cách làm đúng** (khai phiên bản
      mới), không chỉ nói "cấm" — đúng giọng của 0016.
    - `tf_pay_rate_at(p_employee_id, p_date)` trả phiên bản có `effective_from` lớn nhất mà vẫn
      `<= p_date`; trả `null` khi `p_date` nằm trước mọi phiên bản. `security invoker`.
    - `company_settings` thêm `work_mode` (`shift` mặc định — doanh nghiệp đang chạy giữ nguyên
      hành vi), `standard_hours_per_day` và `standard_days_per_month` (**null, không mặc định**).
    - Bốn policy RLS điều kiện duy nhất `tf_is_member(company_id)`.
  </behavior>
  <action>
    Viết migration 0022 + test pgTAP (ít nhất 9 khẳng định: `UPDATE` bị chặn, `DELETE` bị chặn,
    `INSERT` chạy bình thường, tra đúng phiên bản tại ngày sau mốc mới, tra ra phiên bản **cũ**
    tại ngày trước mốc mới, trả `null` khi trước mọi phiên bản, trả `null` khi nhân viên chưa
    khai lần nào, cô lập đọc chéo doanh nghiệp, cô lập ghi chéo). Khai vào `run-all.sql`,
    nâng sàn.

    Khối comment đầu file nói rõ **vì sao append-only ở đây nặng hơn ở 0016**: hệ số tăng ca sai
    làm số liệu sai; mức lương sai làm **tiền trả cho người lao động** sai, và sai về quá khứ.

    Migration làm **chạy-lại-được** (`drop … if exists` + `add column if not exists` ở đầu) —
    khuôn 0018/0021 của Phase 5.
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `npm run check:assertions` thoát 0 với sàn mới.
    - `grep -c "security definer" supabase/migrations/0022_employee_pay_rates.sql` trả `0`.
    - `psql ... -tAc "select column_default from information_schema.columns where table_name='company_settings' and column_name='standard_days_per_month'"` trả rỗng (kiểm qua test tích hợp nếu không có `psql`).
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Mức lương có chỗ để sống, không sửa đè được, và tra được đúng phiên bản của một ngày bất kỳ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Đường đọc và đường ghi mức lương</name>
  <files>src/lib/types/domain.ts, src/lib/validation/api/pay-rates.ts, src/lib/data/pay-rates.ts, src/lib/data/mutations/pay-rates.ts, src/app/api/pay-rates/route.ts, src/lib/data/__tests__/pay-rates.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/overtime-rules.ts` (04-04) — khuôn Server Action ghi vào một bảng
      append-only, gồm cách bắt riêng lỗi trùng `effective_from`.
    - `src/lib/data/overtime-rules.ts` + `src/app/api/overtime-rules/route.ts` — khuôn cặp
      đường đọc.
    - `src/lib/data/__tests__/overtime-rules.test.ts` — khuôn test tích hợp trên database thật.
  </read_first>
  <behavior>
    - `GET /api/pay-rates?employeeId=` trả **toàn bộ lịch sử** mức lương của một nhân viên,
      `effective_from` giảm dần. Chỉ `owner`/`admin` (D-44); vai trò khác nhận 403 **kể cả khi
      hỏi chính mình** — nhân viên chưa xem được lương của mình ở phase này (PAY-05 ở V3).
    - `createPayRate({ employeeId, unit, amount, effectiveFrom })` đòi `owner`/`admin`, ghi một
      dòng, để lại một dòng `audit_log`.
    - Trùng `effective_from` cho cùng nhân viên → thông điệp tiếng Việt rõ ràng, không phải lỗi
      Postgres thô.
    - `employeeId` của doanh nghiệp khác → "Không tìm thấy nhân viên", **không** ghi dòng nào.
    - `amount <= 0` bị chặn ở Server Action **trước khi** chạm database.
  </behavior>
  <action>
    Viết cặp schema hai đầu (D-12d), đường đọc, Server Action; test tích hợp trên database dev
    thật phủ đủ năm hành vi ở `<behavior>`, cộng một bài khẳng định `UPDATE` bị trigger chặn kể
    cả bằng khoá `service_role`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/pay-rates.test.ts` thoát 0, ít nhất 6 test.
    - `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0.
    - `grep -c "logMutation" src/lib/data/mutations/pay-rates.ts` trả ít nhất `1`.
    - `grep -c "companyId" src/lib/validation/api/pay-rates.ts` — không xuất hiện trong schema đầu vào hay tham số truy vấn; kiểm bằng mắt, ghi kết luận vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/pay-rates.test.ts</automated>
  </verify>
  <done>Mức lương khai được qua đúng một đường, và mỗi lần khai để lại vết.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Chế độ tính công và hai mẫu số vào tab Chung</name>
  <files>src/lib/types/domain.ts, src/lib/validation/api/settings.ts, src/lib/settings/company-settings.ts, src/lib/data/mutations/settings.ts, src/components/settings/general-settings-form.tsx, src/lib/constants.ts</files>
  <read_first>
    - `src/lib/data/mutations/settings.ts` — khối comment mở đầu: plan nào thêm cột thì thêm vào
      **hàm này**, không mở đường ghi thứ hai. 05-03 đã dùng đúng lời hứa đó cho trần tăng ca.
    - `src/components/settings/general-settings-form.tsx` (sau 05-03) — form năm trường; trường
      thứ sáu (`work_mode`) là một `Select`, khác bốn trường số hiện có.
  </read_first>
  <behavior>
    - Tab Chung thêm: chọn **cách tính công** (ba lựa chọn, mỗi lựa chọn có một câu giải thích
      hệ quả), và hai ô mẫu số quy đổi — **để trống hợp lệ**.
    - Chọn `daily_hours` mà chưa khai `standard_hours_per_day` → form cảnh báo ngay tại chỗ
      rằng chế độ này cần con số đó mới chạy được; **không chặn lưu** (doanh nghiệp có thể khai
      sau), nhưng nói rõ hệ quả.
    - Ghi vẫn đi qua đúng `updateCompanySettings()`, để lại đúng một dòng audit.
  </behavior>
  <action>
    Mở rộng kiểu, cặp schema, hàm hợp nhất patch và form. Nhãn của ba chế độ phải nói **hệ quả**
    chứ không chỉ tên: "không có ca — một công tính bằng N giờ, vượt N giờ là tăng ca".
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint` thoát 0.
    - `npx vitest run src/lib/data/__tests__/settings.test.ts` thoát 0 với test mới cho `work_mode` và hai mẫu số để trống.
    - `grep -c "await logMutation(" src/lib/data/mutations/settings.ts` vẫn trả `1` — không đường ghi thứ hai.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/settings.test.ts</automated>
  </verify>
  <done>Doanh nghiệp nói được mình tính công kiểu nào, và hệ thống biết mẫu số để quy đổi.</done>
</task>

<task type="auto">
  <name>Task 4: Tab "Thông tin lương" ở hồ sơ nhân viên</name>
  <files>src/components/employees/pay-rate-panel.tsx, src/app/admin/employees/[id]/employee-detail-view.tsx, src/lib/constants.ts</files>
  <read_first>
    - `src/app/admin/employees/[id]/employee-detail-view.tsx` — tab `salary` hiện là một
      `EmptyState` hứa *"sẽ được thiết lập trong giai đoạn tiếp theo"*. Đây là giai đoạn đó.
    - `src/components/settings/overtime-rules-tab.tsx` (04-04) — khuôn màn hình cho một bảng
      **append-only**: hiện lịch sử phiên bản, nút thêm phiên bản mới, không có nút sửa/xoá.
    - `src/lib/format.ts` — `formatVnd()` đã có nhưng **chưa nơi nào gọi**; đây là chỗ dùng nó
      lần đầu.
  </read_first>
  <action>
    Thay `EmptyState` bằng `PayRatePanel`:
    - Mức lương **đang hiệu lực hôm nay** hiện nổi bật ở trên (số tiền + đơn vị), hoặc một dòng
      "chưa khai mức lương" nếu chưa có phiên bản nào.
    - Bên dưới là **lịch sử phiên bản**, mới nhất trước, mỗi dòng: hiệu lực từ ngày nào, đơn vị,
      số tiền, ai khai.
    - Nút "Khai mức lương mới" mở dialog: đơn vị (Select), số tiền, hiệu lực từ ngày.
    - **Không có nút sửa và không có nút xoá** — và màn hình nói rõ vì sao: mỗi lần đổi lương là
      một phiên bản mới, để bảng lương của kỳ đã trả không đổi theo.
    - `constants.ts` thêm khối `PAY_RATE_LABEL` gồm nhãn ba đơn vị.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -c "EmptyState" src/app/admin/employees/[id]/employee-detail-view.tsx` giảm đúng 1 so với trước plan (khối tab lương không còn là EmptyState).
    - `grep -rn "formatVnd" src/components/employees/pay-rate-panel.tsx` trả ít nhất 1 dòng.
    - Với phiên owner và một nhân viên thật: khai mức lương tháng 10.000.000 hiệu lực từ đầu năm, rồi khai 12.000.000 hiệu lực từ tháng sau → lịch sử hiện **hai** dòng, mức đang hiệu lực hôm nay đúng một trong hai. Ghi cả ba quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Lời hứa "giai đoạn tiếp theo" của V1 được đóng lại, và lịch sử lương đọc được.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mức lương → tiền trả cho người lao động | Sai ở đây là sai tiền, và sai về quá khứ |
| `employeeId` do client gửi → dòng dữ liệu | Khai lương cho nhân viên của doanh nghiệp khác phải là bất khả |
| Lịch sử mức lương → bằng chứng sau này | Sửa được lịch sử là mất luôn giá trị của việc ghi nó |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-2-01-01 | Tampering | Sửa đè mức lương cũ → bảng lương kỳ đã trả tính lại ra số khác | high | mitigate | Trigger append-only (D-37a); test khẳng định `UPDATE` bị chặn kể cả bằng khoá `service_role` |
| T-05-2-01-02 | Spoofing | `employeeId` của doanh nghiệp khác | high | mitigate | Đối chiếu `employees` theo `company_id` từ phiên trước khi ghi; test bằng id chéo thật |
| T-05-2-01-03 | Information Disclosure | Nhân viên đọc được mức lương của đồng nghiệp | high | mitigate | `requireRole(role, ["owner","admin"])` ở cả đường đọc; test bằng phiên `employee` thật |
| T-05-2-01-04 | Tampering | Mẫu số quy đổi có mặc định ngầm → sai đơn giá giờ của mọi người mà không ai khai gì | medium | mitigate | Hai cột `null`, không `DEFAULT`; test khẳng định `column_default` rỗng |
| T-05-2-01-05 | Repudiation | Khai mức lương mà không biết ai khai lúc nào | medium | mitigate | `created_by`/`created_at` + một dòng `audit_log` |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run db:push && npm run check:assertions` thoát 0
- Mức lương không sửa và không xoá được, kể cả bằng khoá secret
- Tra mức lương tại một ngày trả đúng phiên bản của ngày đó, `null` khi trước mọi phiên bản
- Hai mẫu số quy đổi không có mặc định
- Vai trò `employee`/`manager` không đọc được mức lương của ai
</verification>

<success_criteria>
Doanh nghiệp khai được lương của từng người và cách tính công của mình — và sáu tháng sau vẫn
trả lời được "tháng đó người này ăn lương bao nhiêu".
</success_criteria>

<output>
Create `.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-01-SUMMARY.md` when done
</output>
