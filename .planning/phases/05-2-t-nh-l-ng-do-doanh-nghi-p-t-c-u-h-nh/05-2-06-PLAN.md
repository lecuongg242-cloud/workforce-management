---
phase: 05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh
plan: 06
type: execute
wave: 5
depends_on: ["05-2-05"]
files_modified:
  - src/__tests__/lib/money-scan.ts
  - src/__tests__/no-hardcoded-money.test.ts
  - scripts/e2e-payroll.mjs
  - package.json
  - .planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-UAT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [PAY-01, PAY-04, PAY-06]

estimate:
  tokens: 66000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Có một cổng tự động chạy trong `npm test` chặn việc nhúng cứng một con số tiền hoặc một mẫu số quy đổi vào mã — đây là lời hứa D-26 áp cho tiền, và nó phải sống sót sau khi phase kết thúc."
    - "Cổng ấy được chứng minh **có răng** bằng một lần phá hoại có kiểm soát: thêm một mẫu số dự phòng vào mô-đun tính lương → đỏ; hoàn tác → xanh; không còn dấu vết."
    - "Ba yêu cầu của phase (PAY-01, PAY-04, PAY-06) được nghiệm thu bằng **quan sát trên hệ thống chạy thật**, không phải bằng suy luận từ test đơn vị."
    - "Một kỳ lương chạy được đầu-cuối một lần qua HTTP thật: khai lương → khai khoản → chốt kỳ công → xem tiền → chốt lương → đổi mức lương → con số **không đổi** → huỷ chốt → con số đổi theo."
    - "ROADMAP, REQUIREMENTS và STATE phản ánh đúng trạng thái sau phase."
  artifacts:
    - path: "src/__tests__/no-hardcoded-money.test.ts"
      provides: "Cổng chặn con số tiền và mẫu số quy đổi nhúng cứng"
    - path: "scripts/e2e-payroll.mjs"
      provides: "Một kỳ lương đầu-cuối qua HTTP thật"
    - path: ".planning/phases/05-2-.../05-2-UAT.md"
      provides: "Biên bản nghiệm thu kèm giới hạn đã biết"
  prohibitions:
    - statement: "MUST NOT đánh dấu phase hoàn thành khi một yêu cầu chỉ được chứng minh bằng test đơn vị. Ba yêu cầu của phase nói về tiền trả cho người thật; chúng phải được quan sát trên hệ thống chạy thật ít nhất một lần."
      category: process
    - statement: "MUST NOT nới lỏng cổng để nó xanh. Bắt nhầm một dòng hợp lệ thì thu hẹp bằng NGỮ NGHĨA và ghi lý do ngay trong mã — không miễn trừ cả một file, vì miễn trừ đường dẫn làm mù toàn bộ file đó về sau."
      category: integrity
    - statement: "MUST NOT bỏ qua việc ghi ra những gì phase KHÔNG làm được. Sáu thứ đã biết: không thuế, không bảo hiểm, không phiếu lương cho nhân viên, không khoản một-lần, phạt đi muộn không phân bậc, và mọi `admin` xem được lương của mọi người."
      category: transparency
---

<objective>
Đóng phase: dựng cổng tự động giữ cho lời hứa "không con số tiền nào nhúng cứng" không bị xói
mòn, chạy một kỳ lương đầy đủ qua HTTP thật, và nghiệm thu ba yêu cầu bằng quan sát.

Purpose: năm plan trước mỗi plan có grep gate riêng trong acceptance criteria, nhưng acceptance
criteria chỉ chạy một lần. Điều dễ mất nhất của phase này là **một mẫu số dự phòng tiện tay** —
một `?? 22` ở đâu đó trong một plan tương lai, và từ đó mọi doanh nghiệp chưa khai mẫu số bỗng
có lương tính bằng một con số không ai chọn.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-CONTEXT.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-04-SUMMARY.md
@.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-05-SUMMARY.md
@.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cổng chặn con số tiền nhúng cứng</name>
  <files>src/__tests__/lib/money-scan.ts, src/__tests__/no-hardcoded-money.test.ts</files>
  <read_first>
    - `src/__tests__/lib/work-rule-scan.ts` + `no-hardcoded-work-rules.test.ts` (04-06) — khuôn
      cổng quét của dự án: bộ quét tách khỏi test để chính nó kiểm được bằng mẫu vi phạm giả
      lập; mỗi quy tắc đòi một **ngữ cảnh**, không chỉ một chuỗi.
    - `src/__tests__/lib/period-write-scan.ts` (05-06) — khuôn gần nhất, gồm `stripComments()`
      tách riêng khi cần nhìn thấy nội dung chuỗi.
  </read_first>
  <behavior>
    - Báo đỏ khi mã dưới `src/lib/payroll/` hoặc `src/lib/attendance/work-mode.ts` chứa:
      1. một **mẫu số dự phòng** cho quy đổi (`?? 22`, `?? 26`, `?? 8`, `|| 8` … trên dòng nói
         về `standardDays`/`standardHours`/`rate`);
      2. một **số tiền** nhúng cứng (số ≥ 1000 nằm trong một phép nhân/cộng với `amount`,
         `pay`, `salary`, `wage`);
      3. một **tỉ lệ phần trăm** nhúng cứng trên dòng nói về khấu trừ/phụ cấp.
    - Không báo nhầm: hằng số có tên và được xuất từ module sở hữu vẫn xanh; comment và nội
      dung chuỗi không tính; số dùng cho định dạng (`100` trong phép làm tròn hai chữ số) không
      tính.
    - Danh sách miễn trừ tường minh, mỗi mục có lý do, **tối đa 3 mục**.
    - Cổng quét được ít nhất 6 file — một cổng đếm 0 file không phải bằng chứng không vi phạm.
  </behavior>
  <action>
    Viết cổng theo khuôn 04-06/05-06.

    **Kiểm răng bắt buộc:** (1) thêm tạm `standardDaysPerMonth ?? 22` vào `rate.ts` → cổng
    **đỏ**; (2) hoàn tác → **xanh**; (3) `git diff` không còn dòng đó. Ghi cả ba bước vào
    SUMMARY — một cổng chưa từng báo đỏ là một cổng chưa được chứng minh là còn thức.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/no-hardcoded-money.test.ts` thoát 0.
    - `npx vitest run src/__tests__/no-hardcoded-work-rules.test.ts` và `no-silent-period-write.test.ts` thoát 0 — hai cổng cũ không hồi quy.
    - `npm test` chạy cả ba cổng.
    - Kết quả ba bước kiểm răng ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/no-hardcoded-money.test.ts src/__tests__/no-hardcoded-work-rules.test.ts src/__tests__/no-silent-period-write.test.ts</automated>
  </verify>
  <done>Lời hứa "doanh nghiệp tự khai, hệ thống không đoán" có một người canh gác chạy tự động cho phần tiền.</done>
</task>

<task type="auto">
  <name>Task 2: Một kỳ lương đầu-cuối qua HTTP thật</name>
  <files>scripts/e2e-payroll.mjs, package.json</files>
  <read_first>
    - `scripts/e2e-approval.mjs` (05-06) — khuôn kịch bản e2e của dự án: tự tạo tài khoản +
      doanh nghiệp qua Admin API, đăng nhập thật, dựng cookie phiên đúng khuôn `@supabase/ssr`,
      gọi HTTP thật, dọn fixture. Đọc kỹ khối comment nói **điều nó không chứng minh được** và
      giữ đúng sự trung thực đó (thao tác ghi là Server Action, script ngoài không gọi được).
  </read_first>
  <action>
    Viết `scripts/e2e-payroll.mjs`, mỗi bước một khẳng định:
    1. Dựng doanh nghiệp + 3 nhân viên + một tháng chấm công (có ngày đủ, ngày thiếu giờ, ngày
       tăng ca, một ngày `leave_unpaid`).
    2. Khai mức lương cho 2 người, **để 1 người chưa khai**.
    3. Khai chế độ tính công + hai mẫu số.
    4. Khai một phụ cấp toàn công ty **trừ 1 người**, và một khoản phạt đi muộn.
    5. Đọc `/api/payroll/summary` — 2 dòng ra tiền, 1 dòng `missing` mang `pay_rate`; người bị
       loại trừ **không** có khoản phụ cấp.
    6. Thử chốt lương khi kỳ công **chưa** chốt → bị từ chối.
    7. Chốt kỳ công, chốt lương → bản chốt có đúng số dòng, tổng bằng tổng các dòng.
    8. **Đổi mức lương** một người → đọc lại, con số của kỳ **không đổi**.
    9. **Huỷ chốt lương** kèm lý do → đọc lại, con số **đổi theo mức mới**.
    10. Dọn sạch; in một dòng tổng kết mỗi bước.

    Bước 8 và 9 nằm **liền nhau** và khẳng định hai kết quả **ngược nhau** — đó là bằng chứng
    bản chốt thật sự tự chứa.

    Khai `test:e2e-payroll` trong `package.json`.
  </action>
  <acceptance_criteria>
    - `npm run test:e2e-payroll` thoát 0 trên môi trường dev thật (cần `npm run dev`).
    - Bước 8 và 9 liền nhau, hai kết quả ngược nhau.
    - `git status` sạch sau khi chạy; dữ liệu tạo ra đã dọn (trừ những gì trigger append-only chặn xoá — nếu có, ghi rõ vào SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e-payroll</automated>
  </verify>
  <done>Một kỳ lương chạy được một lần đầu-cuối trên hệ thống thật, gồm cả vế khó nhất: bản chốt không đổi theo tương lai.</done>
</task>

<task type="checkpoint" gate="blocking">
  <name>Task 3: Nghiệm thu ba yêu cầu và đóng phase</name>
  <files>.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-UAT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 5.2 — năm tiêu chí, nguyên văn.
    - `.planning/phases/05-duy-t-y-u-c-u-v-ch-t-k-c-ng/05-UAT.md` — khuôn biên bản nghiệm thu,
      gồm cả cách nói rõ **điều biên bản không nhận là đã làm**.
    - Năm file SUMMARY của 05-2-01…05-2-05.
  </read_first>
  <action>
    **(a) `05-2-UAT.md`** — với chủ dự án, chạy qua năm tiêu chí của phase trên hệ thống thật,
    mỗi tiêu chí ghi **quan sát cụ thể** (con số tiền thật, thông điệp thật), không ghi "đạt".

    Bắt buộc có một mục riêng: **một phép tính tay đối chiếu**. Chọn một nhân viên thật, tính
    lương bằng giấy bút từ mức lương + số liệu công + các khoản, rồi so với con số hệ thống ra.
    Đây là bài kiểm mà không test tự động nào thay được — nó kiểm chính **định nghĩa** của phép
    tính, không chỉ tính nhất quán của mã.

    Ghi rõ **sáu giới hạn đã biết**:
    1. không thuế TNCN (PAY-02 vẫn ở V3);
    2. không BHXH/BHYT/BHTN (PAY-03 vẫn ở V3);
    3. nhân viên **chưa xem được lương của mình** (PAY-05 vẫn ở V3);
    4. không có khoản một-lần — thưởng/tạm ứng phải tạo khoản rồi tắt (D-40a);
    5. phạt đi muộn không phân bậc theo số phút (D-41);
    6. **mọi `admin` xem được lương của mọi người**, kể cả của nhau (D-44).

    **(b)** cập nhật `ROADMAP.md` (Phase 5.2 → Complete, 6/6, bảng Progress), `REQUIREMENTS.md`
    (PAY-01, PAY-04, PAY-06 → Complete), `STATE.md` (chuyển sang Phase 6, cập nhật Blockers và
    khối Decisions với D-36…D-45).
  </action>
  <acceptance_criteria>
    - `05-2-UAT.md` tồn tại với năm mục, mỗi mục có quan sát cụ thể chứ không phải chữ "đạt".
    - Mục **phép tính tay đối chiếu** có đủ: mức lương, số liệu công, từng khoản, con số tay, con số hệ thống, và chúng **bằng nhau**.
    - Sáu giới hạn đã biết được liệt kê tường minh.
    - `ROADMAP.md` Phase 5.2 đánh dấu `[x]` đủ 6 plan; bảng Progress cập nhật.
    - `REQUIREMENTS.md` ba dòng chuyển Complete.
    - `npm run typecheck && npm run lint && npm test && npm run build` thoát 0 lần cuối.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>
  <done>Phase đóng bằng một phép tính tay khớp với máy, và giới hạn đã biết được viết ra chứ không để người sau tự phát hiện.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cổng tự động → mã nguồn tương lai | Thứ duy nhất của phase còn hiệu lực sau khi phase kết thúc |
| Biên bản nghiệm thu → trạng thái thật | Một biên bản ghi "đạt" mà không có phép tính tay là một lời khẳng định trống |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-2-06-01 | Tampering | Một mẫu số dự phòng tiện tay ở plan tương lai → doanh nghiệp chưa khai bỗng có lương tính bằng con số không ai chọn | high | mitigate | Cổng quét + kiểm răng; tối đa 3 miễn trừ, mỗi mục có lý do |
| T-05-2-06-02 | Repudiation | Phase đánh dấu hoàn thành khi phép tính chưa từng được đối chiếu bằng tay | high | mitigate | Task 3 là checkpoint chặn; biên bản đòi một phép tính tay khớp |
| T-05-2-06-03 | Denial of Service | Kịch bản e2e tạo dữ liệu rác trên môi trường dev và không dọn | medium | mitigate | Bước dọn cuối; acceptance criteria kiểm `git status` và trạng thái database |
| T-05-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Cổng chặn con số tiền nhúng cứng chạy trong `npm test` và đã được chứng minh có răng
- `npm run test:e2e-payroll` thoát 0, gồm cặp khẳng định ngược nhau ở bản chốt
- Năm tiêu chí có biên bản nghiệm thu với quan sát cụ thể và một phép tính tay khớp
- Sáu giới hạn đã biết được viết ra
- ROADMAP / REQUIREMENTS / STATE phản ánh đúng trạng thái sau phase
</verification>

<success_criteria>
Phase kết thúc với một người canh gác tự động cho bất biến trung tâm của nó, một con số lương
đã được đối chiếu bằng giấy bút, và một danh sách giới hạn đã biết được viết ra thay vì để
người sau tự vấp phải.
</success_criteria>

<output>
Create `.planning/phases/05-2-t-nh-l-ng-do-doanh-nghi-p-t-c-u-h-nh/05-2-06-SUMMARY.md` when done
</output>
