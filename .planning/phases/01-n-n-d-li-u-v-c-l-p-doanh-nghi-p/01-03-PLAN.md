---
phase: 01-n-n-d-li-u-v-c-l-p-doanh-nghi-p
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .env.local
  - .env.example
  - .gitignore
  - docs/env
  - package.json
  - scripts/check-bundle-secrets.mjs
autonomous: false
requirements: [AUTH-06]

user_setup:
  - service: supabase
    why: "D-10 — thu hồi cặp khóa legacy HS256 và chuyển hẳn sang mô hình khóa hiện hành. Nếu Management API không có đường tắt cặp khóa legacy thì đây là thao tác dashboard."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens → Generate new token (chỉ cần nếu dùng đường Management API)"
    dashboard_config:
      - task: "Tắt / thu hồi cặp khóa legacy `anon` và `service_role` của project `ujvgagujfsdrlmjdhooi`, giữ lại cặp `sb_publishable_` / `sb_secret_`"
        location: "Supabase Dashboard → Project Settings → API Keys → mục Legacy API keys"

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Gọi REST API của project bằng khóa legacy cũ trả về 401, không còn 200."
    - "`.env.local` chỉ còn cặp khóa mô hình hiện hành (`SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`) cùng `NEXT_PUBLIC_SUPABASE_URL` và `POSTGRES_URL_NON_POOLING`; không còn biến khóa legacy nào."
    - "Sau `next build`, không artifact nào trong `.next/static` chứa giá trị khóa bí mật hay tên biến khóa bí mật."
    - "Thiếu một biến bắt buộc trong `.env.local` làm `npm run test:rls` dừng ngay và in đúng tên biến còn thiếu, thay vì chạy tiếp với chuỗi rỗng."
    - "`docs/env` không còn là nơi giữ khóa; `.env.local` là nguồn duy nhất và đã nằm ngoài git."
    - "`.env.example` liệt kê đủ tên biến bắt buộc mà không chứa giá trị thật nào."
  artifacts:
    - path: "scripts/check-bundle-secrets.mjs"
      provides: "Quét artifact build tìm khóa bí mật lọt xuống client"
    - path: ".env.example"
      provides: "Danh sách tên biến bắt buộc, không giá trị"
  key_links:
    - from: "package.json"
      to: "scripts/check-bundle-secrets.mjs"
      via: "npm script check:secrets"
      pattern: "check:secrets"
---

<!-- planner-discipline-allow: SUPABASE_SERVICE_ROLE_KEY -->
<!-- planner-discipline-allow: SUPABASE_ANON_KEY -->
<!-- planner-discipline-allow: SUPABASE_JWT_SECRET -->
<!-- planner-discipline-allow: sb_secret_ -->

<objective>
Đóng AUTH-06: thu hồi cặp khóa legacy đang nằm plaintext trong `docs/env`, chuyển hẳn sang mô hình khóa hiện
hành, và dựng một lệnh kiểm tra tự động chứng minh không khóa bí mật nào lọt xuống client bundle.

Purpose: D-10 gộp việc này vào Phase 1 vì cấu hình môi trường chỉ phải sửa một lần thay vì hai. Làm xong trước
khi Phase 2 nối Supabase Auth nghĩa là không có giai đoạn nào ứng dụng thật chạy trên khóa đã lộ.

Output: `.env.local` sạch, `.env.example` làm hợp đồng biến môi trường, `scripts/check-bundle-secrets.mjs` và
npm script `check:secrets`, cộng bằng chứng khóa cũ trả 401.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chuyển sang mô hình khóa hiện hành và thu hồi cặp legacy</name>
  <precondition>`.env.local` tồn tại ở gốc repo và chứa `NEXT_PUBLIC_SUPABASE_URL` khác rỗng (plan 01-01 đã tạo)</precondition>
  <read_first>
    - `docs/env` — danh sách biến hiện có, gồm cả cặp legacy lẫn cặp mới đang song song
    - `.gitignore` dòng liên quan tới `.env` và `docs/env`
    - `.planning/PROJECT.md` §"Trạng thái project Supabase" — project `ujvgagujfsdrlmjdhooi` đang chạy song song hai mô hình khóa, ký JWT bất đối xứng đã bật
    - `.planning/research/STACK.md` §Infrastructure — lý do không cấp lại khóa theo mô hình cũ khi xoay
    - `.planning/research/PITFALLS.md` §"Pitfall 3"
  </read_first>
  <files>.env.local, .env.example, docs/env, .gitignore</files>
  <action>
    **Dọn `.env.local`.** Giữ đúng bốn biến: `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`,
    `SUPABASE_SECRET_KEY`, `POSTGRES_URL_NON_POOLING`. Xóa mọi biến thuộc cặp khóa legacy và biến bí mật ký JWT
    còn sót (`SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_JWT_SECRET`) cùng các biến `POSTGRES_*`
    không dùng tới. Chỉ `NEXT_PUBLIC_SUPABASE_URL` được mang tiền tố `NEXT_PUBLIC_` — không bao giờ đặt tiền tố
    đó cho một khóa bí mật, vì Next.js nhúng thẳng mọi biến `NEXT_PUBLIC_*` vào bundle trình duyệt.

    **Tạo `.env.example`.** Liệt kê đúng bốn tên biến trên với giá trị rỗng và một dòng chú thích mỗi biến nói
    lấy ở đâu. File này **được** commit (`.gitignore` đã có ngoại lệ `!.env.example`); nó là hợp đồng biến môi
    trường cho CI, cho máy mới, và cho Phase 2.

    **Bỏ `docs/env` khỏi vai trò kho khóa.** Xóa file và thay bằng một dòng ghi chú trong `docs/` trỏ sang
    `.env.example`, hoặc rút gọn `docs/env` thành đúng dòng trỏ đó. Giữ nguyên các dòng `.gitignore` đang phủ
    `docs/env` và `docs/env.*` để dấu vết cũ không quay lại. Ghi vào SUMMARY rằng khóa trong file cũ phải được
    coi là đã lộ và chỉ mất hiệu lực nhờ bước thu hồi bên dưới, không nhờ việc xóa file.

    **Thu hồi cặp legacy — tự động trước.** Thử qua Supabase Management API với `SUPABASE_ACCESS_TOKEN`: liệt kê
    endpoint hiện có bằng cách gọi `https://api.supabase.com/v1/projects/{ref}/api-keys` cho project ref
    `ujvgagujfsdrlmjdhooi` và tìm đường tắt/thu hồi cặp legacy. Nếu API có đường đó thì gọi và xác nhận bằng
    bước kiểm tra bên dưới. Nếu API không có đường đó (trả 404 hoặc không liệt kê khóa legacy) thì dừng lại và
    để Task 2 xử lý — **không** tự cấp lại khóa theo mô hình cũ trong bất kỳ trường hợp nào (D-10).

    **Kiểm chứng.** Gọi `GET {NEXT_PUBLIC_SUPABASE_URL}/rest/v1/` kèm header `apikey` mang giá trị khóa legacy
    cũ (đọc từ bản sao `docs/env` trước khi xóa, giữ trong bộ nhớ của phiên chạy, không ghi ra file mới) và
    khẳng định mã trả về là 401. Sau đó gọi lại cùng endpoint với khóa publishable mới và khẳng định không phải
    401. Ghi cả hai mã trả về vào SUMMARY. Đây là kiểm chứng thủ công theo `01-VALIDATION.md` §Manual-Only vì
    không có cách tự động khẳng định "khóa cũ đã chết" mà không tự lưu lại khóa cũ ở đâu đó.

    Vẫn dùng project dev duy nhất `ujvgagujfsdrlmjdhooi` (D-01); không tạo project mới ở bước này.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('fs').readFileSync('.env.local','utf8');const bad=['SUPABASE_ANON_KEY','SUPABASE_SERVICE_ROLE_KEY','SUPABASE_JWT_SECRET'].filter(k=>s.includes(k));if(bad.length){console.error(bad.join(','));process.exit(1)}"` thoát 0.
    - `node -e "const s=require('fs').readFileSync('.env.local','utf8');const need=['NEXT_PUBLIC_SUPABASE_URL','SUPABASE_PUBLISHABLE_KEY','SUPABASE_SECRET_KEY','POSTGRES_URL_NON_POOLING'];const miss=need.filter(k=>!new RegExp('^'+k+'=.+','m').test(s));if(miss.length){console.error(miss.join(','));process.exit(1)}"` thoát 0.
    - `node -e "const s=require('fs').readFileSync('.env.local','utf8');const pub=[...s.matchAll(/^NEXT_PUBLIC_[A-Z_]+/gm)].map(m=>m[0]);if(pub.length!==1||pub[0]!=='NEXT_PUBLIC_SUPABASE_URL')process.exit(1)"` thoát 0 — đúng một biến công khai.
    - `test -f .env.example` thoát 0 và `node -e "const s=require('fs').readFileSync('.env.example','utf8');if(/=\s*\S/.test(s))process.exit(1)"` thoát 0 — file mẫu không chứa giá trị thật nào.
    - `git check-ignore .env.local` in ra `.env.local`; `git ls-files --error-unmatch .env.example` thoát 0.
    - `npm run test:rls` vẫn thoát 0 sau khi dọn `.env.local` (chứng minh không xóa nhầm biến `POSTGRES_URL_NON_POOLING`).
    - Ghi vào SUMMARY: mã HTTP khi gọi `/rest/v1/` bằng khóa legacy cũ, và mã HTTP khi gọi bằng khóa publishable mới.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.env.local','utf8');const bad=['SUPABASE_ANON_KEY','SUPABASE_SERVICE_ROLE_KEY','SUPABASE_JWT_SECRET'].filter(k=>s.includes(k));const need=['NEXT_PUBLIC_SUPABASE_URL','SUPABASE_PUBLISHABLE_KEY','SUPABASE_SECRET_KEY','POSTGRES_URL_NON_POOLING'];const miss=need.filter(k=>!new RegExp('^'+k+'=.+','m').test(s));if(bad.length||miss.length)process.exit(1)" &amp;&amp; npm run test:rls</automated>
  </verify>
  <done>`.env.local` chỉ còn khóa mô hình hiện hành và nằm ngoài git; `.env.example` được commit làm hợp đồng biến môi trường; `docs/env` không còn là kho khóa; mã trả về của khóa cũ và khóa mới đã ghi vào SUMMARY.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Tắt cặp khóa legacy trên dashboard (chỉ khi Management API không làm được)</name>
  <read_first>
    - SUMMARY tạm của Task 1 — mã HTTP khi gọi bằng khóa legacy cũ
  </read_first>
  <what-built>Task 1 đã dọn `.env.local`, tạo `.env.example`, gỡ `docs/env` khỏi vai trò kho khóa, và đã thử thu hồi cặp legacy qua Supabase Management API.</what-built>
  <action>Người dùng tắt cặp khóa legacy của project `ujvgagujfsdrlmjdhooi` trên dashboard Supabase. Task này chỉ chạy khi Task 1 ghi nhận khóa legacy vẫn trả HTTP 200 — nghĩa là Management API không có đường thu hồi cặp legacy. Đây là thao tác duy nhất trong phase không có CLI hay API thay thế.</action>
  <instructions>
    **Bỏ qua task này nếu Task 1 đã ghi nhận khóa legacy trả 401.**

    Nếu khóa legacy vẫn trả 200:
    1. Mở https://supabase.com/dashboard/project/ujvgagujfsdrlmjdhooi/settings/api-keys
    2. Vào mục "Legacy API keys" — cặp khóa JWT đời cũ của project.
    3. Tắt / thu hồi cả hai khóa trong mục đó. Không cấp lại khóa theo mô hình cũ — chỉ giữ cặp khóa publishable và secret của mô hình hiện hành (D-10).
    4. Xác nhận mục "API Keys" chỉ còn cặp mô hình hiện hành.
  </instructions>
  <verification>
    Gọi `GET {NEXT_PUBLIC_SUPABASE_URL}/rest/v1/` với header `apikey` mang khóa legacy cũ và xác nhận mã trả về là HTTP 401. Gọi lại cùng endpoint với khóa publishable mới và xác nhận không phải 401.
  </verification>
  <acceptance_criteria>Gọi `GET {NEXT_PUBLIC_SUPABASE_URL}/rest/v1/` với header `apikey` mang khóa legacy cũ trả về HTTP 401. Mã này được ghi vào SUMMARY của plan.</acceptance_criteria>
  <resume-signal>Gõ "approved" khi khóa legacy đã trả 401, hoặc mô tả vấn đề gặp phải</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Lệnh kiểm tra khóa bí mật lọt xuống client bundle</name>
  <precondition>`.env.local` chứa đủ bốn biến bắt buộc (Task 1 đã xong)</precondition>
  <read_first>
    - `package.json` — khối `scripts`, quy ước đặt tên phẳng
    - `next.config.ts` — cấu hình build hiện tại
    - `.planning/research/PITFALLS.md` §"Pitfall 3" §"Security Mistakes"
    - `scripts/db.mjs` (plan 01-01) — quy ước viết script Node chỉ dùng built-in
  </read_first>
  <files>scripts/check-bundle-secrets.mjs, package.json</files>
  <action>
    Viết `scripts/check-bundle-secrets.mjs` — module ESM chỉ dùng built-in Node 22 (`node:fs`, `node:path`,
    `node:process`), không thêm dependency.

    Hành vi: đọc `.env.local` bằng `process.loadEnvFile`; dựng danh sách chuỗi cấm gồm (a) **giá trị** của mọi
    biến bí mật, tức mọi biến không mang tiền tố `NEXT_PUBLIC_` và có giá trị dài hơn 12 ký tự, và (b) các
    **tiền tố khóa bí mật** dạng chuỗi cố định của Supabase cùng tên các biến khóa legacy. Duyệt đệ quy
    `.next/static` (và `.next/server/app` nếu tồn tại) đọc mọi file dạng văn bản; với mỗi chuỗi cấm tìm thấy,
    in đường dẫn file, tên chuỗi bị lộ (không in giá trị) và thoát mã 1. Không tìm thấy gì thì in số file đã
    quét rồi thoát 0.

    Hai điều kiện làm cho lệnh không xanh rỗng: nếu thư mục `.next/static` không tồn tại thì thoát mã 1 với
    thông điệp yêu cầu chạy build trước; nếu số file quét được bằng 0 thì cũng thoát mã 1. Một lệnh quét 0 file
    và báo "không tìm thấy gì" là một cổng đo lường số không.

    Thêm script `check:secrets` vào `package.json` gọi `node scripts/check-bundle-secrets.mjs`, giữ đúng quy ước
    tên phẳng của bốn script đã có.

    Chứng minh cổng có răng: tạm thêm vào một Client Component một biểu thức đọc `process.env.SUPABASE_SECRET_KEY`
    và gán vào một thuộc tính hiển thị, chạy `npm run build`, chạy `npm run check:secrets` và xác nhận nó đỏ;
    gỡ thay đổi tạm, build lại, chạy lại và xác nhận xanh. Ghi kết quả cả hai lần vào SUMMARY.
  </action>
  <acceptance_criteria>
    - `npm run build` thoát 0.
    - `npm run check:secrets` thoát 0 và in ra số file đã quét lớn hơn 0.
    - Sau khi xóa thư mục `.next`, `npm run check:secrets` thoát khác 0 với thông điệp yêu cầu build trước.
    - Với thay đổi tạm làm rò khóa xuống Client Component: `npm run build && npm run check:secrets` thoát khác 0; sau khi gỡ thay đổi và build lại thì thoát 0. Cả hai kết quả ghi vào SUMMARY.
    - `node -e "const p=require('./package.json');process.exit(p.scripts['check:secrets']?0:1)"` thoát 0.
    - `node -e "const {execSync}=require('node:child_process');const b=execSync('git merge-base HEAD origin/main').toString().trim();const base=JSON.parse(execSync('git show '+b+':package.json').toString());const cur=require('./package.json');const k=o=>Object.keys({...o.dependencies,...o.devDependencies}).sort().join(',');if(k(base)!==k(cur))process.exit(1)"` thoát 0 — tập dependency không đổi so với gốc nhánh, tức phase không thêm gói nào.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run check:secrets</automated>
  </verify>
  <done>Có một lệnh chạy được bất cứ lúc nào để chứng minh không khóa bí mật nào nằm trong artifact client; lệnh đó đã được chứng minh là biết đỏ khi thật sự có rò rỉ, và biết đỏ khi chưa có build để quét.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo / lịch sử git → khóa Supabase | Khóa từng nằm plaintext trong `docs/env`; phải coi là đã lộ |
| Server Next.js → bundle trình duyệt | Mọi biến `NEXT_PUBLIC_*` bị nhúng thẳng vào artifact client |
| Khóa legacy đang còn hiệu lực → REST API của project | Khóa cũ vẫn trả 200 tính đến 2026-07-31; đây là ranh giới AUTH-06 phải đóng |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Elevation of Privilege | khóa legacy bỏ qua RLS còn hiệu lực | critical | mitigate | Thu hồi qua Management API, dashboard là đường dự phòng; kiểm chứng bằng lời gọi thật tới `/rest/v1/` kỳ vọng 401, ghi mã trả về vào SUMMARY |
| T-01-12 | Information Disclosure | khóa bí mật lọt xuống client bundle | critical | mitigate | `scripts/check-bundle-secrets.mjs` quét `.next/static` tìm giá trị của mọi biến không mang tiền tố `NEXT_PUBLIC_`; script đỏ khi chưa có build hoặc quét 0 file nên không xanh rỗng |
| T-01-13 | Information Disclosure | khóa mới lọt vào commit | high | mitigate | Khóa chỉ nằm ở `.env.local` đã bị `.gitignore` phủ; `.env.example` được commit và acceptance criteria khẳng định nó không chứa giá trị nào |
| T-01-14 | Spoofing | cấp lại khóa theo mô hình cũ khi xoay, tái lập rủi ro cũ | high | mitigate | D-10 cấm; action nêu rõ không cấp lại mô hình cũ trong mọi trường hợp; `.env.local` chỉ chấp nhận đúng bốn tên biến |
| T-01-15 | Repudiation | khóa cũ vẫn còn trong lịch sử git của repo | medium | accept | Việc thu hồi làm khóa cũ mất hiệu lực nên bản sao trong lịch sử vô hại; viết lại lịch sử git nằm ngoài phạm vi phase và rủi ro tồn dư đã được ghi vào SUMMARY |
| T-01-SC | Tampering | cài gói qua npm/pip/cargo | high | mitigate | Plan không cài gói nào — `scripts/check-bundle-secrets.mjs` chỉ dùng built-in Node 22; acceptance criteria ghi lại số lượng dependency để đối chiếu |
</threat_model>

## Artifacts this plan produces

**File mới** — `.env.example`, `scripts/check-bundle-secrets.mjs`

**File thay đổi** — `.env.local` (rút còn 4 biến), `docs/env` (thôi vai trò kho khóa), `package.json`

**Biến môi trường chốt** — `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, `POSTGRES_URL_NON_POOLING`

**npm script** — `check:secrets`

<verification>
- Khóa legacy trả 401; khóa publishable mới không trả 401.
- `.env.local` chỉ còn bốn biến bắt buộc, đúng một biến mang tiền tố công khai.
- `npm run build && npm run check:secrets` thoát 0; lệnh đã được chứng minh biết đỏ khi có rò rỉ thật.
- `npm run test:rls` vẫn xanh sau khi dọn biến môi trường.
</verification>

<success_criteria>
- Tiêu chí nghiệm thu số 4 của phase đạt: khóa cũ không dùng được, khóa mới ngoài git và không có trong client bundle.
- Có lệnh tái chạy được để kiểm tra rò rỉ ở mọi phase sau, không phải làm lại bằng mắt.
</success_criteria>

<output>
Create `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-03-SUMMARY.md` when done
</output>
