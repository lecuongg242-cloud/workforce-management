---
phase: 01-n-n-d-li-u-v-c-l-p-doanh-nghi-p
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - supabase/seed.sql
  - supabase/tests/05_seed_fixture.sql
  - supabase/tests/run-all.sql
autonomous: true
requirements: [DATA-01, DATA-07]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Hai bộ dữ liệu Ngọc Phát và Bình Minh tồn tại đầy đủ trên Supabase: 2 công ty, 9 phòng ban, 7 ca làm việc, 40 nhân viên (28 + 12), 12 yêu cầu công trong đó 8 yêu cầu ở trạng thái `pending`, và lịch sử chấm công cả tháng cho từng nhân viên."
    - "Mỗi doanh nghiệp có đúng một ca đêm: `cty-01` có ca 22:00–06:00 và `cty-02` có ca 18:00–06:00; cột sinh `overnight` của cả hai bằng true."
    - "Ngày tháng trong seed trượt theo ngày chạy seed chứ không chốt cứng: ngày công lớn nhất cách hôm nay (theo `Asia/Ho_Chi_Minh`) không quá 1 ngày, và lịch sử phủ ít nhất 28 ngày công khác nhau trong 35 ngày gần nhất (D-07)."
    - "Bản ghi chấm công của ca đêm mang `work_date` bằng ngày bắt đầu ca và `worked_minutes` bằng đúng độ dài ca trừ giờ nghỉ theo `public.tf_shift_minutes` (D-08)."
    - "Chạy `npm run db:seed` hai lần liên tiếp cho ra cùng số dòng trên mọi bảng — seed chạy lại được, không nhân đôi dữ liệu."
    - "Không cột enum nào chứa chuỗi tiếng Việt; nhãn hiển thị vẫn ở `src/lib/constants.ts` và không lọt vào database."
    - "Bộ test cô lập của các plan trước vẫn xanh sau khi seed đầy đủ được nạp."
  artifacts:
    - path: "supabase/seed.sql"
      provides: "Bộ dữ liệu hai doanh nghiệp đầy đủ, ngày tháng trượt theo ngày chạy"
      contains: "Bình Minh"
    - path: "supabase/tests/05_seed_fixture.sql"
      provides: "Test khẳng định độ đầy đủ, tính trượt ngày và tính chạy-lại-được của seed"
      contains: "tf_work_date"
  key_links:
    - from: "supabase/seed.sql"
      to: "supabase/migrations/0003_enums_time.sql"
      via: "mốc thời gian của mọi bản ghi chấm công sinh từ public.tf_work_date(now()) chứ không từ hằng số cố định"
      pattern: "tf_work_date"
    - from: "supabase/tests/05_seed_fixture.sql"
      to: "supabase/seed.sql"
      via: "test đếm và đối chiếu chính các dòng seed vừa chèn"
      pattern: "cty-0"
---

<objective>
Port trọn bộ dữ liệu mẫu của V1 sang `supabase/seed.sql` với ngày tháng trượt theo ngày chạy, và khẳng định
bằng test rằng bộ dữ liệu đó đầy đủ, trượt đúng và chạy lại được.

Purpose: seed vừa là fixture đối chiếu cho test cô lập, vừa là dữ liệu thật để Phase 2 kiểm tra từng màn hình
V1 mà không phải nhập tay. Nếu ngày tháng chốt cứng như `REFERENCE_DATE` của V1 thì sau vài tháng dashboard
"7 ngày gần đây" và kỳ công đang mở đều trống, và Phase 2 sẽ tưởng mình làm hỏng truy vấn.

Output: `supabase/seed.sql` hoàn chỉnh và `supabase/tests/05_seed_fixture.sql`.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-CONTEXT.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-PATTERNS.md
@src/lib/types/domain.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port đầy đủ bộ seed hai doanh nghiệp với ngày tháng trượt theo ngày chạy</name>
  <precondition>`psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_tables where schemaname='public'"` trả về `13` (plan 01-05 đã áp toàn bộ schema)</precondition>
  <read_first>
    - `src/lib/mock/seed.ts` — nguồn dữ liệu: `seedCompanies` (2), `seedDepartments` (5) và `seedDepartments2` (4), `seedShifts` (4) và `seedShifts2` (3), `employeeSeeds` (28) và `employee2Seeds` (12), `seedRequests` (9) và `seedRequests2` (3)
    - `src/lib/mock/db.ts` — `buildMonthlyHistory` (chu kỳ 8 ngày sinh giờ vào/ra và trạng thái), `computeWorkedMinutes`, `createInitialDatabase` (thứ tự lắp ráp: công ty → phòng ban → ca → nhân viên → chấm công → yêu cầu)
    - `src/lib/constants.ts` — `REFERENCE_DATE` là hằng số **không** được port sang SQL; nhãn tiếng Việt cũng không vào database
    - `supabase/seed.sql` — phần seed tối thiểu do plan 01-01, 01-04, 01-05 dựng, sẽ được thay bằng bản đầy đủ
    - `supabase/migrations/0003_enums_time.sql` — `public.tf_work_date`, `public.tf_shift_minutes`, `public.tf_tz`
  </read_first>
  <files>supabase/seed.sql</files>
  <action>
    Viết lại `supabase/seed.sql` thành bộ dữ liệu đầy đủ theo D-06, giữ nguyên phần đầu `truncate` phủ cả 13
    bảng và giữ nguyên bốn user cố định `00000000-0000-0000-0000-00000000000{1..4}` mà helper test dựa vào.

    **Mốc thời gian trượt (D-07).** Định nghĩa mốc neo một lần ở đầu file bằng `public.tf_work_date(now())` —
    hôm nay tính theo `Asia/Ho_Chi_Minh` — rồi sinh mọi ngày trong seed bằng phép trừ số ngày từ mốc đó. Không
    viết ngày tháng cố định nào cho dữ liệu vận hành (chấm công, yêu cầu công, kỳ công, `last_accessed_at`).
    Ngày sinh và ngày vào làm của nhân viên là dữ liệu hồ sơ nên giữ nguyên giá trị tuyệt đối từ `seed.ts`.

    **Nội dung, theo thứ tự lắp ráp của `createInitialDatabase`:**
    1. `auth.users` — bốn user cố định, `on conflict (id) do nothing`.
    2. `companies` — `cty-01` Ngọc Phát và `cty-02` Bình Minh, giá trị lấy từ `seedCompanies`. Cột `role`,
       `employeeCount`, `lastAccessedAt` của interface `Company` không có trong bảng; `last_accessed_at` đi vào
       `memberships`.
    3. `memberships` — bốn dòng như plan 01-01 (owner của mỗi bên, một thành viên kép, một user không membership),
       `last_accessed_at` đặt bằng `now()`.
    4. `departments` — 9 dòng (5 của `cty-01`, 4 của `cty-02`). `manager_id` để NULL ở bước này.
    5. `shifts` — 7 dòng (4 của `cty-01`, 3 của `cty-02`), chép nguyên `start_time`, `end_time`, `break_minutes`,
       `late_tolerance_minutes`, `working_days`, `status` từ `seedShifts` và `seedShifts2`. **Không** chèn cột
       `overnight` — đó là cột sinh, tự suy từ giờ.
    6. `employees` — 40 dòng (28 + 12). Nối `department_id` và `shift_id` theo đúng ánh xạ trong `seed.ts`. Gán
       `user_id` cho hai nhân viên đầu tiên của mỗi công ty, trỏ tới user owner tương ứng, để Phase 2 có sẵn cặp
       tài khoản ↔ nhân viên.
    7. `update departments set manager_id = ...` — điền tham chiếu quản lý sau khi nhân viên đã tồn tại, đúng
       cách `seed.ts` đặt `managerId` cho phòng ban trước khi nhân viên được định nghĩa.
    8. `attendance_records` — sinh lịch sử một tháng cho từng nhân viên bằng `generate_series` lùi từ mốc neo,
       tái tạo chu kỳ 8 ngày của `buildMonthlyHistory` trong `src/lib/mock/db.ts`: giờ vào/ra và `status` xoay
       theo chỉ số ngày, bỏ qua ngày không nằm trong `working_days` của ca. `work_date` là ngày bắt đầu ca;
       `check_in_at` và `check_out_at` là `timestamptz` dựng từ `work_date` cộng giờ theo `public.tf_tz()`, và
       với ca qua đêm thì `check_out_at` rơi sang ngày hôm sau trong khi `work_date` vẫn giữ ngày bắt đầu (D-08).
       `worked_minutes` tính bằng `public.tf_worked_minutes(check_in_at, check_out_at, break_minutes)`. Chèn có
       chủ đích ít nhất một bản ghi `check_out_at` NULL với `status = 'missing_checkout'` cho mỗi công ty — trạng
       thái mà dữ liệu thật sinh ra được còn seed trong bộ nhớ của V1 thì không.
    9. `work_requests` — 12 dòng (9 + 3), trong đó đúng 8 dòng `status = 'pending'` như `seed.ts`. `from_date`
       và `to_date` trượt quanh mốc neo; `from_time` và `to_time` chỉ có giá trị với `attendance_supplement` và
       `time_adjustment`, còn lại NULL.
    10. `periods` — kỳ tháng dương lịch hiện tại cho mỗi công ty (D-09), cộng kỳ tháng trước ở trạng thái `open`
        để Phase 5 có dữ liệu chuyển trạng thái.
    11. `work_sites`, `overtime_rules`, `audit_log` — giữ các dòng tối thiểu như plan 01-05, mốc thời gian đổi
        sang trượt theo mốc neo. `holidays` vẫn để rỗng ở cả hai công ty (không áp cứng ngày lễ nào).

    **Chạy lại được.** Toàn bộ file chạy trong một transaction, bắt đầu bằng `truncate ... restart identity
    cascade` nên chạy hai lần liên tiếp cho cùng kết quả. Không dùng `on conflict do update` để che giấu việc
    chèn trùng.

    Giá trị enum chỉ tiếng Anh. Tên người, tên phòng ban, tên ca, địa chỉ, lý do nghỉ là dữ liệu tiếng Việt bình
    thường và giữ nguyên từ `seed.ts`.
  </action>
  <acceptance_criteria>
    - `npm run db:seed` thoát 0.
    - Các phép đếm sau trả đúng: `companies` = `2`; `departments` = `9`; `shifts` = `7`; `employees` = `40`; `employees where company_id='cty-01'` = `28`; `employees where company_id='cty-02'` = `12`; `work_requests` = `12`; `work_requests where status='pending'` = `8`; `holidays` = `0`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from public.shifts where overnight"` trả về `2`, và mỗi công ty có đúng 1 dòng trong đó.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select public.tf_work_date(now()) - max(work_date) from public.attendance_records"` trả về `0` hoặc `1`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(distinct work_date) from public.attendance_records where work_date > public.tf_work_date(now()) - 35"` trả về số lớn hơn hoặc bằng `28`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from public.attendance_records where check_out_at is null"` lớn hơn hoặc bằng `2`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from public.attendance_records a where a.check_in_at is not null and a.work_date <> public.tf_work_date(a.check_in_at)"` trả về `0`.
    - Chạy `npm run db:seed` lần thứ hai rồi đếm lại `employees` và `attendance_records` — cùng con số như lần đầu.
    - `node -e "const s=require('fs').readFileSync('supabase/seed.sql','utf8');if(s.includes('2026-07-27'))process.exit(1)"` thoát 0 — không mang hằng ngày cố định của V1 sang.
    - `npm run test:rls` thoát 0 sau khi nạp seed đầy đủ.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:seed &amp;&amp; npm run db:seed &amp;&amp; npm run test:rls</automated>
  </verify>
  <done>Hai bộ dữ liệu Ngọc Phát và Bình Minh đầy đủ trên project dev với ngày tháng trượt theo ngày chạy seed; chạy lại seed không nhân đôi dữ liệu; bộ test cô lập vẫn xanh.</done>
</task>

<task type="auto">
  <name>Task 2: Test khẳng định độ đầy đủ, tính trượt ngày và quy ước ca đêm của seed</name>
  <precondition>`psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from public.employees"` trả về `40` (Task 1 đã chạy)</precondition>
  <read_first>
    - `supabase/seed.sql` (Task 1) — số lượng thực tế của từng bảng và cách sinh lịch sử chấm công
    - `supabase/tests/04_isolation_v2.sql` — khuôn file test, cách mở transaction và đóng bằng `finish(true)`
    - `.planning/ROADMAP.md` §"Phase 1" tiêu chí 1 và 5
  </read_first>
  <files>supabase/tests/05_seed_fixture.sql, supabase/tests/run-all.sql</files>
  <action>
    Viết `supabase/tests/05_seed_fixture.sql` — file pgTAP chạy với vai trò mặc định của kết nối (không giả lập
    đăng nhập), vì đây là test về **độ đầy đủ của fixture** chứ không phải về cô lập.

    Nhóm khẳng định 1 — độ đầy đủ (tiêu chí nghiệm thu số 1 của phase): số dòng của `companies`, `departments`,
    `shifts`, `employees`, `work_requests` bằng đúng các con số đã chốt ở Task 1; `employees` chia đúng 28 cho
    `cty-01` và 12 cho `cty-02`; đúng 8 `work_requests` ở trạng thái `pending`; **mỗi** bảng trong 12 bảng thuộc
    phạm vi doanh nghiệp trừ `holidays` đều có ít nhất một dòng cho `cty-01` và ít nhất một dòng cho `cty-02`;
    `holidays` rỗng ở cả hai và điều đó được khẳng định tường minh là trạng thái mong muốn.

    Nhóm khẳng định 2 — trượt ngày (D-07): hiệu giữa `public.tf_work_date(now())` và `max(work_date)` của
    `attendance_records` không quá 1; số ngày công khác nhau trong 35 ngày gần nhất ít nhất 28; không bản ghi
    chấm công nào có `work_date` ở tương lai.

    Nhóm khẳng định 3 — ca đêm (tiêu chí nghiệm thu số 5, D-08): mỗi công ty có đúng một ca `overnight`; với mỗi
    bản ghi chấm công thuộc ca đêm và có đủ hai mốc giờ, `work_date` bằng `public.tf_work_date(check_in_at)`,
    `check_out_at` lớn hơn `check_in_at`, và `worked_minutes` bằng `public.tf_shift_minutes(shift.start_time,
    shift.end_time, shift.break_minutes)`. Lặp lại toàn bộ nhóm này sau `set timezone to 'America/New_York'` để
    khẳng định kết quả không đổi khi máy chủ chạy múi giờ khác.

    Nhóm khẳng định 4 — nhãn hiển thị không lọt vào database: đếm số dòng `employees` có `status` không thuộc tập
    giá trị enum bằng 0 (kiểu enum đã bảo đảm, khẳng định này để ghi lại ý định); và đếm số dòng bất kỳ trong
    `attendance_records` có `location` bằng chuỗi rỗng bằng 0.

    Kết thúc bằng `select * from finish(true)` rồi `rollback`. Thêm `\ir 05_seed_fixture.sql` vào
    `supabase/tests/run-all.sql`, đặt sau `04_isolation_v2.sql`.
  </action>
  <acceptance_criteria>
    - `npm run test:db` thoát 0.
    - Output TAP chứa nguyên văn `overnight`, `work_date` và cả hai mã công ty `cty-01`, `cty-02`.
    - Sau khi tạm xóa một dòng `shifts` có `overnight` bằng true của `cty-02`, `npm run test:rls` thoát khác 0; chạy lại `npm run db:seed` thì thoát 0.
    - Sau khi tạm `update public.attendance_records set work_date = work_date + 1 where id = (select id from public.attendance_records limit 1)`, `npm run test:rls` thoát khác 0; chạy lại `npm run db:seed` thì thoát 0.
    - `node -e "const s=require('fs').readFileSync('supabase/tests/run-all.sql','utf8');const need=['00_rls_coverage','01_isolation_companies','02_time_overnight','03_isolation_core','04_isolation_v2','05_seed_fixture'];const miss=need.filter(x=>!s.includes(x));if(miss.length){console.error(miss.join(','));process.exit(1)}"` thoát 0.
    - Lần chạy workflow `db` gần nhất trên nhánh làm việc có kết luận `success` với seed đầy đủ.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:db</automated>
  </verify>
  <done>Độ đầy đủ của hai bộ dữ liệu, tính trượt theo ngày chạy, và quy ước ca đêm đều được khẳng định bằng lệnh; bộ test chứng minh được là biết đỏ bằng hai phép phá hoại có kiểm soát.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dữ liệu seed → tính hợp lệ của mọi test cô lập | Test cô lập chỉ có nghĩa khi cả hai phía đều có dòng; seed rỗng một phía làm test xanh rỗng |
| Mốc thời gian trong seed → nghĩa nghiệp vụ của "kỳ đang mở" | Ngày chốt cứng làm dữ liệu hết hạn im lặng sau vài tháng |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-27 | Information Disclosure | seed thiếu dòng ở một phía làm test cô lập xanh rỗng | high | mitigate | `05_seed_fixture.sql` khẳng định mỗi bảng thuộc phạm vi doanh nghiệp (trừ `holidays` cố ý rỗng) có ít nhất một dòng ở **cả hai** công ty, trước khi bất kỳ khẳng định "đọc 0 dòng" nào có giá trị |
| T-01-28 | Tampering | bản ghi ca đêm gắn sai ngày công làm lệch tổng hợp kỳ | high | mitigate | Nhóm khẳng định 3 đối chiếu `work_date` với `public.tf_work_date(check_in_at)` và `worked_minutes` với `public.tf_shift_minutes`, chạy lại dưới múi giờ phiên khác |
| T-01-29 | Repudiation | seed chốt ngày cứng khiến dữ liệu hết hạn mà không ai nhận ra | medium | mitigate | Khẳng định `max(work_date)` cách hôm nay không quá 1 ngày, cộng kiểm tra file seed không chứa hằng ngày cố định của V1 |
| T-01-30 | Information Disclosure | dữ liệu cá nhân thật lọt vào bộ seed công khai trong repo | medium | mitigate | Toàn bộ tên, email, số điện thoại chép từ `src/lib/mock/seed.ts` — dữ liệu hư cấu dựng sẵn cho V1, không phải người thật; email dùng miền test |
| T-01-31 | Denial of Service | seed sinh lịch sử một tháng cho 40 nhân viên làm chậm mỗi lần chạy test | low | accept | Khoảng 1.000 dòng chấm công, dưới ngưỡng độ trễ phản hồi 40 giây của `01-VALIDATION.md`; đo và ghi thời gian chạy vào SUMMARY |
| T-01-SC | Tampering | cài gói qua npm/pip/cargo | high | mitigate | Plan không cài gói nào — chỉ file SQL; kế thừa kết luận T-01-SC của plan 01-01 |
</threat_model>

## Artifacts this plan produces

**Dữ liệu seed** — 2 `companies` (`cty-01` Ngọc Phát, `cty-02` Bình Minh) · 4 `memberships` · 9 `departments` · 7 `shifts` (mỗi công ty một ca đêm) · 40 `employees` (28 + 12) · lịch sử `attendance_records` một tháng cho từng nhân viên · 12 `work_requests` (8 `pending`) · `periods` tháng hiện tại và tháng trước cho mỗi công ty · `work_sites`, `overtime_rules`, `audit_log` tối thiểu · `holidays` rỗng có chủ đích

**Mốc neo thời gian** — `public.tf_work_date(now())`; mọi ngày vận hành trong seed sinh bằng phép trừ từ mốc này (D-07)

**File mới** — `supabase/tests/05_seed_fixture.sql`

**File thay đổi** — `supabase/seed.sql` (bản đầy đủ thay bản tối thiểu), `supabase/tests/run-all.sql`

<verification>
- Các phép đếm khớp: 2 công ty, 9 phòng ban, 7 ca, 40 nhân viên, 12 yêu cầu (8 pending), 0 ngày lễ.
- `max(work_date)` cách hôm nay không quá 1 ngày; lịch sử phủ ít nhất 28 ngày công trong 35 ngày gần nhất.
- Bản ghi ca đêm gắn đúng ngày bắt đầu ca và ra đúng số phút, kể cả dưới múi giờ phiên khác.
- Chạy `npm run db:seed` hai lần cho cùng số dòng.
- `npm run test:db` xanh; workflow `db` xanh với seed đầy đủ.
</verification>

<success_criteria>
- Tiêu chí nghiệm thu số 1 của phase đạt trọn vẹn: hai bộ dữ liệu đầy đủ, phủ hết thực thể V1 lẫn bảng mới V2.
- Tiêu chí nghiệm thu số 5 được kiểm chứng trên dữ liệu thật, không chỉ trên hàm.
- Phase 2 có sẵn dữ liệu đủ dày để mở từng màn hình V1 mà không phải nhập tay.
</success_criteria>

<output>
Create `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-06-SUMMARY.md` when done
</output>
