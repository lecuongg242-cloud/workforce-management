---
phase: 01-n-n-d-li-u-v-c-l-p-doanh-nghi-p
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - supabase/migrations/0004_core_entities.sql
  - supabase/seed.sql
  - supabase/tests/03_isolation_core.sql
  - supabase/tests/run-all.sql
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Năm thực thể V1 còn lại — `departments`, `shifts`, `employees`, `attendance_records`, `work_requests` — tồn tại trên Postgres với đủ cột ánh xạ từ `src/lib/types/domain.ts` sang snake_case."
    - "Tài khoản Ngọc Phát đọc được lớn hơn 0 dòng của chính mình và đúng 0 dòng của Bình Minh trên từng bảng trong năm bảng đó; chiều ngược lại cũng vậy — mỗi khẳng định nêu đích danh tên bảng."
    - "Tài khoản Ngọc Phát chèn một dòng mang `company_id` của Bình Minh bị từ chối với SQLSTATE 42501 trên từng bảng; `update` và `delete` nhắm vào dòng của Bình Minh tác động đúng 0 dòng."
    - "Các cột nullable của `domain.ts` — `departments.manager_id`, `employees.manager_id`, `attendance_records.check_in_at`, `attendance_records.check_out_at`, `work_requests.from_time`, `work_requests.to_time`, `work_requests.reviewer_id`, `work_requests.review_note` — chèn được giá trị NULL mà không vi phạm ràng buộc nào."
    - "`employees` có index trên `(company_id, full_name, id)` nên truy vấn phân trang của Phase 2 có thứ tự ổn định kể cả khi hai nhân viên trùng tên."
    - "`shifts` có CHECK cấm `start_time = end_time`, và cột `overnight` là cột sinh từ `end_time < start_time` nên không thể lệch khỏi giờ thực của ca."
    - "`attendance_records.work_date` mang ngày bắt đầu ca theo `public.tf_work_date` (D-08), và ràng buộc duy nhất `(employee_id, work_date, shift_id)` ngăn hai bản ghi trùng cho cùng một ngày công."
    - "Mỗi bảng trong năm bảng có RLS bật và đúng bốn policy PERMISSIVE, mỗi lệnh một policy."
  artifacts:
    - path: "supabase/migrations/0004_core_entities.sql"
      provides: "Năm bảng thực thể V1 kèm RLS, policy, index và ràng buộc"
      contains: "tf_is_member"
    - path: "supabase/tests/03_isolation_core.sql"
      provides: "Test cô lập đọc chéo và ghi chéo theo từng bảng cho năm thực thể V1"
      contains: "attendance_records"
  key_links:
    - from: "supabase/migrations/0004_core_entities.sql"
      to: "supabase/migrations/0003_enums_time.sql"
      via: "cột work_date lấy giá trị mặc định/kiểm tra từ public.tf_work_date; cột overnight sinh từ so sánh giờ"
      pattern: "tf_work_date"
    - from: "supabase/tests/03_isolation_core.sql"
      to: "supabase/seed.sql"
      via: "test khẳng định trên chính dòng cty-01 và cty-02 do seed chèn"
      pattern: "cty-0"
---

<objective>
Nhân rộng cơ chế cô lập đã chứng minh ở plan 01-01 ra năm thực thể còn lại của V1, và chứng minh từng bảng
một chứ không phải bằng một khẳng định chung.

Purpose: tiêu chí nghiệm thu số 2 của phase yêu cầu "test báo kết quả theo từng bảng". Bảng nào thiếu policy
phải chỉ ra được đúng bảng đó, nên mỗi bảng cần assertion riêng mang tên bảng trong mô tả.

Output: `supabase/migrations/0004_core_entities.sql`, phần seed tương ứng, và `supabase/tests/03_isolation_core.sql`.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-CONTEXT.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@src/lib/types/domain.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Năm bảng thực thể V1 kèm RLS, index và ràng buộc</name>
  <precondition>`psql "$POSTGRES_URL_NON_POOLING" -tAc "select public.tf_shift_minutes('22:00','06:00',0)"` trả về `480` (plan 01-02 đã áp)</precondition>
  <read_first>
    - `src/lib/types/domain.ts` dòng 82–177 — `Department`, `Shift`, `Employee`, `AttendanceRecord`, `WorkRequest`: tên trường, kiểu, và trường nào nullable
    - `supabase/migrations/0002_tenancy.sql` — mẫu policy `<bảng>_<lệnh>_member` và hàm `public.tf_is_member`
    - `supabase/migrations/0003_enums_time.sql` — tên các kiểu enum và năm hàm thời gian
    - `supabase/seed.sql` — cấu trúc hiện có, hai công ty và bốn user cố định
    - `.planning/research/PITFALLS.md` §"Pitfall 8" (giả định của dữ liệu giả vỡ khi ghi thật) và §"Performance Traps" (index trên cột mà policy tham chiếu)
  </read_first>
  <files>supabase/migrations/0004_core_entities.sql, supabase/seed.sql</files>
  <action>
    **Migration `supabase/migrations/0004_core_entities.sql`** — năm bảng, tất cả trong schema `public`, tên cột
    là snake_case của trường tương ứng trong `domain.ts`. Mỗi bảng có `company_id text not null references
    companies(id) on delete cascade` ngay sau khóa chính, đúng như `companyId` đứng thứ hai trong mọi interface
    thuộc phạm vi doanh nghiệp.

    - `departments`: `id text pk`, `company_id`, `name`, `description`, `manager_id text null`, `status department_status not null default 'active'`.
    - `shifts`: `id text pk`, `company_id`, `name`, `code`, `start_time time not null`, `end_time time not null`, `break_minutes int not null default 0 check (break_minutes >= 0)`, `late_tolerance_minutes int not null default 0 check (late_tolerance_minutes >= 0)`, `overnight boolean generated always as (end_time < start_time) stored`, `working_days smallint[] not null check (working_days <@ array[1,2,3,4,5,6,7]::smallint[] and array_length(working_days,1) between 1 and 7)`, `status shift_status not null default 'active'`, và `check (start_time <> end_time)` — ca độ dài 0 không phải trạng thái hợp lệ.
    - `employees`: `id text pk`, `company_id`, `code`, `full_name`, `email`, `phone`, `date_of_birth date`, `gender gender`, `avatar_url text null`, `department_id text references departments(id)`, `position text`, `contract_type contract_type`, `start_date date`, `manager_id text null`, `shift_id text references shifts(id)`, `work_location text`, `status employee_status`, `system_role system_role`, `invitation_sent boolean not null default false`, `can_view_payslip boolean not null default false`, `can_check_in_remotely boolean not null default false`, `user_id uuid null references auth.users(id) on delete set null`, `unique (company_id, code)`.
    - `attendance_records`: `id text pk`, `company_id`, `employee_id text references employees(id) on delete cascade`, `work_date date not null`, `shift_id text references shifts(id)`, `check_in_at timestamptz null`, `check_out_at timestamptz null`, `worked_minutes int not null default 0 check (worked_minutes >= 0)`, `late_minutes int not null default 0 check (late_minutes >= 0)`, `early_leave_minutes int not null default 0 check (early_leave_minutes >= 0)`, `status attendance_status not null`, `location text`, `needs_supplement boolean not null default false`, `note text null`, `unique (employee_id, work_date, shift_id)`, và `check (check_in_at is null or work_date = public.tf_work_date(check_in_at))` — ràng buộc này là chỗ D-08 được ép ở tầng database chứ không phải chỉ ở tầng ứng dụng.
    - `work_requests`: `id text pk`, `company_id`, `employee_id text references employees(id) on delete cascade`, `type request_type`, `status request_status not null default 'pending'`, `from_date date`, `to_date date`, `from_time time null`, `to_time time null`, `reason text`, `created_at timestamptz not null default now()`, `reviewer_id text null references employees(id)`, `review_note text null`, `reviewed_at timestamptz null`, `check (to_date >= from_date)`.

    Quan hệ tự tham chiếu `departments.manager_id` và `employees.manager_id` đều trỏ tới `employees(id)` nhưng
    `departments` được tạo trước `employees`; thêm hai khóa ngoại đó bằng `alter table ... add constraint` ở
    cuối file, và cả hai để `deferrable initially deferred` để seed chèn được theo thứ tự công ty → phòng ban →
    ca → nhân viên mà không vướng tham chiếu tiến (`src/lib/mock/seed.ts` đặt `managerId` cho phòng ban trước
    khi nhân viên được định nghĩa).

    **Index.** Trên mỗi bảng: index `(company_id)` vì đó là cột mọi policy tham chiếu. Thêm
    `employees (company_id, full_name, id)` cho truy vấn phân trang có thứ tự ổn định khi trùng tên;
    `employees (company_id, department_id)`; `attendance_records (company_id, employee_id, work_date)`;
    `work_requests (company_id, status, created_at desc)`.

    **RLS.** Bật RLS trên cả năm bảng. Mỗi bảng đúng bốn policy PERMISSIVE theo mẫu `<bảng>_<lệnh>_member`,
    điều kiện duy nhất là `public.tf_is_member(<bảng>.company_id)` — `using` cho select/update/delete,
    `with check` cho insert/update. Không policy nào đọc `company_id` do client gửi lên; không dùng biến phiên
    Postgres.

    **Seed.** Bổ sung vào `supabase/seed.sql`, giữ nguyên phần đầu đã có: mở rộng lệnh `truncate` để phủ năm
    bảng mới (thứ tự an toàn nhờ `cascade`). Với **mỗi** công ty `cty-01` và `cty-02`, chèn tối thiểu: một
    phòng ban, một ca ngày và một ca đêm (`22:00`–`06:00` cho `cty-01`, `18:00`–`06:00` cho `cty-02`, khớp
    `seedShifts` và `seedShifts2`), hai nhân viên trong đó một người có `manager_id` NULL và một người có
    `manager_id` trỏ tới người kia, hai bản ghi chấm công trong đó một bản ghi thuộc ca đêm và một bản ghi có
    `check_out_at` NULL, và một yêu cầu công có `from_time`/`to_time` NULL. Bộ tối thiểu này tồn tại để test
    cô lập có dòng ở **cả hai phía**; bộ seed đầy đủ theo D-06 là việc của plan 01-06.

    Giá trị enum và chuỗi hiển thị: chỉ giá trị tiếng Anh vào cột enum. Tên người, tên phòng ban, địa chỉ là
    dữ liệu tiếng Việt bình thường và giữ nguyên.
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_tables where schemaname='public'"` trả về `7`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_policies where schemaname='public' and tablename in ('departments','shifts','employees','attendance_records','work_requests')"` trả về `20`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_class where relnamespace='public'::regnamespace and relkind='r' and not relrowsecurity"` trả về `0`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select indexdef from pg_indexes where schemaname='public' and tablename='employees'"` chứa một dòng có cả `company_id`, `full_name` và `id`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "insert into public.shifts (id,company_id,name,code,start_time,end_time,working_days) values ('tmp','cty-01','x','X','08:00','08:00',array[1]::smallint[])"` thoát khác 0 (CHECK cấm ca độ dài 0).
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select overnight from public.shifts where start_time='22:00' and end_time='06:00'"` trả về `t`.
    - `npm run db:seed` thoát 0; với mỗi bảng trong năm bảng, `select count(*) ... where company_id='cty-01'` và `... ='cty-02'` đều lớn hơn 0.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from public.attendance_records where check_out_at is null"` lớn hơn 0 và `... where check_in_at is null or work_date <> public.tf_work_date(check_in_at)` trả về `0`.
    - `npm run test:rls` thoát 0 (cổng RLS xanh với schema 7 bảng).
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run test:db</automated>
  </verify>
  <done>Bảy bảng trên schema `public`, tất cả bật RLS với đủ bốn policy mỗi bảng; seed có dòng ở cả hai công ty trên từng bảng; cổng RLS xanh.</done>
</task>

<task type="auto">
  <name>Task 2: Test cô lập theo từng bảng cho năm thực thể V1</name>
  <precondition>`psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from public.employees where company_id='cty-02'"` trả về số lớn hơn 0 (seed của Task 1 đã chạy)</precondition>
  <read_first>
    - `supabase/tests/01_isolation_companies.sql` — mẫu assertion đã dùng ở plan 01-01, giữ nguyên cấu trúc và cách đặt tên
    - `supabase/tests/helpers.sql` — `tf_test_login` / `tf_test_logout` và bốn user cố định
    - `supabase/migrations/0004_core_entities.sql` (Task 1) — tên cột bắt buộc khi dựng câu `insert` thử ghi chéo
    - `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-VALIDATION.md` §"Quy tắc ánh xạ" — test đọc một chiều là chưa đủ
  </read_first>
  <files>supabase/tests/03_isolation_core.sql, supabase/tests/run-all.sql</files>
  <action>
    Viết `supabase/tests/03_isolation_core.sql` theo đúng khuôn của `01_isolation_companies.sql`: mở transaction,
    `plan(n)` với n bằng đúng số assertion, kết thúc bằng `select * from finish(true)` rồi `rollback`.

    Với **mỗi** bảng trong `departments`, `shifts`, `employees`, `attendance_records`, `work_requests`, viết đủ
    bảy khẳng định, và **tên bảng phải xuất hiện nguyên văn trong mô tả của từng khẳng định** để khi đỏ thì biết
    ngay bảng nào vỡ:

    1. Đăng nhập user `…0001` (owner Ngọc Phát): số dòng đọc được với `company_id = 'cty-01'` lớn hơn 0. Đây là
       vế chống test xanh rỗng — không có nó thì khẳng định số 2 vô nghĩa.
    2. Cùng phiên: số dòng đọc được với `company_id = 'cty-02'` bằng 0.
    3. Đăng nhập user `…0002` (owner Bình Minh): gương lại hai khẳng định trên theo chiều ngược.
    4. Đăng nhập user `…0001`: `insert` một dòng hợp lệ về mặt kiểu nhưng mang `company_id = 'cty-02'` ném lỗi
       SQLSTATE `42501`, dùng `throws_ok`.
    5. Đăng nhập user `…0001`: `update` đặt một cột văn bản trên các dòng `company_id = 'cty-02'` tác động đúng
       0 dòng (dùng mệnh đề `returning` rồi đếm).
    6. Đăng nhập user `…0001`: `delete` các dòng `company_id = 'cty-02'` tác động đúng 0 dòng.
    7. Đăng nhập user `…0004` (không có membership nào): số dòng đọc được bằng 0 — không membership nghĩa là
       không thấy gì, chứ không phải thấy tất cả.

    Thêm hai khẳng định chung cho cả nhóm: user `…0003` (thành viên của cả hai công ty) đọc được lớn hơn 0 dòng
    của `cty-01` **và** lớn hơn 0 dòng của `cty-02` trên `employees` — policy đánh giá từng dòng độc lập, không
    gộp hai doanh nghiệp làm một.

    Thêm bốn khẳng định về cột nullable: chèn (với tư cách user `…0001`, trong transaction sẽ rollback) một dòng
    `departments` có `manager_id` NULL, một dòng `employees` có `manager_id` NULL, một dòng `attendance_records`
    có `check_out_at` NULL, và một dòng `work_requests` có `from_time` và `to_time` NULL — tất cả phải thành công.

    Thêm `\ir 03_isolation_core.sql` vào `supabase/tests/run-all.sql`, sau `02_time_overnight.sql`.
  </action>
  <acceptance_criteria>
    - `npm run test:db` thoát 0.
    - Output TAP chứa nguyên văn cả năm tên bảng `departments`, `shifts`, `employees`, `attendance_records`, `work_requests`.
    - `node -e "const s=require('fs').readFileSync('supabase/tests/03_isolation_core.sql','utf8');const t=['departments','shifts','employees','attendance_records','work_requests'];const bad=t.filter(x=>(s.match(new RegExp(x,'g'))||[]).length<7);if(bad.length){console.error(bad.join(','));process.exit(1)}"` thoát 0 — mỗi bảng được nhắc tên ít nhất bảy lần, tương ứng bảy khẳng định.
    - Sau khi tạm sửa policy `employees_select_member` thành `using (true)`, `npm run test:rls` thoát khác 0 và output nêu `employees`; hoàn nguyên rồi chạy lại thoát 0.
    - Sau khi tạm `drop policy employees_select_member`, `npm run test:rls` thoát khác 0 (cổng RLS phát hiện bảng còn 3 policy thay vì 4, hoặc test đọc của chính chủ về 0 dòng); hoàn nguyên rồi chạy lại thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:db</automated>
  </verify>
  <done>Bảy bảng thuộc phạm vi doanh nghiệp đều có khẳng định đọc chéo và ghi chéo riêng mang tên bảng; bộ test chứng minh được là có răng bằng hai phép phá hoại có kiểm soát.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT của người dùng doanh nghiệp → policy RLS trên 5 bảng nghiệp vụ | Ranh giới cô lập chính, nhân bản từ mẫu đã chứng minh ở plan 01-01 |
| Khóa ngoại giữa các bảng → phạm vi doanh nghiệp | Một dòng con trỏ tới dòng cha của doanh nghiệp khác sẽ phá cô lập ở tầng dữ liệu |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Elevation of Privilege | policy thiếu hoặc sai điều kiện trên một trong năm bảng | critical | mitigate | Mỗi bảng đúng bốn policy điều kiện `public.tf_is_member(company_id)`; `03_isolation_core.sql` khẳng định riêng cho từng bảng cả đọc chéo lẫn ghi chéo, kèm hai phép phá hoại có kiểm soát chứng minh test biết đỏ |
| T-01-17 | Information Disclosure | khóa ngoại nối sang dòng của doanh nghiệp khác | high | mitigate | `employees.department_id`, `employees.shift_id`, `attendance_records.employee_id`, `work_requests.employee_id`, `work_requests.reviewer_id` đều trỏ vào bảng cũng bật RLS nên phía đọc vẫn bị chặn; ràng buộc chặt hơn ở tầng dữ liệu là việc của Phase 2 khi có Server Action ghi |
| T-01-18 | Tampering | `work_date` không khớp `check_in_at` làm lệch tổng hợp kỳ công | high | mitigate | CHECK trên `attendance_records` ép `work_date = public.tf_work_date(check_in_at)` khi có giờ vào; ràng buộc duy nhất `(employee_id, work_date, shift_id)` chặn trùng bản ghi |
| T-01-19 | Denial of Service | policy quét toàn bảng vì thiếu index trên cột policy tham chiếu | medium | mitigate | Index `(company_id)` trên cả năm bảng cộng ba index tổ hợp cho truy vấn nóng |
| T-01-20 | Repudiation | thao tác ghi trên năm bảng chưa để lại vết | medium | accept | `audit_log` ra đời ở plan 01-05; đường ghi thật chỉ xuất hiện từ Phase 2 nên chưa có thao tác nào cần truy vết trong Phase 1 |
| T-01-SC | Tampering | cài gói qua npm/pip/cargo | high | mitigate | Plan không cài gói nào — chỉ file SQL; kế thừa kết luận T-01-SC của plan 01-01 |
</threat_model>

## Artifacts this plan produces

**Bảng** — `departments`, `shifts`, `employees`, `attendance_records`, `work_requests`

**Cột đáng chú ý** — `shifts.overnight` (sinh từ `end_time < start_time`) · `attendance_records.work_date` (ngày bắt đầu ca, D-08) · `attendance_records.check_in_at` / `check_out_at` (`timestamptz`) · `employees.user_id` (nối sang `auth.users`, Phase 2 dùng)

**Policy** — 20 policy theo mẫu `<bảng>_<select|insert|update|delete>_member`

**Index** — `<bảng>_company_id_idx` trên cả năm bảng · `employees (company_id, full_name, id)` · `employees (company_id, department_id)` · `attendance_records (company_id, employee_id, work_date)` · `work_requests (company_id, status, created_at desc)`

**Ràng buộc** — `shifts` CHECK `start_time <> end_time` · `attendance_records` CHECK `work_date = tf_work_date(check_in_at)` · `attendance_records` UNIQUE `(employee_id, work_date, shift_id)` · `employees` UNIQUE `(company_id, code)` · `work_requests` CHECK `to_date >= from_date`

**File mới** — `supabase/migrations/0004_core_entities.sql`, `supabase/tests/03_isolation_core.sql`

<verification>
- 7 bảng trên `public`, tất cả bật RLS, tổng 28 policy.
- Seed có dòng ở cả `cty-01` và `cty-02` trên từng bảng.
- `npm run test:db` xanh; output TAP nêu đích danh cả năm tên bảng.
- Hai phép phá hoại có kiểm soát (policy `using (true)` và xóa policy) đều làm bộ test đỏ.
</verification>

<success_criteria>
- Tiêu chí nghiệm thu số 2 của phase đạt với năm bảng nghiệp vụ chính: kết quả báo theo từng bảng, có cả đọc chéo lẫn ghi chéo.
- Không bảng nào thoát khỏi cổng RLS.
- Cột `work_date` mang quy ước D-08 và được ràng buộc ở tầng database.
</success_criteria>

<output>
Create `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-04-SUMMARY.md` when done
</output>
