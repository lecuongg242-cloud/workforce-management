---
phase: 01-n-n-d-li-u-v-c-l-p-doanh-nghi-p
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - supabase/migrations/0005_v2_tables.sql
  - supabase/seed.sql
  - supabase/tests/04_isolation_v2.sql
  - supabase/tests/run-all.sql
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Schema `public` có đủ 13 bảng: 6 thực thể của `src/lib/types/domain.ts` cộng 7 bảng mới của V2 — `memberships`, `work_sites`, `attendance_photos`, `holidays`, `overtime_rules`, `audit_log`, `periods`."
    - "Sáu bảng mới của plan này đều bật RLS với đúng bốn policy PERMISSIVE mỗi bảng, điều kiện suy từ `auth.uid()` qua `memberships`."
    - "Tài khoản Ngọc Phát đọc được lớn hơn 0 dòng của chính mình và đúng 0 dòng của Bình Minh trên từng bảng mới; ghi chéo bị từ chối với SQLSTATE 42501 — mỗi khẳng định nêu đích danh tên bảng."
    - "`periods` lưu `start_date` và `end_date` tường minh; seed sinh kỳ theo tháng dương lịch từ ngày 1 đến hết tháng (D-09), và ràng buộc duy nhất `(company_id, start_date)` chặn hai kỳ trùng mốc."
    - "Một doanh nghiệp không có dòng nào trong `holidays`, `work_sites` hay `overtime_rules` vẫn là trạng thái hợp lệ — không ràng buộc nào ép phải có dòng, và doanh nghiệp mới khởi tạo không có ngày lễ cài sẵn."
    - "Cổng RLS chạy trên schema đầy đủ 13 bảng trả xanh, và số bảng quét được lớn hơn 0."
    - "Toàn bộ migration được áp lên project dev qua Supabase CLI và `supabase_migrations.schema_migrations` liệt kê đủ 5 file migration."
  artifacts:
    - path: "supabase/migrations/0005_v2_tables.sql"
      provides: "Sáu bảng mới của V2 kèm enum, RLS, policy và index"
      contains: "periods"
    - path: "supabase/tests/04_isolation_v2.sql"
      provides: "Test cô lập đọc chéo và ghi chéo theo từng bảng cho sáu bảng V2"
      contains: "audit_log"
  key_links:
    - from: "supabase/migrations/0005_v2_tables.sql"
      to: "supabase/migrations/0002_tenancy.sql"
      via: "policy của cả sáu bảng gọi public.tf_is_member(company_id)"
      pattern: "tf_is_member"
    - from: "supabase/tests/00_rls_coverage.sql"
      to: "supabase/migrations/0005_v2_tables.sql"
      via: "cổng quét pg_class/pg_policies bao trùm toàn bộ 13 bảng sau khi migration được áp"
      pattern: "relrowsecurity"
---

<objective>
Hoàn tất schema Phase 1: bổ sung sáu bảng mới của V2 mà `src/lib/types/domain.ts` chưa có, kèm RLS và test cô
lập theo từng bảng, rồi áp toàn bộ migration lên project dev và chạy cổng RLS trên schema đầy đủ.

Purpose: nghiên cứu chỉ ra RLS thường thủng ở bảng sinh ra trong các phase sau chứ không phải ở schema ban
đầu. Sáu bảng này chính là "bảng của phase sau" — Phase 3 dùng `work_sites` và `attendance_photos`, Phase 4
dùng `holidays` và `overtime_rules`, Phase 5 dùng `periods`, Phase 2 trở đi dùng `audit_log`. Dựng chúng ngay
bây giờ, dưới cùng cổng kiểm tra, để không phase nào phải nhớ bật RLS về sau.

Output: `supabase/migrations/0005_v2_tables.sql`, phần seed tương ứng, `supabase/tests/04_isolation_v2.sql`, và
một lần áp schema đầy đủ có kiểm chứng.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/lib/types/domain.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sáu bảng mới của V2 kèm RLS, index và ràng buộc</name>
  <reversibility rating="costly">D-09 — kỳ công theo tháng dương lịch. `periods` lưu mốc tường minh nên schema không phải đổi nếu về sau dùng chu kỳ khác, nhưng các kỳ đã chốt vẫn mang mốc cũ và cần xử lý riêng.</reversibility>
  <precondition>`psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_tables where schemaname='public'"` trả về `7` (plan 01-04 đã áp)</precondition>
  <read_first>
    - `supabase/migrations/0004_core_entities.sql` — mẫu bảng, mẫu policy, quy ước index và ràng buộc đang dùng
    - `.planning/research/ARCHITECTURE.md` §"Pattern 5" — `audit_log` cần ghi được `actor_user_id` và doanh nghiệp bị tác động; §"Attendance photo flow" — đường dẫn object nhúng `company_id`
    - `.planning/research/PITFALLS.md` §"Pitfall 6" (bucket ảnh) và §"Pitfall 18" (chốt kỳ)
    - `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-CONTEXT.md` §D-09
    - `supabase/seed.sql` — cấu trúc hiện có sau plan 01-04
  </read_first>
  <files>supabase/migrations/0005_v2_tables.sql, supabase/seed.sql</files>
  <action>
    **Enum mới:** `period_status` (`open`, `closed`) · `photo_review_status` (`pending`, `approved`, `rejected`)
    · `audit_action` (`insert`, `update`, `delete`). Giá trị tiếng Anh, đúng quy ước đã dùng ở plan 01-02.

    **Sáu bảng, tất cả trong `public`, tất cả có `company_id` ngay sau khóa chính:**

    - `work_sites`: `id text pk`, `company_id text not null references companies(id) on delete cascade`, `name`,
      `latitude numeric(9,6) not null check (latitude between -90 and 90)`, `longitude numeric(9,6) not null
      check (longitude between -180 and 180)`, `radius_meters int not null check (radius_meters > 0)`,
      `is_active boolean not null default true`, `created_at timestamptz not null default now()`.
    - `attendance_photos`: `id uuid pk default gen_random_uuid()`, `company_id`, `attendance_record_id text not
      null references attendance_records(id) on delete cascade`, `kind text not null check (kind in
      ('check_in','check_out'))`, `storage_path text not null`, `captured_at timestamptz not null`,
      `latitude numeric(9,6) null`, `longitude numeric(9,6) null`, `review_status photo_review_status not null
      default 'pending'`, `reviewed_by uuid null references auth.users(id)`, `reviewed_at timestamptz null`,
      `unique (attendance_record_id, kind)`. Ràng buộc `check (storage_path like company_id || '/%')` — đường
      dẫn object phải bắt đầu bằng chính `company_id`, để policy trên `storage.objects` ở Phase 3 dùng lại được
      cùng một ranh giới thay vì phải nghĩ ra mô hình thứ hai.
    - `holidays`: `id uuid pk default gen_random_uuid()`, `company_id`, `holiday_date date not null`, `name text
      not null`, `unique (company_id, holiday_date)`. Không chèn sẵn ngày lễ nào cho bất kỳ doanh nghiệp nào —
      danh sách rỗng là trạng thái khởi tạo đúng.
    - `overtime_rules`: `id uuid pk default gen_random_uuid()`, `company_id`, `rule_key text not null check
      (rule_key in ('weekday','weekend','holiday','night'))`, `multiplier numeric(4,2) not null check
      (multiplier > 0)`, `effective_from date not null`, `unique (company_id, rule_key, effective_from)`. Không
      hệ số nào nhúng cứng làm mặc định trong DDL.
    - `audit_log`: `id bigint pk generated always as identity`, `company_id text null references companies(id) on
      delete set null`, `actor_user_id uuid null references auth.users(id) on delete set null`, `action
      audit_action not null`, `entity_table text not null`, `entity_id text null`, `before jsonb null`, `after
      jsonb null`, `reason text null`, `created_at timestamptz not null default now()`. `company_id` để nullable
      vì Phase 6 cần ghi được thao tác super admin không thuộc doanh nghiệp nào; hệ quả phải hiểu đúng: dòng có
      `company_id` NULL không lọt qua policy thành viên nên không người dùng doanh nghiệp nào đọc được — đúng
      điều mong muốn.
    - `periods`: `id uuid pk default gen_random_uuid()`, `company_id`, `start_date date not null`, `end_date date
      not null`, `status period_status not null default 'open'`, `closed_at timestamptz null`, `closed_by uuid
      null references auth.users(id)`, `unique (company_id, start_date)`, `check (end_date >= start_date)`,
      `check (start_date = date_trunc('month', start_date)::date and end_date = (date_trunc('month',
      start_date) + interval '1 month - 1 day')::date)` — ép kỳ công là trọn một tháng dương lịch theo D-09,
      trong khi vẫn lưu hai mốc tường minh để về sau đổi quy ước không phải đổi schema.

    **Index:** `(company_id)` trên cả sáu bảng · `attendance_photos (company_id, attendance_record_id)` ·
    `holidays (company_id, holiday_date)` · `overtime_rules (company_id, rule_key, effective_from desc)` ·
    `audit_log (company_id, created_at desc)` · `periods (company_id, status)`.

    **RLS:** bật trên cả sáu bảng, mỗi bảng đúng bốn policy PERMISSIVE theo mẫu `<bảng>_<lệnh>_member`, điều
    kiện duy nhất `public.tf_is_member(<bảng>.company_id)`.

    **Seed:** mở rộng lệnh `truncate` phủ sáu bảng mới. Với **mỗi** công ty chèn tối thiểu: một `work_sites`,
    một `attendance_photos` gắn vào bản ghi chấm công đã seed (đường dẫn bắt đầu bằng `company_id`), một
    `overtime_rules` với `rule_key = 'weekday'`, một `audit_log`, và một `periods` cho tháng hiện tại tính theo
    `Asia/Ho_Chi_Minh`. **Không** chèn dòng `holidays` nào — `holidays` là bảng cố ý rỗng ở cả hai công ty; test
    của Task 2 khẳng định trạng thái rỗng này là hợp lệ, và cổng RLS vẫn phải phủ nó.
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0; `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_tables where schemaname='public'"` trả về `13`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_policies where schemaname='public'"` trả về `52`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_class where relnamespace='public'::regnamespace and relkind='r' and not relrowsecurity"` trả về `0`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from pg_class c where c.relnamespace='public'::regnamespace and c.relkind='r' and c.relrowsecurity and not exists (select 1 from pg_policies p where p.schemaname='public' and p.tablename=c.relname)"` trả về `0`.
    - `npm run db:seed` thoát 0; `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from public.holidays"` trả về `0`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "insert into public.periods (company_id,start_date,end_date) values ('cty-01','2026-03-05','2026-04-04')"` thoát khác 0 (CHECK ép trọn tháng dương lịch).
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "insert into public.attendance_photos (company_id,attendance_record_id,kind,storage_path,captured_at) select 'cty-01', id, 'check_in', 'cty-02/x.jpg', now() from public.attendance_records where company_id='cty-01' limit 1"` thoát khác 0 (CHECK ép tiền tố đường dẫn khớp `company_id`).
    - `npm run test:rls` thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run test:db</automated>
  </verify>
  <done>Schema `public` có đủ 13 bảng, tất cả bật RLS với đủ policy; `holidays` rỗng ở cả hai công ty là trạng thái hợp lệ; kỳ công bị ép về trọn tháng dương lịch.</done>
</task>

<task type="auto">
  <name>Task 2: Test cô lập theo từng bảng cho sáu bảng V2</name>
  <precondition>`psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from public.work_sites where company_id='cty-02'"` trả về số lớn hơn 0 (seed của Task 1 đã chạy)</precondition>
  <read_first>
    - `supabase/tests/03_isolation_core.sql` — khuôn bảy khẳng định mỗi bảng, giữ nguyên cấu trúc và cách đặt tên
    - `supabase/migrations/0005_v2_tables.sql` (Task 1) — tên cột bắt buộc khi dựng câu `insert` thử ghi chéo
    - `supabase/tests/helpers.sql` — bốn user cố định
  </read_first>
  <files>supabase/tests/04_isolation_v2.sql, supabase/tests/run-all.sql</files>
  <action>
    Viết `supabase/tests/04_isolation_v2.sql` theo đúng khuôn của `03_isolation_core.sql`.

    Với **mỗi** bảng trong `work_sites`, `attendance_photos`, `overtime_rules`, `audit_log`, `periods`, viết đủ
    bảy khẳng định như plan 01-04 (chính chủ đọc được lớn hơn 0 dòng; đọc 0 dòng của công ty kia; gương lại
    chiều ngược; `insert` chéo ném SQLSTATE `42501`; `update` chéo 0 dòng; `delete` chéo 0 dòng; user không
    membership đọc 0 dòng), tên bảng xuất hiện nguyên văn trong mô tả từng khẳng định.

    `holidays` là ca riêng vì cố ý rỗng ở cả hai công ty — khẳng định đọc "lớn hơn 0 dòng" không áp dụng được.
    Thay vào đó, trong cùng transaction test: đăng nhập user `…0001`, chèn một dòng `holidays` cho `cty-01` và
    khẳng định thành công; khẳng định user `…0002` đọc được 0 dòng; khẳng định user `…0001` chèn dòng
    `holidays` mang `company_id = 'cty-02'` ném SQLSTATE `42501`; khẳng định user `…0004` đọc được 0 dòng. Ghi
    rõ trong mô tả rằng `holidays` bắt đầu rỗng có chủ đích và chính test này cấp dòng để so sánh, để về sau
    không ai nhầm trạng thái rỗng là lỗi.

    Thêm một khẳng định cho `audit_log`: một dòng có `company_id` NULL (chèn bằng vai trò mặc định của kết nối,
    trước khi giả lập đăng nhập) không được người dùng nào trong hai công ty đọc thấy.

    Thêm `\ir 04_isolation_v2.sql` vào `supabase/tests/run-all.sql`, sau `03_isolation_core.sql`.
  </action>
  <acceptance_criteria>
    - `npm run test:db` thoát 0.
    - Output TAP chứa nguyên văn cả sáu tên bảng `work_sites`, `attendance_photos`, `holidays`, `overtime_rules`, `audit_log`, `periods`.
    - `node -e "const s=require('fs').readFileSync('supabase/tests/04_isolation_v2.sql','utf8');const t=['work_sites','attendance_photos','overtime_rules','audit_log','periods'];const bad=t.filter(x=>(s.match(new RegExp(x,'g'))||[]).length<7);if(bad.length){console.error(bad.join(','));process.exit(1)}"` thoát 0.
    - Sau khi tạm `alter table public.periods disable row level security`, `npm run test:rls` thoát khác 0 và output nêu `periods`; hoàn nguyên rồi chạy lại thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:db</automated>
  </verify>
  <done>Cả 12 bảng thuộc phạm vi doanh nghiệp đều có khẳng định đọc chéo và ghi chéo riêng mang tên bảng; ca bảng cố ý rỗng được xử lý tường minh thay vì bỏ qua.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Áp schema đầy đủ lên project dev và chạy cổng trên schema hoàn chỉnh</name>
  <precondition>`.env.local` chứa `POSTGRES_URL_NON_POOLING` khác rỗng và `npx supabase --version` in ra `2.111.0` hoặc mới hơn</precondition>
  <read_first>
    - `scripts/db.mjs` (plan 01-01) — lệnh `push` gọi Supabase CLI với `--db-url` như thế nào
    - `supabase/migrations/` — toàn bộ 5 file, kiểm tra thứ tự từ điển đúng với thứ tự phụ thuộc
    - `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-CONTEXT.md` §D-02
  </read_first>
  <files>supabase/tests/run-all.sql</files>
  <action>
    Bước đẩy schema bắt buộc — build và typecheck của Next.js **không** chạm database nên chúng xanh kể cả khi
    schema chưa được áp; chỉ bước này mới phân biệt được "migration đã viết" với "migration đã chạy".

    Chạy `npm run db:push` để Supabase CLI áp mọi migration còn thiếu lên project dev `ujvgagujfsdrlmjdhooi`
    (D-01, D-02). Nếu CLI dừng vì cần xác nhận tương tác, dùng cờ không tương tác mà `npx supabase db push --help`
    liệt kê; nếu vẫn không đẩy được, dừng lại và báo cáo thay vì áp SQL tay lên dashboard.

    Sau khi đẩy: chạy `npm run db:seed` rồi `npm run test:rls`, và đối chiếu bảng liệt kê trong
    `supabase_migrations.schema_migrations` với danh sách file trong `supabase/migrations/` — hai danh sách phải
    khớp cả về số lượng lẫn thứ tự. Ghi bản đối chiếu vào SUMMARY.

    Xác nhận `supabase/tests/run-all.sql` nạp đủ bốn file test theo đúng thứ tự `00_rls_coverage`,
    `02_time_overnight`, `03_isolation_core`, `04_isolation_v2` (file `01_isolation_companies` nạp ngay sau
    `00_rls_coverage`), và không file test nào bị bỏ sót.

    Cuối cùng, đẩy nhánh làm việc lên `origin` và xác nhận workflow `db` chạy trên Postgres sạch với **toàn bộ**
    13 bảng cũng xanh — đây là lần đầu cổng CI gặp schema hoàn chỉnh.
  </action>
  <acceptance_criteria>
    - `npm run db:push` thoát 0.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from supabase_migrations.schema_migrations"` trả về `5`.
    - `node -e "const fs=require('node:fs');const {execSync}=require('node:child_process');const files=fs.readdirSync('supabase/migrations').filter(f=>f.endsWith('.sql')).sort();if(files.length!==5)process.exit(1)"` thoát 0.
    - `npm run test:db` thoát 0.
    - `node -e "const s=require('fs').readFileSync('supabase/tests/run-all.sql','utf8');const need=['00_rls_coverage','01_isolation_companies','02_time_overnight','03_isolation_core','04_isolation_v2'];const miss=need.filter(x=>!s.includes(x));if(miss.length){console.error(miss.join(','));process.exit(1)}"` thoát 0.
    - Lần chạy workflow `db` gần nhất trên nhánh làm việc có kết luận `success` với schema 13 bảng.
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push &amp;&amp; npm run test:db</automated>
  </verify>
  <done>Toàn bộ 5 migration đã nằm trên project dev và được `supabase_migrations.schema_migrations` xác nhận; cổng RLS và bộ test cô lập xanh trên schema hoàn chỉnh, cả tại máy phát triển lẫn trên Postgres sạch của CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT của người dùng doanh nghiệp → policy RLS trên 6 bảng V2 | Nhóm bảng mà nghiên cứu chỉ ra là dễ thủng nhất vì sinh ra ở các phase sau |
| Đường dẫn object trong `attendance_photos.storage_path` → ranh giới bucket của Phase 3 | Tiền tố đường dẫn là ranh giới cô lập cho storage; sai tiền tố là rò ảnh xuyên doanh nghiệp |
| Trạng thái schema trong repo → trạng thái schema thật trên project dev | Migration đã viết nhưng chưa áp là trạng thái xanh giả |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-21 | Information Disclosure | bảng V2 sinh ra cho phase sau bị quên bật RLS | critical | mitigate | Cả sáu bảng bật RLS và có đủ bốn policy ngay trong cùng migration tạo bảng; cổng `00_rls_coverage.sql` khẳng định 0 bảng thiếu RLS và 0 bảng có RLS mà không policy trên toàn bộ 13 bảng |
| T-01-22 | Information Disclosure | `attendance_photos.storage_path` trỏ sang thư mục của doanh nghiệp khác | high | mitigate | CHECK ép `storage_path` bắt đầu bằng `company_id`; acceptance criteria khẳng định câu `insert` sai tiền tố bị từ chối |
| T-01-23 | Elevation of Privilege | dòng `audit_log` có `company_id` NULL lọt vào tầm nhìn người dùng doanh nghiệp | high | mitigate | Policy điều kiện `public.tf_is_member(company_id)` trả NULL với `company_id` NULL nên dòng bị lọc; test khẳng định người dùng của cả hai công ty đọc 0 dòng loại này |
| T-01-24 | Tampering | kỳ công tạo với mốc không phải trọn tháng làm lệch dữ liệu chốt kỳ | medium | mitigate | CHECK ép `start_date` là đầu tháng và `end_date` là cuối cùng tháng đó (D-09); ràng buộc duy nhất `(company_id, start_date)` chặn hai kỳ trùng mốc |
| T-01-25 | Repudiation | migration đã viết nhưng chưa áp, verification xanh giả | high | mitigate | Task 3 là bước bắt buộc đẩy schema và đối chiếu `supabase_migrations.schema_migrations` với danh sách file trên đĩa; không cho phép áp SQL tay trên dashboard |
| T-01-26 | Denial of Service | `audit_log` phình theo thời gian làm truy vấn tra cứu chậm | low | accept | Index `(company_id, created_at desc)` đủ cho quy mô pilot 1-2 doanh nghiệp; phân vùng hay chính sách lưu giữ nằm ngoài phạm vi V2 |
| T-01-SC | Tampering | cài gói qua npm/pip/cargo | high | mitigate | Plan không cài gói nào — chỉ file SQL; kế thừa kết luận T-01-SC của plan 01-01 |
</threat_model>

## Artifacts this plan produces

**Bảng** — `work_sites`, `attendance_photos`, `holidays`, `overtime_rules`, `audit_log`, `periods`

**Kiểu enum** — `period_status`, `photo_review_status`, `audit_action`

**Cột đáng chú ý** — `work_sites(latitude, longitude, radius_meters, is_active)` · `attendance_photos(storage_path, kind, review_status, reviewed_by, reviewed_at)` · `holidays(holiday_date, name)` · `overtime_rules(rule_key, multiplier, effective_from)` · `audit_log(actor_user_id, action, entity_table, entity_id, before, after, reason)` · `periods(start_date, end_date, status, closed_at, closed_by)`

**Policy** — 24 policy mới, tổng 52 trên toàn schema

**Ràng buộc đáng chú ý** — `attendance_photos` CHECK tiền tố `storage_path` khớp `company_id` · `periods` CHECK trọn tháng dương lịch (D-09) · `overtime_rules` CHECK `rule_key` thuộc bốn giá trị · `holidays` UNIQUE `(company_id, holiday_date)`

**File mới** — `supabase/migrations/0005_v2_tables.sql`, `supabase/tests/04_isolation_v2.sql`

<verification>
- 13 bảng trên `public`, 52 policy, 0 bảng thiếu RLS, 0 bảng có RLS mà không policy.
- `holidays` rỗng ở cả hai công ty và trạng thái đó được test khẳng định là hợp lệ.
- `supabase_migrations.schema_migrations` liệt kê đúng 5 migration, khớp danh sách file trên đĩa.
- `npm run test:db` xanh; workflow `db` xanh với schema hoàn chỉnh.
</verification>

<success_criteria>
- Tiêu chí nghiệm thu số 1 của phase đạt về mặt schema: đủ thực thể V1 cộng bảy bảng mới của V2.
- Tiêu chí nghiệm thu số 3 chứng minh được trên schema thật: cổng RLS quét đủ 13 bảng và biết đỏ khi một bảng bị tắt RLS.
- Không có khoảng cách giữa migration trong repo và schema trên project dev.
</success_criteria>

<output>
Create `.planning/phases/01-n-n-d-li-u-v-c-l-p-doanh-nghi-p/01-05-SUMMARY.md` when done
</output>
