---
phase: 04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai
plan: 06
type: execute
wave: 5
depends_on: ["04-02", "04-03", "04-05"]
files_modified:
  - src/__tests__/no-hardcoded-work-rules.test.ts
  - scripts/e2e-settings.mjs
  - package.json
  - .planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-UAT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [SET-01, SET-02, SET-03, SET-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Có một cổng tự động chạy trong `npm test` chặn mọi con số nghiệp vụ nhúng cứng quay lại: hệ số tăng ca, danh sách ngày lễ, ngưỡng đáng ngờ, biên độ khung giờ ca."
    - "Cổng ấy được chứng minh là **có răng** bằng một lần phá hoại có kiểm soát: thêm một hệ số nhúng cứng vào mã → cổng đỏ; hoàn tác → xanh; `git status` sạch sau khi kiểm."
    - "Bốn tiêu chí của phase được nghiệm thu bằng quan sát trên hệ thống chạy thật, không phải bằng suy luận từ test."
    - "Một doanh nghiệp tạo mới hoàn toàn (qua onboarding thật) có: đúng một dòng `company_settings`, **0** ngày lễ, **0** hệ số tăng ca — và mọi màn hình vẫn dùng được, không màn hình nào vỡ vì thiếu dữ liệu."
    - "ROADMAP, REQUIREMENTS và STATE phản ánh đúng trạng thái sau phase: SET-01…SET-04 chuyển sang Complete, Phase 4 đóng, Phase 5 sẵn sàng."
  artifacts:
    - path: "src/__tests__/no-hardcoded-work-rules.test.ts"
      provides: "Cổng chặn con số nghiệp vụ nhúng cứng, chạy trong bộ test chuẩn"
    - path: ".planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-UAT.md"
      provides: "Biên bản nghiệm thu bốn tiêu chí trên hệ thống chạy thật"
  prohibitions:
    - statement: "MUST NOT đánh dấu phase hoàn thành khi còn một tiêu chí chỉ được chứng minh bằng test đơn vị. Bốn tiêu chí của phase nói về hành vi của hệ thống đối với người dùng thật; chúng phải được quan sát trên hệ thống chạy thật ít nhất một lần."
      category: process
    - statement: "MUST NOT nới lỏng cổng chặn để nó xanh. Nếu cổng bắt nhầm một dòng hợp lệ thì thu hẹp phạm vi quét bằng đường dẫn cụ thể và ghi lý do; không hạ mẫu quét xuống thành thứ không bắt được gì."
      category: integrity
---

<objective>
Đóng phase: dựng cổng tự động giữ cho lời hứa "không con số nghiệp vụ nào nằm trong mã nguồn"
không bị xói mòn theo thời gian, nghiệm thu bốn tiêu chí trên hệ thống chạy thật, và cập nhật
tài liệu kế hoạch.

Purpose: bốn plan trước mỗi plan có grep gate riêng trong acceptance criteria, nhưng
acceptance criteria chỉ chạy một lần lúc thực thi plan đó. Sáu tháng nữa, một người sửa vội
sẽ viết `* 1.5` vào một chỗ nào đó và không gì báo động. Cổng ở plan này là thứ duy nhất còn
sống sau khi phase kết thúc.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-CONTEXT.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-01-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-04-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cổng chặn con số nghiệp vụ nhúng cứng — và chứng minh nó có răng</name>
  <files>src/__tests__/no-hardcoded-work-rules.test.ts</files>
  <read_first>
    - `src/__tests__/route-handlers-get-only.test.ts` — tiền lệ của dự án cho một test quét mã
      nguồn thay vì kiểm hành vi; dùng lại đúng khuôn đó (đọc file bằng `fs`, quét bằng regex,
      danh sách loại trừ tường minh).
    - `scripts/check-bundle-secrets.mjs` — tiền lệ cổng quét có danh sách loại trừ và thông
      điệp lỗi chỉ đúng chỗ vi phạm.
    - Acceptance criteria của 04-01 Task 3, 04-03 Task 3, 04-04 Task 2/3, 04-05 Task 1/3 —
      các mẫu grep đã dùng; cổng này là bản hợp nhất **chạy được lặp lại** của chúng.
  </read_first>
  <behavior>
    - Quét `src/` (trừ `__tests__`) và báo đỏ khi thấy: hệ số tăng ca dạng số nhúng cứng
      (`1.5`, `2.0`, `3.0` trong ngữ cảnh nhân với phút/giờ), tên ngày lễ Việt Nam,
      `SUSPICIOUS_DISTANCE_MULTIPLIER` bị đọc thẳng ngoài mô-đun gốc, và biên độ khung giờ ca
      nhúng cứng ngoài mô-đun gốc.
    - Quét `supabase/seed.sql` và `supabase/migrations/` và báo đỏ khi thấy một `insert` vào
      `holidays` hoặc `overtime_rules` — hai bảng này cố ý rỗng.
    - Thông điệp lỗi nêu **đường dẫn và số dòng** vi phạm cùng một câu nói cách sửa đúng, chứ
      không chỉ nói "phát hiện vi phạm".
    - Danh sách loại trừ là tường minh và có comment lý do cho từng mục; không có mục nào loại
      trừ cả một thư mục lớn.
  </behavior>
  <action>
    Viết `src/__tests__/no-hardcoded-work-rules.test.ts` theo `<behavior>`.

    **Kiểm răng bắt buộc (sabotage-and-revert, theo tiền lệ 02-01):** (1) thêm tạm một hệ số
    nhúng cứng vào một file thật trong `src/lib/attendance/` → chạy cổng, phải **đỏ**; (2)
    thêm tạm một `insert into holidays` vào `seed.sql` → chạy cổng, phải **đỏ**; (3) hoàn tác
    cả hai → chạy cổng, phải **xanh**; (4) `git status` sạch. Ghi cả bốn bước vào SUMMARY —
    một cổng chưa được chứng minh có răng thì chưa phải một cổng.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/no-hardcoded-work-rules.test.ts` thoát 0.
    - `npm test` chạy file này (nó nằm trong pattern mặc định, không cần script riêng).
    - Kết quả bốn bước kiểm răng ghi vào SUMMARY, kèm `git status` sạch ở bước cuối.
    - Số mục trong danh sách loại trừ ≤ 5 và mỗi mục có comment lý do.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/no-hardcoded-work-rules.test.ts &amp;&amp; npm test</automated>
  </verify>
  <done>Lời hứa trung tâm của phase có một người canh gác chạy tự động, và người canh gác ấy đã được chứng minh là thức.</done>
</task>

<task type="auto">
  <name>Task 2: Đường đi đầu-cuối của một doanh nghiệp trắng</name>
  <files>scripts/e2e-settings.mjs, package.json</files>
  <read_first>
    - `scripts/e2e-walkthrough.mjs` và `scripts/e2e-photo.mjs` — khuôn kịch bản e2e của dự án:
      đăng nhập bằng cookie thật, gọi HTTP thật, khẳng định bằng mã trả về và nội dung, dọn
      dữ liệu tạo ra ở cuối.
    - `package.json` — cách khai script `test:e2e*`.
    - `.planning/STATE.md` §Blockers mục 03-07 — bốn tài khoản fixture pgTAP còn nằm trong
      `auth.users` cloud làm `listUsers` trả 500; kịch bản này cần `seed:auth` chạy được nên
      **phải kiểm blocker đã được dọn trước khi chạy**.
  </read_first>
  <action>
    Viết `scripts/e2e-settings.mjs` đi hết đường của một doanh nghiệp mới, mỗi bước một
    khẳng định:
    1. Tạo doanh nghiệp mới qua luồng onboarding thật.
    2. Khẳng định: đúng một dòng `company_settings`, **0** ngày lễ, **0** hệ số.
    3. Mở `/admin/settings` — cả bốn tab tải được, tab Tăng ca hiện "Chưa khai hệ số" bốn lần.
    4. Khai một ngày lễ, khai bốn hệ số, sửa ân hạn của ca mặc định.
    5. Chấm công một lượt (dùng lại đường của `e2e-photo.mjs`), đọc lại bản ghi: mang đúng
       loại ngày và giờ quy đổi theo hệ số vừa khai.
    6. Khai một phiên bản hệ số mới hiệu lực từ hôm nay, đọc lại **bản ghi cũ**: giờ quy đổi
       **không đổi**.
    7. Dọn sạch dữ liệu đã tạo; in một dòng tổng kết mỗi bước.

    Khai `test:e2e-settings` trong `package.json`.
  </action>
  <acceptance_criteria>
    - `npm run test:e2e-settings` thoát 0 trên môi trường dev thật.
    - Bước 6 in ra hai con số bằng nhau và kịch bản khẳng định điều đó, không chỉ in ra.
    - `git status` sạch sau khi chạy (dữ liệu tạo ra đã dọn); kiểm và ghi vào SUMMARY.
    - Nếu blocker 03-07 chưa được dọn: **dừng**, ghi vào SUMMARY, không đánh dấu Task 2 xong.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e-settings</automated>
  </verify>
  <done>Có một kịch bản chạy được đi từ doanh nghiệp trắng tới số liệu công quy đổi, và nó khẳng định chính điều mà phase hứa.</done>
</task>

<task type="checkpoint" gate="blocking">
  <name>Task 3: Nghiệm thu bốn tiêu chí và đóng phase</name>
  <files>.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-UAT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 4 — bốn tiêu chí, nguyên văn.
    - `.planning/phases/03-.../03-UAT.md` — khuôn biên bản nghiệm thu của Phase 3.
    - Bốn file SUMMARY của 04-01…04-05.
  </read_first>
  <action>
    **(a) `04-UAT.md`** — với chủ dự án, chạy qua bốn tiêu chí trên hệ thống thật, mỗi tiêu chí
    ghi lại quan sát cụ thể (con số, ảnh chụp màn hình nếu có), không ghi "đạt":

    1. Đặt giờ làm chuẩn và ân hạn cho từng ca trên `/admin/settings` → lần chấm kế tiếp phân
       loại theo ngưỡng mới.
    2. Thêm / sửa / xoá ngày lễ của riêng doanh nghiệp; doanh nghiệp mới khởi tạo có 0 ngày lễ.
    3. Khai bốn hệ số; không hệ số nào có sẵn trước khi khai.
    4. Sửa quy tắc hôm nay → phân loại của bản ghi hôm qua không đổi.

    Ghi rõ cả những gì **chưa** làm được và các giới hạn đã biết: mô hình không cộng dồn của
    D-28; ngày lễ quá khứ vẫn sửa được (chặn theo kỳ đã chốt thuộc Phase 5); hệ số hiệu lực
    lùi vẫn khai được.

    **(b)** cập nhật `ROADMAP.md` (danh sách plan Phase 4, đánh dấu hoàn thành, bảng Progress),
    `REQUIREMENTS.md` (SET-01…SET-04 → Complete), `STATE.md` (`current_phase: 5`, số plan,
    Blockers còn lại, mục Decisions của phase).
  </action>
  <acceptance_criteria>
    - `04-UAT.md` tồn tại với bốn mục, mỗi mục có quan sát cụ thể chứ không phải chữ "đạt".
    - Giới hạn đã biết được liệt kê tường minh trong `04-UAT.md`.
    - `ROADMAP.md` Phase 4 đánh dấu `[x]` với đủ 6 plan; bảng Progress cập nhật.
    - `REQUIREMENTS.md` bốn dòng SET chuyển Complete.
    - `STATE.md` chuyển sang Phase 5 với Blockers còn lại được ghi đúng.
    - `npm run typecheck && npm run lint && npm test && npm run build` thoát 0 lần cuối trước khi đóng phase.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>
  <done>Phase đóng bằng quan sát trên hệ thống thật, với giới hạn đã biết được viết ra chứ không để người sau tự phát hiện.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cổng tự động → mã nguồn tương lai | Đây là thứ duy nhất của phase còn hiệu lực sau khi phase kết thúc |
| Biên bản nghiệm thu → trạng thái thật của hệ thống | Một biên bản ghi "đạt" mà không có quan sát là một lời khẳng định trống |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Tampering | Cổng bị nới lỏng dần bằng các mục loại trừ cho tới khi không bắt được gì | high | mitigate | Giới hạn ≤ 5 mục loại trừ, mỗi mục có comment lý do; prohibition tường minh |
| T-04-06-02 | Repudiation | Phase được đánh dấu hoàn thành trong khi một tiêu chí chưa từng được quan sát trên hệ thống thật | high | mitigate | Task 3 là checkpoint chặn; biên bản đòi quan sát cụ thể, không nhận chữ "đạt" |
| T-04-06-03 | Denial of Service | Kịch bản e2e tạo dữ liệu rác trên môi trường dev và không dọn | medium | mitigate | Bước 7 dọn sạch; acceptance criteria kiểm `git status` và trạng thái database |
| T-04-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Cổng chặn con số nhúng cứng chạy trong `npm test` và đã được chứng minh có răng
- `npm run test:e2e-settings` thoát 0, đi từ doanh nghiệp trắng tới số liệu quy đổi
- Bốn tiêu chí của phase có biên bản nghiệm thu với quan sát cụ thể
- ROADMAP / REQUIREMENTS / STATE phản ánh đúng trạng thái sau phase
</verification>

<success_criteria>
Phase kết thúc với một người canh gác tự động cho lời hứa trung tâm của nó, một biên bản
nghiệm thu dựa trên quan sát thật, và một danh sách giới hạn đã biết được viết ra thay vì để
người sau tự vấp phải.
</success_criteria>

<output>
Create `.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-06-SUMMARY.md` when done
</output>
