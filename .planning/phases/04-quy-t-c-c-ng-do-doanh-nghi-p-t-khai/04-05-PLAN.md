---
phase: 04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - src/lib/attendance/classification.ts
  - src/lib/attendance/__tests__/classification.test.ts
  - src/lib/validation/api/attendance.ts
  - src/app/api/attendance/route.ts
  - src/app/api/attendance/summary/route.ts
  - src/app/admin/attendance/attendance-view.tsx
  - src/components/employee-app/monthly-summary-card.tsx
  - src/lib/constants.ts
  - src/lib/data/__tests__/attendance-classification.test.ts
autonomous: true
requirements: [SET-04, SET-03]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Mỗi bản ghi chấm công mang **loại ngày** (thường / cuối tuần / lễ) và **số phút đêm**, tính từ quy tắc của chính doanh nghiệp đó — ngày lễ từ bảng `holidays`, ngày nghỉ từ `working_days` của ca, khung giờ đêm từ `company_settings` (SET-04)."
    - "Giờ tăng ca quy đổi tính bằng phút tăng ca nhân hệ số **đang hiệu lực tại `work_date` của bản ghi**, đọc qua `tf_overtime_multiplier()` — không phải hệ số hôm nay (D-25, tiêu chí 4 của phase)."
    - "Sửa hệ số hôm nay rồi mở lại bản ghi của tháng trước: giờ quy đổi của tháng trước **không đổi**. Đây là bài kiểm quan trọng nhất của cả phase và nó phải chạy tự động, không phải kiểm bằng mắt."
    - "Loại ngày và giờ quy đổi được **tính tại thời điểm truy vấn**, không lưu vào cột nào của `attendance_records` (cùng khuôn D-21 của 03-06)."
    - "Hệ số áp cho một phút = hệ số của LOẠI NGÀY (thường | cuối tuần | lễ) CỘNG phụ cấp ca đêm nếu phút đó rơi vào khung giờ đêm (D-28a, chốt 2026-08-06). Không nhân chồng: lễ ×3.0 + đêm 0.3 cho ra ×3.3, không phải ×3.9."
    - "Doanh nghiệp chưa khai hệ số cho loại ngày tương ứng thì giờ quy đổi là `null` và màn hình hiện **\"chưa khai hệ số\"** — tuyệt đối không ngầm lấy 1.0 (D-26)."
    - "Màn hình chấm công của quản trị và tổng hợp tháng của nhân viên hiển thị **cả giờ thô lẫn giờ quy đổi**, kèm câu nói rõ đây là số liệu công chứ chưa phải căn cứ trả lương (D-24, D-28)."
    - "Ca qua đêm 22:00–06:00 được tính trọn vào ngày bắt đầu (D-08) và phút đêm của nó không bị cắt đôi qua hai ngày."
  artifacts:
    - path: "src/lib/attendance/classification.ts"
      provides: "Mô-đun thuần phân loại loại ngày, phút đêm, phút tăng ca, giờ quy đổi"
      exports: ["classifyWorkDay", "nightMinutes", "overtimeMinutes", "convertedOvertimeHours"]
    - path: "src/lib/attendance/__tests__/classification.test.ts"
      provides: "Bằng chứng cho tiêu chí 4 và cho quy ước không-cộng-dồn"
  key_links:
    - from: "src/app/api/attendance/route.ts"
      to: "src/lib/attendance/classification.ts"
      via: "phân loại tính lúc đọc, không đọc từ cột đã lưu"
      pattern: "classifyWorkDay"
    - from: "src/app/api/attendance/summary/route.ts"
      to: "supabase/migrations/0016_overtime_rules_append_only.sql"
      via: "hệ số tra theo work_date của bản ghi, không theo ngày hôm nay"
      pattern: "tf_overtime_multiplier"
  prohibitions:
    - statement: "MUST NOT thêm bất kỳ cột nào vào `attendance_records` để lưu loại ngày, phút đêm hay giờ quy đổi. 03-06 đã chọn khuôn tính-lúc-truy-vấn đúng vì lý do này: một cột đã ghi cứng nghĩa là dữ liệu lịch sử mang phán quyết theo một quy tắc cũ mà không ai còn biết quy tắc đó là gì."
      category: architecture
    - statement: "MUST NOT tra hệ số theo ngày hôm nay. Hệ số phải tra theo `work_date` của chính bản ghi đang tính. Nhầm chỗ này làm tiêu chí 4 của phase sai hoàn toàn trong khi mọi test khác vẫn xanh."
      category: correctness
    - statement: "MUST NOT thay `null` bằng 1.0 ở bất kỳ đâu trên đường quy đổi, kể cả trong một biểu thức hiển thị. Màn hình thà nói 'chưa khai hệ số' còn hơn hiện một con số trông như đã tính đúng."
      category: correctness
    - statement: "MUST NOT trình bày giờ quy đổi như số tiền hoặc như căn cứ trả lương. Tính lương nằm ngoài phạm vi V2 và mô hình không cộng dồn của D-28 thấp hơn cách tính của luật lao động."
      category: values
---

<objective>
Đóng vòng của phase: những gì doanh nghiệp khai ở ba plan trước trở thành cách bản ghi chấm
công được phân loại — theo quy tắc **đang hiệu lực tại thời điểm phát sinh**, không phải quy
tắc hôm nay.

Purpose: đây là tiêu chí 4 của phase và là lý do tồn tại của toàn bộ mô hình append-only ở
04-04. Ba plan trước dựng chỗ để khai; plan này là chỗ duy nhất chứng minh việc khai ấy có
nghĩa. Nếu plan này tra hệ số theo ngày hôm nay thay vì theo `work_date`, toàn bộ công sức
phiên bản hoá của 04-04 thành vô ích và **không một test nào ở các plan trước phát hiện ra**.

Output: `src/lib/attendance/classification.ts`, hai đường đọc chấm công mang thêm phân loại
và giờ quy đổi, hai màn hình hiển thị chúng.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-CONTEXT.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-03-SUMMARY.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-04-SUMMARY.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-08-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Mô-đun | `src/lib/attendance/classification.ts` |
| Hàm | `classifyWorkDay()`, `nightMinutes()`, `overtimeMinutes()`, `convertedOvertimeHours()` |
| Kiểu | `WorkDayType`, `AttendanceClassification` |
| Mở rộng | `attendanceRecordSchema`, `monthlySummarySchema` mang thêm phân loại và giờ quy đổi |
| Test | `classification.test.ts`, `attendance-classification.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mô-đun phân loại — thuần, kiểm được, không phụ thuộc database</name>
  <files>src/lib/attendance/classification.ts, src/lib/attendance/__tests__/classification.test.ts</files>
  <read_first>
    - `src/lib/attendance/suspicious.ts` — khuôn mô-đun thuần của dự án: hằng số mặc định có
      tên, hàm quyết định duy nhất, khối comment giải thích **vì sao** chứ không phải cái gì.
    - `src/lib/attendance/day.ts` — `groupAttendanceByDay()`, `breakToDeductMinutes()`: giờ
      nghỉ được trừ **một lần cho mỗi ngày**, không phải mỗi dòng (migration 0014). Phép tính
      phút tăng ca phải đi qua đúng phép gộp ngày này, nếu không tổng hợp tháng và màn hình
      chấm công sẽ lệch nhau mà không ai biết bên nào đúng.
    - `supabase/migrations/0003_enums_time.sql` — `tf_shift_minutes()`: độ dài ca theo kế
      hoạch, kể cả ca qua đêm. Phút tăng ca là phần vượt trên con số này.
    - `supabase/migrations/0004_core_entities.sql` — `shifts.working_days` (mảng 1..7) quyết
      định ngày nào là ngày nghỉ của ca đó; **không** giả định thứ Bảy/Chủ nhật.
    - `.planning/phases/04-.../04-CONTEXT.md` D-26, D-27, D-28 — nguyên văn, gồm cả lựa chọn
      đã chốt ở 04-04 Task 4.
    - `.planning/phases/01-.../01-02-SUMMARY.md` — D-08: ca qua đêm tính trọn vào ngày bắt đầu.
  </read_first>
  <behavior>
    - `classifyWorkDay({ workDate, holidayDates, workingDays })` trả `'holiday'` khi ngày nằm
      trong danh sách ngày lễ của doanh nghiệp; `'weekend'` khi thứ trong tuần **không** thuộc
      `working_days` của ca; ngược lại `'weekday'`.
    - Ngày vừa là ngày lễ vừa ngoài `working_days` trả `'holiday'` — ưu tiên lễ.
    - `nightMinutes({ checkInAt, checkOutAt, nightStart, nightEnd })` trả số phút giao với
      khung giờ đêm; khung đêm vắt qua nửa đêm (22:00–06:00) được xử lý đúng, không cho ra số
      âm và không cắt bản ghi thành hai ngày.
    - Bản ghi nằm trọn ngoài khung đêm trả 0; bản ghi nằm trọn trong khung đêm trả đúng tổng
      thời lượng của nó.
    - `overtimeMinutes({ workedMinutes, scheduledMinutes, dayType })`: ngày thường trả phần
      vượt trên độ dài ca theo kế hoạch, sàn 0; ngày lễ và ngày nghỉ trả **toàn bộ**
      `workedMinutes` (làm việc vào ngày không phải ngày làm là tăng ca từ phút đầu tiên).
    - Phút đêm được đếm **trong phần phút tăng ca**, không phải trong toàn bộ giờ làm: phụ cấp
      đêm chỉ cộng lên những phút vừa là tăng ca vừa nằm trong khung đêm.
    - `convertedOvertimeHours({ minutesByType, multiplierByType })` trả tổng giờ quy đổi, hoặc
      `null` nếu **bất kỳ** loại nào có phút lớn hơn 0 mà thiếu hệ số. Không bỏ qua phần thiếu
      để trả một con số bộ phận trông như đầy đủ.
    - Mỗi phút được đếm đúng một lần: tổng phút theo loại luôn bằng tổng phút tăng ca, kiểm
      bằng một test bất biến.
  </behavior>
  <action>
    Viết `classification.ts` thuần (không import Supabase, không đọc `process.env`, kiểm được
    bằng Vitest như `suspicious.ts`). Khối comment đầu file ghi ba việc: (1) vì sao mọi thứ ở
    đây tính lúc truy vấn và không có cột nào lưu (khuôn D-21 của 03-06); (2) quy ước ưu tiên
    loại ngày đã chốt kèm giới hạn D-28 — mô hình này **thấp hơn** cách tính của luật lao
    động và V3 sẽ phải dựng lại phép cộng dồn; (3) vì sao thiếu hệ số trả `null` chứ không
    trả 1.0 (D-26).

    Test phủ đủ tám hành vi, trong đó bắt buộc có: ca qua đêm 22:00–06:00, ngày vừa lễ vừa
    cuối tuần, khung đêm tuỳ chỉnh khác mặc định, và test bất biến "mỗi phút đúng một loại".
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/attendance/__tests__/classification.test.ts` thoát 0 và báo ít nhất 12 test.
    - `grep -nE "supabase|process\.env|new Date\(\)" src/lib/attendance/classification.ts` không trả dòng nào.
    - `grep -nE "\?\? *1|\|\| *1\b|= *1\.0" src/lib/attendance/classification.ts` không trả dòng nào.
    - `grep -nE "\b(6|0)\b.*getDay|getDay.*\b(6|0)\b" src/lib/attendance/classification.ts` không trả dòng nào — cuối tuần suy từ `working_days` của ca, không nhúng cứng thứ Bảy/Chủ nhật.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/attendance/__tests__/classification.test.ts</automated>
  </verify>
  <done>Phép phân loại nằm gọn trong một mô-đun thuần, kiểm được từng nhánh, và không nhánh nào bịa ra một con số khi doanh nghiệp chưa khai.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hai đường đọc mang theo phân loại và giờ quy đổi</name>
  <files>src/lib/validation/api/attendance.ts, src/app/api/attendance/route.ts, src/app/api/attendance/summary/route.ts, src/lib/data/__tests__/attendance-classification.test.ts</files>
  <read_first>
    - `src/app/api/attendance/summary/route.ts` toàn file — nó **đã** đọc ca cùng bản ghi để
      phép gộp ngày khớp với giao diện (migration 0014); phân loại phải đi vào đúng chỗ đó,
      không mở một phép gộp thứ hai.
    - `src/app/api/attendance/route.ts` — khuôn Route Handler đọc chấm công có phân quyền.
    - `src/app/api/attendance/review/route.ts` (03-06, sửa ở 04-01) — tiền lệ đọc cấu hình
      doanh nghiệp cùng lúc với truy vấn chính, và comment giải thích vì sao cờ tính lúc truy
      vấn.
    - `supabase/migrations/0016_overtime_rules_append_only.sql` (04-04) —
      `tf_overtime_multiplier(company_id, rule_key, date)`; gọi qua `supabase.rpc()`.
    - `src/lib/data/holidays.ts` (04-03) — đường đọc ngày lễ; ở tầng Route Handler nên truy
      vấn thẳng bảng theo khoảng ngày của kỳ thay vì gọi lại HTTP nội bộ.
    - `src/lib/validation/api/attendance.ts` — cặp schema hiện có; trường mới phải **tuỳ
      chọn** hoặc `nullable` để không phá màn hình nào đang đọc schema này.
  </read_first>
  <behavior>
    - Mỗi phần tử trả về mang thêm: `dayType`, `nightMinutes`, `overtimeMinutes`,
      `convertedOvertimeHours` (có thể `null`), và `multiplierMissing` (danh sách loại thiếu
      hệ số).
    - Hệ số tra theo `work_date` của **chính bản ghi**, không theo ngày hôm nay — với một
      tháng chứa hai phiên bản hệ số khác nhau, các ngày trước ngày hiệu lực dùng hệ số cũ và
      các ngày sau dùng hệ số mới, trong **cùng một phản hồi**.
    - Ngày lễ đọc một lần cho cả khoảng ngày của truy vấn, không một truy vấn cho mỗi bản ghi.
    - Doanh nghiệp chưa khai hệ số nào: `convertedOvertimeHours` là `null` cho mọi phần tử,
      `multiplierMissing` liệt kê đúng các loại thiếu, mã trả về vẫn 200.
    - Tổng hợp tháng cộng đúng: tổng giờ quy đổi của tháng bằng tổng của từng ngày; nếu bất kỳ
      ngày nào thiếu hệ số thì tổng tháng là `null` kèm cờ thiếu, **không** cộng bộ phận.
    - Không cột nào của `attendance_records` được thêm hay ghi.
    - Phân quyền và ranh giới doanh nghiệp giữ nguyên như trước plan này.
  </behavior>
  <action>
    Mở rộng hai Route Handler theo `<behavior>`; mở rộng schema với trường mới ở dạng
    `nullable`/tuỳ chọn. Comment tại chỗ ở nơi gọi `tf_overtime_multiplier` ghi **một câu duy
    nhất nhưng phải có**: hệ số tra theo `work_date` của bản ghi chứ không theo hôm nay — đây
    là điểm mà tiêu chí 4 của phase sống hoặc chết.

    `attendance-classification.test.ts` phủ đủ bảy hành vi, trong đó **bài kiểm bắt buộc**:
    dựng hai phiên bản hệ số (cũ hiệu lực từ đầu tháng trước, mới hiệu lực từ hôm nay), đọc
    một bản ghi của tháng trước và một bản ghi của hôm nay trong cùng một phản hồi, khẳng định
    hai giờ quy đổi tính theo hai hệ số khác nhau.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/data/__tests__/attendance-classification.test.ts` thoát 0 và báo ít nhất 7 test.
    - `npx vitest run` thoát 0 — mọi test cũ của `attendance`/`summary` vẫn xanh (schema mở rộng không phá màn hình nào).
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from information_schema.columns where table_name='attendance_records' and column_name in ('day_type','night_minutes','overtime_minutes','converted_overtime_hours')"` trả về `0`.
    - `grep -c "tf_overtime_multiplier" src/app/api/attendance/summary/route.ts src/app/api/attendance/route.ts` — tổng ít nhất `2`.
    - `grep -nE "work_date|workDate" src/app/api/attendance/summary/route.ts | grep -i "multiplier"` — kiểm bằng mắt rằng tham số ngày truyền vào RPC là `work_date` của bản ghi; ghi kết luận vào SUMMARY.
    - **Kiểm bằng tay:** khai `weekday` = 1.5 hiệu lực từ đầu tháng trước; đọc số giờ quy đổi của một ngày tháng trước; khai `weekday` = 2.0 hiệu lực từ hôm nay; đọc lại **đúng ngày đó** → con số **không đổi**. Ghi cả ba con số vào SUMMARY. Đây là bằng chứng nghiệm thu của tiêu chí 4.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Dữ liệu chấm công đọc ra đã mang phân loại và giờ quy đổi đúng theo quy tắc của ngày phát sinh, và có bằng chứng chạy được rằng sửa quy tắc hôm nay không chạm vào hôm qua.</done>
</task>

<task type="auto">
  <name>Task 3: Hai màn hình nói ra con số — và nói cả giới hạn của nó</name>
  <files>src/app/admin/attendance/attendance-view.tsx, src/components/employee-app/monthly-summary-card.tsx, src/lib/constants.ts</files>
  <read_first>
    - `src/app/admin/attendance/attendance-view.tsx` — bảng chấm công của quản trị; cột mới
      phải vừa vào bố cục hiện có, không dựng bảng thứ hai.
    - `src/components/employee-app/monthly-summary-card.tsx` — thẻ tổng hợp tháng của nhân
      viên trên giao diện mobile; đây là nơi nhân viên đọc số của chính mình nên chữ ở đây
      quan trọng hơn ở màn hình quản trị.
    - `src/components/common/status-badge.tsx` — quy ước biểu tượng cộng nhãn chữ, không phân
      biệt bằng màu đơn thuần.
    - `src/lib/constants.ts` — nhãn `WORK_DAY_TYPE_LABEL` cho ba loại ngày và chữ "chưa khai
      hệ số"; enum giữ tiếng Anh.
    - `src/lib/format.ts` — định dạng giờ và số thập phân; lớp `.num` cho số đo.
    - `.planning/phases/03-.../03-UI-SPEC.md` §"Zero/one/many", §"Loading", §"status never
      color-only" — vẫn áp dụng nguyên trạng.
  </read_first>
  <action>
    **(a) `attendance-view.tsx`** — thêm cột loại ngày (nhãn chữ + biểu tượng) và cột giờ tăng
    ca hiển thị **cả giờ thô lẫn giờ quy đổi** (dạng "6h00 → 9h00 (×1.5)"). Thiếu hệ số thì ô
    hiện "Chưa khai hệ số" kèm liên kết tới `/admin/settings` tab Tăng ca — người đọc thấy
    thiếu thì đi khai được ngay, không phải tự tìm.

    **(b) `monthly-summary-card.tsx`** — thêm dòng giờ tăng ca thô và giờ quy đổi của tháng.
    Nếu tổng tháng là `null` thì hiện "Chưa khai hệ số" thay vì số 0 — **số 0 là một lời nói
    dối ở đây**, nó nói với nhân viên rằng họ không có giờ tăng ca nào.

    **(c) `constants.ts`** — nhãn ba loại ngày, chữ "chưa khai hệ số", và **một câu giới hạn
    dùng chung cho cả hai màn hình**: giờ quy đổi là số liệu công theo quy tắc doanh nghiệp đã
    khai, chưa phải căn cứ tính lương (D-28). Đặt ở một hằng số duy nhất để hai màn hình không
    nói hai câu khác nhau.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -rn "Chưa khai" src/app/admin/attendance/attendance-view.tsx src/components/employee-app/monthly-summary-card.tsx` trả về ít nhất `2` dòng.
    - `grep -rnE "\?\? *0|\|\| *0" src/components/employee-app/monthly-summary-card.tsx` — mọi kết quả phải nằm ngoài đường giờ quy đổi; kiểm bằng mắt và ghi kết luận vào SUMMARY.
    - `grep -c "OVERTIME_DISCLAIMER\|chưa phải căn cứ" src/lib/constants.ts` trả về ít nhất `1`, và cả hai màn hình cùng dùng hằng số đó.
    - **Kiểm bằng tay:** (1) doanh nghiệp đã khai đủ bốn hệ số → cả hai màn hình hiện giờ thô và giờ quy đổi; (2) xoá hệ số bằng cách khai một phiên bản cho một khoá khác rồi xem một doanh nghiệp chưa khai → cả hai màn hình hiện "Chưa khai hệ số", **không** hiện 0; (3) một ngày lễ đã khai → dòng của ngày đó mang nhãn "Ngày lễ" và giờ quy đổi theo hệ số lễ. Ghi cả ba quan sát vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Con số quy đổi hiện ra ở hai nơi người ta thật sự đọc, kèm đúng một câu nói rõ nó là gì và chưa là gì.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quy tắc hiện hành → phân loại bản ghi lịch sử | Ranh giới trung tâm của cả phase; tra sai ngày là viết lại lịch sử một cách vô hình |
| Phép quy đổi → con số nhân viên đọc | Nhân viên dùng con số này để đối chiếu công của mình |
| Thiếu hệ số → giá trị hiển thị | Một số 0 hoặc một mặc định 1.0 ở đây là sai lặng lẽ, khó phát hiện nhất |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Tampering | Hệ số tra theo ngày hôm nay thay vì `work_date`, làm mọi số liệu lịch sử đổi mỗi lần doanh nghiệp sửa hệ số — trong khi toàn bộ test của 04-04 vẫn xanh | high | mitigate | Prohibition tường minh; Task 2 có bài kiểm bắt buộc hai phiên bản hệ số trong cùng một phản hồi; kiểm tay ba con số ghi vào SUMMARY |
| T-04-05-02 | Tampering | `null` bị thay bằng 1.0 hoặc 0 ở một biểu thức hiển thị, làm màn hình hiện số trông đúng khi doanh nghiệp chưa khai gì | high | mitigate | Hai grep gate ở Task 1 và Task 3; `convertedOvertimeHours` trả `null` khi bất kỳ loại nào thiếu hệ số, không trả tổng bộ phận |
| T-04-05-03 | Repudiation | Nhân viên và quản trị đọc hai con số khác nhau cho cùng một tháng vì hai đường tính khác nhau | high | mitigate | Cả hai màn hình đi qua cùng mô-đun `classification.ts` và cùng phép gộp ngày của `day.ts` (migration 0014); test bất biến "mỗi phút đúng một loại" |
| T-04-05-04 | Information Disclosure | Truy vấn ngày lễ hoặc hệ số vượt ranh giới doanh nghiệp khi đọc theo lô | medium | mitigate | Mọi truy vấn lọc `.eq("company_id", companyId)` từ phiên; RLS là lớp thứ hai; hàm RPC là `security invoker` (04-04) |
| T-04-05-05 | Repudiation | Con số quy đổi bị dùng làm căn cứ trả lương trong khi mô hình không cộng dồn thấp hơn luật | medium | mitigate | D-28: một câu giới hạn dùng chung, hiện ở cả hai màn hình; grep gate đòi hằng số dùng chung |
| T-04-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Không cột nào được thêm vào `attendance_records`
- Hệ số tra theo `work_date` của bản ghi, chứng minh bằng test hai phiên bản trong một phản hồi
- Sửa hệ số hôm nay không đổi giờ quy đổi của tháng trước, kiểm cả tự động lẫn bằng tay
- Thiếu hệ số cho ra "chưa khai hệ số" ở cả hai màn hình, không ra 0 và không ra 1.0
- Câu giới hạn D-28 hiện ở cả hai màn hình từ một hằng số duy nhất
</verification>

<success_criteria>
Những gì doanh nghiệp khai đã thật sự quyết định cách công của người lao động được phân loại,
theo quy tắc của **ngày đó** chứ không phải quy tắc của hôm nay — và ở chỗ chưa khai, hệ thống
nói thẳng là chưa khai thay vì hiện một con số trông như đã tính đúng.
</success_criteria>

<output>
Create `.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-05-SUMMARY.md` when done
</output>
