---
phase: 04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/settings/shift-rules-tab.tsx
  - src/app/admin/settings/settings-view.tsx
  - src/components/shifts/shift-dialog.tsx
  - src/lib/constants.ts
  - src/lib/data/__tests__/shift-rules-effect.test.ts
autonomous: true
requirements: [SET-01]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Tab \"Ca làm việc\" của `/admin/settings` cho chủ doanh nghiệp đặt giờ bắt đầu, giờ kết thúc và thời gian ân hạn đi muộn cho **từng ca**, không phải một giá trị chung cho cả doanh nghiệp (SET-01)."
    - "Sửa ân hạn xong thì lần chấm công **kế tiếp** đã phân loại theo ngưỡng mới — không cần khởi động lại, không cần một bước áp dụng riêng (tiêu chí 1 của phase)."
    - "Bản ghi chấm công đã tồn tại **không đổi** `late_minutes` hay `status` sau khi ân hạn được sửa: giá trị đó được tính lúc chấm và đã nằm trong bản ghi (tiêu chí 4 của phase)."
    - "Tab này và màn hình `/admin/shifts` sửa **cùng một dữ liệu** qua **cùng một Server Action** `updateShift()`; không có đường ghi thứ hai và không có hai chỗ hiển thị hai con số khác nhau."
    - "Ca đang bị nhân viên dùng dở một lượt chấm công chưa tan ca vẫn sửa được, nhưng màn hình nói rõ thay đổi chỉ áp cho lượt chấm sau đó."
    - "Ân hạn âm bị từ chối; ân hạn bằng 0 là hợp lệ và nghĩa là không có ân hạn."
  artifacts:
    - path: "src/components/settings/shift-rules-tab.tsx"
      provides: "Tab giờ làm chuẩn và ân hạn đi muộn theo từng ca"
      min_lines: 90
    - path: "src/lib/data/__tests__/shift-rules-effect.test.ts"
      provides: "Bằng chứng hai chiều: hiệu lực ngay cho bản ghi mới, bất động với bản ghi cũ"
  key_links:
    - from: "src/components/settings/shift-rules-tab.tsx"
      to: "src/lib/data/mutations/shifts.ts"
      via: "dùng lại Server Action đã có, không mở đường ghi thứ hai"
      pattern: "updateShift"
    - from: "src/components/settings/shift-rules-tab.tsx"
      to: "src/lib/data/shifts.ts"
      via: "đọc danh sách ca qua Route Handler đã có"
      pattern: "listShifts"
  prohibitions:
    - statement: "MUST NOT viết một Server Action thứ hai để sửa ca chỉ vì màn hình khác. Hai đường ghi vào cùng một bảng là hai bộ quy tắc hợp lệ sẽ lệch nhau — và ở đây chúng quyết định ai bị tính đi muộn."
      category: architecture
    - statement: "MUST NOT tính lại `late_minutes` của các bản ghi đã tồn tại khi ân hạn thay đổi, kể cả khi có vẻ 'sửa cho đúng'. Đó chính là hành vi mà tiêu chí 4 của phase cấm: người lao động đã được thông báo kết quả chấm công của ngày hôm đó rồi."
      category: correctness
    - statement: "MUST NOT gộp giờ làm chuẩn của ca vào `company_settings`. Giờ làm là thuộc tính của **ca**, và mỗi doanh nghiệp có nhiều ca — gộp lên cấp doanh nghiệp là làm sai chính điều SET-01 yêu cầu."
      category: correctness
---

<objective>
Đưa giờ làm chuẩn và ân hạn đi muộn của từng ca lên trang cài đặt, và **chứng minh bằng test**
hai vế đối nhau của cùng một quy tắc: đổi ngưỡng có hiệu lực ngay với cái sắp xảy ra, và
không chạm được vào cái đã xảy ra.

Purpose: dữ liệu và đường ghi cho việc này **đã tồn tại** từ Phase 1-2 (`shifts.start_time`,
`shifts.end_time`, `shifts.late_tolerance_minutes`, `updateShift()`), và `late_minutes` đã
được tính lúc chấm rồi lưu vào bản ghi (`mutations/attendance.ts:458`). Nghĩa là tiêu chí 1
và tiêu chí 4 của phase **đã đúng sẵn ở vế đi muộn** — nhưng chưa ai chứng minh, và chủ doanh
nghiệp chưa có chỗ nào ngoài màn hình quản lý ca để chạm tới. Plan này làm hai việc đó, không
xây lại thứ đang chạy.

Output: tab "Ca làm việc" trong `/admin/settings`, và một file test là bằng chứng cho tiêu
chí 1 + 4.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-CONTEXT.md
@.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-01-SUMMARY.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-06-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Component | `ShiftRulesTab` |
| Test | `src/lib/data/__tests__/shift-rules-effect.test.ts` |
| Nhãn | khối `SETTINGS_SHIFT_*` trong `constants.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bằng chứng hai chiều cho quy tắc đi muộn</name>
  <files>src/lib/data/__tests__/shift-rules-effect.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/attendance.ts` dòng 440-470 — nơi `late_minutes` được tính:
      hiệu giữa `check_in_at` thật và giờ bắt đầu ca theo kế hoạch, trừ
      `late_tolerance_minutes`, chặn sàn ở 0. Đọc kỹ điều kiện `isFirstPunchOfDay` — chỉ lượt
      đầu tiên trong ngày mới bị tính đi muộn (migration 0013).
    - `src/lib/data/__tests__/attendance.test.ts` — khuôn test tầng dữ liệu có mock Supabase.
    - `src/lib/data/mutations/shifts.ts` — `updateShift()` và những gì nó ghi audit.
    - `supabase/migrations/0010_check_in_time_functions.sql` — `tf_local_instant()` dựng giờ
      bắt đầu ca theo kế hoạch; test phải đi qua cùng đường đó, không tự cộng offset múi giờ.
  </read_first>
  <behavior>
    - Ca có ân hạn 15 phút, nhân viên vào ca muộn 10 phút → `late_minutes` bằng 0, trạng thái
      `on_time`.
    - Sửa ân hạn của chính ca đó xuống 5 phút, rồi một nhân viên khác vào ca muộn 10 phút →
      `late_minutes` bằng 5, trạng thái `late`. **Không** có bước áp dụng nào ở giữa.
    - Bản ghi tạo ra ở bước một, đọc lại sau khi ân hạn đã đổi, vẫn mang `late_minutes` bằng 0
      và trạng thái `on_time` — giá trị cũ, không bị tính lại.
    - Sửa giờ bắt đầu ca sớm hơn 30 phút cũng chỉ ảnh hưởng lượt chấm sau đó; bản ghi cũ giữ
      nguyên cả `late_minutes` lẫn `status`.
    - Lượt chấm thứ hai trong cùng ngày không bị tính đi muộn dù ân hạn đã siết — quy tắc của
      migration 0013 không bị plan này phá.
  </behavior>
  <action>
    Viết `src/lib/data/__tests__/shift-rules-effect.test.ts` phủ đủ năm hành vi trên. Mỗi test
    có một comment một dòng nói **tiêu chí nào của phase** nó đang chứng minh (tiêu chí 1 hay
    tiêu chí 4) — file này là bằng chứng nghiệm thu, người đọc sáu tháng sau phải biết nó tồn
    tại để làm gì.

    Nếu và chỉ nếu test phát hiện hành vi thật khác mô tả trên: **không sửa test cho khớp mã**.
    Dừng lại, ghi chênh lệch vào SUMMARY, vì khi đó tiêu chí 4 của phase đang không đúng và đó
    là phát hiện quan trọng nhất của cả plan.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/data/__tests__/shift-rules-effect.test.ts` thoát 0 và báo ít nhất 5 test.
    - `grep -c "tiêu chí" src/lib/data/__tests__/shift-rules-effect.test.ts` trả về ít nhất `5`.
    - `grep -rn "update.*late_minutes\|late_minutes.*=" src/lib/data/mutations/shifts.ts` không trả dòng nào — sửa ca không đụng tới bản ghi chấm công.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/shift-rules-effect.test.ts</automated>
  </verify>
  <done>Hai vế của tiêu chí 1 và tiêu chí 4 có bằng chứng chạy được, không phải một khẳng định trong tài liệu.</done>
</task>

<task type="auto">
  <name>Task 2: Tab "Ca làm việc" trong trang cài đặt</name>
  <files>src/components/settings/shift-rules-tab.tsx, src/app/admin/settings/settings-view.tsx, src/components/shifts/shift-dialog.tsx, src/lib/constants.ts</files>
  <read_first>
    - `src/app/admin/shifts/*` và `src/components/shifts/shift-dialog.tsx`,
      `shift-card.tsx` — màn hình quản lý ca đã có; tab mới **dùng lại** `ShiftDialog` chứ
      không dựng form thứ hai cho cùng ba trường.
    - `src/app/admin/settings/settings-view.tsx` (04-01) — khung tab đã dành sẵn chỗ; plan này
      chỉ lắp nội dung vào tab thứ hai, **không** đổi thứ tự tab, không sửa tab Chung.
    - `src/lib/data/shifts.ts` + `src/app/api/shifts/route.ts` — đường đọc đã có.
    - `src/lib/data/mutations/shifts.ts` — `updateShift()` đã có, kèm audit.
    - `src/components/common/data-table-skeleton.tsx`, `empty-state.tsx`, `error-state.tsx`.
    - `src/lib/format.ts` — định dạng `HH:mm`.
  </read_first>
  <action>
    **(a) `shift-rules-tab.tsx`** — `"use client"`:
    - `useDataQuery(() => listShifts(), [])`; ca đã lưu trữ (`status='archived'`) không hiện,
      vì quy tắc của một ca không còn dùng thì không phải thứ cần khai lại.
    - Bảng gọn: tên ca, mã, giờ bắt đầu → giờ kết thúc, ân hạn đi muộn, số nhân viên đang
      thuộc ca, một nút mở `ShiftDialog`.
    - Ca qua đêm hiện một nhãn chữ nhỏ "qua đêm" cạnh khoảng giờ — cột `overnight` là cột sinh
      trong database, **chỉ đọc lại, không bao giờ tính lại ở tầng ứng dụng** (D của 02-06).
    - Trạng thái tải dùng `DataTableSkeleton` sẵn có; lỗi dùng `ErrorState` kèm `onRetry`.
    - Doanh nghiệp chưa có ca nào (không xảy ra sau onboarding nhưng vẫn phải có đường):
      `EmptyState` dẫn thẳng sang `/admin/shifts` để tạo ca.
    - Một dòng chữ trợ giúp ở đầu tab nói đúng hệ quả, không mô tả lại giao diện: thay đổi ở
      đây áp cho **những lần chấm công sau đó**; các bản ghi đã có giữ nguyên cách phân loại
      của ngày hôm đó.

    **(b) `settings-view.tsx`** — render `ShiftRulesTab` vào tab thứ hai, bỏ khối "Đang xây
    dựng".

    **(c) `shift-dialog.tsx`** — chỉ chỉnh nếu cần để dùng lại được trong ngữ cảnh tab (ví dụ
    nhận `onSaved` để tab tự tải lại). Không đổi schema, không đổi tập trường.

    **(d) `constants.ts`** — nhãn `SETTINGS_SHIFT_*`.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0; `npx vitest run` thoát 0.
    - `grep -c "updateShift" src/components/settings/shift-rules-tab.tsx src/components/shifts/shift-dialog.tsx` — tổng số nơi gọi không tăng thêm một đường ghi mới; ghi kết luận vào SUMMARY.
    - `grep -rn "from(\"shifts\")" src/lib/data/mutations/ | wc -l` không tăng so với trước plan.
    - `grep -c "ShiftDialog" src/components/settings/shift-rules-tab.tsx` trả về ít nhất `1`.
    - `grep -ci "archived" src/components/settings/shift-rules-tab.tsx` trả về ít nhất `1`.
    - **Kiểm vòng đóng bằng tay:** với phiên owner, sửa ân hạn của ca hành chính từ 15 xuống 5; chấm công một lượt vào ca muộn ~10 phút bằng tài khoản nhân viên thật; bản ghi mới mang `late_minutes` bằng 5 và trạng thái "Đi muộn", trong khi bản ghi của ngày trước đó giữ nguyên. Khôi phục ân hạn. Ghi cả ba quan sát vào SUMMARY.
    - Kiểm audit: `select before->>'late_tolerance_minutes', after->>'late_tolerance_minutes' from audit_log where entity_table='shifts' order by created_at desc limit 1` trả đúng cặp giá trị vừa sửa. Ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Chủ doanh nghiệp đặt được giờ làm chuẩn và ân hạn của từng ca ngay trong trang cài đặt, qua đúng một đường ghi duy nhất, và biết rõ thay đổi của mình áp từ lúc nào.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt quản trị → `updateShift()` | Sửa ngưỡng quyết định ai bị tính đi muộn |
| Quy tắc hiện hành → bản ghi lịch sử | Ranh giới giữa "áp cho tương lai" và "viết lại quá khứ" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Tampering | Một lượt tính lại `late_minutes` cho dữ liệu cũ được thêm vào "cho nhất quán", âm thầm xoá lịch sử đi muộn của nhân viên | high | mitigate | `<prohibitions>` cấm tường minh; Task 1 có test khẳng định bản ghi cũ bất động; grep gate chứng minh `mutations/shifts.ts` không chạm bảng chấm công |
| T-04-02-02 | Elevation of Privilege | Vai trò thấp hơn sửa ân hạn qua tab mới | high | mitigate | `updateShift()` đã có `requireRole(role,["owner","admin"])` từ 02-06; tab không mở đường ghi mới nên không mở bề mặt mới |
| T-04-02-03 | Repudiation | Ân hạn bị siết ngay trước kỳ chốt mà không ai biết ai đổi | medium | mitigate | `updateShift()` ghi audit before/after sẵn từ 02-06; acceptance criteria kiểm dòng audit thật |
| T-04-02-04 | Tampering | Hai màn hình sửa ca qua hai đường ghi khác nhau, lệch quy tắc kiểm tra | medium | mitigate | Prohibition + grep gate đếm số nơi ghi bảng `shifts` không tăng |
| T-04-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Đổi ân hạn có hiệu lực ngay với lượt chấm kế tiếp, quan sát được trên hệ thống chạy thật
- Bản ghi chấm công đã tồn tại không đổi `late_minutes` và `status`
- Không đường ghi thứ hai nào vào bảng `shifts` được tạo ra
- Mỗi lần sửa ca để lại dòng audit mang before/after
</verification>

<success_criteria>
Chủ doanh nghiệp khai được giờ làm và ân hạn của từng ca ở đúng nơi họ tìm — trang cài đặt —
và hệ thống chứng minh được rằng thay đổi ấy áp cho ngày mai chứ không viết lại hôm qua.
</success_criteria>

<output>
Create `.planning/phases/04-quy-t-c-c-ng-do-doanh-nghi-p-t-khai/04-02-SUMMARY.md` when done
</output>
