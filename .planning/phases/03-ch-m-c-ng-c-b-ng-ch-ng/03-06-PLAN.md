---
phase: 03-ch-m-c-ng-c-b-ng-ch-ng
plan: 06
type: execute
wave: 4
depends_on: ["03-02", "03-05"]
files_modified:
  - src/lib/attendance/suspicious.ts
  - src/lib/attendance/__tests__/suspicious.test.ts
  - src/lib/validation/api/attendance-review.ts
  - src/lib/data/attendance-review.ts
  - src/app/api/attendance/review/route.ts
  - src/app/admin/attendance/review/page.tsx
  - src/app/admin/attendance/review/attendance-review-view.tsx
  - src/lib/nav.ts
  - src/lib/data/__tests__/attendance-review.test.ts
autonomous: true
requirements: [ATT-07]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Một lần chấm công cách tâm điểm làm việc quá xa bị đánh dấu đáng ngờ và xuất hiện trong danh sách quản trị cần xem lại (ATT-07)."
    - "Ngưỡng đáng ngờ mặc định là 5 lần bán kính của chính điểm làm việc đó, và nó là một hằng số có tên khai đúng một chỗ — không nhúng cứng rải rác, để Phase 4 đọc được từ cấu hình doanh nghiệp mà không phải đi tìm (D-21, D-21a)."
    - "Cờ đáng ngờ được **tính tại thời điểm truy vấn** từ `distance_meters` so với `radius_meters` nhân ngưỡng hiện hành; không có cột boolean nào lưu cứng nó. Đổi ngưỡng thì danh sách tự cập nhật, không cần ghi đè hàng loạt lên dữ liệu lịch sử."
    - "Nhân viên có `canCheckInRemotely` bằng `true` bị **loại khỏi** điều kiện lọc danh sách cần xem lại: với họ, khoảng cách xa là kỳ vọng bình thường chứ không phải bất thường. `distance_meters` của họ vẫn được ghi và vẫn xem lại được từ hồ sơ cá nhân."
    - "Cách đo đáng ngờ **không** dựa trên tốc độ di chuyển giữa hai lần chấm liên tiếp. Chuỗi chấm công của sản phẩm luôn là vào rồi ra, cách nhau trọn một ca, nên luật theo tốc độ hầu như không bao giờ kích hoạt (D-21)."
    - "Danh sách hiển thị mỗi dòng gồm: nhân viên, điểm làm việc, khoảng cách kèm bội số so với bán kính, thời điểm, trạng thái xem xét, và một hành động mở Dialog xem lại (ATT-07, UI-SPEC: Screen — needs-review list)."
    - "Danh sách rỗng hiện một trạng thái trấn an, không phải một ngõ cụt: \"Không có bản ghi nào cần xem lại\" kèm \"Mọi lần chấm công gần đây đều nằm trong hoặc gần bán kính điểm làm việc.\" (UI-SPEC: Empty — needs-review list)."
    - "Đang tải dùng đúng khối `Skeleton` của bảng như các màn hình quản trị khác, không phải một mẫu tải mới (UI-SPEC: Loading — admin lists)."
    - "Một kết quả và nhiều kết quả dùng cùng một mẫu dòng bảng; không có bố cục riêng cho trường hợp đúng một kết quả (UI-SPEC: zero/one/many)."
    - "Mọi trạng thái trong bảng đi kèm biểu tượng và nhãn chữ, không phân biệt bằng màu đơn thuần (UI-SPEC: status never color-only)."
    - "Bản ghi có `distance_meters` bằng `null` (doanh nghiệp chưa khai điểm làm việc, hoặc bản ghi tạo trước phase này) **không** rơi vào danh sách — thiếu phép đo không phải bằng chứng của bất thường."
  artifacts:
    - path: "src/lib/attendance/suspicious.ts"
      provides: "Ngưỡng đáng ngờ có tên và một hàm quyết định duy nhất"
      exports: ["SUSPICIOUS_DISTANCE_MULTIPLIER", "isSuspiciousPunch", "suspiciousMultiplier"]
    - path: "src/app/api/attendance/review/route.ts"
      provides: "Route Handler danh sách bản ghi cần xem lại"
      exports: ["GET", "dynamic"]
    - path: "src/app/admin/attendance/review/attendance-review-view.tsx"
      provides: "Màn hình quản trị danh sách cần xem lại"
      min_lines: 90
  key_links:
    - from: "src/app/api/attendance/review/route.ts"
      to: "src/lib/attendance/suspicious.ts"
      via: "ngưỡng đọc từ một chỗ duy nhất, không nhúng cứng trong truy vấn"
      pattern: "SUSPICIOUS_DISTANCE_MULTIPLIER"
    - from: "src/app/admin/attendance/review/attendance-review-view.tsx"
      to: "src/components/attendance/attendance-photo-dialog.tsx"
      via: "hành động Xem chi tiết mở đúng Dialog xem lại của plan 03-05"
      pattern: "AttendancePhotoDialog"
    - from: "src/lib/nav.ts"
      to: "src/app/admin/attendance/review/page.tsx"
      via: "mục điều hướng quản trị trỏ tới danh sách cần xem lại"
      pattern: "/admin/attendance/review"
  prohibitions:
    - statement: "MUST NOT đưa nhân viên đã được đánh dấu cho phép chấm công từ xa vào danh sách cần xem lại chỉ vì họ ở xa. Một danh sách toàn người hợp lệ là một danh sách không ai đọc, và sau D-20 đây là lớp phát hiện gian lận CHÍNH chứ không còn là lớp phụ — làm nó thành nhiễu là gỡ bỏ biện pháp kiểm soát duy nhất còn lại mà vẫn để nguyên vẻ ngoài của nó."
      category: fairness
    - statement: "MUST NOT trình bày một dòng trong danh sách cần xem lại như một cáo buộc gian lận. Khoảng cách lớn có thể là GPS đo sai, có thể là nhân viên đi công tác, có thể là điểm làm việc khai sai toạ độ. Chữ trên màn hình phải mời người đọc kiểm tra, không kết luận thay họ."
      category: values
    - statement: "MUST NOT lưu cứng kết quả đánh giá đáng ngờ vào một cột dữ liệu. Ngưỡng sẽ đổi khi Phase 4 dựng trang cài đặt; một cột đã ghi cứng nghĩa là dữ liệu lịch sử mang phán quyết theo một ngưỡng cũ mà không ai còn biết ngưỡng đó là bao nhiêu."
      category: transparency
---

<objective>
Hiện thực lớp phát hiện của D-21: lần chấm công cách tâm điểm làm việc quá xa được đánh dấu và hiện
ra trong một danh sách để quản trị xem lại.

Purpose: sau D-20, GPS không còn chặn được ai — nó chỉ còn làm chứng. Ảnh hiện trường chứng minh
"một thiết bị đã ở đúng nơi", không chứng minh "đúng người", vì ảnh là ảnh nhà xưởng chứ không có
mặt người để đối chiếu. Kiểu gian lận dễ nhất còn lại là nhờ đồng nghiệp chấm hộ, và lớp này là lớp
duy nhất bắt được nó (D-21b). Đây **không** phải một tiện ích phụ — nó là biện pháp phát hiện chính
của toàn phase.

Output: `src/lib/attendance/suspicious.ts`, `GET /api/attendance/review`, màn hình
`/admin/attendance/review`, mục điều hướng.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-05-SUMMARY.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-02-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Hằng số | `SUSPICIOUS_DISTANCE_MULTIPLIER` (mặc định 5) |
| Hàm | `isSuspiciousPunch()`, `suspiciousMultiplier()` |
| Route Handler | `GET /api/attendance/review` |
| Hàm dữ liệu | `listAttendanceReview()` |
| Schema Zod | `attendanceReviewRowSchema`, `attendanceReviewItemSchema`, `attendanceReviewListResponseSchema`, `attendanceReviewQuerySchema` |
| Trang | `/admin/attendance/review` |
| Component | `AttendanceReviewView` |
| Điều hướng | mục `ADMIN_NAV_ITEMS` cho `/admin/attendance/review` |
| Test | `src/lib/attendance/__tests__/suspicious.test.ts`, `src/lib/data/__tests__/attendance-review.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Quy tắc đáng ngờ — một ngưỡng, một hàm, không cột lưu cứng</name>
  <files>src/lib/attendance/suspicious.ts, src/lib/attendance/__tests__/suspicious.test.ts</files>
  <read_first>
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md` D-21, D-21a, D-21b — nội dung đầy đủ
      của quyết định, gồm cả lý do bác bỏ cách đo theo tốc độ di chuyển.
    - `.planning/REQUIREMENTS.md` §ATT-07 kèm ghi chú sửa phạm vi ngày 2026-08-02 — đọc cả ghi chú,
      không chỉ dòng yêu cầu.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md` §"Pitfall 4" — `canCheckInRemotely`
      là trường V1 đã tồn tại và đã hiển thị trên giao diện; nếu không xử lý thì danh sách này thành
      nhiễu.
    - `src/lib/types/domain.ts` dòng 110-145 — `Employee.canCheckInRemotely`; và `WorkSite` (do 03-01
      thêm) với `radiusMeters`.
    - `src/components/employee-app/attendance-status-card.tsx` dòng 190-195 — chữ hiện tại đã nói với
      nhân viên rằng họ được phép chấm công ngoài địa điểm; quy tắc mới phải nhất quán với lời hứa đó.
    - `src/lib/attendance/camera.ts` — khuôn module thuần không phụ thuộc React, để test bằng Vitest.
    - `supabase/migrations/0011_attendance_evidence.sql` (03-01) — comment giải thích vì sao **không**
      thêm cột boolean lưu cứng.
  </read_first>
  <behavior>
    - `SUSPICIOUS_DISTANCE_MULTIPLIER` bằng 5 và được khai đúng một chỗ trong toàn repo.
    - `isSuspiciousPunch({ distanceMeters, radiusMeters, canCheckInRemotely })` trả `true` khi khoảng cách lớn hơn bán kính nhân ngưỡng và nhân viên không được phép chấm công từ xa.
    - Cùng dữ liệu nhưng `canCheckInRemotely` bằng `true` trả `false` — dù khoảng cách lớn tới đâu.
    - `distanceMeters` bằng `null` trả `false` (chưa đo được thì không kết luận gì).
    - `radiusMeters` bằng `null` hoặc `0` trả `false` (không có mốc thì không có bội số).
    - Khoảng cách bằng đúng bán kính nhân ngưỡng trả `false` — biên là "quá xa", không phải "bằng".
    - Khoảng cách lớn hơn bán kính nhưng nhỏ hơn bán kính nhân ngưỡng trả `false` — nằm ngoài bán kính KHÔNG đủ để thành đáng ngờ (D-20).
    - `suspiciousMultiplier(distanceMeters, radiusMeters)` trả bội số làm tròn tới một chữ số thập phân, dùng để hiển thị; trả `null` khi thiếu một trong hai đầu vào.
    - Không hàm nào trong module nhận hai lần chấm liên tiếp làm đầu vào — cách đo theo tốc độ di chuyển đã bị bác bỏ và không được để lại một mặt phẳng cho nó quay lại.
  </behavior>
  <action>
    **(a) `src/lib/attendance/suspicious.ts`** — module thuần:
    - `SUSPICIOUS_DISTANCE_MULTIPLIER = 5`, kèm khối comment nói ba việc: (1) đây là giá trị **mặc
      định**, Phase 4 sẽ đọc nó từ cấu hình doanh nghiệp khi trang cài đặt tồn tại (D-21a), nên nó
      được để ở đây làm một điểm duy nhất chứ không nhúng vào truy vấn; (2) doanh nghiệp có chi nhánh
      xa nhau cần nới ngưỡng này; (3) đây là ngưỡng để **hỏi**, không phải ngưỡng để **kết luận**.
    - `isSuspiciousPunch(input)` và `suspiciousMultiplier(...)` theo `<behavior>`.
    - Khối comment giải thích vì sao `canCheckInRemotely` loại một bản ghi khỏi danh sách: trường này
      đã tồn tại từ V1 và giao diện nhân viên đã hứa với họ rằng họ được phép chấm công ngoài địa
      điểm. Đưa họ vào danh sách cần xem lại vừa mâu thuẫn với lời hứa đó, vừa làm danh sách đầy nhiễu
      tới mức quản trị ngừng đọc — và sau D-20 đây là lớp phát hiện chính, nên làm nó thành nhiễu là
      gỡ bỏ biện pháp kiểm soát duy nhất còn lại. Khoảng cách của họ **vẫn được ghi** và vẫn xem lại
      được từ hồ sơ cá nhân; chỉ điều kiện lọc danh sách là loại họ ra.
    - Khối comment ghi rõ vì sao module này **không** có hàm nào so hai lần chấm liên tiếp: chuỗi chấm
      công của sản phẩm luôn là vào rồi ra, cách nhau trọn một ca tám tiếng, nên luật theo tốc độ di
      chuyển hầu như không bao giờ kích hoạt — và một luật không bao giờ chạy thì tệ hơn không có luật
      vì nó tạo cảm giác an toàn giả.

    **(b) `src/lib/attendance/__tests__/suspicious.test.ts`** — khẳng định đủ chín hành vi ở
    `<behavior>`, bao gồm cả hai trường hợp biên (bằng đúng ngưỡng; ngoài bán kính nhưng chưa tới
    ngưỡng) và hai trường hợp thiếu dữ liệu.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/attendance/__tests__/suspicious.test.ts` thoát 0 và báo ít nhất 9 test.
    - `grep -rn "SUSPICIOUS_DISTANCE_MULTIPLIER *=" src/ | wc -l` trả về `1` — ngưỡng khai đúng một chỗ.
    - `grep -rnE "\* *5\b|\bradius[A-Za-z]* *\* *[0-9]" src/app/api/ src/lib/data/ | grep -v suspicious` không trả về dòng nào — không nhúng cứng bội số ở nơi khác.
    - `grep -icE "speed|velocity|tốc độ|km/h" src/lib/attendance/suspicious.ts` — mọi kết quả phải nằm trong khối comment **giải thích vì sao không dùng**; kiểm bằng mắt và ghi kết luận vào SUMMARY.
    - `grep -c "canCheckInRemotely" src/lib/attendance/suspicious.ts` trả về ít nhất `2`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from information_schema.columns where table_name='attendance_photos' and column_name like '%suspicious%'"` trả về `0` — không cột lưu cứng nào được thêm.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/attendance/__tests__/suspicious.test.ts</automated>
  </verify>
  <done>Quy tắc đáng ngờ nằm gọn trong một hàm với một ngưỡng có tên; nhân viên được phép làm việc từ xa không sinh ra nhiễu; không có phán quyết nào bị ghi cứng vào dữ liệu.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Đường đọc danh sách cần xem lại</name>
  <files>src/lib/validation/api/attendance-review.ts, src/lib/data/attendance-review.ts, src/app/api/attendance/review/route.ts, src/lib/data/__tests__/attendance-review.test.ts</files>
  <read_first>
    - `src/app/api/attendance/route.ts` — khuôn Route Handler có tham số truy vấn, tầng quyền theo vai
      trò, và cách sắp xếp xác định.
    - `src/app/api/attendance-photos/route.ts` (03-05) — phép nối lấy tên điểm làm việc đã dựng ở đó;
      dùng lại cùng hình dạng truy vấn thay vì viết một truy vấn thứ hai khác kiểu.
    - `src/lib/attendance/suspicious.ts` (Task 1) — ngưỡng và hàm quyết định.
    - `src/lib/validation/api/attendance-photos.ts` (03-01/03-05) — khuôn cặp schema hai đầu.
    - `src/lib/data/employees.ts` — cách dự án nối `employees` để lấy tên và ảnh đại diện.
    - `supabase/migrations/0005_v2_tables.sql` và `0011_attendance_evidence.sql` — cột thật để dựng
      phép nối ba bảng (`attendance_photos` → `attendance_records` → `employees`, cộng `work_sites`).
    - `src/lib/data/__tests__/attendance.test.ts` — khuôn test.
  </read_first>
  <behavior>
    - `GET /api/attendance/review` với vai trò không phải `owner`/`admin` trả 403.
    - Bản ghi có `distance_meters` bằng `null` không xuất hiện trong kết quả.
    - Bản ghi của nhân viên có `canCheckInRemotely` bằng `true` không xuất hiện, kể cả khi khoảng cách rất lớn.
    - Bản ghi có khoảng cách lớn hơn bán kính nhưng nhỏ hơn bán kính nhân ngưỡng không xuất hiện.
    - Bản ghi có khoảng cách lớn hơn bán kính nhân ngưỡng và nhân viên không được phép làm việc từ xa xuất hiện đúng một lần.
    - Mỗi phần tử mang: định danh ảnh, định danh bản ghi chấm công, tên nhân viên, tên điểm làm việc, khoảng cách, bội số, thời điểm chụp, loại chấm, và trạng thái xem xét.
    - Kết quả sắp xếp xác định: khoảng cách giảm dần rồi thời điểm giảm dần rồi định danh tăng dần, nên hai lần gọi liên tiếp cho cùng thứ tự.
    - Chỉ đọc dữ liệu của doanh nghiệp trong phiên; cùng URL với cookie của doanh nghiệp khác cho một tập định danh không giao nhau.
    - Doanh nghiệp không có bản ghi nào vượt ngưỡng trả mảng rỗng với mã 200.
    - Tham số truy vấn tuỳ chọn lọc theo khoảng thời gian và theo trạng thái xem xét; không tham số nào khai định danh doanh nghiệp.
  </behavior>
  <action>
    **(a) `src/lib/validation/api/attendance-review.ts`** — cặp schema hai đầu theo khuôn D-12d cộng
    `attendanceReviewQuerySchema` (khoảng thời gian và trạng thái xem xét, đều tuỳ chọn; **không**
    khai định danh doanh nghiệp). Schema phần tử mang cả `distanceMeters` lẫn `multiplier` lẫn
    `accuracyMeters` — bội số tính ở server để giao diện không phải lặp lại phép chia, và độ chính xác
    đi kèm để bảng không bao giờ hiển thị khoảng cách trần trụi.

    **(b) `src/app/api/attendance/review/route.ts`** — chỉ xuất `dynamic` và `GET`.
    `requireRole(role, ["owner","admin"])`. Truy vấn nối `attendance_photos` với `attendance_records`,
    `employees` và `work_sites`, lọc `.eq("company_id", companyId)`. Điều kiện đáng ngờ áp bằng
    `isSuspiciousPunch()` — nếu áp được ngay trong truy vấn SQL thì vẫn phải đọc ngưỡng từ
    `SUSPICIOUS_DISTANCE_MULTIPLIER`, không viết số 5 vào truy vấn. Comment tại chỗ ghi rõ: cờ được
    tính lúc truy vấn chứ không đọc từ một cột, để khi Phase 4 đổi ngưỡng thành cấu hình doanh nghiệp
    thì danh sách tự cập nhật mà không phải chạy một lượt ghi đè lên dữ liệu lịch sử.
    Sắp xếp xác định. Bậc thang `catch` theo đúng khuôn bốn lớp lỗi.

    **(c) `src/lib/data/attendance-review.ts`** — `listAttendanceReview(query)` qua `fetchJson`.

    **(d) `src/lib/data/__tests__/attendance-review.test.ts`** — khẳng định đủ mười hành vi ở
    `<behavior>`. Ba trường hợp lọc (khoảng cách `null`; được phép làm việc từ xa; ngoài bán kính
    nhưng chưa tới ngưỡng) là những bài kiểm quan trọng nhất của file này — chúng là ba cách danh sách
    có thể lặng lẽ trở thành vô dụng.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/data/__tests__/attendance-review.test.ts` thoát 0 và báo ít nhất 10 test.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/attendance/review/` trả về `0`.
    - `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0.
    - `grep -c "SUSPICIOUS_DISTANCE_MULTIPLIER" src/app/api/attendance/review/route.ts` trả về ít nhất `1`.
    - `grep -c "canCheckInRemotely\|can_check_in_remotely" src/app/api/attendance/review/route.ts` trả về ít nhất `1`.
    - Với cookie owner Ngọc Phát: `curl -b <cookie> -s "http://localhost:3000/api/attendance/review"` trả 200; gọi hai lần liên tiếp cho cùng thứ tự phần tử.
    - Với cookie Bình Minh: cùng URL trả một tập định danh không giao nhau với tập của Ngọc Phát.
    - Với cookie một nhân viên thường: cùng URL trả `403`.
    - Tạm đặt `can_check_in_remotely` bằng `true` cho một nhân viên đang xuất hiện trong danh sách: nhân viên đó biến mất khỏi phản hồi; khôi phục sau khi kiểm. Kết quả ghi vào SUMMARY.
    - Tạm tạo một dòng `attendance_photos` với `distance_meters` bằng `null`: dòng đó không xuất hiện trong phản hồi; dọn sau khi kiểm. Kết quả ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/attendance-review.test.ts src/__tests__/route-handlers-get-only.test.ts</automated>
  </verify>
  <done>Danh sách cần xem lại đọc được đúng dữ liệu đáng ngờ thật, không kèm nhiễu của nhân viên hợp lệ và không kèm bản ghi chưa đo được khoảng cách.</done>
</task>

<task type="auto">
  <name>Task 3: Màn hình danh sách cần xem lại</name>
  <files>src/app/admin/attendance/review/page.tsx, src/app/admin/attendance/review/attendance-review-view.tsx, src/lib/nav.ts</files>
  <read_first>
    - `src/app/admin/employees/employees-view.tsx` — khuôn màn hình dạng bảng của quản trị: bộ lọc,
      `Table`, khối `Skeleton` cho dòng bảng, và trạng thái rỗng.
    - `src/components/common/data-table-skeleton.tsx` — khối tải sẵn có cho bảng; dùng lại nguyên
      trạng, không dựng mẫu tải mới.
    - `src/components/common/empty-state.tsx`, `error-state.tsx`, `filter-bar.tsx`.
    - `src/components/common/status-badge.tsx` — quy ước biểu tượng cộng nhãn chữ.
    - `src/components/attendance/attendance-photo-dialog.tsx` (03-05) — Dialog xem lại đầy đủ; hành
      động "Xem chi tiết" mở **chính** component đó, không dựng một Dialog thứ hai.
    - `src/lib/constants.ts` khối nhãn của phase (03-01) — chữ trạng thái rỗng và nhãn trạng thái xem
      xét lấy từ đó.
    - `src/lib/nav.ts` — `ADMIN_NAV_ITEMS`; mục mới đặt trong nhóm chấm công.
    - `src/lib/format.ts` — định dạng số và ngày giờ; số đo dùng lớp `.num`.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md` §"Copywriting Contract" (hai dòng
      trạng thái rỗng của danh sách này) và §"UI Considerations" các hàng "Screen — needs-review list",
      "Empty — needs-review list", "Loading — admin lists", "Zero/one/many".
  </read_first>
  <action>
    **(a) `src/app/admin/attendance/review/page.tsx`** — Server Component với `metadata`, render View.

    **(b) `attendance-review-view.tsx`** — `"use client"`:
    - `PageHeader` với số đếm.
    - `useDataQuery(() => listAttendanceReview(filters), [filters])`.
    - Đang tải: dùng lại khối `Skeleton` bảng sẵn có.
    - Lỗi: `ErrorState` kèm `onRetry`.
    - Rỗng: `EmptyState` với đúng hai dòng chữ đã chốt — tông trấn an, vì một danh sách rỗng ở đây là
      trạng thái **lành mạnh**, không phải một ngõ cụt.
    - Bảng với sáu cột: nhân viên, điểm làm việc, khoảng cách, thời điểm, trạng thái xem xét, hành
      động. Cột khoảng cách in đậm, tông cảnh báo hổ phách, và hiển thị **cả** con số mét **lẫn** bội
      số so với bán kính. Ngay cạnh đó, độ chính xác GPS ở cỡ chữ nhỏ hơn — không bao giờ để một con
      số khoảng cách đứng một mình trong bảng này, vì người đọc cần phân biệt được đo sai với đứng xa
      thật (D-20 đã lấy đúng điều này làm lý do bỏ chặn cứng).
    - Trạng thái xem xét dùng `StatusBadge` với biểu tượng cộng nhãn chữ.
    - Hành động "Xem chi tiết" mở `AttendancePhotoDialog` của plan 03-05.
    - Tên điểm làm việc dài cắt bằng `truncate` kèm `title`.
    - Một kết quả và nhiều kết quả dùng chung mẫu dòng; không có bố cục riêng cho trường hợp một.
    - Chữ tiêu đề và chữ trợ giúp của màn hình phải **mời kiểm tra**, không kết luận: đây là danh sách
      để người xem lại, và một khoảng cách lớn có thể là GPS đo sai, có thể là đi công tác, có thể là
      điểm làm việc khai sai toạ độ.

    **(c) `src/lib/nav.ts`** — thêm một mục trỏ tới `/admin/attendance/review`, đặt trong nhóm chấm
    công, icon lucide phù hợp.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0.
    - `npx vitest run` thoát 0 (toàn bộ bộ test).
    - `grep -c "/admin/attendance/review" src/lib/nav.ts` trả về ít nhất `1`.
    - `grep -c "AttendancePhotoDialog" src/app/admin/attendance/review/attendance-review-view.tsx` trả về ít nhất `1`.
    - `grep -cE "Skeleton|DataTableSkeleton" src/app/admin/attendance/review/attendance-review-view.tsx` trả về ít nhất `1`.
    - `grep -c "EmptyState" src/app/admin/attendance/review/attendance-review-view.tsx` trả về ít nhất `1`.
    - `grep -icE "accuracy" src/app/admin/attendance/review/attendance-review-view.tsx` trả về ít nhất `1` — độ chính xác luôn đi kèm khoảng cách.
    - `grep -icE "gian lận|vi phạm|giả mạo|man khai" src/app/admin/attendance/review/attendance-review-view.tsx src/lib/constants.ts` trả về `0` — màn hình không kết luận thay người đọc.
    - `grep -c "StatusBadge" src/app/admin/attendance/review/attendance-review-view.tsx` trả về ít nhất `1`.
    - `grep -c "truncate" src/app/admin/attendance/review/attendance-review-view.tsx` trả về ít nhất `1`.
    - Với phiên owner Ngọc Phát và ít nhất một bản ghi vượt ngưỡng (tạo bằng cách tạm hạ `radius_meters` của điểm làm việc): danh sách hiện đúng dòng đó kèm bội số; bấm "Xem chi tiết" mở Dialog đúng bản ghi. Khôi phục bán kính sau khi kiểm. Kết quả quan sát ghi vào SUMMARY.
    - Với một doanh nghiệp không có bản ghi nào vượt ngưỡng: màn hình hiện trạng thái rỗng đúng hai dòng chữ đã chốt. Kết quả quan sát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Quản trị có một danh sách các lần chấm công đáng hỏi lại, mỗi dòng nói đủ để người đọc biết nên hỏi gì, và một danh sách rỗng đọc như một tin tốt chứ không như một màn hình hỏng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt quản trị → `GET /api/attendance/review` | Danh sách này gộp toạ độ và khoảng cách của nhiều nhân viên trong một phản hồi |
| Ngưỡng đáng ngờ → kết luận về một con người | Sau D-20 đây là lớp phát hiện chính; hỏng nó là hỏng biện pháp kiểm soát duy nhất còn lại |
| Toạ độ do client khai → phép đo khoảng cách | Toạ độ giả lập cho ra khoảng cách nhỏ và không bao giờ vào danh sách |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Spoofing | Nhờ đồng nghiệp chấm hộ — kiểu gian lận mà ảnh hiện trường và GPS đều không bắt được vì ảnh không có mặt người | high | mitigate | Đây là lý do tồn tại của cả plan này (D-21b). Biện pháp là khoảng cách bất thường so với mốc, cộng danh sách để người xem lại. Giới hạn phải ghi rõ: lớp này bắt được người chấm hộ **từ xa**, không bắt được người chấm hộ đứng ngay tại chỗ |
| T-03-06-02 | Denial of Service | Danh sách đầy nhiễu tới mức quản trị ngừng đọc, và biện pháp kiểm soát chết trong khi vẫn còn nguyên vẻ ngoài | high | mitigate | `canCheckInRemotely` loại khỏi bộ lọc; ngưỡng 5 lần bán kính chứ không phải "ngoài bán kính". Acceptance criteria có hai bài kiểm thủ công bật/tắt cờ và tạo dòng thiếu khoảng cách |
| T-03-06-03 | Spoofing | Toạ độ giả lập bởi client đã sửa đổi cho ra khoảng cách nhỏ và không bao giờ bị đánh dấu | high | accept | Web Geolocation API không lộ tín hiệu chống giả lập nào cho JavaScript; không có biện pháp kỹ thuật ở tầng này. Chấp nhận có ý thức, đã ghi từ plan 03-01 (T-03-10) và từ nghiên cứu milestone |
| T-03-06-04 | Information Disclosure | Danh sách gộp toạ độ nhiều nhân viên bị đọc bởi vai trò không được phép | high | mitigate | `requireRole(role, ["owner","admin"])` cộng lọc theo doanh nghiệp từ phiên; acceptance criteria đòi 403 bằng cookie nhân viên thật và hai tập id không giao nhau |
| T-03-06-05 | Tampering | Ngưỡng bị nhúng cứng ở nhiều chỗ và lệch nhau khi Phase 4 đổi cấu hình | medium | mitigate | Ngưỡng khai đúng một chỗ; acceptance criteria đếm số nơi khai và đòi bằng 1, cộng một grep chặn bội số nhúng cứng ở tầng dữ liệu |
| T-03-06-06 | Repudiation | Một dòng trong danh sách bị đọc như một cáo buộc và dùng làm căn cứ kỷ luật | medium | mitigate | Khoảng cách không bao giờ hiển thị thiếu độ chính xác đi kèm; chữ trên màn hình mời kiểm tra chứ không kết luận. Acceptance criteria có grep chặn từ ngữ buộc tội |
| T-03-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Ngưỡng đáng ngờ khai đúng một chỗ trong toàn repo và không nhúng cứng ở tầng dữ liệu
- Không cột nào lưu cứng kết quả đánh giá đáng ngờ
- Nhân viên được phép làm việc từ xa không xuất hiện trong danh sách
- Bản ghi chưa đo được khoảng cách không xuất hiện trong danh sách
- Nhân viên thường nhận 403; hai doanh nghiệp thấy hai tập không giao nhau
- Khoảng cách trong bảng luôn đi kèm bội số và độ chính xác

</verification>

<success_criteria>
Lớp phát hiện chính của phase tồn tại thật và đọc được: một lần chấm công cách mốc quá xa nổi lên
đúng chỗ quản trị nhìn, kèm đủ ngữ cảnh để người đọc quyết định có nên hỏi hay không — và danh sách
không bị nhiễu bởi chính những người đã được cho phép ở xa.
</success_criteria>

<output>
Create `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-06-SUMMARY.md` when done
</output>
