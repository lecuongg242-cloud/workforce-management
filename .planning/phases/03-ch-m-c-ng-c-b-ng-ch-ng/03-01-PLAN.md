---
phase: 03-ch-m-c-ng-c-b-ng-ch-ng
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0011_attendance_evidence.sql
  - supabase/tests/09_attendance_evidence.sql
  - supabase/tests/run-all.sql
  - scripts/check-pgtap-assertions.mjs
  - scripts/storage-bucket.mjs
  - package.json
  - next.config.ts
  - src/lib/types/domain.ts
  - src/lib/constants.ts
  - src/lib/storage/attendance-photos.ts
  - src/lib/validation/api/attendance-photos.ts
  - src/lib/attendance/camera.ts
  - src/lib/data/mutations/attendance.ts
  - src/app/api/attendance-photos/[id]/route.ts
  - src/app/api/attendance-photos/[id]/__tests__/route.test.ts
  - src/components/employee-app/camera-sheet.tsx
  - src/components/employee-app/attendance-status-card.tsx
  - src/app/employee/employee-home-view.tsx
  - src/components/attendance/attendance-photo-dialog.tsx
  - src/app/admin/employees/[id]/employee-detail-view.tsx
autonomous: false
requirements: [ATT-01, ATT-02, ATT-04, ATT-05, ATT-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Một nhân viên bấm \"Vào ca\" mở được camera trực tiếp, chụp một khung hình, và gửi được lần chấm công đó kèm ảnh và toạ độ thật — toàn bộ đường đi từ trình duyệt tới Postgres và Storage chạy end-to-end trong đúng một lát cắt (tracer)."
    - "Màn hình chấm công không có bất kỳ phần tử chọn tệp nào: đường duy nhất để có ảnh là khung hình trực tiếp từ `getUserMedia()` (ATT-01)."
    - "Server tự tính khoảng cách từ toạ độ nhận được tới tâm `work_site` bằng `public.tf_distance_meters()` và ghi kết quả vào `attendance_photos.distance_meters`; client không bao giờ gửi lên một giá trị khoảng cách (ATT-02)."
    - "Chấm công ở ngoài bán kính KHÔNG bị từ chối: bản ghi vẫn được ghi, khoảng cách thật vẫn được lưu (D-20). Không có nhánh nào trong `checkIn` ném lỗi vì lý do khoảng cách (D-20a)."
    - "`captured_at` của ảnh là dấu thời gian do database cấp qua `tf_server_now()`, không phải giờ do thiết bị gửi lên (ATT-06, D-19)."
    - "Ảnh nằm trong bucket Storage riêng tư; không đường nào trong ứng dụng phát hành một liên kết ký thô ra trình duyệt — mọi lần xem ảnh đi qua `GET /api/attendance-photos/[id]`, và route đó lấy `company_id` từ `getSessionContext()` rồi so lại trên MỖI lần gọi (ATT-05, tiêu chí 4 của ROADMAP)."
    - "Một phiên đăng nhập của doanh nghiệp cty-02 gọi đúng URL ảnh của cty-01 nhận về 404 — không phải 200, không phải một ảnh trắng (ATT-05)."
    - "Quản trị mở một bản ghi chấm công có ảnh và xem được ảnh đó ngay trong giao diện quản trị (ATT-04)."
    - "Ảnh không tự xoá: không có job dọn theo lịch, không có TTL, không có cron nào trong repo sau plan này (D-22)."
    - "Giữa lúc chạm nút và lúc khung hình trực tiếp hiện ra, màn hình hiển thị trạng thái \"Đang mở camera…\" — không có khoảng trống đen câm nào (UI-SPEC: Loading — camera opening)."
    - "Trong lúc lấy toạ độ, một chip trạng thái hiện \"Đang lấy vị trí…\" và nút gửi bị vô hiệu cho tới khi có toạ độ (UI-SPEC: Loading — GPS acquisition)."
    - "Nút \"Gửi chấm công\" chuyển sang \"Đang gửi…\" và bị vô hiệu trong lúc gửi, không gửi được hai lần (UI-SPEC: Loading — submit in flight)."
    - "Bị từ chối quyền camera thì toàn bộ vùng khung hình bị thay bằng khối giải thích kèm nút \"Thử lại\", không phải một toast thoáng qua (UI-SPEC: Permission-denied — camera)."
    - "Trong lúc route ảnh của quản trị đang tải, ô ảnh hiện `Skeleton` giữ đúng tỉ lệ khung, không nhảy bố cục (UI-SPEC: Loading — photo, đã đối chiếu lại theo kiến trúc broker)."
    - statement: "Một bản ghi chấm công KHÔNG có dòng `attendance_photos` nào (dữ liệu tạo trước phase này) hiển thị dòng chữ \"Bản ghi này không có ảnh đính kèm.\" trong ô ảnh thay vì một ảnh vỡ — kiểm bằng chính hai bản ghi seed hiện có không có ảnh."
      verification: backstop
    - statement: "`npm run check:assertions` báo tổng assertion lớn hơn hoặc bằng 191 và `MIN_ASSERTIONS` trong `scripts/check-pgtap-assertions.mjs` đã được nâng lên đúng tổng mới — sàn không bao giờ được hạ."
      verification: backstop
  artifacts:
    - path: "supabase/migrations/0011_attendance_evidence.sql"
      provides: "accuracy_meters/work_site_id/distance_meters trên attendance_photos + hàm tf_distance_meters()"
      contains: "tf_distance_meters"
    - path: "src/lib/storage/attendance-photos.ts"
      provides: "Tên bucket, hàm dựng đường dẫn chuẩn hoá, danh sách MIME cho phép"
      exports: ["ATTENDANCE_PHOTO_BUCKET", "buildAttendancePhotoPath", "ALLOWED_PHOTO_MIME_TYPES"]
    - path: "src/app/api/attendance-photos/[id]/route.ts"
      provides: "Broker Route Handler GET — kiểm company_id mỗi lần gọi rồi stream byte ảnh"
      exports: ["GET", "dynamic"]
    - path: "src/lib/attendance/camera.ts"
      provides: "openCamera/closeCamera/captureFrame/acquireLocation + phân loại lỗi getUserMedia"
      exports: ["openCamera", "closeCamera", "captureFrame", "acquireLocation", "GPS_TIMEOUT_MS"]
    - path: "src/components/employee-app/camera-sheet.tsx"
      provides: "Sheet toàn màn hình: viewfinder → chụp → xem lại → gửi"
      min_lines: 120
    - path: "src/components/attendance/attendance-photo-dialog.tsx"
      provides: "Dialog quản trị xem ảnh qua broker route"
      min_lines: 40
  key_links:
    - from: "src/lib/data/mutations/attendance.ts"
      to: "public.tf_distance_meters"
      via: "khoảng cách do server tính qua RPC, không nhận từ client"
      pattern: "tf_distance_meters"
    - from: "src/app/api/attendance-photos/[id]/route.ts"
      to: "src/lib/auth/session-context.ts"
      via: "company_id lấy từ phiên trên mỗi lần gọi, không từ tham số URL"
      pattern: "getSessionContext"
    - from: "src/components/attendance/attendance-photo-dialog.tsx"
      to: "src/app/api/attendance-photos/[id]/route.ts"
      via: "thẻ ảnh trỏ thẳng vào broker route, không vào miền storage của Supabase"
      pattern: "/api/attendance-photos/"
    - from: "src/components/employee-app/camera-sheet.tsx"
      to: "src/lib/attendance/camera.ts"
      via: "khung hình lấy từ luồng media trực tiếp rồi vẽ ra canvas"
      pattern: "captureFrame"
  prohibitions:
    - statement: "MUST NOT phát hành ra trình duyệt bất kỳ liên kết ảnh nào còn dùng được khi tách khỏi phiên đăng nhập đã tạo ra nó. Tiêu chí 4 của ROADMAP viết rất chặt — \"người ở doanh nghiệp khác cầm đúng liên kết vẫn không xem được\" — nên một liên kết ký hạn ngắn vẫn là vi phạm: nó là một bearer token sống độc lập, dán được vào tin nhắn nội bộ hay lấy được từ tab Network, và không tái kiểm quyền lần nào nữa cho tới khi hết hạn."
      category: privacy
    - statement: "MUST NOT mở bất kỳ đường nào cho phép một ảnh có sẵn trong máy trở thành bằng chứng chấm công — kể cả một nhánh dự phòng \"khi camera hỏng\". Ảnh hiện trường là toàn bộ nội dung của ATT-01; một đường vòng qua thư viện ảnh làm bằng chứng thành lời khai mà giao diện vẫn trông y hệt như khi nó thật."
      category: safety
    - statement: "MUST NOT ghi byte ảnh (hoặc chuỗi base64 của ảnh) vào `audit_log`. `before`/`after` lưu nguyên dòng theo D-18, nên một dòng `attendance_photos` chỉ được lưu đường dẫn và siêu dữ liệu; nhân đôi chính ảnh sang một bảng có vòng đời khác biến rủi ro tích tụ dữ liệu cá nhân của D-22a thành gấp đôi mà không ai chủ ý quyết định điều đó."
      category: privacy
---

<!-- planner-discipline-allow: type="file" -->
<!-- planner-discipline-allow: createSignedUrl -->
<!-- planner-discipline-allow: capture -->
<!-- planner-discipline-allow: is_suspicious -->

<objective>
Lát cắt bằng chứng đầu-cuối cho ĐÚNG MỘT đường đi: một nhân viên bấm "Vào ca" → camera trực tiếp
mở ra → chụp một khung hình → toạ độ được lấy → Server Action tải ảnh lên bucket riêng tư, hỏi
database khoảng cách tới điểm làm việc, ghi bản ghi với giờ do server cấp → quản trị mở bản ghi đó
và nhìn thấy ảnh qua một Route Handler tự kiểm quyền trên mỗi lần gọi.

Purpose: đây là plan tracer của phase. Ba thứ trong phase này chưa từng tồn tại trong dự án —
camera trình duyệt, Supabase Storage, và một đường đọc nhị phân có phân quyền — và cả ba đều có
thể sai theo cách mà demo bằng tay vẫn "trông như đạt". Chứng minh toàn bộ hình dạng kiến trúc
trên một đường duy nhất, bằng token đầu context còn tốt nhất, rẻ hơn nhiều so với phát hiện ra
đường đọc ảnh rò rỉ xuyên doanh nghiệp sau khi đã dựng xong bốn màn hình trên nền đó.

Đây là lát cắt sản xuất, không phải bản thử: nó mang `<verify>` thật và ở lại làm bộ xương của
phần còn lại. Những chỗ còn thiếu ở đây (tan ca, ba lý do từ chối, bốn nhánh lỗi camera, màn hình
khai báo điểm làm việc, danh sách cần xem lại) là **thiếu chức năng**, không phải thiếu kiến trúc —
các plan sau lấp vào mà không phải đổi hình dạng nào của lát cắt này.

Output: migration `0011`, hàm `tf_distance_meters()`, bucket riêng tư, `src/lib/storage/`,
`src/lib/attendance/camera.ts`, broker Route Handler, Camera Sheet, Dialog xem ảnh của quản trị.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-CONTEXT.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-08-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Migration | `supabase/migrations/0011_attendance_evidence.sql` |
| Hàm SQL | `public.tf_distance_meters(numeric, numeric, numeric, numeric) returns numeric` |
| Cột mới | `attendance_photos.accuracy_meters`, `attendance_photos.work_site_id`, `attendance_photos.distance_meters` |
| Chỉ mục | `attendance_photos_work_site_id_idx` |
| Test pgTAP | `supabase/tests/09_attendance_evidence.sql` (thêm vào `run-all.sql` qua `\ir`) |
| Bucket Storage | `attendance-photos` (riêng tư) |
| Script | `scripts/storage-bucket.mjs`; npm script `db:bucket` |
| Route Handler | `GET /api/attendance-photos/[id]` (broker, trả byte ảnh) |
| Server Action | `checkIn(companyId, employeeId, date, time, evidence)` — chữ ký mở rộng |
| Module storage | `ATTENDANCE_PHOTO_BUCKET`, `buildAttendancePhotoPath()`, `ALLOWED_PHOTO_MIME_TYPES` |
| Module camera | `openCamera()`, `closeCamera()`, `captureFrame()`, `acquireLocation()`, `GPS_TIMEOUT_MS`, `CameraPermissionDeniedError` |
| Schema Zod | `punchEvidenceSchema`, `attendancePhotoRowSchema`, `attendancePhotoSchema` |
| Kiểu domain | `WorkSite`, `WorkSiteInput`, `AttendancePhoto`, `PunchEvidence`, `PhotoReviewStatus`, `AttendanceRejectionReason` |
| Hằng số nhãn | `ATTENDANCE_EVIDENCE_LABEL`, `ATTENDANCE_REJECTION_LABEL`, `WORK_SITE_LABEL`, `PHOTO_REVIEW_STATUS_LABEL` |
| Component | `CameraSheet`, `AttendancePhotoDialog` |
| Cấu hình | `next.config.ts` → `experimental.serverActions.bodySizeLimit` |

<tasks>

<task type="tracer">
  <name>Task 1: Nền dữ liệu cho bằng chứng — ba cột thiếu, hàm khoảng cách, và đẩy schema</name>
  <precondition>`.env.local` có `POSTGRES_URL_NON_POOLING` trỏ tới project Supabase dev; `npx supabase` chạy được (đã dùng ở Phase 1-2 qua `scripts/db.mjs`).</precondition>
  <reversibility rating="costly">Ba cột thêm vào một bảng đã có dữ liệu seed; gỡ ra sau này phải viết migration đảo và dựng lại mọi truy vấn đọc chúng. Hàm `tf_distance_meters` thì reversible (một hàm, một chỗ gọi).</reversibility>
  <files>supabase/migrations/0011_attendance_evidence.sql, supabase/tests/09_attendance_evidence.sql, supabase/tests/run-all.sql, scripts/check-pgtap-assertions.mjs</files>
  <read_first>
    - `supabase/migrations/0005_v2_tables.sql` dòng 20-60 và 125-185 — hình dạng thật của `work_sites`
      và `attendance_photos`, gồm ràng buộc `unique (attendance_record_id, kind)`, ràng buộc
      `check (storage_path like company_id || '/%')`, và bộ bốn policy `<bảng>_<lệnh>_member`.
      **Không sửa file này** — nó đã được áp dụng.
    - `supabase/migrations/0010_check_in_time_functions.sql` — `tf_server_now()`, `tf_local_instant()`;
      đây là khuôn "hàm tiện ích `tf_*` đặt trong schema `public`" mà hàm khoảng cách phải theo đúng,
      gồm cả `revoke execute ... from public` / `grant execute ... to authenticated` nếu khuôn đó có.
    - `supabase/migrations/0003_enums_time.sql` — `tf_tz()`, `tf_work_date()`; xác nhận quy ước đặt tên
      và mức `immutable`/`stable` mà dự án đang dùng.
    - `supabase/tests/04_isolation_v2.sql` — khuôn pgTAP hiện có cho chính hai bảng này; file test mới
      đứng cạnh nó chứ không sửa nó.
    - `supabase/tests/helpers.sql` — `tf_test_login()`/`tf_test_logout()`.
    - `supabase/tests/run-all.sql` — thứ tự `\ir`; `scripts/check-pgtap-assertions.mjs` CHỈ đếm theo
      đúng danh sách này, nên một file test không có trong đây là một file không được đếm.
    - `scripts/db.mjs` dòng 130-200 — cổng chặn D-15: `test`/`testdb` **từ chối chạy lên host cloud**.
      Test pgTAP mới phải chạy trên Postgres tạm, không phải `npm run db:push` rồi test lên cloud.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md` §"Schema Gap" và §"Pattern 4".
  </read_first>
  <action>
    **(a) `supabase/migrations/0011_attendance_evidence.sql`** — file MỚI, không sửa `0005`.
    - `alter table attendance_photos` thêm ba cột nullable: `accuracy_meters numeric(7,2)`,
      `work_site_id text references work_sites (id) on delete set null`,
      `distance_meters numeric(10,2)`. Cả ba nullable vì các dòng seed hiện có không có giá trị và
      vì một lần chấm công ở doanh nghiệp chưa khai điểm làm việc nào vẫn phải ghi được.
    - `create index attendance_photos_work_site_id_idx on attendance_photos (work_site_id);`
    - `create function public.tf_distance_meters(p_lat1 numeric, p_lng1 numeric, p_lat2 numeric,
      p_lng2 numeric) returns numeric language sql immutable` — công thức haversine với bán kính
      Trái Đất 6371000 m. **Bắt buộc** kẹp đối số của `acos()` bằng `least(1.0, greatest(-1.0, …))`:
      sai số dấu phẩy động đẩy giá trị ra ngoài `[-1, 1]` khi hai toạ độ gần trùng nhau, và trường
      hợp đó chính là trường hợp phổ biến nhất (nhân viên đứng đúng chỗ) — bỏ bước kẹp thì hàm trả
      `NaN` đúng lúc hệ thống chạy đúng nhất. Comment tại chỗ ghi rõ lý do này.
      Theo đúng khuôn cấp quyền của các hàm `tf_*` trong `0010` (đọc file rồi lặp lại, không tự nghĩ
      ra một khuôn thứ hai).
    - **KHÔNG** thêm cột boolean lưu cứng cờ đáng ngờ. Cờ được tính tại thời điểm truy vấn (plan
      03-06) bằng cách so `distance_meters` với `radius_meters` của `work_site_id` nhân ngưỡng hiện
      hành, để khi Phase 4 chuyển ngưỡng từ hằng số sang cấu hình doanh nghiệp (D-21a) thì danh sách
      tự cập nhật, không phải ghi đè hàng loạt lên dữ liệu lịch sử. Ghi lý do này thành comment
      trong migration.

    **(b) `supabase/tests/09_attendance_evidence.sql`** — pgTAP, theo khuôn `04_isolation_v2.sql`:
    - Ba khẳng định `has_column` cho ba cột mới trên `attendance_photos`.
    - Một khẳng định `has_function` cho `tf_distance_meters`.
    - Khoảng cách bằng 0: hai toạ độ **giống hệt nhau** cho ra `0` (không phải `NULL`, không phải
      `NaN`) — đây là bài kiểm cho bước kẹp `acos()`, không phải một bài kiểm hình thức.
    - Một khoảng cách đã biết: hai toạ độ cách nhau khoảng 1 độ vĩ tuyến cho ra giá trị nằm trong
      khoảng 110000–112000 m (dung sai rộng, vì mục tiêu là bắt lỗi công thức/đơn vị chứ không phải
      đo độ chính xác trắc địa).
    - Tính đối xứng: đổi chỗ hai điểm cho cùng kết quả.
    - Một khẳng định cô lập: user 0001 cập nhật `distance_meters` cho một dòng `attendance_photos`
      của cty-02 bị từ chối với SQLSTATE `42501` (RLS vẫn phủ các cột mới).
    Thêm `\ir 09_attendance_evidence.sql` vào cuối `supabase/tests/run-all.sql`.

    **(c) `scripts/check-pgtap-assertions.mjs`** — sau khi đo lại tổng thật, nâng `MIN_ASSERTIONS`
    lên đúng tổng mới (tổng trước plan này là 191). Sàn chỉ được tăng. Cập nhật cả comment đầu file
    ghi ngày đo và lý do tăng.

    **(d) [BLOCKING] Đẩy schema.** Chạy `npm run db:push` (bọc `scripts/db.mjs push`) SAU khi file
    migration hoàn tất và TRƯỚC mọi bước kiểm chứng ở Task 2. Không chạy `supabase db push` trần.
    Bộ pgTAP chạy riêng bằng `npm run test:db` trên Postgres tạm — cổng D-15 trong `scripts/db.mjs`
    từ chối nạp fixture pgTAP lên host cloud, đó là hành vi đúng, không phải lỗi cần lách.
  </action>
  <acceptance_criteria>
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from information_schema.columns where table_name='attendance_photos' and column_name in ('accuracy_meters','work_site_id','distance_meters')"` trả về `3`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select public.tf_distance_meters(21.028500,105.854200,21.028500,105.854200)"` trả về `0` (không phải rỗng, không phải `NaN`).
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select public.tf_distance_meters(21.000000,105.000000,22.000000,105.000000) between 110000 and 112000"` trả về `t`.
    - `grep -c "09_attendance_evidence.sql" supabase/tests/run-all.sql` trả về `1`.
    - `npm run test:db` thoát 0.
    - `npm run check:assertions` thoát 0 và in `Tong:` với giá trị lớn hơn hoặc bằng `191`; giá trị đó ghi vào SUMMARY.
    - `grep -v '^ *--' scripts/check-pgtap-assertions.mjs | grep -c "MIN_ASSERTIONS = 170"` trả về `0` — sàn đã được nâng.
    - `grep -v '^ *--' supabase/migrations/0011_attendance_evidence.sql | grep -cE "least\(1\.0, *greatest\(-1\.0"` trả về ít nhất `1`.
    - `git diff --name-only HEAD~1 -- supabase/migrations/0005_v2_tables.sql` trả về rỗng — migration cũ không bị sửa.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:db &amp;&amp; npm run check:assertions</automated>
  </verify>
  <done>Ba cột mà D-20/D-21/ATT-02/ATT-07 cần đã tồn tại trên database thật; `tf_distance_meters()` trả 0 cho hai điểm trùng nhau thay vì `NaN`; bộ pgTAP xanh và sàn assertion đã tăng.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Tên bucket và cấu trúc đường dẫn lưu ảnh chấm công</decision>
  <context>
CONTEXT.md trao việc này cho Claude quyết ("Tên bucket Storage, cấu trúc thư mục, thời hạn liên kết
ký" — Claude's Discretion), nhưng nó là một cánh cửa một chiều nên vẫn phải được xác nhận trước khi
đi qua: ngay khi có ảnh thật, đổi tên bucket hoặc đổi bố cục thư mục nghĩa là di chuyển từng object
đã lưu **và** viết lại `storage_path` của từng dòng `attendance_photos` trong một thao tác không
nguyên tử. Ràng buộc đã có trong database thu hẹp lựa chọn: `0005_v2_tables.sql` mang
`check (storage_path like company_id || '/%')`, nên mọi đường dẫn BẮT BUỘC bắt đầu bằng `company_id`.

Quyết định này còn giao thoa với **D-22 (ảnh giữ vĩnh viễn)**. D-22 là quyết định đã khoá của chủ dự
án và không được mở lại ở đây — nhưng hệ quả của nó thuộc về chính lựa chọn đường dẫn: vì không có
job dọn nào, cấu trúc thư mục là thứ duy nhất quyết định một job dọn **về sau** có thể nhắm được vào
cái gì. Một bố cục theo `employee_id` cho phép xoá theo người (hữu ích cho nhóm PRIV ở V3); một bố
cục theo năm/tháng cho phép xoá theo tuổi. Chọn sai bây giờ thì lựa chọn kia không còn rẻ nữa. D-22a
đã ghi nhận: ảnh hiện trường kèm toạ độ GPS là dữ liệu cá nhân theo NĐ 13/2023, và giữ vĩnh viễn
nghĩa là tích tụ không thời hạn.
  </context>
  <options>
    <option id="option-a">
      <name>`attendance-photos` · `{company_id}/{employee_id}/{photo_id}.jpg`</name>
      <pros>Xoá được theo từng nhân viên bằng một lần liệt kê tiền tố — đúng thứ nhóm PRIV (V3) cần cho "rút lại đồng ý" và "tự xuất dữ liệu cá nhân". Tiền tố `company_id` khớp thẳng ràng buộc `check` đã có và làm policy `storage.objects` theo tiền tố viết được gọn. `photo_id` là uuid nên không đoán tuần tự được.</pros>
      <cons>Không nhóm theo thời gian, nên một job dọn theo tuổi về sau phải đọc `captured_at` từ bảng thay vì suy từ đường dẫn.</cons>
    </option>
    <option id="option-b">
      <name>`attendance-photos` · `{company_id}/{yyyy}/{mm}/{photo_id}.jpg`</name>
      <pros>Xoá theo kỳ/theo tuổi làm được bằng một lần liệt kê tiền tố; hợp với hướng "vòng đời ảnh gắn với chốt kỳ" nếu Phase 5 sau này muốn.</pros>
      <cons>Xoá theo một nhân viên cụ thể phải quét toàn bộ thư mục của doanh nghiệp — đúng thao tác mà nhóm PRIV V3 cần. CONTEXT.md đã bác rõ việc gắn vòng đời ảnh vào chốt kỳ ở phase này (D-22).</cons>
    </option>
    <option id="option-c">
      <name>Một bucket riêng cho mỗi doanh nghiệp (`attendance-photos-{company_id}`)</name>
      <pros>Cô lập ở mức bucket, khó nhầm lẫn nhất.</pros>
      <cons>Phải tạo bucket lúc onboarding một doanh nghiệp mới (một bước vận hành mới chưa hề tồn tại), và làm hỏng ràng buộc `check` đã có vì `storage_path` không còn bắt đầu bằng `company_id`. Đắt hơn hẳn phần lợi so với option-a vốn đã có RLS cộng broker route làm hai lớp.</cons>
    </option>
  </options>
  <resume-signal>Chọn: option-a, option-b, hoặc option-c</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Đường ghi và đường đọc phía server — bucket riêng tư, khoảng cách do server đo, broker route</name>
  <precondition>`.env.local` có `SUPABASE_SECRET_KEY` (đã dùng bởi `scripts/seed-auth.mjs` từ 02-03); Task 1 đã chạy `npm run db:push` thành công.</precondition>
  <reversibility rating="one-way">Bố cục bucket/đường dẫn đã chốt ở checkpoint trên: sau khi có ảnh thật, đổi lại là một cuộc di chuyển object cộng viết lại `storage_path` từng dòng, không phải một lần refactor.</reversibility>
  <files>scripts/storage-bucket.mjs, package.json, next.config.ts, src/lib/types/domain.ts, src/lib/constants.ts, src/lib/storage/attendance-photos.ts, src/lib/validation/api/attendance-photos.ts, src/lib/data/mutations/attendance.ts, src/app/api/attendance-photos/[id]/route.ts, src/app/api/attendance-photos/[id]/__tests__/route.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/attendance.ts` (toàn bộ, 349 dòng) — đây là hàm sẽ được mở rộng. Chú ý
      đặc biệt: khối comment đầu file nói rõ `date`/`time` còn trong chữ ký CHỈ để call site Phase 2
      không phải sửa, và **ATT-06 ở phase này sẽ bỏ hẳn hai tham số đó**; khuôn
      đọc-trước-insert-hoặc-update thay cho upsert; và mọi phép tính thời gian đi qua RPC.
    - `src/lib/auth/session-context.ts` dòng 84-158 và 176-180 — `getSessionContext()` (không nhận
      tham số, `companyId` chỉ đến từ cookie đã đối chiếu `memberships`) và `requireRole()`.
    - `src/lib/data/audit.ts` (toàn bộ) — `logMutation`, và khối comment D-18a đã cảnh báo trước rằng
      từ Phase 3 `audit_log` thành bản sao thứ hai của dữ liệu cá nhân.
    - `src/app/api/shifts/route.ts` (toàn bộ) — khuôn Route Handler GET-only: `export const dynamic`,
      bậc thang `catch` phân biệt `UnauthenticatedError`/`ForbiddenError`/`NoMembershipError`.
    - `src/app/api/employees/[id]/route.ts` — khuôn route có tham số động (`params` là Promise ở
      Next 15).
    - `src/__tests__/lib/route-handler-check.ts` và `src/__tests__/route-handlers-get-only.test.ts` —
      cổng cơ học D-12c chỉ chấp nhận `export function GET`/`export const GET`; bất kỳ export nào
      khác trong `route.ts` làm cổng đỏ.
    - `src/lib/supabase/server.ts` — `createServerSupabase()` dùng khoá publishable rằng buộc cookie,
      nên RLS vẫn là lớp phòng thủ thứ hai.
    - `src/lib/validation/api/attendance.ts` — khuôn schema hai đầu (`*RowSchema` transform ở server,
      `*Schema` plain dùng cả hai đầu, D-12d).
    - `supabase/seed.sql` dòng 639-665 — `work_sites` đã có sẵn một điểm cho mỗi doanh nghiệp, và hai
      dòng `attendance_photos` seed **không** có `distance_meters` (dữ liệu "trước phase này").
    - `scripts/seed-auth.mjs` — khuôn script dùng `SUPABASE_SECRET_KEY` + `createClient` với quyền
      admin, và khuôn báo lỗi tiếng Việt khi thiếu biến môi trường.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md` §"Pattern 5" và §"Pitfall 2".
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md` §"Copywriting Contract" — nguồn chữ
      cho khối hằng số nhãn.
  </read_first>
  <behavior>
    - `buildAttendancePhotoPath()` luôn trả về một chuỗi bắt đầu bằng `companyId` cộng dấu gạch chéo, khớp ràng buộc `check (storage_path like company_id || '/%')` của database.
    - `checkIn` gọi với bằng chứng hợp lệ ghi một dòng `attendance_photos` mang `kind = 'check_in'`, `storage_path` đúng đường dẫn vừa tải lên, và `captured_at` bằng giá trị `tf_server_now()` của chính lần gọi đó.
    - `checkIn` với toạ độ nằm NGOÀI `radius_meters` của điểm làm việc vẫn ghi thành công và vẫn trả về bản ghi — không ném lỗi (D-20).
    - `checkIn` ghi `distance_meters` bằng kết quả `tf_distance_meters()` do server gọi; một giá trị khoảng cách do nơi gọi truyền vào (nếu có) bị bỏ qua hoàn toàn.
    - `checkIn` chọn điểm làm việc có khoảng cách NHỎ NHẤT trong số các `work_sites` đang `is_active` của doanh nghiệp, và ghi id của điểm đó vào `work_site_id`.
    - Doanh nghiệp chưa khai điểm làm việc nào: `checkIn` vẫn ghi thành công với `work_site_id` và `distance_meters` để `null` — không ném lỗi.
    - `checkIn` gọi mà thiếu ảnh ném lỗi mang phân loại `missing_photo`, và ném TRƯỚC khi chạm Storage.
    - `GET /api/attendance-photos/[id]` với phiên thuộc đúng doanh nghiệp của ảnh trả 200 kèm `content-type` ảnh và thân phản hồi khác rỗng.
    - `GET /api/attendance-photos/[id]` với phiên thuộc doanh nghiệp KHÁC trả 404 — không phải 403 (403 xác nhận id tồn tại), không phải 200.
    - `GET /api/attendance-photos/[id]` với vai trò không phải `owner`/`admin` trả 403.
    - `GET /api/attendance-photos/[id]` với một id không tồn tại trả 404, cùng thân phản hồi với trường hợp xuyên doanh nghiệp.
    - Phản hồi 200 của route đó mang header `cache-control` chứa `no-store`.
  </behavior>
  <action>
    **(a) `scripts/storage-bucket.mjs`** + npm script `db:bucket` (`node --env-file=.env.local
    scripts/storage-bucket.mjs`), theo khuôn `scripts/seed-auth.mjs`: đọc `SUPABASE_SECRET_KEY` và
    `NEXT_PUBLIC_SUPABASE_URL`, thiếu thì in thông điệp tiếng Việt rồi `process.exit(1)`. Script tạo
    bucket theo phương án đã chốt ở checkpoint, với `public: false`, `allowedMimeTypes` giới hạn ở
    JPEG (và PNG nếu cần), và `fileSizeLimit` đặt ở 4 MB. Chạy lại nhiều lần phải an toàn: bucket đã
    tồn tại thì cập nhật cấu hình chứ không lỗi. Ghi rõ trong comment đầu file: giới hạn MIME và
    kích thước đặt ở **cấu hình bucket phía server** vì đó là chỗ duy nhất không tin lời khai của
    client về kiểu nội dung.

    **(b) `next.config.ts`** — thêm `experimental.serverActions.bodySizeLimit: "4mb"`. Giới hạn mặc
    định là 1 MB; một ảnh JPEG từ camera điện thoại đời mới vẫn có thể vượt, và khi đó người dùng
    thấy một lỗi không thuộc ba lý do của D-20b trong khi mạng và camera đều bình thường. Comment
    tại chỗ ghi rõ đây là biên an toàn đi kèm mục tiêu nén phía client, không thay thế nó.

    **(c) `src/lib/types/domain.ts`** — thêm toàn bộ kiểu mà PHASE này cần (các plan sau chỉ đọc, không
    sửa lại file này):
    - `WorkSite` (`id`, `companyId`, `name`, `latitude`, `longitude`, `radiusMeters`, `isActive`,
      `createdAt`) và `WorkSiteInput`.
    - `PhotoReviewStatus = "pending" | "approved" | "rejected"` (khớp enum `photo_review_status` đã có).
    - `AttendancePhoto` (`id`, `companyId`, `attendanceRecordId`, `kind`, `capturedAt`, `latitude`,
      `longitude`, `accuracyMeters`, `workSiteId`, `workSiteName`, `distanceMeters`, `reviewStatus`).
      **Không** khai trường nào chứa URL ảnh — ảnh chỉ đến qua đường `/api/attendance-photos/{id}`.
    - `PunchEvidence` — `{ photo: Blob; latitude: number; longitude: number; accuracyMeters: number }`,
      thứ client gửi kèm khi chấm công.
    - `AttendanceRejectionReason = "missing_photo" | "outside_shift" | "network_error"` (giá trị enum
      giữ tiếng Anh theo quy ước dự án; nhãn tiếng Việt nằm ở `constants.ts`).

    **(d) `src/lib/constants.ts`** — thêm **toàn bộ** khối chữ tiếng Việt của phase, lấy nguyên văn từ
    `03-UI-SPEC.md` §"Copywriting Contract": `ATTENDANCE_REJECTION_LABEL` (ba lý do, mỗi lý do có
    tiêu đề và phần thân), `ATTENDANCE_EVIDENCE_LABEL` (nhãn đang mở camera, đang lấy vị trí, vị trí
    đã xác định, đang gửi, nút gửi, nút chụp lại, khối từ chối quyền camera, khối từ chối quyền vị
    trí, khối không có camera, khối camera đang bận, banner ngoài bán kính kèm nút "Đã hiểu"),
    `WORK_SITE_LABEL` (rỗng/nút thêm/xác nhận ngừng sử dụng), `PHOTO_REVIEW_STATUS_LABEL`. Toàn bộ
    chữ của phase tập trung ở đây trong plan này để các plan wave sau chạy song song mà không tranh
    cùng một file — ghi lý do đó thành comment đầu khối.

    **(e) `src/lib/storage/attendance-photos.ts`** — `ATTENDANCE_PHOTO_BUCKET`,
    `ALLOWED_PHOTO_MIME_TYPES`, và `buildAttendancePhotoPath({ companyId, employeeId, photoId, kind })`
    dựng đường dẫn theo đúng phương án đã chốt. Comment đầu file ghi rõ ràng buộc `check` của database
    đòi đường dẫn bắt đầu bằng `company_id`, và phần cuối đường dẫn là uuid của `attendance_photos.id`
    chứ không phải một số thứ tự — để không ai liệt kê được ảnh bằng cách đoán.

    **(f) `src/lib/validation/api/attendance-photos.ts`** — `punchEvidenceSchema` (vĩ độ trong
    `[-90, 90]`, kinh độ trong `[-180, 180]`, khớp đúng ràng buộc `check` của `work_sites`; độ chính
    xác là số không âm; ảnh là `Blob` có `type` nằm trong danh sách MIME cho phép và `size` lớn hơn 0),
    `attendancePhotoRowSchema` (dòng thô từ Supabase → camelCase, chỉ dùng ở server) và
    `attendancePhotoSchema` (plain, dùng cả hai đầu — D-12d). **Không** khai trường khoảng cách như
    một trường đầu vào ở đâu cả.

    **(g) `src/lib/data/mutations/attendance.ts`** — mở rộng `checkIn`:
    - Chữ ký thành `checkIn(companyId, employeeId, date, time, evidence: PunchEvidence)`. Hai tham số
      `date`/`time` giữ nguyên hành vi hiện tại ở plan này (đối chiếu ngày, bỏ qua giờ); việc bỏ hẳn
      chúng theo ATT-06 nằm ở plan 03-04 cùng lúc với `checkOut`, để lát cắt tracer không phải sửa
      call site hai lần.
    - `evidence` thiếu hoặc `punchEvidenceSchema` không parse được → ném lỗi mang phân loại
      `missing_photo`, ném **trước** khi chạm Storage hay ghi bản ghi.
    - Sau khi có `nowIso` từ `tf_server_now()` (đã có sẵn trong hàm) và sau khi bản ghi
      `attendance_records` được ghi: đọc mọi `work_sites` đang `is_active` của doanh nghiệp; với mỗi
      điểm gọi `supabase.rpc("tf_distance_meters", …)` rồi giữ khoảng cách **nhỏ nhất** cùng id điểm
      tương ứng. Không có điểm nào thì để cả hai giá trị là `null` và đi tiếp.
    - **Không có nhánh nào so khoảng cách rồi ném lỗi.** Ngoài bán kính là một ghi chú, không phải một
      điều kiện (D-20, D-20a). Comment tại chỗ ghi rõ điều này để một lần sửa sau không lặng lẽ biến
      nó lại thành điều kiện chặn.
    - Sinh `photoId` bằng `randomUUID()`, dựng đường dẫn bằng `buildAttendancePhotoPath()`, tải ảnh
      lên bucket bằng `supabase.storage.from(...).upload(...)` với `contentType` lấy từ `evidence.photo.type`
      đã được schema kiểm. Tải lên thất bại thì ném lỗi tiếng Việt và **không** để lại dòng
      `attendance_photos` mồ côi.
    - Ghi (hoặc cập nhật, theo ràng buộc `unique (attendance_record_id, kind)`) dòng
      `attendance_photos` với `id = photoId`, `kind = 'check_in'`, `captured_at = nowIso`,
      `latitude`/`longitude`/`accuracy_meters` từ `evidence`, `work_site_id` và `distance_meters` từ
      phép đo ở trên.
    - `logMutation` một dòng riêng cho `entityTable: "attendance_photos"` — `after` là **dòng
      `attendance_photos` vừa ghi**, tức là đường dẫn và siêu dữ liệu, tuyệt đối không phải byte ảnh
      hay chuỗi base64 của ảnh (D-18a).

    **(h) `src/app/api/attendance-photos/[id]/route.ts`** — broker, GET-only:
    - `export const dynamic = "force-dynamic"` và **chỉ** `export async function GET`. Không export gì
      khác — cổng cơ học D-12c sẽ đỏ.
    - `getSessionContext()` trước mọi thứ; `requireRole(role, ["owner", "admin"])` (ATT-04: chỉ quản
      trị xem lại).
    - Đọc dòng `attendance_photos` theo `id` **kèm** `.eq("company_id", companyId)` — kiểm tường minh
      ở tầng ứng dụng, RLS là lớp hai (khuôn đã dùng khắp Phase 2). Không thấy → 404 với đúng cùng
      một thân phản hồi như trường hợp id không tồn tại, để không phân biệt được "id sai" với "id
      đúng nhưng của doanh nghiệp khác".
    - Lấy byte ảnh bằng `supabase.storage.from(...).download(path)` **ở phía server**, rồi trả thẳng
      byte đó. Tuyệt đối không gọi hàm ký liên kết của Supabase Storage và không đưa bất kỳ URL nào
      của miền `*.supabase.co` vào phản hồi: một liên kết ký, một khi đã phát hành, dùng được cho bất
      kỳ ai cầm nó cho tới lúc hết hạn và không tái kiểm quyền lần nào nữa — đúng thứ tiêu chí 4 của
      ROADMAP cấm. Comment tại chỗ ghi rõ lý do này.
    - Header phản hồi: `content-type` theo kiểu của blob (mặc định JPEG), `cache-control` chứa
      `private` và `no-store`.
    - Tải xuống lỗi → 502 với thông điệp tiếng Việt.

    **(i) `src/app/api/attendance-photos/[id]/__tests__/route.test.ts`** — test tích hợp gọi thẳng hàm
    `GET` đã export với `params` giả lập, `getSessionContext` được mock lần lượt thành phiên cty-01 và
    phiên cty-02, chạy trên Postgres dev thật đã seed. Khẳng định đúng bảy hành vi liệt kê trong
    `<behavior>` ở trên. Đây là **lớp kiểm chính** cho cô lập ảnh xuyên doanh nghiệp: pgTAP không
    kiểm được policy trên `storage.objects` vì `0001_supabase_compat.sql` không dựng schema `storage`
    trên Postgres tạm của CI. Nói thẳng điều đó trong comment đầu file test: policy `storage.objects`
    là lớp phòng thủ **backstop chưa được chứng minh bằng cổng tự động**, còn lớp được chứng minh là
    chính đoạn kiểm `company_id` trong route này.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npm run check:secrets` thoát 0.
    - `npx vitest run src/app/api/attendance-photos` thoát 0 và báo ít nhất 7 test.
    - Một `GET` trên broker route với phiên cty-02 cho một ảnh của cty-01 trả về mã `404` (khẳng định trong test ở trên).
    - `grep -rn "createSignedUrl" src/ | grep -v __tests__` không trả về dòng nào.
    - `grep -rn "supabase.co/storage" src/` không trả về dòng nào.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/attendance-photos/` trả về `0`.
    - `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0.
    - `grep -v '^ *//' src/lib/data/mutations/attendance.ts | grep -c "tf_distance_meters"` trả về ít nhất `1`.
    - `grep -v '^ *//' src/lib/data/mutations/attendance.ts | grep -cE "distance_meters *[><]|distanceMeters *[><]"` trả về `0` — không có nhánh nào so sánh khoảng cách để chặn (D-20).
    - `node -e "const c=require('./next.config.ts')" ` không áp dụng; thay bằng: `grep -c "bodySizeLimit" next.config.ts` trả về ít nhất `1`.
    - Sau `npm run db:bucket`: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select public from storage.buckets where name='attendance-photos'"` trả về `f`; kết quả ghi vào SUMMARY.
    - Sau một lần chấm công thật qua giao diện: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from attendance_photos where distance_meters is not null and captured_at is not null"` tăng đúng `1`.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_table='attendance_photos' and (after::text like '%base64%' or length(after::text) > 4000)"` trả về `0`.
    - `grep -rn "setInterval\|cron\|schedule" scripts/storage-bucket.mjs` không trả về dòng nào — không có job dọn ảnh nào được dựng (D-22).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run check:secrets &amp;&amp; npx vitest run src/app/api/attendance-photos src/__tests__/route-handlers-get-only.test.ts</automated>
  </verify>
  <done>Ảnh nằm trong bucket riêng tư và chỉ ra khỏi server qua một route tự kiểm doanh nghiệp mỗi lần gọi; khoảng cách do server đo và ghi cùng bản ghi; ngoài bán kính vẫn được nhận; phiên của doanh nghiệp khác cầm đúng URL nhận 404.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: Đường đi của người dùng — camera trực tiếp cho "Vào ca", và quản trị nhìn thấy ảnh</name>
  <files>src/lib/attendance/camera.ts, src/components/employee-app/camera-sheet.tsx, src/components/employee-app/attendance-status-card.tsx, src/app/employee/employee-home-view.tsx, src/components/attendance/attendance-photo-dialog.tsx, src/app/admin/employees/[id]/employee-detail-view.tsx</files>
  <read_first>
    - `src/app/employee/employee-home-view.tsx` (toàn bộ, 196 dòng) — `handleCheckIn(time)` hiện gọi
      thẳng `checkInService`; đây là chỗ Camera Sheet chen vào giữa.
    - `src/components/employee-app/attendance-status-card.tsx` (toàn bộ, 223 dòng) — nút "Vào ca" ở
      dòng 180-188, và **miễn trừ rule D-19a có ghi lý do ở dòng 41-53**: đồng hồ tick thật khởi tạo
      sau khi component gắn vào DOM. Miễn trừ đó hợp lệ và **không được gỡ**.
    - `src/components/ui/sheet.tsx` — component Sheet đã cài sẵn; Camera Sheet dựng trên nó ở chiều
      cao toàn khung nhìn.
    - `src/components/ui/dialog.tsx`, `src/components/ui/skeleton.tsx`, `src/components/ui/button.tsx`
      — Button đã có sẵn biến thể `mobile` (cao 44px) và `icon-mobile` (44×44px); không thêm biến thể
      kích thước mới.
    - `src/app/admin/employees/[id]/employee-detail-view.tsx` dòng 420-460 — bảng lịch sử chấm công
      hiện có của quản trị; đây là nơi gắn nút mở Dialog xem ảnh.
    - `src/lib/constants.ts` (khối vừa thêm ở Task 2) — toàn bộ chữ tiếng Việt lấy từ đây, không viết
      chuỗi tiếng Việt rải rác trong component.
    - `src/app/globals.css` — token `--tf-warning`, `--tf-error`, `.num`, `.surface-card`,
      `--tf-bottomnav-h`.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md` §"Color" (ngoại lệ 60/30/10 cho vùng
      khung hình, nút chụp **không** phải màu nhấn), §"Spacing Scale" (nút chụp 72px, chạm tối thiểu
      44px, Sheet thay hẳn thanh điều hướng dưới), §"UI Considerations".
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md` §"Pattern 1", §"Pattern 2",
      §"Pattern 3", §"Testing".
  </read_first>
  <behavior>
    - `openCamera()` yêu cầu `facingMode` với giá trị `ideal` là camera sau (không phải `exact`: trên thiết bị chỉ có một camera, `exact` ném lỗi ràng buộc dù camera đó dùng được).
    - `openCamera()` khi `getUserMedia` reject với `DOMException` tên `NotAllowedError` ném `CameraPermissionDeniedError`, không phải lỗi chung.
    - `closeCamera(stream)` gọi `stop()` trên TỪNG track — bỏ tham chiếu stream thôi thì đèn camera vẫn sáng.
    - `captureFrame(video)` trả về một `Blob` kiểu JPEG dựng từ canvas có kích thước bằng đúng `videoWidth`/`videoHeight` của thẻ video.
    - `acquireLocation()` gọi `getCurrentPosition` với `enableHighAccuracy` bật, `timeout` bằng `GPS_TIMEOUT_MS`, và `maximumAge` bằng 0 — mỗi lần chấm công là một phép đo mới, không dùng vị trí cache.
    - `GPS_TIMEOUT_MS` là một hằng số có tên, giá trị 15000, khai đúng một chỗ.
    - Camera Sheet ở trạng thái đang mở camera hiển thị nhãn "Đang mở camera…"; không có trạng thái nào vẽ một khung đen không kèm chữ.
    - Nút "Gửi chấm công" bị vô hiệu khi chưa có ảnh, và vẫn bị vô hiệu khi đã có ảnh nhưng chưa có toạ độ.
    - Trong lúc gửi, nút hiển thị nhãn đang gửi và bị vô hiệu — bấm hai lần không tạo hai lần gọi.
    - `getUserMedia` reject với `NotAllowedError`: toàn bộ vùng khung hình bị thay bằng khối giải thích kèm nút "Thử lại", và nút đó gọi lại `openCamera()` chứ không tải lại trang.
    - Đóng Sheet ở bất kỳ trạng thái nào đều gọi `closeCamera()`.
    - `AttendancePhotoDialog` cho một bản ghi có ảnh dựng `src` của thẻ ảnh trỏ vào `/api/attendance-photos/{id}`, không vào một miền ngoài.
    - `AttendancePhotoDialog` cho một bản ghi không có dòng `attendance_photos` nào hiển thị "Bản ghi này không có ảnh đính kèm." trong ô ảnh, không hiển thị một ảnh vỡ.
  </behavior>
  <action>
    **(a) `src/lib/attendance/camera.ts`** — module thuần, không phụ thuộc React, để test được bằng
    Vitest với `navigator` giả lập:
    - `GPS_TIMEOUT_MS = 15_000` — hằng số có tên, kèm comment: cold GPS fix trong nhà xưởng mất
      10–20 giây, nên mốc 5 giây thường thấy trong ví dụ tài liệu tạo ra thất bại giả đúng trên kịch
      bản mà D-20 lo ngại. Con số này là điểm khởi đầu và còn cần đo tại văn phòng thật (mục QA thủ
      công ở plan 03-07) — ghi thẳng vào comment.
    - `openCamera()` yêu cầu camera sau qua `facingMode` với giá trị `ideal`, `audio` tắt. Bắt
      `DOMException` và phân loại theo `name`; ở plan này hiện thực đầy đủ nhánh `NotAllowedError`
      (`CameraPermissionDeniedError`) và để ba nhánh còn lại ném tiếp lỗi gốc — plan 03-03 phân loại
      nốt. Định nghĩa sẵn cả bốn lớp lỗi ở đây để 03-03 chỉ điền nhánh, không phải đổi hình dạng.
    - `closeCamera(stream)` dừng từng track.
    - `captureFrame(video)` theo Pattern 2 của RESEARCH: canvas đúng kích thước video, `drawImage`,
      `toBlob` kiểu JPEG chất lượng 0.9. Comment ghi rõ: ảnh dựng từ canvas **không mang EXIF** ngay
      từ đầu, nên không cần thêm bước tách EXIF — đó là hệ quả cấu trúc của chính kiến trúc mà ATT-01
      đòi hỏi.
    - `acquireLocation()` bọc `getCurrentPosition` thành Promise với ba tuỳ chọn nói ở `<behavior>`.

    **(b) `src/components/employee-app/camera-sheet.tsx`** — `"use client"`, dựng trên `Sheet` ở chiều
    cao toàn khung nhìn, thay hẳn thanh điều hướng dưới khi mở (không phủ lên nó). Máy trạng thái:
    `idle → requesting → streaming → captured → submitting → error`. Ở plan tracer này hiện thực đủ
    đường hạnh phúc cộng nhánh từ chối quyền camera:
    - `requesting`: nền tối kèm chữ và spinner theo hằng số nhãn — không để một khung trống câm.
    - `streaming`: thẻ `<video>` phát luồng; chrome đè lên là lớp phủ mờ tối theo ngoại lệ đã ghi ở
      UI-SPEC (vùng khung hình không phải một mặt nền canvas). Nút chụp là hình tròn 72px, **màu
      trung tính chứ không phải màu nhấn** — nó chỉ chụp, không gửi, và giữ đúng một hành động màu
      nhấn cho mỗi trạng thái màn hình. Nút đóng nhỏ, ở góc trên.
    - Chip trạng thái vị trí đặt cạnh nút gửi: đang lấy → đã xác định. Gọi `acquireLocation()` song
      song với việc mở camera, không nối tiếp.
    - `captured`: hiện ảnh vừa chụp, nút "Chụp lại" và nút "Gửi chấm công" (đây mới là nút màu nhấn).
      Nút gửi vô hiệu tới khi có **cả** ảnh và toạ độ.
    - `submitting`: nút đổi nhãn và vô hiệu.
    - Nhánh từ chối quyền camera: thay toàn bộ vùng khung hình bằng khối giải thích kèm nút "Thử lại"
      gọi lại `openCamera()`.
    - **Không** dùng phần tử chọn tệp của HTML ở bất kỳ đâu trong file này hay trong cây
      `src/app/employee/`. Thuộc tính gợi ý chụp ảnh của phần tử đó cũng không đủ: trên iOS Safari hệ
      điều hành vẫn luôn hiện bộ chọn thư viện ảnh bên cạnh camera, nên đường duy nhất loại trừ được
      thư viện ảnh là luồng media trực tiếp. Ghi lý do đó thành comment đầu file.
    - Không đọc đồng hồ thiết bị ở lần vẽ đầu (rule D-19a); dấu thời gian đến từ server.
    - Dọn dẹp: `useEffect` trả hàm dừng luồng; đóng Sheet ở bất kỳ trạng thái nào cũng dừng luồng.

    **(c) `attendance-status-card.tsx`** — đổi `onCheckIn` từ `(time: string) => void` thành một lời
    gọi không tham số mở Camera Sheet. **Giữ nguyên** khối đồng hồ tick và hai dòng miễn trừ rule
    D-19a kèm lý do — không gỡ, không đổi phạm vi miễn trừ.

    **(d) `employee-home-view.tsx`** — giữ `handleCheckIn` nhưng đổi nó thành nơi nhận `PunchEvidence`
    từ Camera Sheet rồi gọi `checkInService(...)`. Quản lý trạng thái mở/đóng Sheet ở view này.

    **(e) `src/components/attendance/attendance-photo-dialog.tsx`** — Dialog tối giản của quản trị:
    một ô ảnh tỉ lệ cố định hiển thị `Skeleton` trong lúc tải, rồi thẻ ảnh trỏ vào
    `/api/attendance-photos/{photoId}`. Tải lỗi → chữ tiếng Việt trong đúng ô ảnh kèm nút tải lại,
    **không** làm đỏ cả Dialog. Bản ghi không có dòng ảnh nào → dòng "Bản ghi này không có ảnh đính
    kèm." trong ô ảnh. Ở plan tracer này Dialog chỉ hiển thị ảnh; khoảng cách, toạ độ, độ chính xác,
    liên kết bản đồ và hành động "Đánh dấu đã xem xét" thuộc plan 03-05 và cắm vào chính component
    này mà không đổi hình dạng nó.

    **(f) `employee-detail-view.tsx`** — thêm một cột hành động vào bảng lịch sử chấm công đang có,
    mở `AttendancePhotoDialog` cho bản ghi tương ứng. Không đổi bố cục bảng ngoài cột này.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0.
    - `grep -rE '<input[^>]*type="file"' src/app/employee/ src/components/employee-app/` không trả về dòng nào.
    - `grep -rE 'accept="image' src/app/employee/ src/components/employee-app/` không trả về dòng nào.
    - `grep -c "getUserMedia" src/lib/attendance/camera.ts` trả về ít nhất `1`.
    - `grep -v '^ *//' src/lib/attendance/camera.ts | grep -c "15_000\|15000"` trả về ít nhất `1`, và `grep -rc "15000\|15_000" src/components/employee-app/camera-sheet.tsx` trả về `0` — con số chỉ khai một chỗ.
    - `grep -c "getTracks" src/lib/attendance/camera.ts` trả về ít nhất `1`.
    - `grep -c "no-date-in-client" src/components/employee-app/attendance-status-card.tsx` trả về ít nhất `2` — miễn trừ D-19a hợp lệ vẫn nguyên vẹn.
    - `grep -c "/api/attendance-photos/" src/components/attendance/attendance-photo-dialog.tsx` trả về ít nhất `1`.
    - `grep -rEc "new Date\(\)|Date\.now\(\)" src/components/employee-app/camera-sheet.tsx` trả về `0`.
    - Với dev server đang chạy và một phiên nhân viên thật, trên một thiết bị có camera: bấm "Vào ca" mở khung hình trực tiếp, chụp, gửi — và `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from attendance_photos where kind='check_in'"` tăng đúng `1`. Kết quả quan sát ghi vào SUMMARY.
    - Với phiên owner của cùng doanh nghiệp: mở hồ sơ nhân viên đó, mở Dialog ảnh của bản ghi vừa tạo — ảnh hiển thị. Kết quả quan sát ghi vào SUMMARY.
    - Với một bản ghi chấm công seed không có dòng `attendance_photos`: Dialog hiện đúng dòng "Bản ghi này không có ảnh đính kèm.". Kết quả quan sát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Một nhân viên chấm công vào ca bằng camera trực tiếp kèm toạ độ thật, và quản trị mở đúng bản ghi đó nhìn thấy ảnh qua broker route — toàn bộ lát cắt chạy đầu-cuối trên dữ liệu thật.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt nhân viên → Server Action `checkIn` | Ảnh, toạ độ và độ chính xác đều là dữ liệu do client khai; không giá trị nào trong đó là bằng chứng cho tới khi server tự đo lại phần đo được |
| Trình duyệt quản trị → `GET /api/attendance-photos/[id]` | URL công khai, xác thực bằng cookie; đây là ranh giới mà tiêu chí 4 của ROADMAP nói tới |
| Ứng dụng → Supabase Storage | Lần đầu tiên dự án lưu dữ liệu nhị phân; bucket là một mặt phẳng tên gọi có thể liệt kê nếu đường dẫn đoán được |
| Bảng `attendance_photos` → `audit_log` | `before`/`after` lưu nguyên dòng (D-18) nên mọi thứ trong dòng đó được nhân bản sang một bảng có vòng đời khác |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Liên kết ảnh bị sao chép sang phiên của doanh nghiệp khác | critical | mitigate | Không phát hành liên kết ký; broker Route Handler đọc `company_id` từ `getSessionContext()` và so lại trên MỖI lần gọi rồi `download()` ở phía server. Acceptance criteria: phiên cty-02 gọi URL ảnh cty-01 nhận `404`; hai lệnh grep chặn liên kết ký và miền storage quay lại mã nguồn |
| T-03-02 | Information Disclosure | Liệt kê ảnh bằng cách đoán `storage_path` | high | mitigate | Phần cuối đường dẫn là uuid của `attendance_photos.id`, không phải số thứ tự; tiền tố `company_id` cộng policy `storage.objects` là lớp hai. **Lớp hai này chưa có cổng tự động** — pgTAP không dựng được schema `storage` trên Postgres tạm; lớp được chứng minh là đoạn kiểm `company_id` trong route |
| T-03-03 | Elevation of Privilege | Nhân viên thường mở ảnh của người khác qua broker route | high | mitigate | `requireRole(role, ["owner","admin"])` trong route; test tích hợp khẳng định `403` |
| T-03-04 | Tampering | Client gửi lên một giá trị khoảng cách bịa | high | mitigate | `distance_meters` **không** là trường đầu vào ở bất kỳ schema nào; server luôn tự gọi `tf_distance_meters()`. Acceptance criteria có grep chặn mọi nhánh so sánh khoảng cách trong đường ghi |
| T-03-05 | Tampering | Client gửi lên dấu thời gian chụp ảnh của riêng nó | high | mitigate | `captured_at` ghi bằng `tf_server_now()` của chính lần gọi; không tham số nào của `checkIn` chạm tới nó (ATT-06, nối tiếp D-19) |
| T-03-06 | Tampering | Ảnh có sẵn trong máy được nộp thay cho ảnh hiện trường | high | mitigate | Không có phần tử chọn tệp nào trong cây nhân viên; ảnh chỉ dựng từ luồng media trực tiếp qua canvas. Acceptance criteria có hai lệnh grep chặn |
| T-03-07 | Tampering / DoS | Tệp không phải ảnh giả dạng ảnh, hoặc tệp quá lớn | medium | mitigate | `allowedMimeTypes` và `fileSizeLimit` đặt ở **cấu hình bucket phía server**, không tin kiểu nội dung client khai; `bodySizeLimit` 4 MB ở Next làm biên trên thứ hai |
| T-03-08 | Information Disclosure | `audit_log` thành bản sao thứ hai của ảnh/toạ độ (D-22a) | medium | accept | Chấp nhận có ý thức: nhóm PRIV nằm ngoài phạm vi V2. Giảm thiểu phần làm được ngay: chỉ đường dẫn và siêu dữ liệu vào `after`, không bao giờ byte ảnh — acceptance criteria đếm dòng audit có dấu hiệu base64 hoặc quá dài phải bằng 0 |
| T-03-09 | Repudiation | Ảnh tích tụ vô thời hạn không ai chịu trách nhiệm (D-22) | medium | accept | Quyết định đã khoá của chủ dự án; ghi nhận rủi ro NĐ 13/2023 ở D-22a. Bố cục đường dẫn chọn ở checkpoint là thứ quyết định một job dọn về sau có nhắm được vào cái gì không |
| T-03-10 | Spoofing | Toạ độ bị giả lập bởi một client đã sửa đổi | high | accept | Web Geolocation API không lộ ra bất kỳ tín hiệu chống giả lập nào cho JavaScript; không có biện pháp kỹ thuật nào ở tầng này. Sau D-20, lớp phát hiện thay thế là quy tắc cách-tâm-quá-xa (D-21) ở plan 03-06 — chấp nhận ở đây và chuyển tiếp có tên |
| T-03-11 | Information Disclosure | Khoá bí mật lọt xuống bundle qua script tạo bucket | high | mitigate | `scripts/storage-bucket.mjs` nằm ngoài `src/`; `npm run check:secrets` là acceptance criteria của Task 2 |
| T-03-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này **không cài gói npm mới nào**. Gói duy nhất của phase (`browser-image-compression`, mang thẻ `[ASSUMED]` trong RESEARCH) được cài ở plan 03-03 sau một checkpoint xác minh chặn cứng |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run test:db && npm run check:assertions` thoát 0, tổng assertion ≥ 191 và sàn đã được nâng
- `npm run check:secrets` thoát 0
- Phiên của doanh nghiệp khác cầm đúng URL ảnh nhận 404
- Không dòng mã nguồn nào trong `src/` gọi hàm ký liên kết của Storage hay nhắc miền storage
- Không phần tử chọn tệp nào tồn tại trong cây nhân viên
- Một lần chấm công thật ghi đúng một dòng `attendance_photos` có `distance_meters` và `captured_at`
- Quản trị mở được ảnh của bản ghi đó trong giao diện
</verification>

<success_criteria>
Toàn bộ hình dạng kiến trúc của phase đã chạy thật trên một đường đi duy nhất: camera trực tiếp →
toạ độ → Server Action → Storage riêng tư → khoảng cách do database đo → giờ do database cấp →
broker route có phân quyền → mắt quản trị. Mọi việc còn lại của phase là lặp lại và mở rộng trên
hình dạng này, không phải phát minh thêm hình dạng mới.
</success_criteria>

<output>
Create `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-01-SUMMARY.md` when done
</output>
