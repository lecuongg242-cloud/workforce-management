---
phase: 03-ch-m-c-ng-c-b-ng-ch-ng
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/validation/api/work-sites.ts
  - src/lib/validation/schemas.ts
  - src/lib/data/work-sites.ts
  - src/lib/data/mutations/work-sites.ts
  - src/app/api/work-sites/route.ts
  - src/app/admin/work-sites/page.tsx
  - src/app/admin/work-sites/work-sites-view.tsx
  - src/components/work-sites/work-site-card.tsx
  - src/components/work-sites/work-site-dialog.tsx
  - src/lib/nav.ts
  - src/lib/data/__tests__/work-sites.test.ts
autonomous: true
requirements: [ATT-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Quản trị khai được một điểm làm việc gồm tên, vĩ độ, kinh độ và bán kính cho phép, rồi thấy nó xuất hiện trong danh sách (ATT-03)."
    - "Quản trị sửa được điểm làm việc đang có và ngừng sử dụng nó bằng cách hạ cờ `is_active` — không xoá cứng dòng dữ liệu, vì mọi bản ghi chấm công cũ vẫn tham chiếu tới nó qua `work_site_id`."
    - "`companyId` của một điểm làm việc luôn đến từ `getSessionContext()`; không đường nào cho phép nó đến từ tham số truy vấn hay thân yêu cầu (D-12b)."
    - "Mỗi lần tạo, sửa, hoặc ngừng sử dụng điểm làm việc để lại đúng một dòng `audit_log` với `entity_table = 'work_sites'` (D-17)."
    - "`src/app/api/work-sites/route.ts` chỉ xuất `GET` và `dynamic`; mọi thao tác ghi đi qua Server Action (D-12c)."
    - "Doanh nghiệp chưa có điểm làm việc nào thấy trạng thái rỗng nói rõ vì sao cần khai — \"Khai báo điểm làm việc đầu tiên để hệ thống tính được khoảng cách khi nhân viên chấm công.\" — kèm ngay nút hành động, không phải một màn hình trắng (UI-SPEC: Empty — work-site list)."
    - "Danh sách đang tải hiển thị đúng khối `Skeleton` như `ShiftsView` đang dùng, không phải một mẫu tải mới (UI-SPEC: Loading — admin lists)."
    - "Xác nhận ngừng sử dụng nói rõ hệ quả — \"Nhân viên chấm công sẽ không còn được tính khoảng cách theo điểm này nữa.\" — chứ không chỉ hỏi \"bạn có chắc không\"."
    - "Vĩ độ ngoài khoảng [-90, 90], kinh độ ngoài khoảng [-180, 180], hoặc bán kính không dương đều bị chặn ở tầng validation trước khi chạm database, và người dùng thấy thông báo tiếng Việt tại đúng ô nhập."
    - statement: "Tên điểm làm việc dài bị cắt bằng `truncate` kèm thuộc tính `title` ở thẻ và ở bảng — CONTEXT.md không đặt giới hạn ký tự, nên đây là biện pháp bố cục chứ không phải một ràng buộc dữ liệu; kiểm bằng một điểm làm việc tên dài hơn 80 ký tự."
      verification: backstop
  artifacts:
    - path: "src/app/api/work-sites/route.ts"
      provides: "Route Handler đọc danh sách điểm làm việc của doanh nghiệp trong phiên"
      exports: ["GET", "dynamic"]
    - path: "src/lib/data/mutations/work-sites.ts"
      provides: "Server Action tạo/sửa/ngừng sử dụng điểm làm việc kèm audit"
      exports: ["createWorkSite", "updateWorkSite", "archiveWorkSite"]
    - path: "src/app/admin/work-sites/work-sites-view.tsx"
      provides: "Màn hình quản trị khai báo điểm làm việc"
      min_lines: 80
    - path: "src/components/work-sites/work-site-dialog.tsx"
      provides: "Form tạo/sửa điểm làm việc"
      min_lines: 60
  key_links:
    - from: "src/app/admin/work-sites/work-sites-view.tsx"
      to: "src/lib/data/work-sites.ts"
      via: "useDataQuery gọi listWorkSites qua Route Handler"
      pattern: "listWorkSites"
    - from: "src/lib/data/mutations/work-sites.ts"
      to: "src/lib/data/audit.ts"
      via: "mỗi thao tác ghi để lại một dòng audit nguyên dòng"
      pattern: "logMutation"
    - from: "src/lib/nav.ts"
      to: "src/app/admin/work-sites/page.tsx"
      via: "mục điều hướng quản trị trỏ tới màn hình mới"
      pattern: "/admin/work-sites"
  prohibitions:
    - statement: "MUST NOT xoá cứng một dòng `work_sites`. Từ plan 03-01, `attendance_photos.work_site_id` trỏ vào bảng này để trả lời câu \"khoảng cách này đo so với điểm nào\"; xoá cứng biến mọi bản ghi chấm công lịch sử thành một con số khoảng cách không còn mốc so sánh, và hỏng đúng bằng chứng mà phase này đang xây."
      category: safety
    - statement: "MUST NOT trình bày bán kính cho phép như một ngưỡng chặn chấm công. Sau D-20, nằm ngoài bán kính không từ chối ai — bán kính chỉ còn là mốc để đo. Một nhãn hay dòng trợ giúp nói \"nhân viên phải đứng trong bán kính này mới chấm công được\" làm người quản trị tin vào một biện pháp kiểm soát không tồn tại, và đó chính là cảm giác an toàn giả mà D-21b cảnh báo."
      category: transparency
---

<!-- planner-discipline-allow: is_active -->

<objective>
Lát cắt khai báo điểm làm việc: Route Handler đọc, Server Action ghi, và màn hình quản trị theo đúng
khuôn đã lặp sáu lần ở Phase 2 (`ShiftsView`). Đây là dữ liệu mốc mà toàn bộ phép đo khoảng cách của
phase dựa vào.

Purpose: `tf_distance_meters()` và cột `work_site_id` đã chạy thật từ plan tracer, nhưng dữ liệu mốc
hiện chỉ có một dòng seed cho mỗi doanh nghiệp. Không có màn hình khai báo thì ATT-03 không tồn tại
và một doanh nghiệp thật không tự dựng được mốc đo của mình.

Output: `GET /api/work-sites`, ba Server Action, màn hình `/admin/work-sites`, `WorkSiteCard`,
`WorkSiteDialog`, mục điều hướng quản trị.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-01-SUMMARY.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-06-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Route Handler | `GET /api/work-sites` |
| Server Action | `createWorkSite()`, `updateWorkSite()`, `archiveWorkSite()` |
| Hàm dữ liệu | `listWorkSites()` |
| Schema Zod | `workSiteRowSchema`, `workSiteSchema`, `workSiteListResponseSchema`, `workSiteInputSchema`, `workSiteFormSchema` |
| Kiểu form | `WorkSiteFormValues` |
| Trang | `/admin/work-sites` |
| Component | `WorkSitesView`, `WorkSiteCard`, `WorkSiteDialog` |
| Điều hướng | mục `ADMIN_NAV_ITEMS` cho `/admin/work-sites` |
| Test | `src/lib/data/__tests__/work-sites.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Đường đọc và đường ghi cho điểm làm việc</name>
  <files>src/lib/validation/api/work-sites.ts, src/lib/data/work-sites.ts, src/lib/data/mutations/work-sites.ts, src/app/api/work-sites/route.ts, src/lib/data/__tests__/work-sites.test.ts</files>
  <read_first>
    - `src/app/api/shifts/route.ts` (toàn bộ, 91 dòng) — khuôn Route Handler GET-only đầy đủ: hằng
      danh sách cột, hàm `load*ForCompany`, bậc thang `catch` với bốn lớp lỗi, và quy ước "chưa thuộc
      doanh nghiệp nào thì trả mảng rỗng chứ không phải lỗi".
    - `src/lib/data/mutations/shifts.ts` (toàn bộ) — khuôn Server Action ghi: `getSessionContext()` →
      `requireRole(role, ["owner","admin"])` → đọc nguyên dòng trước → ghi kèm
      `.eq("company_id", companyId)` → đọc nguyên dòng sau → `logMutation` ngay trong cùng hàm.
    - `src/lib/data/shifts.ts` — khuôn hàm dữ liệu phía client qua `fetchJson`, và quy ước giữ tham số
      `companyId` trong chữ ký nhưng **không** gửi lên server.
    - `src/lib/validation/api/shifts.ts` — khuôn cặp schema `*RowSchema` (transform snake_case →
      camelCase, chỉ dùng ở server) và `*Schema` plain (dùng cả hai đầu, D-12d).
    - `supabase/migrations/0005_v2_tables.sql` dòng 20-35 và 145-165 — cột thật của `work_sites`
      (`latitude numeric(9,6)` với `check between -90 and 90`, `longitude numeric(9,6)` với
      `check between -180 and 180`, `radius_meters int check > 0`, `is_active boolean default true`)
      và bốn policy RLS đã có.
    - `src/lib/types/domain.ts` — kiểu `WorkSite`/`WorkSiteInput` do plan 03-01 thêm; **đọc và dùng,
      không sửa lại**.
    - `src/lib/data/__tests__/shifts.test.ts` — khuôn test: parse schema trực tiếp cộng `fetch` giả
      lập bằng `vi.stubGlobal`.
  </read_first>
  <behavior>
    - `workSiteRowSchema` chuyển `radius_meters` → `radiusMeters`, `is_active` → `isActive`, và giữ `latitude`/`longitude` ở dạng số.
    - `workSiteInputSchema` từ chối vĩ độ nhỏ hơn -90 hoặc lớn hơn 90, kinh độ nhỏ hơn -180 hoặc lớn hơn 180, và bán kính nhỏ hơn hoặc bằng 0, mỗi trường hợp kèm một thông điệp tiếng Việt.
    - `workSiteInputSchema` không khai bất kỳ trường định danh doanh nghiệp nào.
    - `GET /api/work-sites` cho một phiên chưa có doanh nghiệp hiện hành trả về mảng rỗng với mã 200, không phải lỗi.
    - `GET /api/work-sites` trả danh sách sắp xếp xác định: `name` tăng dần rồi `id` tăng dần, nên hai lần gọi liên tiếp cho cùng thứ tự.
    - `GET /api/work-sites` mặc định trả **cả** điểm đang dùng lẫn điểm đã ngừng, kèm cờ `isActive` — màn hình tự phân nhóm; đường ghi chấm công thì chỉ đọc điểm đang dùng.
    - `createWorkSite` với vai trò `manager` hoặc `employee` ném `ForbiddenError` trước khi chạm database.
    - `archiveWorkSite` đặt `is_active` thành `false` và trả về dòng đã cập nhật; số dòng trong bảng không đổi.
    - Mỗi trong ba Server Action ghi đúng một dòng `audit_log` với `entity_table = 'work_sites'`, `before` là dòng trước (hoặc `null` khi tạo) và `after` là dòng sau.
  </behavior>
  <action>
    **(a) `src/lib/validation/api/work-sites.ts`** — bốn schema theo khuôn `shifts.ts`:
    `workSiteRowSchema` (dòng thô → camelCase), `workSiteSchema` (plain), `workSiteListResponseSchema`
    (mảng của schema plain), `workSiteInputSchema` (đầu vào ghi, có ràng buộc số khớp đúng ràng buộc
    `check` của database, không khai định danh doanh nghiệp — D-12b).

    **(b) `src/lib/validation/schemas.ts`** — thêm `workSiteFormSchema` và kiểu `WorkSiteFormValues`
    cho form React Hook Form, theo đúng khuôn `shiftFormSchema` đang có trong file. Thông điệp lỗi
    tiếng Việt tại từng trường.

    **(c) `src/app/api/work-sites/route.ts`** — theo khuôn `shifts/route.ts`, chỉ xuất `dynamic` và
    `GET`. Đọc `.eq("company_id", companyId)` với `companyId` từ `getSessionContext()`. Sắp xếp xác
    định. Bậc thang `catch` sao đúng bốn lớp lỗi của khuôn.

    **(d) `src/lib/data/work-sites.ts`** — `listWorkSites(companyId)` qua `fetchJson`, giữ tham số
    `companyId` trong chữ ký cho nhất quán với các module chị em nhưng không gửi lên server; kèm
    comment như các module trước. Re-export ba Server Action.

    **(e) `src/lib/data/mutations/work-sites.ts`** — `"use server"`, ba hàm theo đúng khuôn
    `mutations/shifts.ts`. `archiveWorkSite(id)` **chỉ** hạ `is_active` — không có lệnh xoá dòng nào
    trong file này. Comment tại chỗ ghi rõ lý do: `attendance_photos.work_site_id` (plan 03-01) trỏ
    vào đây, nên xoá cứng làm mọi bản ghi lịch sử mất mốc so sánh.

    **(f) `src/lib/data/__tests__/work-sites.test.ts`** — khẳng định đủ chín hành vi liệt kê ở
    `<behavior>`: bốn nhóm cho schema (chuyển tên trường; ba nhóm biên toạ độ/bán kính bị từ chối),
    và các nhóm còn lại qua `fetch` giả lập theo khuôn `shifts.test.ts`.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/data/__tests__/work-sites.test.ts` thoát 0 và báo ít nhất 9 test.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/work-sites/` trả về `0`.
    - `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0.
    - `grep -iEc "\.delete\(|drop |delete from" src/lib/data/mutations/work-sites.ts` trả về `0`.
    - `grep -c "logMutation" src/lib/data/mutations/work-sites.ts` trả về `3`.
    - `grep -c "requireRole" src/lib/data/mutations/work-sites.ts` trả về `3`.
    - `grep -iEc "companyId|company_id" src/lib/validation/api/work-sites.ts` — mọi kết quả nằm trong `workSiteRowSchema`/`workSiteSchema` (đường đọc), không nằm trong `workSiteInputSchema`; kiểm bằng mắt và ghi kết luận vào SUMMARY.
    - Với cookie owner Ngọc Phát: `curl -b <cookie> -s "http://localhost:3000/api/work-sites"` trả 200 với ít nhất 1 phần tử; với cookie Bình Minh trả về một tập hợp id không giao nhau.
    - Sau một lần tạo điểm làm việc: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_table='work_sites'"` tăng đúng `1`.
    - Sau một lần ngừng sử dụng: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from work_sites"` không đổi, và số dòng có `is_active = false` tăng đúng `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/work-sites.test.ts src/__tests__/route-handlers-get-only.test.ts</automated>
  </verify>
  <done>Điểm làm việc đọc được qua Route Handler và ghi được qua Server Action theo đúng khuôn Phase 2; ngừng sử dụng là hạ cờ chứ không xoá; mỗi thao tác ghi để lại đúng một vết audit.</done>
</task>

<task type="auto">
  <name>Task 2: Màn hình khai báo điểm làm việc</name>
  <files>src/app/admin/work-sites/page.tsx, src/app/admin/work-sites/work-sites-view.tsx, src/components/work-sites/work-site-card.tsx, src/components/work-sites/work-site-dialog.tsx, src/lib/nav.ts</files>
  <read_first>
    - `src/app/admin/shifts/shifts-view.tsx` (toàn bộ, 220 dòng) — khuôn màn hình đầy đủ:
      `PageHeader` kèm số đếm và một nút màu nhấn, `useDataQuery`, bộ ba skeleton/rỗng/lỗi,
      `ConfirmDialog` cho thao tác lưu trữ, `toast` cho kết quả, `invalidate()` sau mỗi lần ghi.
    - `src/app/admin/shifts/page.tsx` — khuôn Server Component chỉ render View kèm `metadata`.
    - `src/components/shifts/shift-card.tsx` và `src/components/shifts/shift-dialog.tsx` — khuôn thẻ
      và khuôn form (React Hook Form + `zodResolver`).
    - `src/components/common/empty-state.tsx`, `error-state.tsx`, `confirm-dialog.tsx` — ba component
      dùng lại nguyên trạng.
    - `src/lib/nav.ts` — `ADMIN_NAV_ITEMS`: hình dạng từng mục (`href`, `label`, icon) và thứ tự.
    - `src/lib/constants.ts` — khối `WORK_SITE_LABEL` do plan 03-01 thêm; **mọi chữ tiếng Việt của màn
      hình này lấy từ đó**, không viết chuỗi rải rác trong component.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md` §"Copywriting Contract" và hai hàng
      "Screen — work-site declaration" / "Empty — work-site list" / "Loading — admin lists" /
      "Long text — work-site name".
  </read_first>
  <action>
    **(a) `src/app/admin/work-sites/page.tsx`** — Server Component, `export const metadata` theo khuôn
    `shifts/page.tsx`, render `WorkSitesView`.

    **(b) `src/app/admin/work-sites/work-sites-view.tsx`** — `"use client"`, sao khuôn `ShiftsView`
    gần như một-một:
    - `PageHeader` với số đếm và **đúng một** nút màu nhấn "Thêm điểm làm việc".
    - `useDataQuery(() => listWorkSites(session.companyId), [session.companyId])`.
    - Đang tải: lưới khối `Skeleton` **y hệt** `ShiftsView` đang dùng — không dựng mẫu tải mới.
    - Lỗi: `ErrorState` kèm `onRetry={reload}`.
    - Rỗng: `EmptyState` với tiêu đề và phần thân lấy từ `WORK_SITE_LABEL`, kèm chính nút "Thêm điểm
      làm việc" ngay trong trạng thái rỗng.
    - Lưới thẻ `WorkSiteCard`. Điểm đã ngừng sử dụng vẫn hiện nhưng được phân biệt bằng
      `StatusBadge` (không phân biệt bằng màu đơn thuần).
    - Ngừng sử dụng đi qua `ConfirmDialog` với tông destructive, tiêu đề và phần thân lấy từ
      `WORK_SITE_LABEL`, nhãn xác nhận "Ngừng sử dụng".
    - Sau mỗi lần ghi: `invalidate()` rồi `toast`.

    **(c) `src/components/work-sites/work-site-card.tsx`** — theo khuôn `ShiftCard`: tên (cắt bằng
    `truncate` kèm `title` để tên dài không phá bố cục), toạ độ và bán kính hiển thị bằng lớp `.num`
    (chữ số đều), `StatusBadge` cho trạng thái, và menu hành động sửa / ngừng sử dụng. Dòng phụ mô tả
    bán kính phải nói đúng vai trò của nó — mốc để **đo khoảng cách**, không phải điều kiện để chấm
    công được (D-20). Lấy chữ từ `WORK_SITE_LABEL`.

    **(d) `src/components/work-sites/work-site-dialog.tsx`** — theo khuôn `ShiftDialog`: React Hook
    Form + `zodResolver(workSiteFormSchema)`, bốn trường (tên, vĩ độ, kinh độ, bán kính), lỗi hiển thị
    tại từng trường bằng tiếng Việt, nút gửi vô hiệu trong lúc đang gửi. Dùng chung cho cả tạo mới lẫn
    sửa (`editing` khác `null` thì nạp giá trị mặc định).

    **(e) `src/lib/nav.ts`** — thêm một mục `ADMIN_NAV_ITEMS` trỏ tới `/admin/work-sites`, đặt cạnh
    mục ca làm việc (cùng nhóm "khai báo dữ liệu nền"), icon lucide phù hợp (ví dụ `MapPin`).
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0.
    - `grep -c "/admin/work-sites" src/lib/nav.ts` trả về ít nhất `1`.
    - `grep -c "WORK_SITE_LABEL" src/app/admin/work-sites/work-sites-view.tsx` trả về ít nhất `1`.
    - `grep -c "Skeleton" src/app/admin/work-sites/work-sites-view.tsx` trả về ít nhất `1`.
    - `grep -c "EmptyState" src/app/admin/work-sites/work-sites-view.tsx` trả về ít nhất `1`.
    - `grep -c "ConfirmDialog" src/app/admin/work-sites/work-sites-view.tsx` trả về ít nhất `1`.
    - `grep -c "truncate" src/components/work-sites/work-site-card.tsx` trả về ít nhất `1`, và `grep -c "title=" src/components/work-sites/work-site-card.tsx` trả về ít nhất `1`.
    - `grep -rEc "new Date\(\)|Date\.now\(\)" src/app/admin/work-sites/ src/components/work-sites/` trả về `0` cho mọi file.
    - `grep -oE "className=\"[^\"]*bg-brand[^\"]*\"|<Button(?![^>]*variant)" src/app/admin/work-sites/work-sites-view.tsx | wc -l` — đếm nút màu nhấn; kết quả phải là `1` cho trạng thái danh sách có dữ liệu. Ghi con số đo được vào SUMMARY.
    - Với dev server đang chạy và phiên owner Ngọc Phát: mở `/admin/work-sites`, tạo một điểm mới, sửa nó, rồi ngừng sử dụng — cả ba thao tác thành công và danh sách cập nhật ngay. Kết quả quan sát ghi vào SUMMARY.
    - Với một doanh nghiệp không có điểm làm việc nào (tạm hạ cờ mọi điểm rồi khôi phục): màn hình hiện trạng thái rỗng đúng chữ trong `WORK_SITE_LABEL` kèm nút hành động. Kết quả quan sát ghi vào SUMMARY.
    - Với một điểm làm việc tên dài hơn 80 ký tự: thẻ không tràn bố cục và thuộc tính `title` mang tên đầy đủ. Kết quả quan sát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Quản trị khai báo, sửa và ngừng sử dụng được điểm làm việc từ giao diện; màn hình dùng lại đúng bộ ba skeleton/rỗng/lỗi của `ShiftsView` và giữ đúng một nút màu nhấn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt quản trị → `GET /api/work-sites` | URL công khai xác thực bằng cookie; toạ độ điểm làm việc của một doanh nghiệp là thông tin nội bộ |
| Trình duyệt quản trị → Server Action ghi điểm làm việc | Mọi giá trị trong form là dữ liệu do client khai |
| Người dùng vai trò thấp → dữ liệu mốc đo | Đổi được toạ độ hoặc bán kính nghĩa là đổi được kết quả đo khoảng cách của mọi lần chấm công sau đó |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Elevation of Privilege | Nhân viên hoặc quản lý tự sửa toạ độ điểm làm việc để mọi lần chấm công của mình trở thành "đúng chỗ" | high | mitigate | `requireRole(role, ["owner","admin"])` chạy trước mọi thao tác I/O trong cả ba Server Action; acceptance criteria đếm số lần gọi phải bằng 3 |
| T-03-02-02 | Information Disclosure | Đọc được điểm làm việc của doanh nghiệp khác | high | mitigate | `companyId` chỉ từ `getSessionContext()`, `.eq("company_id", companyId)` tường minh, RLS là lớp hai; acceptance criteria gọi cùng URL bằng hai cookie và đòi hai tập id không giao nhau |
| T-03-02-03 | Tampering | Toạ độ hoặc bán kính vô lý đi lọt vào database và làm phép đo khoảng cách trả kết quả vô nghĩa | medium | mitigate | Ràng buộc Zod khớp đúng ràng buộc `check` của `0005_v2_tables.sql`; database là lớp hai |
| T-03-02-04 | Repudiation | Ai đó đổi mốc đo mà không để lại vết | high | mitigate | `logMutation` trong cả ba hàm ghi, before/after nguyên dòng; acceptance criteria đếm dòng audit tăng đúng 1 |
| T-03-02-05 | Denial of Service | Xoá cứng một điểm làm việc làm mọi `attendance_photos.work_site_id` trỏ tới nó thành `null` và mất mốc so sánh lịch sử | high | mitigate | Không có lệnh xoá nào trong module ghi; ngừng sử dụng là hạ cờ. Acceptance criteria có grep chặn, cộng phép đếm số dòng bảng không đổi |
| T-03-02-06 | Spoofing | Thêm một `POST` vào Route Handler cho tiện, tạo lỗ CSRF | medium | mitigate | Cổng cơ học D-12c (`route-handlers-get-only.test.ts`) chạy trong `<verify>` |
| T-03-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- `npm run test:db && npm run check:assertions` không giảm so với plan 03-01
- Cổng GET-only xanh; không có lệnh xoá cứng nào trong module ghi điểm làm việc
- Hai doanh nghiệp thấy hai tập điểm làm việc không giao nhau
- Mỗi thao tác ghi để lại đúng một dòng audit
- Trạng thái rỗng, đang tải và lỗi đều hiện đúng như `ShiftsView`

</verification>

<success_criteria>
Một doanh nghiệp thật tự khai được mốc đo của mình — tên, toạ độ, bán kính — và ngừng sử dụng được
mốc cũ mà không làm hỏng bằng chứng đã ghi theo mốc đó.
</success_criteria>

<output>
Create `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-02-SUMMARY.md` when done
</output>
