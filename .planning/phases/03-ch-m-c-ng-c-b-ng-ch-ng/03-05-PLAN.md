---
phase: 03-ch-m-c-ng-c-b-ng-ch-ng
plan: 05
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - src/lib/validation/api/attendance-photos.ts
  - src/lib/data/attendance-photos.ts
  - src/lib/data/mutations/attendance-photos.ts
  - src/app/api/attendance-photos/route.ts
  - src/components/attendance/attendance-photo-dialog.tsx
  - src/app/admin/employees/[id]/employee-detail-view.tsx
  - src/lib/data/__tests__/attendance-photos.test.ts
autonomous: true
requirements: [ATT-04, ATT-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Quản trị mở một bản ghi chấm công bất kỳ và xem được: ảnh của lần vào, ảnh của lần ra, toạ độ thô, khoảng cách tới điểm làm việc, tên điểm làm việc dùng làm mốc, và độ chính xác GPS mà thiết bị khai (ATT-04)."
    - "Độ chính xác GPS chỉ hiển thị ở màn hình quản trị, kèm cách diễn giải rõ ràng — nó là bán kính vòng tròn tin cậy do thiết bị tự khai, không phải một phép đo của hệ thống."
    - "Có một liên kết mở toạ độ trong bản đồ ngoài, và liên kết đó dựng từ chính hai con số vĩ độ/kinh độ đã lưu — không nhúng bản đồ, không gọi một dịch vụ bản đồ nào từ phía server."
    - "Quản trị đánh dấu một ảnh đã xem xét được; thao tác đó ghi `review_status`, `reviewed_by`, `reviewed_at` và để lại một dòng `audit_log`."
    - "Vai trò không phải `owner`/`admin` không đọc được siêu dữ liệu ảnh và không đổi được trạng thái xem xét."
    - "Bản ghi không có dòng `attendance_photos` nào hiện đúng dòng \"Bản ghi này không có ảnh đính kèm.\" trong ô ảnh — không phải một ảnh vỡ, không phải một Dialog rỗng (UI-SPEC: Empty — record has no photo)."
    - "Ô ảnh giữ tỉ lệ khung cố định và hiện `Skeleton` trong lúc tải; tải hỏng thì hiện lỗi kèm nút tải lại **chỉ trong ô ảnh**, không làm đỏ cả Dialog (UI-SPEC: Loading — photo)."
    - "`GET /api/attendance-photos` chỉ trả siêu dữ liệu; không trường nào trong phản hồi chứa một URL của miền lưu trữ hay một liên kết ký (ATT-05)."
    - "Đúng một nút màu nhấn trong Dialog: \"Đánh dấu đã xem xét\"."
    - statement: "Bản ghi chỉ có ảnh của một đầu (vào hoặc ra, không đủ cặp) hiển thị đúng ô có ảnh và đúng ô báo thiếu — kiểm bằng một bản ghi tạo có chủ đích chỉ có lần vào."
      verification: backstop
  artifacts:
    - path: "src/app/api/attendance-photos/route.ts"
      provides: "Route Handler đọc siêu dữ liệu ảnh của một bản ghi chấm công"
      exports: ["GET", "dynamic"]
    - path: "src/lib/data/mutations/attendance-photos.ts"
      provides: "Server Action đánh dấu ảnh đã xem xét"
      exports: ["markPhotoReviewed"]
    - path: "src/components/attendance/attendance-photo-dialog.tsx"
      provides: "Dialog xem lại đầy đủ: ảnh, toạ độ, khoảng cách, độ chính xác, liên kết bản đồ, hành động xem xét"
      min_lines: 120
  key_links:
    - from: "src/components/attendance/attendance-photo-dialog.tsx"
      to: "src/app/api/attendance-photos/[id]/route.ts"
      via: "thẻ ảnh trỏ vào broker route, siêu dữ liệu lấy từ route danh sách"
      pattern: "/api/attendance-photos/"
    - from: "src/lib/data/mutations/attendance-photos.ts"
      to: "src/lib/data/audit.ts"
      via: "mỗi lần đánh dấu đã xem xét để lại một dòng audit"
      pattern: "logMutation"
  prohibitions:
    - statement: "MUST NOT trình bày ảnh hiện trường như bằng chứng về DANH TÍNH người chấm công. Ảnh là ảnh nhà xưởng, không có mặt người để đối chiếu — nó chứng minh một thiết bị đã ở đúng nơi, không chứng minh đúng người. Một nhãn hay dòng trợ giúp gợi ý ngược lại khiến quản trị dùng nó làm căn cứ kỷ luật một người mà bằng chứng không hề nói tới."
      category: transparency
    - statement: "MUST NOT hiển thị khoảng cách như một con số chắc chắn mà không đặt cạnh độ chính xác GPS. Chính lý do D-20 đổi từ chặn cứng sang ghi nhận là GPS trong nhà xưởng sai 20–50m; một dòng \"cách 620m\" đứng một mình khiến người đọc không phân biệt được đo sai với đứng xa thật, và đó là cách một biện pháp kiểm soát trở thành một cỗ máy buộc tội oan."
      category: fairness
---

<objective>
Mở rộng Dialog tối giản của lát cắt tracer thành màn hình xem lại đầy đủ mà ATT-04 yêu cầu: hai ảnh
của một ca, toạ độ thô, khoảng cách tới mốc, tên mốc, độ chính xác GPS, một lối mở bản đồ ngoài, và
hành động đánh dấu đã xem xét.

Purpose: bằng chứng chỉ có giá trị khi có người nhìn được nó và đọc được nó **cùng với** mức tin cậy
của nó. Một con số khoảng cách đứng một mình là thứ nguy hiểm nhất mà phase này có thể sản xuất ra —
nó trông chính xác trong khi nó là kết quả của một phép đo có sai số 20–50m, và người đọc không có
cách nào biết điều đó nếu giao diện không nói.

Output: `GET /api/attendance-photos`, `markPhotoReviewed()`, và `AttendancePhotoDialog` đầy đủ.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-01-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Route Handler | `GET /api/attendance-photos?attendanceRecordId=…` (siêu dữ liệu, không byte) |
| Server Action | `markPhotoReviewed(photoId, status)` |
| Hàm dữ liệu | `listAttendancePhotos(attendanceRecordId)` |
| Schema Zod | `attendancePhotoListResponseSchema`, `attendancePhotoQuerySchema`, `photoReviewInputSchema` |
| Component | `AttendancePhotoDialog` (bản đầy đủ) |
| Test | `src/lib/data/__tests__/attendance-photos.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Đường đọc siêu dữ liệu ảnh và hành động đánh dấu đã xem xét</name>
  <files>src/lib/validation/api/attendance-photos.ts, src/lib/data/attendance-photos.ts, src/lib/data/mutations/attendance-photos.ts, src/app/api/attendance-photos/route.ts, src/lib/data/__tests__/attendance-photos.test.ts</files>
  <read_first>
    - `src/app/api/attendance-photos/[id]/route.ts` (do 03-01 tạo) — broker trả **byte**. Route mới
      nằm cạnh nó trả **siêu dữ liệu**; hai đường phải rõ ràng là hai thứ khác nhau và route mới
      tuyệt đối không trả URL nào.
    - `src/app/api/attendance/route.ts` — khuôn Route Handler có tham số truy vấn và có tầng quyền
      theo vai trò (nhân viên thường hỏi dữ liệu người khác thì 403).
    - `src/lib/validation/api/attendance-photos.ts` (do 03-01 tạo) — `attendancePhotoRowSchema` và
      `attendancePhotoSchema` đã có; task này thêm schema danh sách, schema tham số truy vấn, và
      schema đầu vào cho hành động xem xét.
    - `src/lib/data/mutations/shifts.ts` — khuôn Server Action ghi kèm `logMutation`.
    - `src/lib/auth/session-context.ts` — `requireRole`, và `userId` để điền `reviewed_by`.
    - `supabase/migrations/0005_v2_tables.sql` dòng 38-55 — cột `review_status`, `reviewed_by`
      (tham chiếu `auth.users`), `reviewed_at`; enum `photo_review_status` đã đủ dùng nguyên trạng.
    - `supabase/migrations/0011_attendance_evidence.sql` (do 03-01 tạo) — ba cột mới.
    - `src/lib/data/__tests__/attendance.test.ts` — khuôn test.
  </read_first>
  <behavior>
    - `GET /api/attendance-photos` không kèm tham số bản ghi trả 400 với thông điệp tiếng Việt.
    - `GET /api/attendance-photos` cho một bản ghi của doanh nghiệp khác trả mảng rỗng, không trả 403 (không tiết lộ id đó có tồn tại hay không).
    - `GET /api/attendance-photos` với vai trò không phải `owner`/`admin` trả 403.
    - Phản hồi trả tối đa hai phần tử, sắp xếp `check_in` trước `check_out`.
    - Mỗi phần tử mang `id`, `kind`, `capturedAt`, `latitude`, `longitude`, `accuracyMeters`, `workSiteId`, `workSiteName`, `distanceMeters`, `reviewStatus`; **không** mang trường nào chứa URL.
    - Bản ghi không có ảnh nào trả mảng rỗng với mã 200, không phải 404.
    - `markPhotoReviewed` với vai trò không phải `owner`/`admin` ném `ForbiddenError` trước khi chạm database.
    - `markPhotoReviewed` đặt `review_status`, `reviewed_by` bằng `userId` của phiên, và `reviewed_at` bằng `tf_server_now()` — không nhận thời điểm từ nơi gọi.
    - `markPhotoReviewed` cho một ảnh của doanh nghiệp khác ném lỗi và không đổi dữ liệu.
    - Mỗi lần đánh dấu để lại đúng một dòng `audit_log` với `entity_table = 'attendance_photos'`.
  </behavior>
  <action>
    **(a) `src/lib/validation/api/attendance-photos.ts`** — thêm `attendancePhotoQuerySchema` (đúng
    một trường: định danh bản ghi chấm công; **không** khai định danh doanh nghiệp — D-12b),
    `attendancePhotoListResponseSchema`, và `photoReviewInputSchema` (chỉ nhận trạng thái xem xét
    trong enum, không nhận thời điểm và không nhận định danh người xem xét — cả hai do server cấp).
    Mở rộng `attendancePhotoRowSchema` để mang thêm tên điểm làm việc lấy từ phép nối bảng.

    **(b) `src/app/api/attendance-photos/route.ts`** — theo khuôn `attendance/route.ts`, chỉ xuất
    `dynamic` và `GET`. `requireRole(role, ["owner","admin"])`. Đọc `attendance_photos` kèm
    `.eq("company_id", companyId)` và phép nối lấy `work_sites.name`. Comment đầu file phân biệt rõ
    hai route cùng thư mục: route này trả siêu dữ liệu dạng JSON, route có tham số động trả byte ảnh;
    **không** route nào trả một liên kết tới miền lưu trữ.

    **(c) `src/lib/data/attendance-photos.ts`** — `listAttendancePhotos(attendanceRecordId)` qua
    `fetchJson`; re-export `markPhotoReviewed`.

    **(d) `src/lib/data/mutations/attendance-photos.ts`** — `"use server"`, `markPhotoReviewed` theo
    khuôn `mutations/shifts.ts`: `getSessionContext()` → `requireRole` → đọc nguyên dòng trước → cập
    nhật với `.eq("company_id", companyId)` → đọc nguyên dòng sau → `logMutation`. `reviewed_at` lấy
    từ RPC `tf_server_now()`, không từ `new Date()` ở tầng ứng dụng.

    **(e) `src/lib/data/__tests__/attendance-photos.test.ts`** — khẳng định đủ mười hành vi ở
    `<behavior>`; các nhánh chạm database dùng khuôn test tích hợp mà plan 03-04 đã dựng.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/data/__tests__/attendance-photos.test.ts` thoát 0 và báo ít nhất 10 test.
    - `grep -rEc "export (async )?function (POST|PUT|PATCH|DELETE|HEAD|OPTIONS)" src/app/api/attendance-photos/` trả về `0`.
    - `npx vitest run src/__tests__/route-handlers-get-only.test.ts` thoát 0.
    - `grep -icE "signedUrl|publicUrl|\burl\b" src/lib/validation/api/attendance-photos.ts` trả về `0` — không schema nào khai một trường URL.
    - `grep -rn "createSignedUrl\|getPublicUrl" src/ | grep -v __tests__` không trả về dòng nào.
    - `grep -v '^ *[/*]' src/lib/data/mutations/attendance-photos.ts | grep -cE "new Date\(|Date\.now\("` trả về `0`.
    - `grep -c "logMutation" src/lib/data/mutations/attendance-photos.ts` trả về `1`.
    - Với cookie owner Ngọc Phát: `curl -b <cookie> -s "http://localhost:3000/api/attendance-photos?attendanceRecordId=<id cty-01>"` trả 200 với ít nhất 1 phần tử; với cookie Bình Minh cho **cùng** id trả `[]`.
    - Với cookie một nhân viên thường: cùng URL trả `403`.
    - Sau một lần đánh dấu đã xem xét: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from attendance_photos where review_status<>'pending' and reviewed_by is not null and reviewed_at is not null"` tăng đúng `1`, và số dòng `audit_log` với `entity_table='attendance_photos'` tăng đúng `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/attendance-photos.test.ts src/__tests__/route-handlers-get-only.test.ts</automated>
  </verify>
  <done>Siêu dữ liệu ảnh đọc được bởi đúng vai trò và đúng doanh nghiệp, không kèm một liên kết nào; trạng thái xem xét ghi được kèm người và thời điểm do server cấp, có vết audit.</done>
</task>

<task type="auto">
  <name>Task 2: Dialog xem lại đầy đủ — ảnh, vị trí, khoảng cách và mức tin cậy của nó</name>
  <files>src/components/attendance/attendance-photo-dialog.tsx, src/app/admin/employees/[id]/employee-detail-view.tsx</files>
  <read_first>
    - `src/components/attendance/attendance-photo-dialog.tsx` (bản tối giản do 03-01 tạo) — ô ảnh,
      `Skeleton`, và trạng thái không có ảnh đã có sẵn; task này cắm phần còn lại vào **cùng** hình
      dạng component, không dựng lại.
    - `src/app/admin/employees/[id]/employee-detail-view.tsx` dòng 420-460 — bảng lịch sử chấm công và
      cột hành động do 03-01 thêm.
    - `src/components/ui/dialog.tsx`, `src/components/ui/skeleton.tsx`, `src/components/ui/badge.tsx`.
    - `src/components/common/status-badge.tsx` — quy ước biểu tượng cộng nhãn chữ; thêm ánh xạ cho
      trạng thái xem xét nếu component hỗ trợ `kind` mở rộng, nếu không thì dùng biến thể `custom`.
    - `src/lib/constants.ts` khối `PHOTO_REVIEW_STATUS_LABEL` (do 03-01 thêm).
    - `src/lib/format.ts` — hàm định dạng số và ngày giờ đã có; số đo dùng lớp `.num`.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md` §"UI Considerations" các hàng
      "Screen — attendance record review", "Empty — record has no photo", "Loading — signed photo URL"
      (đã đối chiếu lại: kiến trúc là broker route nên chữ báo lỗi **không** nói tới liên kết hết hạn,
      mà nói "Không tải được ảnh."), và §"Color" (đúng một nút màu nhấn: "Đánh dấu đã xem xét").
    - `.planning/PROJECT.md` §Key Decisions — dòng "ảnh là ảnh hiện trường, không phải selfie" và hệ
      quả "chứng minh đúng nơi, không chứng minh đúng người".
  </read_first>
  <behavior>
    - Dialog cho một bản ghi có đủ hai ảnh hiển thị hai ô ảnh có nhãn phân biệt lần vào và lần ra.
    - Mỗi ô ảnh dựng `src` từ `/api/attendance-photos/{id}`; không thuộc tính `src` nào trong file trỏ tới một miền ngoài.
    - Trong lúc ảnh đang tải, ô ảnh giữ tỉ lệ khung và hiện `Skeleton`; bố cục không nhảy khi ảnh về.
    - Ảnh tải hỏng hiện chữ lỗi và nút tải lại **bên trong** ô ảnh đó; các ô còn lại và phần siêu dữ liệu vẫn hiển thị bình thường.
    - Bản ghi không có ảnh nào hiện đúng một dòng "Bản ghi này không có ảnh đính kèm.".
    - Bản ghi chỉ có ảnh một đầu hiện một ô có ảnh và một ô báo thiếu.
    - Khoảng cách hiển thị bằng lớp chữ số đều, và **luôn** đi kèm độ chính xác GPS cùng một câu diễn giải rằng độ chính xác là bán kính tin cậy do thiết bị khai.
    - Toạ độ thô hiển thị bằng lớp chữ số đều, kèm một liên kết mở bản đồ ngoài dựng từ chính hai con số đó, mở ở tab mới.
    - Dialog hiển thị tên điểm làm việc dùng làm mốc; không có điểm nào thì hiện một dòng nói rõ doanh nghiệp chưa khai điểm làm việc nên không có mốc để đo.
    - Dialog có đúng một nút màu nhấn.
    - Không dòng chữ nào trong Dialog nói hoặc gợi ý rằng ảnh xác nhận danh tính người chấm công.
  </behavior>
  <action>
    **(a) `attendance-photo-dialog.tsx`** — mở rộng component đã có:
    - Nạp siêu dữ liệu bằng `listAttendancePhotos(recordId)` qua `useDataQuery`; ô ảnh vẫn trỏ thẳng
      vào broker route như bản tối giản.
    - Bố cục: cột trái là (các) ô ảnh, cột phải là khối siêu dữ liệu. Trên màn hình hẹp thì xếp dọc.
    - Khối siêu dữ liệu cho mỗi lần chấm: thời điểm chụp, tên điểm làm việc, khoảng cách, độ chính
      xác GPS, toạ độ thô, trạng thái xem xét bằng `StatusBadge` (biểu tượng cộng chữ).
    - **Khoảng cách và độ chính xác luôn đứng cạnh nhau**, kèm một câu ngắn giải thích rằng độ chính
      xác là bán kính vòng tròn tin cậy do chính thiết bị khai — một giá trị 50 nghĩa là vị trí thật
      có thể lệch tới 50m theo bất kỳ hướng nào. Ghi comment tại chỗ nối điều này với D-20: đây chính
      là lý do "trong bán kính" đã thôi là điều kiện bắt buộc, và người đọc màn hình này cần thấy cả
      hai con số để phân biệt "GPS đo sai" với "thực sự đứng xa".
    - Liên kết bản đồ ngoài dựng từ hai con số toạ độ, mở ở tab mới với `rel` an toàn. Không nhúng
      khung bản đồ và không gọi một dịch vụ bản đồ nào từ phía server — làm vậy sẽ gửi toạ độ của
      nhân viên sang một bên thứ ba mà không ai quyết định điều đó.
    - Nút "Đánh dấu đã xem xét" gọi `markPhotoReviewed`, là **nút màu nhấn duy nhất** trong Dialog.
      Sau khi ghi: `invalidate()` rồi `toast`.
    - Ô ảnh lỗi: chữ "Không tải được ảnh." cộng nút tải lại **chỉ trong ô đó**. UI-SPEC viết chữ này
      dưới giả định kiến trúc liên kết ký; kiến trúc đã chốt là broker route nên không có liên kết
      nào để hết hạn — dùng chữ trung tính và ghi lý do đối chiếu lại thành comment.
    - Một dòng ghi chú ngắn dưới ảnh nói đúng phạm vi bằng chứng: ảnh là ảnh hiện trường tại thời
      điểm chấm, không dùng để đối chiếu khuôn mặt. Đây không phải chữ trang trí — nó ngăn người đọc
      dùng ảnh làm căn cứ cho một kết luận mà ảnh không hề nói tới.

    **(b) `employee-detail-view.tsx`** — cột hành động đã có từ 03-01 nay mở Dialog bản đầy đủ. Trong
    bảng lịch sử, thêm một dấu hiệu nhỏ cho biết bản ghi có ảnh hay không, để quản trị không phải mở
    từng dòng mới biết. Không đổi gì khác trong bố cục bảng.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0.
    - Đếm số thuộc tính `src` KHÔNG trỏ vào broker route phải bằng `0`: `grep -oE 'src=[^ >]+' src/components/attendance/attendance-photo-dialog.tsx | grep -vc '/api/attendance-photos/'`.
    - `grep -icE "https?://" src/components/attendance/attendance-photo-dialog.tsx` — mọi kết quả phải là liên kết bản đồ ngoài do người dùng chủ động bấm, không có kết quả nào là nguồn ảnh; kiểm bằng mắt và ghi kết luận vào SUMMARY.
    - `grep -icE "accuracyMeters|accuracy" src/components/attendance/attendance-photo-dialog.tsx` trả về ít nhất `1`.
    - `grep -c "distanceMeters" src/components/attendance/attendance-photo-dialog.tsx` trả về ít nhất `1`.
    - `grep -c "Skeleton" src/components/attendance/attendance-photo-dialog.tsx` trả về ít nhất `1`.
    - `grep -icE "khuôn mặt|nhận diện|danh tính|xác thực người" src/components/attendance/attendance-photo-dialog.tsx` — mọi kết quả phải nằm trong câu **phủ định** phạm vi bằng chứng; kiểm bằng mắt và ghi kết luận vào SUMMARY.
    - `grep -icE "liên kết đã hết hạn|hết hạn" src/components/attendance/attendance-photo-dialog.tsx` trả về `0` — không còn giả định kiến trúc liên kết ký.
    - `grep -c "PHOTO_REVIEW_STATUS_LABEL" src/components/attendance/attendance-photo-dialog.tsx` trả về ít nhất `1`.
    - `grep -c 'target="_blank"' src/components/attendance/attendance-photo-dialog.tsx` trả về ít nhất `1`, và mỗi lần đều kèm `rel` chứa `noopener`.
    - Với phiên owner: mở một bản ghi có đủ hai ảnh — cả hai ảnh hiện, khoảng cách và độ chính xác đứng cạnh nhau, liên kết bản đồ mở đúng toạ độ. Kết quả quan sát ghi vào SUMMARY.
    - Với một bản ghi seed không có ảnh: Dialog hiện đúng dòng "Bản ghi này không có ảnh đính kèm." và không có ảnh vỡ. Kết quả quan sát ghi vào SUMMARY.
    - Với một bản ghi chỉ có ảnh lần vào: một ô hiện ảnh, một ô báo thiếu. Kết quả quan sát ghi vào SUMMARY.
    - Tạm chặn đường mạng tới broker route trong DevTools: ô ảnh hiện chữ lỗi kèm nút tải lại, phần siêu dữ liệu vẫn hiển thị bình thường. Kết quả quan sát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Quản trị mở một bản ghi bất kỳ và thấy đủ bằng chứng của cả hai đầu ca cùng mức tin cậy của phép đo vị trí; đánh dấu đã xem xét được và thao tác đó có vết.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt quản trị → `GET /api/attendance-photos` | Siêu dữ liệu ảnh gồm toạ độ GPS của nhân viên — dữ liệu cá nhân theo NĐ 13/2023 |
| Dialog quản trị → dịch vụ bản đồ bên ngoài | Một khung bản đồ nhúng gửi toạ độ nhân viên sang bên thứ ba mà không ai quyết định |
| Con số khoảng cách → kết luận của người đọc | Một phép đo có sai số 20–50m trình bày như một sự thật chắc chắn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | Route siêu dữ liệu lộ toạ độ nhân viên của doanh nghiệp khác | high | mitigate | `requireRole` cộng `.eq("company_id", companyId)` từ phiên; acceptance criteria gọi cùng id bằng hai cookie và đòi mảng rỗng cho bên kia |
| T-03-05-02 | Elevation of Privilege | Nhân viên thường đọc được siêu dữ liệu vị trí của đồng nghiệp | high | mitigate | `requireRole(role, ["owner","admin"])`; acceptance criteria đòi mã 403 bằng cookie nhân viên thật |
| T-03-05-03 | Information Disclosure | Toạ độ nhân viên bị gửi sang bên thứ ba qua khung bản đồ nhúng | medium | mitigate | Chỉ một liên kết mở tab mới do người dùng chủ động bấm; không nhúng khung, không gọi dịch vụ bản đồ từ server. Acceptance criteria kiểm thuộc tính `rel` an toàn |
| T-03-05-04 | Information Disclosure | Một trường URL lọt vào phản hồi JSON và trở thành liên kết sống độc lập với phiên | critical | mitigate | Không schema nào khai trường URL; acceptance criteria có hai lệnh grep chặn (schema và toàn `src/`) |
| T-03-05-05 | Repudiation | Trạng thái xem xét đổi mà không biết ai đổi và lúc nào | medium | mitigate | `reviewed_by` từ phiên, `reviewed_at` từ `tf_server_now()`, cộng một dòng audit; acceptance criteria đếm cả ba |
| T-03-05-06 | Tampering | Thời điểm xem xét do client khai | medium | mitigate | Schema đầu vào không khai trường thời điểm; acceptance criteria có grep chặn đồng hồ client trong module ghi |
| T-03-05-07 | Information Disclosure | Ảnh bị đọc lẫn qua cache trình duyệt giữa hai phiên trên cùng máy | medium | mitigate | Broker route (03-01) đã đặt `cache-control` chứa `private` và `no-store`; Dialog không tự thêm lớp cache nào |
| T-03-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Không trường URL nào tồn tại trong schema ảnh và không lời gọi ký liên kết nào trong `src/`
- Hai doanh nghiệp không đọc được siêu dữ liệu ảnh của nhau; nhân viên thường nhận 403
- Khoảng cách không bao giờ hiển thị mà thiếu độ chính xác đi kèm
- Bản ghi không có ảnh và bản ghi thiếu một đầu đều hiện đúng trạng thái
- Trạng thái xem xét ghi được kèm người, thời điểm server và một dòng audit

</verification>

<success_criteria>
Quản trị nhìn được toàn bộ bằng chứng của một bản ghi, và nhìn được cùng lúc mức tin cậy của bằng
chứng đó — nên một con số khoảng cách lớn dẫn tới một câu hỏi, không dẫn thẳng tới một kết luận.
</success_criteria>

<output>
Create `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-05-SUMMARY.md` when done
</output>
