---
phase: 03-ch-m-c-ng-c-b-ng-ch-ng
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - src/lib/attendance/rejection.ts
  - src/lib/data/mutations/attendance.ts
  - src/lib/validation/api/attendance.ts
  - src/lib/data/attendance.ts
  - src/components/employee-app/camera-sheet.tsx
  - src/components/employee-app/attendance-status-card.tsx
  - src/app/employee/employee-home-view.tsx
  - src/lib/data/__tests__/attendance-evidence.test.ts
autonomous: true
requirements: [ATT-01, ATT-02, ATT-06, ATT-08]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tan ca cũng bắt buộc kèm ảnh chụp trực tiếp và toạ độ, đi qua đúng Camera Sheet mà vào ca đang dùng — ATT-01 nói \"vào/ra\", không chỉ vào."
    - "`checkOut` ghi một dòng `attendance_photos` thứ hai với `kind = 'check_out'`, có `distance_meters`, `accuracy_meters` và `work_site_id` riêng của lần ra — ràng buộc `unique (attendance_record_id, kind)` cho đúng hai dòng mỗi bản ghi."
    - "`checkIn` và `checkOut` không còn nhận tham số ngày hay giờ nào từ nơi gọi; mọi dấu thời gian đến từ `tf_server_now()` (ATT-06). Đổi đồng hồ thiết bị không đổi được một giá trị nào trên bản ghi."
    - "Chấm công ngoài khung giờ ca được phân bị server **từ chối** với phân loại `outside_shift`, và lý do đó tới được màn hình nhân viên nguyên vẹn thay vì thành một lỗi chung (ATT-08, D-20b)."
    - "Chấm công thiếu ảnh bị server từ chối với phân loại `missing_photo` trước khi chạm Storage (D-20b)."
    - "Không có phân loại từ chối thứ tư nào được tạo ra ở tầng server. Ngoài bán kính vẫn không nằm trong danh sách lý do từ chối (D-20, D-20b)."
    - "Mỗi lỗi ném ra từ đường ghi chấm công mang một phân loại máy đọc được, không chỉ một chuỗi tiếng Việt — client phân nhánh theo phân loại, không so khớp chuỗi."
    - "Một lần tan ca để lại đúng hai dòng `audit_log`: một cho `attendance_records`, một cho `attendance_photos`."
  artifacts:
    - path: "src/lib/attendance/rejection.ts"
      provides: "Lớp lỗi mang phân loại từ chối, dùng chung giữa Server Action và giao diện"
      exports: ["AttendanceRejectedError", "isAttendanceRejection"]
    - path: "src/lib/data/__tests__/attendance-evidence.test.ts"
      provides: "Bài kiểm tích hợp cho hai lý do từ chối do server quyết và cho bằng chứng của lần ra"
      min_lines: 70
  key_links:
    - from: "src/components/employee-app/camera-sheet.tsx"
      to: "src/lib/attendance/rejection.ts"
      via: "giao diện phân nhánh theo phân loại lỗi, không so khớp chuỗi tiếng Việt"
      pattern: "isAttendanceRejection"
    - from: "src/lib/data/mutations/attendance.ts"
      to: "public.tf_server_now"
      via: "mọi dấu thời gian của cả lần vào lẫn lần ra đến từ đồng hồ database"
      pattern: "tf_server_now"
  prohibitions:
    - statement: "MUST NOT thêm một lý do từ chối thứ tư ở tầng server. D-20b cho đúng ba lý do và cố tình không cho phép mở rộng: mỗi lý do mới là một cách nữa để một nhân viên đứng đúng chỗ vào đúng giờ vẫn không chấm công được, và người viết mã không phải người chịu hậu quả của việc đó."
      category: values
    - statement: "MUST NOT để giao diện phân nhánh lý do từ chối bằng cách so khớp chuỗi thông báo tiếng Việt. Một lần sửa chính tả sẽ lặng lẽ biến ba khối giải thích riêng thành một khối \"có lỗi xảy ra\", và không có bài kiểm nào đỏ lên."
      category: transparency
    - statement: "MUST NOT nhận bất kỳ giá trị thời gian nào từ nơi gọi trong đường ghi chấm công, kể cả để \"đối chiếu\". Sau plan này chữ ký không còn tham số giờ hay ngày; một tham số đối chiếu còn sót lại là một chỗ mà lần sửa sau có thể lặng lẽ đọc vào."
      category: safety
---

<objective>
Đưa lần **tan ca** lên cùng mức bằng chứng với lần vào ca, bỏ hẳn hai tham số thời gian còn sót
trong chữ ký chấm công (ATT-06), và hiện thực hai lý do từ chối mà server thật sự quyết được:
thiếu ảnh và ngoài giờ ca.

Purpose: lát cắt tracer chứng minh kiến trúc trên lần vào ca. ATT-01 viết "vào/ra", nên một bản ghi
chỉ có ảnh ở đầu vào là một bản ghi mới có nửa bằng chứng. Và hai tham số `date`/`time` còn sót lại
trong chữ ký từ Phase 2 là một mặt phẳng để một lần sửa sau lặng lẽ đọc giờ từ thiết bị trở lại —
comment trong `mutations/attendance.ts` đã ghi rõ ATT-06 ở phase này phải gỡ chúng.

Output: `checkOut` mang bằng chứng, chữ ký sạch thời gian, `AttendanceRejectedError` có phân loại,
và bộ test tích hợp trên Postgres thật.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-01-SUMMARY.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-03-SUMMARY.md
@.planning/phases/02-phi-n-th-t-v-c-t-t-ng-d-li-u-gi/02-08-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Lớp lỗi | `AttendanceRejectedError` (mang `reason: AttendanceRejectionReason`), vệ sĩ kiểu `isAttendanceRejection()` |
| Server Action | `checkIn(employeeId, evidence)`, `checkOut(recordId, evidence)` — chữ ký cuối cùng |
| Test | `src/lib/data/__tests__/attendance-evidence.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lỗi từ chối mang phân loại, và hai lý do mà server thật sự quyết được</name>
  <files>src/lib/attendance/rejection.ts, src/lib/data/mutations/attendance.ts, src/lib/validation/api/attendance.ts</files>
  <read_first>
    - `src/lib/data/mutations/attendance.ts` (toàn bộ, sau khi plan 03-01 mở rộng) — hai hàm và mọi
      chỗ đang ném `new Error(...)` với chuỗi tiếng Việt trần.
    - `src/lib/auth/session-context.ts` dòng 16-48 — khuôn lớp lỗi của dự án: kế thừa `Error`, đặt
      `this.name`, thông điệp tiếng Việt trong hàm dựng. `AttendanceRejectedError` theo đúng khuôn
      này, cộng thêm một trường phân loại.
    - `src/lib/types/domain.ts` — `AttendanceRejectionReason` (do 03-01 thêm): ba giá trị tiếng Anh.
    - `src/lib/data/fetch-json.ts` — cách lỗi được chuyển thành `Error` mang thông điệp tiếng Việt ở
      biên đọc; đường ghi qua Server Action giữ được kiểu nên không cần bước chuyển đó.
    - `supabase/migrations/0003_enums_time.sql` — `tf_shift_minutes()`, `tf_local_instant()`,
      `tf_overnight()`; phép so "đang trong khung giờ ca hay không" phải dựng trên các hàm này, không
      viết lại một quy ước múi giờ thứ hai (bài học D-19 của 02-08).
    - `src/lib/data/__tests__/attendance.test.ts` — khuôn test của dự án.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md` D-20b.
  </read_first>
  <behavior>
    - `AttendanceRejectedError` mang một trường phân loại là một trong ba giá trị của `AttendanceRejectionReason`, và một thông điệp tiếng Việt lấy từ khối hằng số nhãn.
    - `isAttendanceRejection(cause)` trả `true` cho một `AttendanceRejectedError` và `false` cho một `Error` thường — vệ sĩ kiểu này hoạt động qua ranh giới Server Action.
    - `checkIn` và `checkOut` gọi không kèm bằng chứng ném `AttendanceRejectedError` với phân loại `missing_photo`, và ném **trước** khi chạm Storage hoặc ghi bản ghi.
    - `checkIn` gọi ở thời điểm nằm ngoài khung giờ ca được phân của nhân viên (cộng biên độ ân hạn hai đầu) ném `AttendanceRejectedError` với phân loại `outside_shift`, và không ghi dòng nào.
    - `checkOut` gọi cho một bản ghi chưa có giờ vào ném `AttendanceRejectedError` với phân loại `outside_shift`.
    - Với một ca qua đêm, phép so khung giờ dùng đúng quy ước D-08 (ca tính trọn vào ngày bắt đầu) qua các hàm thời gian đã có, không tự tính lại phép trừ giờ.
    - Không hàm nào trong module ném `AttendanceRejectedError` với một phân loại nằm ngoài ba giá trị đã khai.
    - Khoảng cách vượt bán kính vẫn không sinh ra lỗi nào ở bất kỳ nhánh nào.
  </behavior>
  <action>
    **(a) `src/lib/attendance/rejection.ts`** — `AttendanceRejectedError` kế thừa `Error` theo khuôn
    của `session-context.ts`, mang thêm trường `reason` kiểu `AttendanceRejectionReason` chỉ đọc.
    Thông điệp tiếng Việt lấy từ `ATTENDANCE_REJECTION_LABEL`, không viết lại chuỗi. Kèm
    `isAttendanceRejection(cause: unknown)` — vệ sĩ kiểu kiểm cả `name` lẫn sự có mặt của trường
    `reason` với một giá trị hợp lệ, vì một lớp lỗi đi qua ranh giới Server Action có thể mất chuỗi
    nguyên mẫu; comment tại chỗ ghi rõ lý do kiểm theo hình dạng chứ không dùng `instanceof`.

    **(b) `src/lib/data/mutations/attendance.ts`** — hai chỗ ném lỗi được nâng cấp:
    - Thiếu bằng chứng (hoặc `punchEvidenceSchema` không parse được) → `AttendanceRejectedError` với
      phân loại `missing_photo`, ném ở đầu hàm.
    - Ngoài khung giờ ca → `AttendanceRejectedError` với phân loại `outside_shift`. Cách xác định:
      sau khi có `nowIso` từ `tf_server_now()` và ca của nhân viên, dựng thời điểm bắt đầu và kết
      thúc ca theo kế hoạch bằng `tf_local_instant()` cộng `tf_shift_minutes()` (đúng khuôn `checkOut`
      hiện có đang dùng để tính về sớm), nới hai đầu bằng một biên độ có tên
      (`SHIFT_WINDOW_GRACE_MINUTES`, đặt cùng chỗ với các hằng số khác của module). Nằm ngoài khoảng
      đó thì từ chối. Comment tại chỗ: biên độ nới rộng có chủ đích, vì một hệ thống từ chối người
      đến sớm mười phút là một hệ thống người ta sẽ tìm cách đi vòng.
    - Mọi lỗi hạ tầng còn lại (không đọc được đồng hồ server, không tải được ảnh lên, không ghi được
      audit) vẫn là `Error` thường — chúng **không** phải lý do từ chối theo nghĩa của D-20b và không
      được gán một phân loại nào.
    - Comment ở đầu file được cập nhật: ba lý do của D-20b là `missing_photo` và `outside_shift` do
      server quyết, cộng `network_error` do client phân loại khi lời gọi không tới nơi. Không có lý do
      thứ tư, và khoảng cách không bao giờ là một lý do.

    **(c) `src/lib/validation/api/attendance.ts`** — nếu hình dạng phản hồi của đường ghi cần mang
    thêm kết quả đo (tên điểm làm việc và khoảng cách, để giao diện dựng banner ngoài bán kính), thêm
    một schema kết quả ở đây theo khuôn hai đầu D-12d. **Không** thêm trường thời gian nào vào bất kỳ
    schema đầu vào nào.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `grep -oE "\"(missing_photo|outside_shift|network_error)\"" src/lib/data/mutations/attendance.ts src/lib/attendance/rejection.ts src/lib/types/domain.ts | sort -u | wc -l` trả về `3` — đúng ba phân loại, không hơn.
    - `grep -v '^ *[/*]' src/lib/data/mutations/attendance.ts | grep -cE "AttendanceRejectedError"` trả về ít nhất `3`.
    - `grep -v '^ *[/*]' src/lib/data/mutations/attendance.ts | grep -cE "distance_?[Mm]eters *[<>]"` trả về `0` — không nhánh nào chặn theo khoảng cách.
    - `grep -c "SHIFT_WINDOW_GRACE_MINUTES" src/lib/data/mutations/attendance.ts` trả về ít nhất `2` (khai một lần, dùng ít nhất một lần).
    - `grep -v '^ *[/*]' src/lib/data/mutations/attendance.ts | grep -cE "new Date\(|Date\.now\("` trả về nhiều nhất `1` (chỉ phép cộng epoch cho thời điểm kết thúc ca theo kế hoạch đã có từ 02-08); giá trị đo được ghi vào SUMMARY.
    - `npx vitest run src/lib/data/__tests__` thoát 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__</automated>
  </verify>
  <done>Hai lý do từ chối mà server thật sự quyết được đã tồn tại, mang phân loại máy đọc được, và khoảng cách vẫn không phải một trong số đó.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tan ca mang bằng chứng, và chữ ký chấm công sạch mọi tham số thời gian</name>
  <files>src/lib/data/mutations/attendance.ts, src/lib/data/attendance.ts, src/lib/data/__tests__/attendance-evidence.test.ts</files>
  <read_first>
    - `src/lib/data/mutations/attendance.ts` — `checkOut` hiện tại (dòng 222-349 của bản Phase 2, đã
      được 03-01 mở rộng phần `checkIn`): thứ tự đọc-trước, kiểm quyền suy từ chính bản ghi, và ba lời
      gọi RPC tính phút. Phần thêm ảnh/toạ độ phải **lặp lại đúng khuôn** đã dựng ở `checkIn`, không
      dựng khuôn thứ hai.
    - `src/lib/storage/attendance-photos.ts` (03-01) — `buildAttendancePhotoPath()` nhận `kind`.
    - `supabase/migrations/0005_v2_tables.sql` dòng 38-55 — ràng buộc `unique (attendance_record_id,
      kind)`: mỗi bản ghi nhiều nhất một ảnh vào và một ảnh ra.
    - `src/lib/data/attendance.ts` — re-export và chữ ký phía client.
    - `src/app/employee/employee-home-view.tsx` — hai nơi gọi `checkInService`/`checkOutService`; chữ
      ký đổi thì đây là call site duy nhất phải sửa.
    - `src/lib/data/__tests__/attendance.test.ts` — khuôn test.
    - `supabase/seed.sql` dòng 520-545 — hai bản ghi chấm công "hôm nay" dùng làm dữ liệu đối chiếu.
  </read_first>
  <behavior>
    - `checkIn` có chữ ký `checkIn(employeeId: string, evidence: PunchEvidence)` — không còn tham số doanh nghiệp, ngày, hay giờ.
    - `checkOut` có chữ ký `checkOut(recordId: string, evidence: PunchEvidence)` — không còn tham số giờ.
    - `checkOut` với bằng chứng hợp lệ ghi một dòng `attendance_photos` mang `kind = 'check_out'`, `captured_at` bằng `tf_server_now()` của chính lần gọi.
    - Sau một lần vào rồi ra đầy đủ, bản ghi đó có đúng hai dòng `attendance_photos`, một mỗi loại.
    - Tan ca lần thứ hai trên cùng bản ghi cập nhật dòng ảnh loại `check_out` đang có thay vì tạo dòng thứ ba — ràng buộc `unique` của database là lớp hai.
    - `checkOut` đo khoảng cách của lần **ra** một cách độc lập; giá trị của lần vào không được chép sang.
    - `checkOut` cho một bản ghi thuộc doanh nghiệp khác vẫn ném lỗi và không đổi dữ liệu, không đổi Storage.
    - Một lần tan ca để lại đúng hai dòng `audit_log`, một cho mỗi bảng.
    - Vai trò `employee`/`manager` chỉ tan ca được cho chính mình; kiểm tra này chạy trước mọi thao tác Storage.
  </behavior>
  <action>
    **(a) `checkOut` trong `src/lib/data/mutations/attendance.ts`** — thêm tham số `evidence` và lặp
    lại đúng chuỗi bước mà `checkIn` đã dựng ở plan 03-01: kiểm bằng chứng → kiểm quyền → đọc điểm
    làm việc đang dùng → gọi `tf_distance_meters` cho từng điểm rồi giữ giá trị nhỏ nhất → sinh
    `photoId` → tải ảnh lên với `kind` là loại ra → ghi hoặc cập nhật dòng `attendance_photos` →
    `logMutation` riêng cho bảng ảnh. **Không** viết một hàm phụ trợ riêng nếu nó chỉ được gọi hai
    lần với hai giá trị `kind` khác nhau là hợp lý hơn — tách phần dùng chung thành một hàm nội bộ
    trong cùng file, đặt tên nói rõ nó ghi bằng chứng cho một lần chấm, và cho cả `checkIn` lẫn
    `checkOut` gọi nó.
    - Kiểm quyền chạy **trước** mọi thao tác Storage: một lần tải ảnh lên rồi mới phát hiện không có
      quyền là một tệp rác nằm lại trong bucket vĩnh viễn (D-22 không có job dọn).

    **(b) Gỡ tham số thời gian.** `checkIn(employeeId, evidence)` và `checkOut(recordId, evidence)`.
    Xoá cả hai tham số `date`/`time` cùng mọi dòng `void` giữ chúng, và xoá đoạn đối chiếu ngày —
    ngày công suy hoàn toàn từ `tf_work_date(tf_server_now())`. Cập nhật khối comment đầu file: ATT-06
    đã hoàn tất, không còn đường nào cho một giá trị thời gian của client chạm vào bản ghi.

    **(c) `src/lib/data/attendance.ts`** — cập nhật re-export và mọi khai báo kiểu theo chữ ký mới.

    **(d) `src/lib/data/__tests__/attendance-evidence.test.ts`** — test tích hợp gọi thẳng hai module
    Server Action (import module, không qua HTTP) trên Postgres dev thật đã seed, theo hướng dẫn ở
    RESEARCH §"Testing". Khẳng định đủ chín hành vi ở `<behavior>` của task này cộng bốn hành vi từ
    chối ở Task 1 (thiếu ảnh trên cả hai hàm; ngoài giờ ca; tan ca khi chưa có giờ vào). Mỗi bài kiểm
    dọn dẹp dòng và tệp nó tạo ra, hoặc chạy trong một giao dịch được cuộn ngược — ghi rõ cách chọn
    vào comment đầu file.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/data/__tests__/attendance-evidence.test.ts` thoát 0 và báo ít nhất 13 test.
    - `grep -nE "export async function (checkIn|checkOut)" -A 5 src/lib/data/mutations/attendance.ts | grep -icE "\bdate\b|\btime\b"` trả về `0` — chữ ký không còn tham số thời gian.
    - `grep -c "void time\|void companyId\|void date" src/lib/data/mutations/attendance.ts` trả về `0`.
    - Sau một lần vào rồi ra đầy đủ trên giao diện: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from attendance_photos where attendance_record_id='<id>'"` trả về `2`, và `select count(distinct kind) ...` trả về `2`.
    - Sau lần đó: `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from audit_log where entity_id='<id>' or entity_table='attendance_photos'"` cho thấy đúng hai dòng mới; con số ghi vào SUMMARY.
    - `psql "$POSTGRES_URL_NON_POOLING" -tAc "select count(*) from attendance_photos p1 join attendance_photos p2 on p1.attendance_record_id=p2.attendance_record_id and p1.kind<>p2.kind where p1.distance_meters is not null and p1.distance_meters=p2.distance_meters and p1.latitude=p2.latitude"` — nếu lớn hơn 0, kiểm bằng mắt rằng đó là hai lần đo thật trùng nhau chứ không phải giá trị bị chép sang; kết luận ghi vào SUMMARY.
    - Tan ca hai lần liên tiếp: `select count(*) from attendance_photos where attendance_record_id='<id>'` vẫn trả về `2`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/data/__tests__/attendance-evidence.test.ts</automated>
  </verify>
  <done>Cả hai đầu của một ca đều mang ảnh và toạ độ riêng đo tại chỗ; chữ ký chấm công không còn một tham số thời gian nào, nên không còn đường nào để đồng hồ thiết bị chạm vào bản ghi.</done>
</task>

<task type="auto">
  <name>Task 3: Nút "Tan ca" đi qua Camera Sheet, và lý do từ chối hiển thị đúng khối</name>
  <files>src/components/employee-app/camera-sheet.tsx, src/components/employee-app/attendance-status-card.tsx, src/app/employee/employee-home-view.tsx</files>
  <read_first>
    - `src/components/employee-app/attendance-status-card.tsx` — nút "Tan ca" ở nhánh đang làm việc
      (khoảng dòng 122-131), hiện gọi `onCheckOut(clock)` với giờ đọc từ đồng hồ client. **Giữ nguyên**
      khối đồng hồ tick và hai dòng miễn trừ rule D-19a kèm lý do; chỉ đổi phần gọi hàm.
    - `src/app/employee/employee-home-view.tsx` — `handleCheckIn`/`handleCheckOut` và trạng thái mở
      Sheet do plan 03-01 dựng.
    - `src/components/employee-app/camera-sheet.tsx` (sau plan 03-03) — máy trạng thái đủ nhánh; task
      này chỉ thêm tham số phân biệt loại chấm (vào hay ra) để nhãn nút và lời gọi đúng.
    - `src/lib/attendance/rejection.ts` (Task 1) — vệ sĩ kiểu để phân nhánh.
    - `src/lib/constants.ts` khối `ATTENDANCE_EVIDENCE_LABEL` / `ATTENDANCE_REJECTION_LABEL`.
  </read_first>
  <action>
    **(a) `attendance-status-card.tsx`** — đổi `onCheckOut` thành một lời gọi không tham số mở Camera
    Sheet ở chế độ tan ca. Giữ nguyên đồng hồ tick và cả hai dòng miễn trừ rule D-19a kèm lý do —
    đó là miễn trừ hợp lệ duy nhất đã biết và không được gỡ.

    **(b) `camera-sheet.tsx`** — nhận thêm một thuộc tính phân biệt loại chấm. Nhãn nút gửi và chữ
    trong banner kết quả tra theo loại đó từ khối hằng số. Máy trạng thái không đổi.

    **(c) `employee-home-view.tsx`** — `handleCheckOut` nhận `PunchEvidence` từ Sheet rồi gọi
    `checkOutService(recordId, evidence)`. Cả hai hàm bắt lỗi bằng `isAttendanceRejection(cause)`:
    đúng thì chuyển phân loại xuống Sheet để hiện đúng khối; sai thì phân loại thành lỗi mạng nếu lời
    gọi không tới nơi, còn lại thì hiện thông điệp lỗi chung. **Không** so khớp chuỗi tiếng Việt để
    quyết định hiển thị khối nào.
  </action>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` thoát 0.
    - `npx vitest run` thoát 0 (toàn bộ bộ test).
    - `grep -c "isAttendanceRejection" src/app/employee/employee-home-view.tsx` trả về ít nhất `1`.
    - `grep -icE "cause\.message *===|cause\.message *\.includes|message *=== *\"" src/app/employee/employee-home-view.tsx` trả về `0` — không phân nhánh bằng so khớp chuỗi.
    - `grep -c "no-date-in-client" src/components/employee-app/attendance-status-card.tsx` trả về ít nhất `2` — miễn trừ D-19a hợp lệ vẫn nguyên vẹn.
    - `grep -cE "onCheckOut\(clock\)|onCheckIn\(clock\)" src/components/employee-app/attendance-status-card.tsx` trả về `0` — giờ client không còn được truyền vào đường chấm công.
    - Trên thiết bị thật với một phiên nhân viên đang trong ca: bấm "Tan ca" mở camera, chụp, gửi — bản ghi có giờ ra và có ảnh loại ra. Kết quả quan sát ghi vào SUMMARY.
    - Đặt đồng hồ thiết bị lệch 3 giờ rồi chấm công: giờ ghi trên bản ghi vẫn đúng giờ thật. Kết quả quan sát ghi vào SUMMARY (đây là bài kiểm trực tiếp của ATT-06).
    - Tạm sửa ca của một nhân viên thử nghiệm sang một khung giờ không chứa thời điểm hiện tại rồi chấm công: màn hình hiện đúng khối "Ngoài giờ ca làm", không phải một lỗi chung. Khôi phục ca sau khi kiểm. Kết quả quan sát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>Cả hai nút chấm công đều đi qua camera; lý do từ chối tới màn hình nguyên vẹn qua phân loại chứ không qua chuỗi chữ; đổi đồng hồ thiết bị không đổi được giờ đã ghi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Đồng hồ thiết bị → bản ghi chấm công | Sau plan này không còn tham số nào để giờ client đi qua |
| Nhân viên → bản ghi chấm công của người khác | `checkOut` suy chủ thể từ chính bản ghi, không từ tham số |
| Trình duyệt → bucket Storage | Một lần tải ảnh lên trước khi kiểm quyền là một tệp rác vĩnh viễn (D-22 không có job dọn) |
| Server → giao diện | Lý do từ chối phải đi qua phân loại máy đọc được, không qua chuỗi hiển thị |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | Tampering | Đồng hồ thiết bị bị đổi để chấm công lệch giờ | high | mitigate | Chữ ký không còn tham số thời gian nào; acceptance criteria có một bài kiểm thủ công đổi đồng hồ thiết bị 3 giờ và đòi giờ ghi vẫn đúng |
| T-03-04-02 | Spoofing | Nhân viên tan ca hộ người khác | high | mitigate | Chủ thể suy từ chính dòng bản ghi cộng `.eq("company_id", …)`; kiểm quyền chạy trước mọi thao tác Storage |
| T-03-04-03 | Denial of Service | Tệp rác tích tụ trong bucket từ những lần bị từ chối sau khi đã tải ảnh lên | medium | mitigate | Kiểm bằng chứng và kiểm quyền đều chạy trước bước tải lên; không có job dọn nên thứ tự này là biện pháp duy nhất |
| T-03-04-04 | Tampering | Khoảng cách của lần vào bị chép sang lần ra làm bằng chứng của lần ra thành bịa | medium | mitigate | Hàm ghi bằng chứng đo lại độc lập cho từng lần chấm; acceptance criteria có một truy vấn đối chiếu hai dòng ảnh của cùng bản ghi |
| T-03-04-05 | Repudiation | Nhân viên bị từ chối không biết vì sao và bấm lại mãi | medium | mitigate | Phân loại máy đọc được thay cho so khớp chuỗi; acceptance criteria có grep chặn so khớp chuỗi cộng một bài kiểm thủ công đổi ca |
| T-03-04-06 | Elevation of Privilege | Một lý do từ chối thứ tư được thêm vào và biến thành một cách chặn người dùng hợp lệ | medium | mitigate | Acceptance criteria đếm số phân loại tồn tại trong mã nguồn và đòi đúng bằng 3 |
| T-03-SC | Tampering | Gói cài từ trình quản lý gói bị tráo | high | mitigate | Plan này không cài gói npm mới nào |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Chữ ký `checkIn`/`checkOut` không còn tham số thời gian nào
- Một ca đầy đủ cho đúng hai dòng `attendance_photos` và hai dòng `audit_log`
- Đúng ba phân loại từ chối tồn tại trong mã nguồn, không hơn
- Không nhánh nào chặn chấm công theo khoảng cách
- Đổi đồng hồ thiết bị không đổi được giờ đã ghi

</verification>

<success_criteria>
Cả hai đầu của một ca làm việc đều mang bằng chứng đo tại chỗ, và không còn một đường nào cho giá trị
thời gian của thiết bị chạm vào bản ghi chấm công.
</success_criteria>

<output>
Create `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-04-SUMMARY.md` when done
</output>
