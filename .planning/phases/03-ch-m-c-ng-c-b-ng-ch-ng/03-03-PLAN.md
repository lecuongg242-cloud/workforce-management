---
phase: 03-ch-m-c-ng-c-b-ng-ch-ng
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/attendance/camera.ts
  - src/lib/attendance/__tests__/camera.test.ts
  - src/components/employee-app/camera-sheet.tsx
  - src/components/employee-app/__tests__/camera-sheet.test.tsx
autonomous: false
requirements: [ATT-01, ATT-08]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Bốn loại lỗi camera được phân biệt và mỗi loại có một khối giải thích riêng: bị từ chối quyền, không có camera nào, camera đang bị ứng dụng khác giữ, và ràng buộc không khớp — không loại nào rơi vào một thông báo \"có lỗi xảy ra\" chung (ATT-01)."
    - "Thiết bị không có camera dùng được thì màn hình dẫn thẳng người dùng sang đường bổ sung công `/employee/requests?type=attendance_supplement` — đúng lối thoát mà D-23 đã chọn cho trường hợp mất mạng. Không có lựa chọn chọn ảnh từ thư viện nào được thêm vào (UI-SPEC: No-camera-device, đã được điều phối viên chốt)."
    - "Bị từ chối quyền vị trí hiện một khối riêng kèm nút \"Thử lại\", và đây **không** phải một lý do từ chối thứ tư của server — nó là cổng chặn phía client, chưa từng có yêu cầu nào rời khỏi thiết bị (UI-SPEC: Permission-denied — location, D-20b)."
    - "Ba lý do từ chối của D-20b hiển thị phân biệt được bằng ba biểu tượng khác nhau, ba tiêu đề khác nhau và ba phần thân khác nhau — cùng họ màu báo lỗi nhưng không bao giờ chỉ khác nhau ở màu (ATT-08, UI-SPEC: Rejection — visual distinguishability)."
    - "Lỗi mất mạng không dùng bất kỳ chữ nào ám chỉ đang chờ, đang đồng bộ, hay sẽ tự gửi lại; nút ghi đúng \"Gửi lại\" và người dùng phải tự chạm (D-23)."
    - "Khi gửi lại sau lỗi mạng, ảnh đã chụp và toạ độ đã lấy vẫn còn trong bộ nhớ của phiên màn hình — người dùng không phải chụp lại. Không giá trị nào được ghi xuống bộ nhớ lâu bền của trình duyệt (D-23 cấm hàng đợi, không cấm giữ trong bộ nhớ lần thử hiện tại)."
    - "Chấm công được nhận nhưng ở xa điểm làm việc hiện một banner cảnh báo màu hổ phách mở đầu bằng \"Đã ghi nhận\" kèm khoảng cách thật và tên điểm làm việc, có biểu tượng riêng khác biểu tượng của ba lý do từ chối, và đòi một lần chạm \"Đã hiểu\" chứ không phải một toast thoáng qua (ATT-08, D-20)."
    - "Ảnh được nén ở phía client trước khi rời thiết bị, với mục tiêu kích thước đặt ở một hằng số có tên."
    - "`GPS_TIMEOUT_MS` bằng 15000 và khai đúng một chỗ; hết thời gian chờ thì chip trạng thái đổi thành một nút thử lại chứ không treo vô hạn (UI-SPEC: GPS acquisition timeout, đã được điều phối viên chốt)."
    - "Mọi biểu tượng trạng thái đi kèm nhãn chữ; không trạng thái nào phân biệt bằng màu đơn thuần (UI-SPEC: status never color-only)."
    - "Nút chụp và cụm nút gửi/chụp lại neo ở đáy khung nhìn trong tầm ngón cái; nút đóng nhỏ và ở góc trên (UI-SPEC: one-handed use)."
    - "Sheet camera thay hẳn thanh điều hướng dưới khi mở và trả lại nó khi đóng — không phủ lên (UI-SPEC: bottom nav occlusion)."
    - statement: "Camera bị ứng dụng khác giữ hiển thị đúng chữ \"Không mở được camera — có thể đang dùng ở nơi khác. Đóng ứng dụng khác rồi thử lại.\" — đây là điều kiện trình duyệt hiếm và khó dựng lại thật, nên nhánh mã và chữ được hiện thực nhưng không dựng một bài kiểm tái tạo lỗi trình duyệt thật."
      verification: backstop
    - statement: "`browser-image-compression` là gói mang thẻ [ASSUMED] trong RESEARCH.md (tên gói phát hiện qua tìm kiếm web, không phải tài liệu chính thức); tính hợp lệ phải được người xác minh trên trang npm trước khi cài, và kích thước bundle sau khi cài phải được đo và ghi lại."
      verification: backstop
  artifacts:
    - path: "src/lib/attendance/camera.ts"
      provides: "Bốn lớp lỗi camera phân loại từ DOMException + timeout GPS có tên"
      exports: ["openCamera", "closeCamera", "captureFrame", "acquireLocation", "GPS_TIMEOUT_MS", "CameraPermissionDeniedError", "NoCameraDeviceError", "CameraInUseError", "LocationPermissionDeniedError"]
    - path: "src/lib/attendance/__tests__/camera.test.ts"
      provides: "Bài kiểm bốn nhánh lỗi getUserMedia và ba tuỳ chọn geolocation"
      min_lines: 60
    - path: "src/components/employee-app/__tests__/camera-sheet.test.tsx"
      provides: "Bài kiểm máy trạng thái và các khối lỗi của Camera Sheet"
      min_lines: 80
  key_links:
    - from: "src/components/employee-app/camera-sheet.tsx"
      to: "src/lib/constants.ts"
      via: "toàn bộ chữ tiếng Việt lấy từ khối hằng số, không viết rải rác"
      pattern: "ATTENDANCE_(EVIDENCE|REJECTION)_LABEL"
    - from: "src/components/employee-app/camera-sheet.tsx"
      to: "/employee/requests"
      via: "lối thoát khi thiết bị không có camera dùng được"
      pattern: "attendance_supplement"
  prohibitions:
    - statement: "MUST NOT thêm bất kỳ lối thoát nào cho phép ảnh không phải chụp trực tiếp trở thành bằng chứng — kể cả dưới danh nghĩa \"dự phòng khi camera hỏng\". Khi camera không dùng được, câu trả lời đúng là đường bổ sung công có người duyệt, chứ không phải một ảnh không ai biết chụp lúc nào ở đâu."
      category: safety
    - statement: "MUST NOT dùng chữ ám chỉ bản ghi đang chờ, đang đồng bộ, hay sẽ tự động gửi lại khi mất mạng. Giờ do server cấp là tiêu chí 3 của phase; một dòng chữ hứa hẹn gửi sau khiến nhân viên tin mình đã chấm công trong khi chưa có bản ghi nào, và đó là kiểu hiểu nhầm chỉ vỡ ra vào cuối kỳ lương."
      category: transparency
    - statement: "MUST NOT trình bày cảnh báo \"ở xa điểm làm việc\" bằng ngôn ngữ hay màu sắc của một lần thất bại. Sau D-20 lần chấm công đó ĐÃ được ghi nhận; làm nó trông như lỗi khiến nhân viên bấm lại nhiều lần và khiến người đứng đúng chỗ nhưng GPS lệch 40m cảm thấy bị buộc tội."
      category: values
---

<!-- planner-discipline-allow: type="file" -->
<!-- planner-discipline-allow: capture -->
<!-- planner-discipline-allow: browser-image-compression -->

<objective>
Đưa Camera Sheet từ đường hạnh phúc của lát cắt tracer lên đủ mọi nhánh mà một thiết bị thật sẽ đi
qua: bốn loại lỗi camera, hai loại từ chối quyền, hết thời gian chờ GPS, ba lý do từ chối của D-20b,
và banner "đã ghi nhận nhưng ở xa" của D-20. Cộng thêm nén ảnh phía client.

Purpose: ATT-01 và ATT-08 sống hay chết ở đúng những nhánh này. Một màn hình chấm công chỉ chạy được
khi mọi thứ thuận lợi là một màn hình sẽ chặn nhân viên vào đúng lúc họ cần nó nhất — và một thông
báo "có lỗi xảy ra" khiến người ta bấm lại mãi mà không ai biết vì sao.

Output: `src/lib/attendance/camera.ts` đủ bốn nhánh lỗi, Camera Sheet đủ trạng thái, hai bộ test
Vitest, và gói nén ảnh sau một cổng xác minh chặn cứng.
</objective>

<execution_context>
@E:/externalProjects/workforce-management/.claude/gsd-core/workflows/execute-plan.md
@E:/externalProjects/workforce-management/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md
@.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-01-SUMMARY.md
</context>

## Artifacts this plan produces

| Loại | Tên |
|---|---|
| Gói npm | `browser-image-compression` (sau cổng xác minh) |
| Lớp lỗi | `CameraPermissionDeniedError`, `NoCameraDeviceError`, `CameraInUseError`, `LocationPermissionDeniedError`, `LocationTimeoutError` |
| Hằng số | `GPS_TIMEOUT_MS`, `PHOTO_MAX_SIZE_MB`, `PHOTO_MAX_DIMENSION` |
| Hàm | `compressPhoto()` |
| Trạng thái UI | khối từ chối quyền camera, khối không có camera, khối camera đang bận, khối từ chối quyền vị trí, ba banner từ chối, banner ngoài bán kính |
| Test | `src/lib/attendance/__tests__/camera.test.ts`, `src/components/employee-app/__tests__/camera-sheet.test.tsx` |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Cổng xác minh tính hợp lệ của gói `browser-image-compression` trước khi cài</name>
  <what-built>
Chưa xây gì — đây là cổng chặn trước khi cài gói npm. `03-RESEARCH.md` §"Package Legitimacy Audit"
trả verdict `OK` cho `browser-image-compression` (npm, 1.431.223 lượt tải/tuần, repo
`github.com/Donaldcwl/browser-image-compression`, không có `postinstall`), nhưng ghi chú của chính
mục đó nói rõ: **tên gói được phát hiện qua tìm kiếm web chứ không phải tài liệu chính thức**, nên
nó vẫn mang thẻ `[ASSUMED]` cho nguồn gốc tên. Một gói `[ASSUMED]` không được cài mà không có người
nhìn — đây đúng là bề mặt mà một cuộc tấn công tráo tên gói nhắm vào.
  </what-built>
  <how-to-verify>
1. Mở `https://www.npmjs.com/package/browser-image-compression`.
2. Xác nhận trường Repository trỏ tới `github.com/Donaldcwl/browser-image-compression` (đúng chủ sở
   hữu, không phải một bản sao tên gần giống).
3. Xác nhận lượt tải hàng tuần ở mức hàng triệu, và phiên bản mới nhất là `2.0.2` phát hành
   2023-03-06.
4. Mở tab Code hoặc `package.json` của gói và xác nhận **không có** script `postinstall`.
5. So tên gói từng ký tự — chú ý các biến thể tráo chữ (`browser-images-compression`,
   `browser-image-compress`, dấu gạch nối lệch chỗ).
6. Chạy `npm view browser-image-compression name repository.url dist-tags.latest` và so kết quả với
   những gì vừa đọc trên trang web.

Nếu bất kỳ điểm nào lệch: **không phê duyệt**. Phương án thay thế được phép là tự nén bằng canvas
(giảm kích thước rồi `toBlob` với chất lượng thấp hơn) — kém hơn về mặt xử lý trên luồng nền và xử
lý hướng ảnh, nhưng không kéo một gói lạ vào cây phụ thuộc.
  </how-to-verify>
  <resume-signal>Gõ "approved" kèm URL repo đã kiểm, hoặc mô tả điểm lệch phát hiện được</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Bốn nhánh lỗi camera, hết giờ chờ GPS, và nén ảnh phía client</name>
  <files>package.json, package-lock.json, src/lib/attendance/camera.ts, src/lib/attendance/__tests__/camera.test.ts</files>
  <read_first>
    - `src/lib/attendance/camera.ts` (toàn bộ, do plan 03-01 tạo) — bốn lớp lỗi đã được **định nghĩa
      sẵn** ở đó; plan này điền nốt các nhánh phân loại, không đổi hình dạng module.
    - `src/lib/constants.ts` khối `ATTENDANCE_EVIDENCE_LABEL` (do 03-01 thêm) — chữ cho từng khối lỗi.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md` §"Pattern 1" (bảng phân loại lỗi
      đầy đủ và lý do dùng `ideal` thay vì `exact`), §"Pattern 3" (ba tuỳ chọn geolocation và ý nghĩa
      của `coords.accuracy`), §"Pitfall 5" (giới hạn kích thước thân yêu cầu), §"Testing" (khuôn gán
      `navigator.mediaDevices` / `navigator.geolocation` bằng `Object.defineProperty` trong jsdom).
    - `src/lib/data/__tests__/attendance.test.ts` — khuôn test Vitest của dự án: `vi.stubGlobal`,
      `afterEach(() => vi.unstubAllGlobals())`, mô tả test bằng tiếng Việt không dấu.
    - `vitest.config.ts` — môi trường test và alias `@/`.
    - `package.json` — khối `dependencies` để chèn gói mới đúng chỗ, và khối `scripts` giữ nguyên.
  </read_first>
  <behavior>
    - `getUserMedia` reject với `DOMException` tên `NotAllowedError` → `openCamera()` ném `CameraPermissionDeniedError`.
    - `getUserMedia` reject với tên `NotFoundError` → ném `NoCameraDeviceError`.
    - `getUserMedia` reject với tên `OverconstrainedError` → ném `NoCameraDeviceError` (cùng lối thoát: không có camera nào dùng được).
    - `getUserMedia` reject với tên `NotReadableError` → ném `CameraInUseError`.
    - `getUserMedia` reject với một tên khác bốn tên trên → lỗi gốc được ném tiếp, không bị nuốt thành một lớp lỗi sai.
    - Ràng buộc gửi vào `getUserMedia` dùng `ideal` cho camera sau, không dùng `exact`.
    - `acquireLocation()` reject với mã `PERMISSION_DENIED` → ném `LocationPermissionDeniedError`.
    - `acquireLocation()` reject với mã `TIMEOUT` → ném `LocationTimeoutError`.
    - `acquireLocation()` gọi `getCurrentPosition` với `timeout` đúng bằng `GPS_TIMEOUT_MS` và `maximumAge` bằng 0.
    - `compressPhoto(blob)` trả về một `Blob` có `size` nhỏ hơn hoặc bằng đầu vào, và không bao giờ ném khi đầu vào là một JPEG hợp lệ — nén thất bại thì trả lại ảnh gốc thay vì chặn lần chấm công.
    - `closeCamera()` gọi `stop()` trên từng track kể cả khi luồng có nhiều hơn một track.
  </behavior>
  <action>
    **(a) Cài gói.** `npm install browser-image-compression` — chỉ chạy sau khi cổng xác minh ở trên
    được phê duyệt. Ghim phiên bản chính xác trong `package.json` (không dùng dấu ngã), theo đúng quy
    ước ghim cứng mà dự án đã dùng cho `@supabase/supabase-js`.

    **(b) `src/lib/attendance/camera.ts`** — hoàn thiện module:
    - Điền nốt bảng phân loại `DOMException.name` → lớp lỗi theo `<behavior>`. Nhánh mặc định ném tiếp
      lỗi gốc; **không** gộp mọi thứ chưa biết vào một lớp lỗi đã có, vì như vậy màn hình sẽ hiển thị
      một lời giải thích sai về một nguyên nhân chưa biết.
    - Comment tại nhánh `NotAllowedError`: lỗi này cũng là lỗi ném ra khi trang chạy ngoài ngữ cảnh
      an toàn (HTTP thay vì HTTPS/localhost). Khi QA thử trên điện thoại thật qua một địa chỉ LAN,
      camera sẽ hỏng với đúng lỗi này dù chưa ai được hỏi quyền — dễ nhầm là lỗi quyền trong khi là
      lỗi giao thức.
    - `acquireLocation()`: bắt `GeolocationPositionError` và phân loại theo mã thành
      `LocationPermissionDeniedError` / `LocationTimeoutError`.
    - `PHOTO_MAX_SIZE_MB` và `PHOTO_MAX_DIMENSION` là hằng số có tên, kèm comment nối chúng với giới
      hạn kích thước thân yêu cầu đã nâng ở `next.config.ts` (03-01): nén ở client là biện pháp thứ
      nhất, biên an toàn ở cấu hình là biện pháp thứ hai, và không được dựa vào chỉ một trong hai.
    - `compressPhoto(blob)` bọc thư viện nén: gọi với hai hằng số trên, dùng luồng nền để không đứng
      hình giao diện khi nén ảnh lớn. Bọc trong `try/catch` — nén hỏng thì trả lại ảnh gốc và ghi một
      dòng `console.warn`, vì một lần chấm công không được thất bại chỉ vì bước tối ưu băng thông.

    **(c) `src/lib/attendance/__tests__/camera.test.ts`** — gán `navigator.mediaDevices` và
    `navigator.geolocation` bằng `Object.defineProperty` theo khuôn ở RESEARCH §"Testing". Khẳng định
    đủ mười hành vi liệt kê ở `<behavior>`: năm nhánh `getUserMedia` (bốn phân loại cộng một nhánh
    lỗi lạ), một khẳng định ràng buộc dùng `ideal`, hai nhánh geolocation, một khẳng định ba tuỳ chọn
    geolocation đúng giá trị, và một khẳng định `closeCamera` dừng từng track của một luồng hai track.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/lib/attendance/__tests__/camera.test.ts` thoát 0 và báo ít nhất 10 test.
    - `node -e "const p=require('./package.json');const v=p.dependencies['browser-image-compression'];if(!v||/[\^~]/.test(v)){console.error('chua ghim cung: '+v);process.exit(1)}"` thoát 0.
    - `npm ls browser-image-compression` in đúng phiên bản đã ghim và không báo cây phụ thuộc hỏng.
    - `node -e "const p=require('browser-image-compression/package.json');if(p.scripts&&p.scripts.postinstall){process.exit(1)}"` thoát 0.
    - `grep -v '^ *//' src/lib/attendance/camera.ts | grep -c "exact"` trả về `0` — ràng buộc camera dùng `ideal`.
    - `grep -v '^ *//' src/lib/attendance/camera.ts | grep -cE "NotAllowedError|NotFoundError|OverconstrainedError|NotReadableError"` trả về ít nhất `4`.
    - `grep -v '^ *//' src/lib/attendance/camera.ts | grep -c "15_000\|15000"` trả về đúng `1`.
    - `npm run build` thoát 0; kích thước bundle của route nhân viên trước và sau khi thêm gói nén được ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/lib/attendance/__tests__/camera.test.ts</automated>
  </verify>
  <done>Mỗi loại lỗi camera và lỗi vị trí có một lớp lỗi riêng để giao diện nói đúng nguyên nhân; hết giờ chờ GPS là một trạng thái có tên chứ không phải một lời hứa treo; ảnh được nén trước khi rời thiết bị và nén hỏng không chặn được lần chấm công.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Camera Sheet đủ trạng thái — bốn khối lỗi camera, hai khối từ chối quyền, lối thoát khi không có camera</name>
  <files>src/components/employee-app/camera-sheet.tsx, src/components/employee-app/__tests__/camera-sheet.test.tsx</files>
  <read_first>
    - `src/components/employee-app/camera-sheet.tsx` (toàn bộ, do 03-01 tạo) — máy trạng thái hiện có
      và nhánh từ chối quyền camera đã hiện thực; plan này thêm nhánh, không đổi hình dạng máy trạng
      thái.
    - `src/lib/attendance/camera.ts` (sau Task 1) — năm lớp lỗi sẽ được phân nhánh.
    - `src/lib/constants.ts` khối `ATTENDANCE_EVIDENCE_LABEL` — bốn khối chữ cho bốn tình huống.
    - `src/components/ui/button.tsx` — biến thể kích thước `mobile` (cao 44px) và `icon-mobile`
      (44×44px) đã có; không thêm biến thể mới.
    - `src/components/employee-app/quick-actions.tsx` — đường dẫn thật tới màn hình yêu cầu và cách
      dự án truyền tham số loại yêu cầu qua URL.
    - `src/app/employee/requests/requests-view.tsx` — xác nhận màn hình đọc được tham số
      `type=attendance_supplement` từ URL; nếu chưa, thêm phần đọc tham số đó ở đây là việc nhỏ nằm
      trong phạm vi task này.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md` §"UI Considerations" các hàng
      "Permission-denied — camera", "Permission-denied — location", "No-camera-device",
      "Camera-in-use", "GPS acquisition timeout", "Mobile constraint — one-handed use",
      "Mobile constraint — bottom nav occlusion", "status never color-only".
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-RESEARCH.md` §"Testing" — khuôn mock jsdom.
  </read_first>
  <behavior>
    - `NoCameraDeviceError` hiển thị khối chứa một liên kết tới `/employee/requests?type=attendance_supplement`; khối đó **không** chứa nút nào mở bộ chọn tệp.
    - `CameraInUseError` hiển thị khối với chữ riêng nhắc đóng ứng dụng khác, khác chữ của khối từ chối quyền.
    - `CameraPermissionDeniedError` hiển thị khối kèm nút "Thử lại" gọi lại `openCamera()`; nhấn nút đó không tải lại trang.
    - `LocationPermissionDeniedError` hiển thị khối riêng, khác cả bốn khối lỗi camera; nút gửi không tồn tại ở trạng thái này vì không có toạ độ nào để gửi.
    - `LocationTimeoutError` đổi chip trạng thái vị trí thành một nút thử lại; Sheet không đóng và ảnh đã chụp (nếu có) vẫn còn.
    - Mỗi trong sáu khối trên hiển thị một biểu tượng **và** một nhãn chữ; không khối nào chỉ khác nhau ở màu.
    - Đóng Sheet từ bất kỳ trạng thái lỗi nào cũng gọi `closeCamera()`.
    - Nút chụp và cụm nút gửi/chụp lại nằm trong nửa dưới khung nhìn; nút đóng nằm ở góc trên.
  </behavior>
  <action>
    **(a) `camera-sheet.tsx`** — thêm bốn trạng thái lỗi camera/vị trí vào máy trạng thái đã có, mỗi
    trạng thái thay **toàn bộ** vùng khung hình (không phải một toast chồng lên khung hình đang chạy):
    - Từ chối quyền camera: biểu tượng camera gạch chéo, chữ từ hằng số, nút "Thử lại" gọi lại
      `openCamera()` — trên di động, cấp quyền trong cài đặt trình duyệt thường không cần tải lại
      trang, nên thử lại tại chỗ là hành vi đúng.
    - Không có camera: cùng kiểu trình bày, nhưng thay nút thử lại bằng một liên kết sang
      `/employee/requests?type=attendance_supplement`. Ghi comment tại chỗ: đây là lối thoát đã chốt,
      cùng lối mà D-23 chọn cho mất mạng; **không** thêm đường chọn ảnh từ thư viện vì làm vậy là gỡ
      bỏ chính ATT-01.
    - Camera đang bận: chữ riêng nhắc đóng ứng dụng khác, kèm nút thử lại.
    - Từ chối quyền vị trí: khối riêng, biểu tượng ghim bản đồ gạch chéo, nút "Thử lại" gọi lại
      `acquireLocation()`. Comment tại chỗ ghi rõ: đây là **cổng chặn phía client**, không phải một
      lý do từ chối thứ tư — D-20b cho đúng ba lý do và không cho phép phát minh thêm; ở tình huống
      này chưa có toạ độ nào nên chưa có yêu cầu nào rời khỏi thiết bị.
    - Hết giờ chờ GPS: không thay cả khung hình (khung hình vẫn dùng được, chỉ thiếu toạ độ) — chip
      trạng thái vị trí đổi thành nút thử lại, nút gửi vẫn vô hiệu.
    - Bố cục: cụm nút thao tác neo đáy khung nhìn; nút đóng nhỏ ở góc trên; Sheet chiếm trọn khung
      nhìn nên thay hẳn thanh điều hướng dưới thay vì phủ lên nó.
    - Chèn `compressPhoto()` vào giữa bước chụp và bước gửi.

    **(b) `src/components/employee-app/__tests__/camera-sheet.test.tsx`** — theo khuôn mock jsdom ở
    RESEARCH §"Testing": cho `getUserMedia` lần lượt reject với bốn `DOMException` khác nhau và khẳng
    định bốn khối chữ khác nhau xuất hiện; cho geolocation reject với hai mã và khẳng định hai hành vi
    khác nhau; khẳng định khối không-có-camera chứa đường dẫn bổ sung công; khẳng định đóng Sheet gọi
    `stop()` trên track. Mô tả test viết bằng tiếng Việt không dấu theo khuôn các file test hiện có.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/components/employee-app/__tests__/camera-sheet.test.tsx` thoát 0 và báo ít nhất 8 test.
    - `grep -rE '<input[^>]*type="file"' src/app/employee/ src/components/employee-app/` không trả về dòng nào.
    - `grep -c "attendance_supplement" src/components/employee-app/camera-sheet.tsx` trả về ít nhất `1`.
    - `grep -cE "NoCameraDeviceError|CameraInUseError|CameraPermissionDeniedError|LocationPermissionDeniedError|LocationTimeoutError" src/components/employee-app/camera-sheet.tsx` trả về ít nhất `5`.
    - `grep -c "ATTENDANCE_EVIDENCE_LABEL" src/components/employee-app/camera-sheet.tsx` trả về ít nhất `1`, và `grep -cE "\"[^\"]*[àáảãạăâđêôơư][^\"]*\"" src/components/employee-app/camera-sheet.tsx` trả về `0` — không chuỗi tiếng Việt nào viết thẳng trong component.
    - `npm run build` thoát 0.
    - Trên một thiết bị thật, từ chối quyền camera trong cài đặt trình duyệt rồi mở màn hình: khối giải thích hiện ra và nút "Thử lại" hoạt động sau khi cấp lại quyền mà không cần tải lại trang. Kết quả quan sát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/components/employee-app/__tests__/camera-sheet.test.tsx</automated>
  </verify>
  <done>Mỗi tình huống camera hoặc vị trí hỏng có một màn hình nói đúng chuyện gì đang xảy ra và một hành động tiếp theo cụ thể; thiết bị không có camera được dẫn sang đường bổ sung công thay vì rơi vào ngõ cụt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ba lý do từ chối phân biệt được, và banner "đã ghi nhận nhưng ở xa"</name>
  <files>src/components/employee-app/camera-sheet.tsx, src/components/employee-app/__tests__/camera-sheet.test.tsx</files>
  <read_first>
    - `src/lib/constants.ts` khối `ATTENDANCE_REJECTION_LABEL` (do 03-01 thêm) — ba cặp tiêu đề/thân
      lấy nguyên văn từ UI-SPEC.
    - `src/lib/types/domain.ts` — `AttendanceRejectionReason` (do 03-01 thêm).
    - `src/components/common/status-badge.tsx` — quy ước tông màu (`success`/`warning`/`danger`/
      `info`/`neutral`) và quy ước luôn kèm biểu tượng cộng nhãn chữ.
    - `src/app/globals.css` — token `--tf-error` và `--tf-warning`; UI-SPEC nói rõ tông hổ phách
      **không bao giờ** dùng cho một lần từ chối thật và cũng không dùng cho một lần thành công bình
      thường.
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-UI-SPEC.md` §"Copywriting Contract" (ba dòng lỗi
      cộng dòng flagged-but-accepted) và §"UI Considerations" các hàng "Rejection — missing photo",
      "Rejection — outside shift", "Rejection — network failure",
      "Rejection — visual distinguishability", "Flagged-but-accepted".
    - `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-CONTEXT.md` D-20, D-20b, D-23.
  </read_first>
  <behavior>
    - Lý do `missing_photo` hiển thị biểu tượng camera gạch chéo, tiêu đề "Thiếu ảnh chấm công", và **không** loại bỏ khung hình đã chụp nếu còn.
    - Lý do `outside_shift` hiển thị biểu tượng đồng hồ, tiêu đề "Ngoài giờ ca làm", và kèm một liên kết phụ sang đường bổ sung công — đây là lối đi cho một nhầm lẫn thật, không phải nút gửi lại chính lần chấm công vừa bị từ chối.
    - Lý do `network_error` hiển thị biểu tượng wifi gạch chéo, tiêu đề "Mất kết nối mạng", và một nút ghi đúng "Gửi lại".
    - Ba khối trên có ba biểu tượng khác nhau, ba tiêu đề khác nhau và ba phần thân khác nhau; chúng dùng cùng một tông màu báo lỗi.
    - Sau lỗi mạng, ảnh đã chụp và toạ độ đã lấy vẫn còn trong trạng thái React của Sheet — bấm "Gửi lại" không bắt chụp lại.
    - Không có bất kỳ bộ đếm giờ nào tự gửi lại, và không có giá trị nào được ghi vào `localStorage` hay `sessionStorage`.
    - Kết quả "được nhận nhưng ở xa" hiển thị một banner tông hổ phách, biểu tượng khác cả ba biểu tượng lỗi, tiêu đề mở đầu bằng "Đã ghi nhận", phần thân chứa tên điểm làm việc và khoảng cách thật, và một nút "Đã hiểu".
    - Banner đó chỉ đóng khi người dùng chạm "Đã hiểu"; nó không tự biến mất sau một khoảng thời gian.
    - Kết quả thành công bình thường (trong bán kính) dùng toast nhẹ như hiện tại, không dùng banner.
    - Banner "ở xa" **không** hiển thị độ chính xác GPS — con số đó chỉ dành cho màn hình quản trị.
  </behavior>
  <action>
    **(a) `camera-sheet.tsx`** — thêm tầng trình bày kết quả sau khi Server Action trả về:
    - Một component con nhận `AttendanceRejectionReason` và tra ba cặp chữ từ
      `ATTENDANCE_REJECTION_LABEL`, mỗi lý do có biểu tượng riêng. Cả ba dùng cùng họ màu báo lỗi —
      tín hiệu "lần này bị từ chối" phải nhất quán — nhưng phân biệt bằng biểu tượng và chữ, theo
      đúng quy tắc không bao giờ phân biệt bằng màu đơn thuần của dự án.
    - Phân loại `network_error` xảy ra ở **phía client**: khi lời gọi Server Action thất bại vì
      transport (không phải vì server trả một lỗi nghiệp vụ). Ghi comment tại chỗ giải thích điều này
      cho rõ ràng: D-20b nói ba lý do do server quyết, nhưng server không thể tự nói với thiết bị
      rằng thiết bị mất mạng — hai lý do `missing_photo` và `outside_shift` do server quyết và client
      chỉ hiển thị, còn `network_error` là phân loại của client cho một lần gửi không tới nơi. Cả ba
      dùng chung một hình dạng trình bày.
    - Khối lỗi mạng: nút "Gửi lại" gửi lại **chính** ảnh và toạ độ đang giữ trong trạng thái React.
      Không có bộ đếm giờ tự động, không có chữ nào ám chỉ đang chờ hay sẽ tự đồng bộ, và không ghi
      gì xuống bộ nhớ lâu bền của trình duyệt — D-23 cấm hàng đợi, không cấm giữ lại lần thử hiện tại
      trong bộ nhớ. Ghi rõ ranh giới đó thành comment.
    - Banner "được nhận nhưng ở xa": tông hổ phách, biểu tượng riêng, tiêu đề mở đầu bằng "Đã ghi
      nhận" **trước** khi nói tới điều bất thường, phần thân chèn tên điểm làm việc và khoảng cách
      thật do server trả về, đóng bằng nút "Đã hiểu". Dùng một lần chạm xác nhận chứ không phải toast
      vì đây là thông tin nhân viên cần nhớ (bản ghi của họ sẽ bị xem lại), khác với đường thành công
      bình thường vốn chỉ cần một toast nhẹ. **Không** hiện độ chính xác GPS ở đây — con số đó client
      tự khai, nhân viên không làm gì được với nó, và đặt nó ở đây làm nó trông có thẩm quyền hơn
      thực tế.

    **(b) Mở rộng `camera-sheet.test.tsx`** — khẳng định đủ mười hành vi ở `<behavior>`: ba khối từ
    chối có ba tiêu đề và ba biểu tượng khác nhau; nút của khối mạng ghi đúng "Gửi lại"; gửi lại
    không gọi lại `getUserMedia`; không có lời gọi nào tới `localStorage`/`sessionStorage`; banner
    hổ phách chứa tên điểm làm việc và khoảng cách và chỉ đóng khi bấm "Đã hiểu"; banner không chứa
    con số độ chính xác.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` và `npm run lint` thoát 0.
    - `npx vitest run src/components/employee-app/__tests__/camera-sheet.test.tsx` thoát 0 và báo ít nhất 18 test (8 từ Task 2 cộng ít nhất 10 của task này).
    - `grep -rcE "localStorage|sessionStorage|indexedDB" src/components/employee-app/camera-sheet.tsx` trả về `0`.
    - `grep -rcE "setTimeout\([^)]*retry|setInterval" src/components/employee-app/camera-sheet.tsx` trả về `0` — không có gửi lại tự động.
    - `grep -icE "đang đồng bộ|sẽ gửi lại|đang chờ gửi|xếp hàng" src/lib/constants.ts` trả về `0`.
    - `grep -c "ATTENDANCE_REJECTION_LABEL" src/components/employee-app/camera-sheet.tsx` trả về ít nhất `1`.
    - `grep -icE "accuracy|độ chính xác" src/components/employee-app/camera-sheet.tsx` trả về `0` — độ chính xác không xuất hiện ở màn hình nhân viên.
    - `npm run build` thoát 0.
    - Với một điểm làm việc tạm đặt cách vị trí thử nghiệm vài trăm mét: chấm công thành công và banner hổ phách hiện đúng tên điểm và khoảng cách; banner không tự biến mất. Kết quả quan sát ghi vào SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run src/components/employee-app/__tests__/camera-sheet.test.tsx</automated>
  </verify>
  <done>Nhân viên bị từ chối luôn biết vì sao và làm gì tiếp; nhân viên chấm công ở xa biết mình ĐÃ chấm được và sẽ bị xem lại, chứ không tưởng là mình vừa thất bại.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trình duyệt nhân viên → luồng media của thiết bị | Đây là chỗ duy nhất ảnh trở thành bằng chứng; mọi đường khác dẫn ảnh vào hệ thống đều phá bằng chứng đó |
| Cây phụ thuộc npm → bundle chạy trên máy nhân viên | Gói nén ảnh chạy trên chính khung hình vừa chụp trước khi nó rời thiết bị |
| Kết quả từ server → chữ hiển thị cho nhân viên | Client chỉ được hiển thị lý do, không được tự phát minh ra lý do |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-SC | Tampering | Gói `browser-image-compression` mang thẻ `[ASSUMED]` bị tráo tên (slopsquat) | high | mitigate | Cổng `checkpoint:human-verify` chặn cứng trước khi cài: người kiểm repo, lượt tải, phiên bản, và sự vắng mặt của `postinstall` trên trang npm. Acceptance criteria có thêm hai lệnh kiểm bằng máy: ghim cứng phiên bản, và khẳng định gói không có `postinstall` |
| T-03-03-01 | Tampering | Một nhánh dự phòng cho phép ảnh thư viện lọt vào luồng chấm công | critical | mitigate | Lối thoát khi không có camera là đường bổ sung công có người duyệt, không phải một bộ chọn ảnh. Acceptance criteria có grep chặn phần tử chọn tệp trong toàn cây nhân viên, chạy ở cả Task 2 |
| T-03-03-02 | Repudiation | Nhân viên tin mình đã chấm công trong khi chưa có bản ghi nào (chữ ám chỉ đang chờ gửi) | high | mitigate | Không hàng đợi, không gửi lại tự động, không ghi bộ nhớ lâu bền; acceptance criteria có ba lệnh grep chặn (bộ nhớ trình duyệt, hẹn giờ gửi lại, chữ ám chỉ đồng bộ) |
| T-03-03-03 | Information Disclosure | Ảnh nén còn sót siêu dữ liệu định vị của ứng dụng camera hệ thống | low | accept | Không phát sinh: ảnh dựng từ canvas nên không mang siêu dữ liệu ngay từ đầu, và thư viện nén cũng vẽ lại qua canvas. Ghi nhận để không ai thêm một bước tách siêu dữ liệu thừa |
| T-03-03-04 | Spoofing | Người dùng bị dẫn tới một lời giải thích sai về nguyên nhân hỏng camera và tin rằng thiết bị mình hỏng | medium | mitigate | Nhánh mặc định ném tiếp lỗi gốc thay vì gộp vào một lớp lỗi đã biết; acceptance criteria đòi bài kiểm cho chính nhánh lỗi lạ |
| T-03-03-05 | Information Disclosure | Độ chính xác GPS hiển thị cho nhân viên trông như một con số có thẩm quyền | low | mitigate | Con số client tự khai chỉ hiển thị ở màn hình quản trị nơi nó có giá trị điều tra; acceptance criteria có grep chặn ở màn hình nhân viên |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run build` thoát 0
- Không phần tử chọn tệp nào trong toàn cây nhân viên
- Bốn nhánh lỗi camera và hai nhánh lỗi vị trí đều có bài kiểm riêng và đều xanh
- Không lời gọi bộ nhớ lâu bền nào và không hẹn giờ gửi lại nào trong Camera Sheet
- Gói npm mới được ghim cứng, không có `postinstall`, và đã qua cổng xác minh có người nhìn
- Độ chính xác GPS không xuất hiện ở màn hình nhân viên

</verification>

<success_criteria>
Camera Sheet chịu được mọi tình huống một thiết bị thật sẽ ném vào nó, và mỗi tình huống trả lời được
hai câu hỏi của người đang đứng chờ chấm công: chuyện gì đang xảy ra, và bây giờ làm gì.
</success_criteria>

<output>
Create `.planning/phases/03-ch-m-c-ng-c-b-ng-ch-ng/03-03-SUMMARY.md` when done
</output>
